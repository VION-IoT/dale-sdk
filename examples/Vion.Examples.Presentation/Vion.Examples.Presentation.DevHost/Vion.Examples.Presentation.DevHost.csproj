<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>false</ImplicitUsings>
        <Nullable>enable</Nullable>
        <IsPackable>false</IsPackable>
    </PropertyGroup>

    <ItemGroup>
        <!--
            ProjectReference (not PackageReference) on the DevHost as well, so the entire chain
            consumes the in-source SDK. Flip when the SDK ships.
        -->
        <ProjectReference Include="..\..\..\Vion.Dale.DevHost.Web\Vion.Dale.DevHost.Web.csproj" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\Vion.Examples.Presentation\Vion.Examples.Presentation.csproj" />
    </ItemGroup>

</Project>
