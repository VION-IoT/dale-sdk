# Cross-cutting invariants

`SYS-` rules every area page cites instead of restating: identifier stability, determinism,
the emission floor. **Stub — populated as area passes land.** The corpus roster, area codes and
tiers live in [`../spec-process.md`](../spec-process.md); pages arrive one per pass, each carrying
`trace: enforced` frontmatter from the day it lands.

No page exists yet: until an area's pass, its current behavior is whatever the code and tests
pin — the pass is what turns that into stated truth here.
