---
slug: gate-pass
status: archived
blocked-on: none           # for parked docs: what's blocking + ref
areas: GATE
author: jonasbertsch
created: 2026-09-02
updated: 2026-09-02
supersedes: none           # path of a superseded change doc, or none
---

# GATE area pass — instantiation parameters and config-time structural gating

> Change doc — one in-flight change. The `Spec delta` below is distilled into the current-truth
> pages under `docs/specs/` in the implementation PR, then this doc is archived
> (`pwsh scripts/spec-change.ps1 archive gate-pass`). Process: `docs/spec-process.md`. Never put
> change narrative inline in a spec page — it lives here.

## At a glance

### Summary

Third SDD area pass. Turns config-time structural gating — the `[InstantiationParameter]` value
channel that runs before `Configure`, the `[IncludedWhen]` predicate the three declarative binders
resolve against it, the definition/live binding split, the two DALE diagnostics that police the
declaration, and the four wire fields the gates reach — into a current-truth spec page with an
id-cited test suite. The area is thinnest where it is riskiest: 19 tests in the rewrite over an
11-site production surface, with the parameter *value* path (types, ranges, nulls, decode failures)
almost entirely uncovered. The yield is coverage **and** a family of silences: a runtime write that
reverses a parameter after its gates resolved, a gate placement the analyzer does not judge, and a
DevHost boot that reports success over a block that never initialized.

### Spec implications

Creates `docs/specs/config-gating.md` (new, Tier A, `trace: enforced`) carrying `AC-GATE-*` criteria
for: the parameter value channel (application before `Configure`, identifier resolution, the decode
grammar and its failures, persistence exclusion, runtime immutability); gate evaluation (fail-closed
strict evaluation, the parameter-only context, the JSON-scalar encoding); the `Definition` / `Live`
binding split; what an excluded interface binding, contract binding and service-bearing component
each lose; the runtime message paths that tolerate a gated-out endpoint; gates and persistence (no
dormancy); the four wire fields (`services[].includedWhen`, the binding `IncludedWhen` annotation,
`schema.readOnly`, `runtime.instantiationParameter` + `runtime.default`); and DALE043/DALE044.
`docs/specs/emission.md` and `docs/specs/plugin-loading.md` are untouched. `_invariants.md` gains
nothing: the one cross-cutting candidate — fail-closed at bind, fail-open in the editor — is already
stated in `sdk-surface-conventions.md` § 7 and is cited, not restated.

### Consumer-visible change — relay this in the PR body

**1. DALE043 now reaches component types.** `[IncludedWhen]` on a type that is not a logic block, or
on any member of one, was accepted silently and did nothing; it is now an **error**-severity DALE043
naming the misplacement, matching what DALE044 has always done for a misplaced
`[InstantiationParameter]`. This can redden a consumer build on the version bump. Swept before
landing: `logic-block-libraries` — the SDK's first consumer, 56 gate and parameter sites across three
blocks — declares **no** such usage, so nothing there moves.

**2. A runtime write to an `[InstantiationParameter]` is now refused.** A `SetServicePropertyValue`
naming a parameter used to be applied, leaving the block reporting a value its bound member set did
not match; it is now refused and answered with the unchanged value. The attribute's documented
contract ("never settable at runtime") is unchanged — the code now matches it. A consumer whose
tooling wrote a parameter at runtime was already relying on undefined behavior and will now see the
value stay put.

**3. A second configuration of a live block is now refused.** Re-configuring in place was accepted
and applied additively — a gate flipping closed left its member bound. The documented contract is
that changing a parameter re-activates and recycles the block, so the second configuration now throws
naming that contract. **Relay to the `dale` runtime repo before the next bump:** confirm that no path
re-sends `InitializeLogicBlock` to a live actor. Nothing in this repo does; the runtime's
`LogicSystemConfigurationInitializer` is the one caller this pass cannot read.

**4. `dotnet pack` now refuses a block whose gate could never work.** A predicate that does not parse,
or that names something the block does not declare as an `[InstantiationParameter]`, fails the
introspection the pack target runs — so `dale build` / `dotnet pack` refuse the artifact instead of
shipping one that fails every activation. DALE043 already refused these at build time, so a library
that builds clean is unaffected; a library that suppressed the diagnostic, or that never wired the
analyzer pack, now fails at pack with the member and the predicate named. Sweep before the bump:
`logic-block-libraries` builds with the pack and carries no suppression.

**5. A topology may now carry `null` for a nullable instantiation parameter.** The schema typed the
values `boolean | integer | string`, so the JSON null the decode path already accepts was unreachable
from a topology file. Widening is compatible in the direction that matters — the dashboard validates
only its own output against its vendored copy, and dale-sdk is the canonical owner — so that copy can
be refreshed at leisure.

### Decisions

- `D1` — The area's anchors are the **two attributes** (A1), the **two DALE descriptors and the
  fifteen rules they carry** (A2), the **four gating fields the introspection JSON emits** (A5, read
  out of `Vion.Dale.LogicBlockParser` over a probe plugin this session), and the **PublicApi manifest
  entries** (A6, exactly two). The gate model under `Configuration/` is `internal` except
  `BindingMode`, so every row is stated as behavior of *a declared member*, never of `InclusionGate`.
- `D2` — The 26 analyzer tests in `Vion.Dale.Sdk.Generators.Test` (`IncludedWhenPredicateAnalyzerTests`,
  `InstantiationParameterAnalyzerTests`) live in the `ANLZ` area's project. They are **mapped, not
  rewritten** (the pilot's D3, the EMIT pass's D3). Row 61's fix adds a test there; the rest of that
  project is untouched.
- `D3` — The TestKit's `WithInstantiationParameter` is in scope as **gating semantics**: it is the
  only supported way a test drives the parameter channel, and it exercises the same encode/decode
  path the config payload uses (row 53). Its ergonomics — the selector overload, the builder's own
  validation — are `TKIT`'s and are recorded out of scope in A8.
- `D4` — `examples/Vion.Examples.Gating` is **Tier C**: its 16 xunit cases are not rewritten and cite
  no ids. It is read as the consumer lens (rows 24–27, 53); its README is corrected where row 67
  falsifies it, which is a docs edit, not a test rewrite.
- `D5` — `Vion.Dale.DevHost.Web/wwwroot/wiring.js` is Tier C (SPA internals, `spec-process.md`
  § Corpus). Its gate handling is walked as the operator lens and recorded in row 57's evidence, but
  it declares no criteria and is not rewritten.

### Reviewer's questions

1. **(propose-and-wait)** Row 60 is the pass's sharpest finding: a `SetServicePropertyValue` reaching
   a running block is applied to an `[InstantiationParameter]` property like any other — verified this
   session (`Count` 2 → 3 after `Build()`). The attribute's own XML doc says the opposite twice
   ("never settable at runtime", "every SetPropertyValue is rejected"), and DALE044 spends two of its
   rules — auto-property, no in-code assignment — enforcing exactly the read-back honesty this breaks:
   after the write the block reports a parameter value its bound member set does not match. The SDK's
   general posture is that `readOnly` is a wire flag the cloud enforces, so the options are (a) add a
   parameter guard in `HandleSetServicePropertyValueRequest` and reject the write, or (b) leave the
   behavior and correct the two doc sentences. My recommendation: **(a)** — a parameter is the one
   `readOnly` whose value is load-bearing for structure, and the guard is one site.
2. **(propose-and-wait)** Row 61: `[IncludedWhen]` on a member of a **component** type gets no
   diagnostic and is silently ignored at bind — confirmed with a positive control in the same
   compilation (the identical mistake on the block reports DALE043; on the component, nothing).
   `InstantiationParameterAnalyzer` already walks every class and reports DALE044 for the mirror-image
   misplacement, so the fix is to give `IncludedWhenPredicateAnalyzer` the same reach. Recommendation:
   `fix`. It widens what an existing **error**-severity rule reports, which can redden a consumer
   build on the next version bump — say the word if that makes it a `park` instead.
3. **(propose-and-wait)** Row 65: a second `InitializeLogicBlock` on an already-configured block is
   accepted, re-applies the parameters and re-runs the binders over the **same** `ServiceBinder` — so a
   gate that flips *open* adds its member and a gate that flips *closed* leaves the old member bound
   (both verified this session). Nothing in this repo sends a second one; the documented contract is
   that changing a parameter re-activates and recycles the block. Options: (a) refuse a second
   configuration, naming the recycle contract in the exception, or (b) state the additive behavior as
   intended. Recommendation: **(a)** — a half-applied reconfiguration is the silent-wrongness class
   this repo fixes rather than documents, and refusing is a guard plus a test.
4. **(decide-and-document)** Row 66: a DevHost topology naming an unresolvable parameter identifier,
   or a value that will not decode, reports **"Development host started successfully"** and leaves the
   block uninitialized — the initialize throw happens inside the actor, after `SendTo` returned, so
   `DevLogicSystemInitializer`'s `catch` at `:435` never sees it. The operator sees a block whose
   properties all read null and a service list computed from the *defaults*, which disagrees with the
   nothing the block actually bound. The cheap area-local half is to resolve each topology
   `instantiationParameters` key against the block type's `[InstantiationParameter]` properties in
   `DevTopologyLoader`, alongside the `mappingErrors` collect-then-report already there — that catches
   the typo case at load / validate / save, where the operator is. The decode case, and the general
   "an actor failed to initialize but the host says started" gap, are bigger and belong to `CTRL`.
   **My call unless overruled: fix the loader half, park the rest.**
5. **(decide-and-document)** Rows 62 and 64 are one family: predicate strings the runtime refuses that
   the definition view passes through unchecked. `[IncludedWhen("")]` is reported as *ungated* in the
   introspection JSON (`string.IsNullOrEmpty` at three sites) and then fails every bind; an
   unparseable or unresolvable predicate is emitted verbatim to the wire and fails every bind. DALE043
   is the compile-time half, but it is an analyzer — suppressible, and absent from any project that
   does not wire the pack (`sdk-surface-conventions.md` § 5, and probe 3 below hit exactly that).
   Row 62 (the empty string) is a three-site inconsistency and I take it as a `fix`; row 64
   (parse-checking in `Definition` mode) would make `dotnet pack` fail on a block that is broken
   anyway, which is a better failure but a behavior change to the pack path — recorded as `park`
   unless you want it in.
6. **(ratified — cite)** RFCs to absorb: **one** — `docs/rfcs/0016-config-time-structural-gating.md`,
   per the brief. Deleted in Phase B with the reference sweep. `0017` and `0019` stay; the
   presentation-visibility half of `0017` is `INTRO`'s and no row of this pass needs it.
7. **(decide-and-document)** `BindingMode` is `public` and unmarked, so it is absent from
   `docs/snapshots/publicapi-manifest.json` and from the generated API reference —
   `Vion.Dale.Sdk.Configuration` declares no `[assembly: PublicApiNamespace]`, so DALE014 never judged
   it. Unlike the EMIT pass's `IChangeThreshold<T>`, no consumer implements or names it: `Configure`
   is `internal`, and the only public surface exposing it is `ILogicBlockConfigurationBuilder.Mode`,
   itself unmarked. Recommendation: leave both as they are and state the split in the page rather than
   widen the documented surface (`sdk-surface-conventions.md` D1). Marking `[InternalApi]` would be
   tidier but arms DALE014 over the whole namespace, which is `BIND`'s pass to absorb.

---

## Full design

### Anchor inventory (step 2 — the completeness checklist for the sweep)

Enumerated mechanically this session, not recalled.

**A1 — the two gating attributes.** Both `[PublicApi]`, both in `Vion.Dale.Sdk/Core/`:

| Attribute | Targets | Parameters | Site |
| --- | --- | --- | --- |
| `InstantiationParameterAttribute` | `Property` | **none** — a bare marker; every knob it modifies (`Title`, `Minimum`, `Maximum`, `WriteOnly`) is read off the paired `[ServiceProperty]` | `InstantiationParameterAttribute.cs:74`–`:78` |
| `IncludedWhenAttribute` | `Property \| Class \| Method` | **one** — `Predicate` (constructor-positional, `string`) | `IncludedWhenAttribute.cs:80`–`:98` |

`IncludedWhenAttribute`'s `AttributeUsage` admits `Class` and `Method`, which the analyzer then
rejects — the attribute is deliberately appliable where it is invalid so DALE043 can name the mistake
instead of the compiler saying "not valid on this declaration type" (rows 44–45).

**A2 — the gating diagnostics.** **Two** descriptors, **two** analyzers, **fifteen** distinct rules:

| Id | Severity | Descriptor | Analyzer(s) | Rules | Tests |
| --- | --- | --- | --- | --- | --- |
| DALE043 | Error | `DaleDiagnostics.cs:540` | `IncludedWhenPredicateAnalyzer.cs` | 6 (class placement `:52`; method placement `:63`; not gateable `:98`; re-gated override/new `:109`; predicate parse `:121`; reference resolution `:135`) | 10 methods + 4 rows |
| DALE044 | Error | `DaleDiagnostics.cs:557` | `IncludedWhenPredicateAnalyzer.cs` (2) + `InstantiationParameterAnalyzer.cs` (7) | 9 (predicate literal type `:126`; predicate type check `:140`; not on a block `:67`; missing `[ServiceProperty]` `:73`; `WriteOnly` `:77`; disallowed type `:83`; computed getter `:90`; re-declared override/new `:95`; assignment outside constructor `:104`) | 16 methods |

Unprefixed analyzer line references are to the analyzer named in the same row.

**A3 — the gate model under `Vion.Dale.Sdk/Configuration/`.** One public type; the rest `internal`
or plumbing on already-public types:

| Member | Visibility | Site |
| --- | --- | --- |
| `BindingMode` (`Definition`, `Live`) | **public** (unmarked — see Q7) | `BindingMode.cs:7`, `:15`, `:24` |
| `InclusionGate.ReadPredicate` / `.IsIncluded` / `.BuildParameterContext` | internal | `InclusionGate.cs:22`, `:35`, `:53` |
| `ILogicBlockConfigurationBuilder.Mode` / `LogicBlockConfigurationBuilder.Mode` | public (unmarked) | `ILogicBlockConfigurationBuilder.cs:22`, `LogicBlockConfigurationBuilder.cs:51` |
| `ContractMetaData.IncludedWhen` / `FunctionInterfaceMetaData.IncludedWhen` | public | `ContractMetaData.cs:20`, `FunctionInterfaceMetaData.cs:20` |
| `LogicInterfaceExtensions.WithIncludedWhen` | public | `LogicInterfaceExtensions.cs:30` |
| `ServiceBinder.RegisterServiceIncludedWhen` / `.GetServiceIncludedWhen` | public | `ServiceBinder.cs:123`, `:132` |

**A4 — the gate resolution sites.** One per declarative binder, each the same two-line shape
(`ReadPredicate`, then `IsIncluded` with `continue` on false):

| Binder | Gate site | What is skipped |
| --- | --- | --- |
| `DeclarativeInterfaceBinder` | `:84`–`:88` | a property-based interface binding |
| `DeclarativeContractBinder` | `:33`–`:37` | a contract binding (property left null) |
| `DeclarativeServiceBinder` | `:58`–`:62` | a service-bearing component's whole service |

All three are reached from `LogicBlockBase.Configure` (`:545` builds the context, `:552`–`:554` calls
them). A fourth and fifth reader are in `PersistentData` (`:197`, `:237`), which re-resolves the
gates rather than inferring the included set from binder keys.

**A5 — the wire fields**, read out of `Vion.Dale.LogicBlockParser` over a probe plugin this session
(see *The emitted shape*): **four**.

| Field | Producer | Shape |
| --- | --- | --- |
| `services[].includedWhen` | `LogicBlockIntrospection.cs:288` ← `ServiceBinder.cs:132` | predicate string; `null` when ungated |
| binding `annotations["IncludedWhen"]` | `ContractMetaData.cs:43`, `FunctionInterfaceMetaData.cs:43` | predicate string; key absent when ungated |
| `properties[].schema.readOnly` | `PropertyMetadataBuilder.cs:265` | forced `true` for a parameter |
| `properties[].runtime.instantiationParameter` + `.default` | `LogicBlockIntrospection.cs:612`–`:625` | `true` plus the default instance's JSON-scalar value |

**A6 — PublicApi manifest**: 12 assemblies, **119 types**. **Two** entries in this area —
`Vion.Dale.Sdk.Core.IncludedWhenAttribute` and `Vion.Dale.Sdk.Core.InstantiationParameterAttribute`.
`BindingMode` is public but unmarked, so it is absent (Q7); `Vion.Dale.Sdk.Configuration` declares no
`[assembly: PublicApiNamespace]` (`PublicApiConfig.cs:6`–`:8` declares `.Core`, `.Emission`, `.Utils`
only), which is why DALE014 never fired on it.

**A7 — the config message**: `InitializeLogicBlock.InstantiationParameterValues`
(`Messages/ActorMessages.cs:62`), an optional `List<SetLogicConfigurationPayload.InstantiationParameterValue>`
from `Vion.Contracts`. It is the **only** channel — nothing else in `ActorMessages.cs` mentions either
attribute.

**A8 — the TestKit parameter surface**: `LogicBlockTestContextBuilder.WithInstantiationParameter`
(`:129`–`:139`) with its list (`:33`), passed at `:266`. The builder resolves the selector to a
`PropertyInfo` and encodes through `PropertyValueCodec.ClrToJson`; it does **not** check that the
property carries `[InstantiationParameter]`, so a selector naming an ordinary property compiles and
fails later inside `ApplyInstantiationParameters`. Test ergonomics — recorded out of scope (`TKIT`, D3).

**A9 — in-repo consumers walked** (step 4):

| Consumer | Site | Tier |
| --- | --- | --- |
| DevHost topology file + schema | `DevTopologyFile.cs:273`, `topology.schema.json:31`–`:35` | A (`SCEN`/`CTRL` own the file; the field is GATE's) |
| DevHost topology loader | `DevTopologyLoader.cs:79`–`:82` | A |
| DevHost initializer | `DevLogicSystemInitializer.cs:420`–`:430` | A |
| DevHost live view | `DevHostIntrospection.cs:456`, `:533`–`:587` | A |
| DevHost block catalog | `LogicBlockDefinition.cs:35`, `:60`, `:72`, `:89`–`:164`, `:218`, `:229`, `:236` | A |
| DevHost configuration output | `ConfigurationOutput.cs:65` | A |
| DevHost catalog reflection | `DevConfigurationBuilder.cs:315`, `:352` | A |
| DevHost SPA wiring guard | `wiring.js:113`–`:190` | **C** (D5) |
| CLI `dale list` | `ListCommand.cs:83`, `CliListOutput.cs:45`, `DalePluginInfo.cs:69` | B (`CLI`) |
| `examples/Vion.Examples.Gating` | `ChargingStationBlock.cs:95`–`:141`, `README.md` | **C** (D4) |
| `logic-block-libraries` (read-only add-dir, hints only) | 3 blocks, 56 gate/parameter sites; every parameter written `{ get; init; }` | — |

**A10 — persistence interaction** (`Vion.Dale.Sdk/Persistence/`, read for rows, owns nothing):
`PersistentData.cs:165` (parameter exclusion), `:197` and `:237` (gate re-resolution), `:325`
(unknown-key fall-through).

**Out of scope, recorded so the checklist is complete.** `Introspection/StructFieldPresentationBuilder.cs`
touches gating only in a doc cross-reference (`:27`) — a parameter cannot be a struct (DALE044), so
struct-field metadata carries no gating field at all. `Introspection/TypeRefBuilder.cs` is schema
shape (`INTRO`); `Introspection/MockActorContext.cs` is the definition-mode stand-in (`INTRO`).

### Behavior table (steps 3 and 4)

Walked statement by statement over the two attributes, `Configuration/InclusionGate.cs`,
`Configuration/BindingMode.cs`, the three declarative binders' gate paths, the config-time paths of
`Core/LogicBlockBase.cs` (`:169`–`:265`, `:266`–`:290`, `:366`–`:382`, `:539`–`:598`, `:900`–`:911`),
`Messages/ActorMessages.cs:48`–`:62`, `Persistence/PersistentData.cs:144`–`:330`,
`Introspection/LogicBlockIntrospection.cs:100`–`:120`, `:280`–`:295`, `:390`–`:475`, `:604`–`:626`,
`Introspection/PropertyMetadataBuilder.cs:36`–`:80`, `:215`–`:270`, the two gating analyzers, the
TestKit's parameter surface, and the DevHost consumer sites in A9. Unprefixed line references are to
the file named in the section heading. Rows marked **(probe)** were confirmed by running the behavior
this session — see *What the probes ran*.

#### The parameter value channel — `Core/LogicBlockBase.cs`

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 1 | WHEN a logic block is configured THE SYSTEM SHALL apply the operator-chosen `[InstantiationParameter]` values to their properties before `Configure` runs. | `:190`–`:192`, then `:194` | `ConfigTimeStructuralGatingShould.ApplyParameterValuesBeforeConfigureSoGatesResolve` | intended | the gates the binders resolve at `:552`–`:554` read these properties; applying after would resolve every gate against the C# default |
| 2 | WHEN no value is supplied for a declared parameter THE SYSTEM SHALL leave the property at its C# initializer default. | `:570`–`:573` (a null or empty list returns before the loop) | `InstantiationParameterGatingShould.ResolveAgainstTheCSharpDefaultWhenNoParameterIsSupplied` | intended | a block added with no parameter choices must still bind its default shape |
| 3 | WHEN a supplied parameter identifier names no `[InstantiationParameter]` property of the block THE SYSTEM SHALL fail block initialization with an `InvalidOperationException` naming the identifier and the block type. | `:578`–`:583` | `ConfigTimeStructuralGatingShould.FailClosedOnAnUnknownParameterIdentifier` | intended | resolving gates against a value the operator believes they set is worse than not starting |
| 4 | THE SYSTEM SHALL match a supplied parameter identifier to the property name case-sensitively. | `:578` (`GetProperty(name, Public \| Instance)`, no `IgnoreCase`); **(probe)** `count` for `Count` produces row 3's failure | GAP | intended | parameter identifiers are the cloud's translation keys (`identifier-stability.md`); a case-insensitive match would make two spellings interchangeable on the wire |
| 5 | WHEN a supplied parameter value cannot be decoded into the property's type THE SYSTEM SHALL fail block initialization with an `InvalidOperationException` naming the identifier, the block type and the decode error, and SHALL carry the decode failure as its inner exception. | `:585`–`:595` | `ConfigTimeStructuralGatingShould.FailClosedOnAParameterDecodeError` | intended | same fail-closed posture as row 3; the inner exception is what tells the author which of the six decode failures they hit |
| 6 | THE SYSTEM SHALL decode a parameter value from the shared JSON-scalar form — an enum from its member-name string, an integer kind from a JSON number — and SHALL refuse a value of any other JSON shape. | `:587`–`:589` (`TypeRefBuilder.BuildForProperty`, then `PropertyValueCodec.JsonToClr`); **(probe)** a float, a numeric string and an enum ordinal are each refused | GAP | intended | one encoding across cloud, dashboard, topology file and TestKit; a tolerant decoder here is where those four drift apart |
| 7 | WHEN a supplied enum parameter value names no member of the declared enum THE SYSTEM SHALL fail block initialization. | `:589`; **(probe)** `"Ristretto"` against a two-member enum → `Unknown enum member` | GAP | intended | a stale config naming a retired member must not fall silently to the zero member, which is a different gate outcome |
| 8 | WHEN a supplied integer parameter value is outside the declared property type's range THE SYSTEM SHALL fail block initialization. | `:589`; **(probe)** `2^40` into an `int` → `out of range for int` | GAP | intended | the wire carries integers 64-bit-backed, so the narrowing has to be checked somewhere |
| 9 | WHEN a supplied value is JSON null and the parameter's declared type is not nullable THE SYSTEM SHALL fail block initialization, and WHEN it is nullable THE SYSTEM SHALL set the property to null. | `:589`; **(probe)** `int` refuses, `int?` and `string?` accept | GAP | intended | the nullable half is what makes row 20's fail-closed gate reachable rather than theoretical |
| 10 | THE SYSTEM SHALL apply a parameter value whatever the accessor shape of the property, including an `init`-only one. | `:597` (`PropertyInfo.SetValue`; `init` is a compile-time restriction, not a reflection one), recorded at `InstantiationParameterAttribute.cs:57`–`:58` | GAP | intended | `{ get; init; }` is the shape the docs recommend and the shape the first consumer writes 56 times |
| 11 | THE SYSTEM SHALL apply the values in the order the configuration supplies them, so a repeated identifier ends at its last value. | `:576`–`:597` (a `foreach` with no duplicate check); **(probe)** `Count` 3 then 2 → 2 | GAP | intended | both producers are maps — `topology.schema.json:31` an object, the cloud payload a dictionary — so no supported producer can emit a duplicate |
| 12 | THE SYSTEM SHALL apply a parameter declared on a base logic-block class to the derived instance. | `:578` (`GetProperty` walks the hierarchy); `InclusionGate.cs:56` (`GetProperties(Public \| Instance)`) does the same for the gate context | GAP at runtime — the authoring half is `IncludedWhenPredicateAnalyzerTests.GateAndParameterDeclaredOnABaseClass_ResolveOnTheLeaf_NoDiagnostic` | intended | a block family declares its parameters once, the way it already declares its schema |

#### Gate evaluation — `Configuration/InclusionGate.cs`

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 13 | THE SYSTEM SHALL treat a member carrying no `[IncludedWhen]` as part of every configured instance. | `:22` (`ReadPredicate` returns null), `:37`–`:40` | `ConfigTimeStructuralGatingShould.BindOnlyIncludedComponentServices_ForACountParameter` (its `Point1` assertion) | intended | gating is opt-in; every block written before it stays byte-identical |
| 14 | WHEN a member carries `[IncludedWhen]` THE SYSTEM SHALL include it in the configured instance only while its predicate evaluates true against that instance's parameter values. | `:35`–`:43`, reached from `LogicBlockBase.cs:545` | `ConfigTimeStructuralGatingShould.BindOnlyIncludedComponentServices_ForACountParameter` | intended | the whole contract of the attribute |
| 15 | THE SYSTEM SHALL build the predicate's evaluation context from the block's `[InstantiationParameter]` properties and nothing else. | `:56`–`:66` (the attribute filter is the only membership test) | GAP — the authoring half is `IncludedWhenPredicateAnalyzerTests.ReferenceToANonParameterProperty_ReportsDALE043` | intended | a gate that could read an ordinary service property would be runtime-reactive structure, which is the thing this design deliberately is not |
| 16 | THE SYSTEM SHALL encode each parameter into that context in the shared JSON-scalar form, so an enum parameter compares against its member name and an integer against a number. | `:68` (`PropertyValueCodec.ClrToJson` over `TypeRefBuilder.BuildForProperty`) | `ConfigTimeStructuralGatingShould.ResolveAGatedEnumViaTheJsonMemberNameContext` | intended | an `(int)` cast or a `ToString()` here mis-resolves every enum membership predicate while the shared conformance vector still passes |
| 17 | THE SYSTEM SHALL read each parameter's current value when it builds that context, so the applied configuration value is what the gates see and an unsupplied parameter contributes its C# default. | `:64`, ordered after `LogicBlockBase.cs:192` | `InstantiationParameterGatingShould.ApplyTheParameterValueThroughTheBuilderAndResolveGates` | intended | the pairing that makes row 1's ordering observable |
| 18 | WHEN a gate's predicate does not parse THE SYSTEM SHALL fail block initialization with a `PredicateSyntaxException`. | `:42` (`Predicate.Parse`); **(probe)** `Count >>> 2` → `expected a literal` | GAP | intended | fail-closed: a block whose shape is undecidable must not bind an arbitrary half of itself |
| 19 | WHEN a gate's predicate references a name the parameter context does not carry, or a value of the wrong type, THE SYSTEM SHALL fail block initialization with a `PredicateEvaluationException`. | `:42` (`Evaluate`, strict profile); **(probe)** `Missing >= 2` → `reference … is not present in the evaluation context` | GAP | intended | same posture as row 18, and the runtime half of DALE043's reference rule |
| 20 | WHEN a gate's predicate references a parameter whose value is null THE SYSTEM SHALL fail block initialization. | `:68` (a null value passes through as JSON null rather than through the non-nullable codec path), then `:42` | `ConfigTimeStructuralGatingShould.FailClosedAtBindWhenAGateEvaluatesAgainstANullParameter` | intended | a null-defaulted string parameter is the one shape where "no value supplied" and "value supplied as null" are indistinguishable |

#### The two binding modes — `Configuration/BindingMode.cs`

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 21 | WHEN a block is introspected THE SYSTEM SHALL bind its full declared member set regardless of gates and record each gated member's predicate. | `LogicBlockIntrospection.cs:115` (`BindingMode.Definition`), `InclusionGate.cs:37`–`:40` | `ConfigTimeStructuralGatingShould.EmitTheFullMemberSetWithPredicatesInDefinitionMode` | intended | the packed artifact has no configuration; it must describe every shape the block can take |
| 22 | THE SYSTEM SHALL evaluate no predicate while binding the definition set. | `InclusionGate.cs:37`–`:40` (the mode check short-circuits ahead of `Parse`); **(probe)** an unparseable predicate introspects without throwing | GAP | intended | the default instance's parameter values are not the operator's, so evaluating there would answer a question nobody asked — row 64 is what that costs |
| 23 | WHEN a block is configured for a running instance THE SYSTEM SHALL evaluate every gate and bind only the included members. | `LogicBlockBase.cs:202` (`BindingMode.Live`), `:545`, `:552`–`:554` | `ConfigTimeStructuralGatingShould.BindOnlyIncludedComponentServices_ForACountParameter` | intended | the live half of the split; the pair is the seam the whole design turns on |

#### What a gate removes — the three declarative binders

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 24 | WHEN a gated property-based interface binding is excluded THE SYSTEM SHALL not bind it, so the endpoint is absent from the block's interfaces. | `DeclarativeInterfaceBinder.cs:84`–`:88` | `ConfigTimeStructuralGatingShould.SkipAMessageTargetingAGatedOutInterfaceInsteadOfThrowing` (drives the absence) | intended | an excluded endpoint that still bound would be wireable and would publish |
| 25 | WHEN a gated contract binding is excluded THE SYSTEM SHALL leave the property null and never construct the contract. | `DeclarativeContractBinder.cs:33`–`:37` (the skip precedes `contractFactory.Create` at `:42`) | `ConfigTimeStructuralGatingShould.LeaveAGatedOutContractPropertyNull` | intended | the documented authoring hazard — a gated contract property must be declared nullable and its fan-out null-guarded |
| 26 | WHEN a gated service-bearing component is excluded THE SYSTEM SHALL bind neither its service properties nor its measuring points. | `DeclarativeServiceBinder.cs:58`–`:62` (the skip precedes `BindServiceWithInterfaces` at `:71`) | `ConfigTimeStructuralGatingShould.BindOnlyIncludedComponentServices_ForACountParameter` (its helper unions both binding maps) | intended | gating the property that holds a component gates the whole service the binders derive from it |
| 27 | THE SYSTEM SHALL leave an excluded component instance itself untouched, so the author's own object stays non-null and reachable from block code. | `DeclarativeServiceBinder.cs:59`–`:62` and `DeclarativeInterfaceBinder.cs:85`–`:88` skip the binding, never the object; `IncludedWhenAttribute.cs:60`–`:64` states the contrast with a contract | GAP | intended | the showcase's timer calls `Point2.Sample()` unconditionally (`ChargingStationBlock.cs:150`–`:152`) and depends on exactly this |
| 28 | THE SYSTEM SHALL bind the block's own root service unconditionally. | `DeclarativeServiceBinder.cs:16`–`:21` (created before the gated loop at `:40`) | GAP | intended | whole-block existence is the operator adding the instance or not — and a gate there would have no parameter to read, because the parameters live on that service |
| 29 | THE SYSTEM SHALL bind a class-implemented interface unconditionally. | `DeclarativeInterfaceBinder.cs:22`–`:24`, `:35`–`:57` (neither the mode nor the context reaches it) | GAP at runtime — the authoring half is `IncludedWhenPredicateAnalyzerTests.GateOnTheBlockClass_ReportsDALE043` | intended | there is no member to carry the gate; an author who needs one converts to a property-based binding |
| 30 | WHEN a gated service-bearing component property's value is null THE SYSTEM SHALL omit the member from the definition view entirely, reporting neither the member nor its gate. | `DeclarativeServiceBinder.cs:64`–`:68` (the null check runs in both modes, ahead of the registration at `:73`–`:76`); **(probe)** a block whose gated component is null introspects to its root service alone | GAP | intended | the definition view enumerates a component's members off the *instance*, and there is nothing to describe for a null one — stated so an author knows the component must exist by the time `Configure` runs |

#### Gates and the runtime message paths — `Core/LogicBlockBase.cs`

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 31 | WHEN a configuration maps a contract that this instance's gates excluded THE SYSTEM SHALL skip the mapping and keep the block running. | `:210`–`:226` | `ConfigTimeStructuralGatingShould.SkipAndWarnWhenAContractMappingTargetsAGatedOutContract` | intended | a stale or hand-built payload must not take a whole block down with a `KeyNotFoundException` |
| 32 | WHEN a configuration links an interface that this instance's gates excluded THE SYSTEM SHALL skip the link and keep the block running. | `:266`–`:284` | GAP | intended | the sender-side mirror of row 31; a topology lowered from three points to two still carries the link |
| 33 | WHEN a message is routed to an interface that this instance's gates excluded THE SYSTEM SHALL drop the message and keep the block running. | `:366`–`:381` | `ConfigTimeStructuralGatingShould.SkipAMessageTargetingAGatedOutInterfaceInsteadOfThrowing` | intended | the receiver-side mirror — and the behavior row 67's doc still denies |

#### Gates and persistence — `Persistence/PersistentData.cs`

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 34 | THE SYSTEM SHALL exclude every `[InstantiationParameter]` from persistence discovery. | `:161`–`:168` (a dedicated skip; discovery keys off the public setter a parameter deliberately has) | `ConfigTimeStructuralGatingShould.ExcludeInstantiationParametersFromPersistenceDiscovery` | intended | the config channel is a parameter's only source of truth; a persisted value restored after the gates resolved would contradict the shape already bound |
| 35 | THE SYSTEM SHALL capture the `[Persistent]` members of an included component and skip those of an excluded one, re-resolving the gates against the same parameter values the configuration used. | `:197`, `:236`–`:240` | `ConfigTimeStructuralGatingShould.NotCaptureAGatedOutComponentsPersistentMembers`, `.CaptureAnIncludedInterfaceOnlyComponentsPersistentMembers`, `.NotCaptureAnExcludedInterfaceOnlyComponentsPersistentMembers` | intended | inferring the included set from binder keys instead would miss an included interface-only component, which has no service-binding key |
| 36 | WHEN a later configuration excludes a component a previous one included THE SYSTEM SHALL stop capturing that component's persistent state. | `:236`–`:240` | `ConfigTimeStructuralGatingShould.DiscardAGatedOutMembersPersistentStateOnExclusion` | intended | no dormancy — capturing an inert object's members would write entries no restore can use |
| 37 | WHEN restoring persistent data that names a member this instance's gates exclude THE SYSTEM SHALL leave the block's state unchanged and complete the restore. | `:298`–`:326` (neither metadata map carries the key, so the method falls through at `:325`) | GAP | intended | what makes row 36 safe: one persistence file survives a gate flip in either direction |

#### The wire — `Introspection/`

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 38 | THE SYSTEM SHALL report a gated component service's predicate on that service, and report null for an ungated one. | `LogicBlockIntrospection.cs:288` ← `ServiceBinder.cs:123`, `:132`; packed artifact `services[].includedWhen` | `ConfigTimeStructuralGatingShould.EmitTheFullMemberSetWithPredicatesInDefinitionMode` | intended | the field every downstream live-view resolver reads |
| 39 | THE SYSTEM SHALL report a gated contract binding's and a gated interface binding's predicate as an annotation on that binding. | `ContractMetaData.cs:43`–`:45`, `FunctionInterfaceMetaData.cs:43`–`:45`; **(probe)** `annotations: IncludedWhen=Count >= 2` on the gated contract | `LogicBlockIntrospectionShould.ReportADevelopmentOnlyContractEvenWhenItsBindingIsGated` (contract half); interface half GAP | intended | bindings have no first-class field for it, and the annotation bag is the passthrough both consumers already read |
| 40 | THE SYSTEM SHALL report an `[InstantiationParameter]` property as read-only in its schema whatever its C# accessors are. | `PropertyMetadataBuilder.cs:265`; packed artifact `schema.readOnly: true` on all four probe parameters | `ConfigTimeStructuralGatingShould.EmitTheParameterRuntimeMarkerDefaultAndReadOnly` | intended | the parameter deliberately has a public setter so the SDK can apply the value; the forced flag is what stops the dashboard offering it as state |
| 41 | THE SYSTEM SHALL report an `[InstantiationParameter]` property with a runtime marker and the declaring block's default value, JSON-scalar-encoded. | `LogicBlockIntrospection.cs:612`–`:625`, reached from all four binding paths (`:404`, `:427`, `:445`, `:468`); packed artifact `runtime.instantiationParameter` + `runtime.default` | `ConfigTimeStructuralGatingShould.EmitTheParameterRuntimeMarkerDefaultAndReadOnly`, `.EmitTheEnumParameterDefaultAsItsMemberName` | intended | a client rendering the parameter editor needs the default to prefill, and to resolve gates for an instance that overrides nothing |
| 42 | THE SYSTEM SHALL report a parameter whose declared default is null with a null default. | `LogicBlockIntrospection.cs:623`; packed artifact (`int?` with no initializer → `"default": null`) | GAP | intended | the wire has no separate "unknown" sentinel, so a consumer reading null as unknown falls open — which matches row 57's stated editor posture |
| 43 | THE SYSTEM SHALL publish the `Minimum` and `Maximum` declared on a parameter's paired `[ServiceProperty]` as its editor bounds, and SHALL apply a supplied value outside them. | `LogicBlockDefinition.cs:145`–`:155` and `PropertyMetadataBuilder.cs:230`–`:254` (bounds out) against `LogicBlockBase.cs:585`–`:597` (no bounds check in); **(probe)** `Count = 99` and `Count = -1` on a `Minimum = 1, Maximum = 3` parameter both apply | GAP | intended | bounds are presentation everywhere in this SDK, never validation — stated here because a *structural* parameter is where an author is most likely to assume otherwise |

#### Authoring diagnostics — DALE043 and DALE044

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 44 | WHEN `[IncludedWhen]` is applied to a logic-block class THE SYSTEM SHALL report DALE043. | `IncludedWhenPredicateAnalyzer.cs:52`–`:60` | `IncludedWhenPredicateAnalyzerTests.GateOnTheBlockClass_ReportsDALE043` | intended | whole-block existence is the operator adding the instance; row 29 is the runtime half |
| 45 | WHEN `[IncludedWhen]` is applied to a method THE SYSTEM SHALL report DALE043. | `:63`–`:74` | `.GateOnATimerMethod_ReportsDALE043` | intended | a `[Timer]` is not in the definition view, so there is nothing for a gate to remove |
| 46 | WHEN `[IncludedWhen]` is applied to a member that is neither a property-based interface binding, a contract binding, nor a service-bearing component THE SYSTEM SHALL report DALE043. | `:98`–`:107` with `:167`–`:182` | `.GateOnAScalarServiceProperty_ReportsDALE043`, `.GateOnAScalarMeasuringPoint_ReportsDALE043` | intended | a scalar keeps publishing whatever the gate says, and the diagnostic names `[Presentation(VisibleWhen = …)]` as the thing the author wanted |
| 47 | WHEN `[IncludedWhen]` or `[InstantiationParameter]` is re-declared on an `override` or `new` member THE SYSTEM SHALL report a diagnostic. | `IncludedWhenPredicateAnalyzer.cs:109`–`:118` with `:196`–`:219` (DALE043); `InstantiationParameterAnalyzer.cs:95`–`:100` with `:156`–`:179` (DALE044) | `.RegatedOverride_ReportsDALE043`, `.RegatedNewShadow_ReportsDALE043`, `InstantiationParameterAnalyzerTests.RedeclaredOnAnOverride_ReportsDALE044` | intended | both runtime readers key by property **name** (`InclusionGate.cs:68`, `LogicBlockBase.cs:578`), so two declarations of one name is an ambiguity neither can resolve |
| 48 | WHEN an `[IncludedWhen]` predicate does not parse, is qualified, or references a property that is not an `[InstantiationParameter]` of the same block THE SYSTEM SHALL report DALE043. | `:121`–`:133`, `:135`–`:147`, `:28`–`:29` (the sentinel service id no qualified first segment can name) | `.PredicateOutsideTheGrammar_ReportsDALE043`, `.QualifiedReference_ReportsDALE043`, `.ReferenceToANonParameterProperty_ReportsDALE043` | intended | rows 18–19 are the runtime half; the analyzer is what turns a deploy-time failure into a build error |
| 49 | WHEN an `[IncludedWhen]` predicate compares a parameter against a literal of another type, or names an enum member unquoted, THE SYSTEM SHALL report DALE044. | `:126`–`:129`, `:140`–`:143` | `.TypeMismatchInPredicate_ReportsDALE044`, `.UnquotedEnumMember_ReportsDALE044` | intended | an unquoted right-hand identifier is indistinguishable from a property reference, so the grammar cannot accept it |
| 50 | THE SYSTEM SHALL evaluate no `[IncludedWhen]` predicate at compile time. | `IncludedWhenPredicateAnalyzer.cs:15`–`:18` (parse and type-check only; no evaluator is referenced) | GAP | intended | the analyzer has no operator values, so evaluating the default instance's would report a shape nobody configured |
| 51 | WHEN an `[InstantiationParameter]` is declared on a type that is not a logic block, without a paired `[ServiceProperty]`, combined with `WriteOnly`, on a type outside bool / enum / an integer kind / string, with a computed getter, or is assigned in the declaring block's own code outside its constructor or an object initializer, THE SYSTEM SHALL report DALE044. | `InstantiationParameterAnalyzer.cs:67`–`:71`, `:73`–`:81`, `:83`–`:89`, `:90`–`:93`, `:104`–`:127` | `InstantiationParameterAnalyzerTests.DeclaredOnAComponentType_…`, `.MissingServicePropertyPairing_…`, `.WriteOnlyParameter_…`, `.DisallowedDoubleType_…`, `.DisallowedArrayType_…`, `.DisallowedStructType_…`, `.ComputedGetter_…`, `.InCodeAssignmentOutsideConstructor_…` | intended | six of the seven protect read-back honesty — the value block code reads must provably be the value the gates evaluated |
| 52 | THE SYSTEM SHALL accept an `[InstantiationParameter]` declared with a plain public setter as well as with `init`. | `InstantiationParameterAnalyzer.cs:90`–`:93` (only a computed getter is rejected); `InstantiationParameterAttribute.cs:58`–`:62` records that `init` is a recommendation, not the mechanism | `InstantiationParameterAnalyzerTests.PlainSetterParameter_NoDiagnostic` | intended | reflection application works with either, and `init` needs an `IsExternalInit` polyfill on `netstandard2.1` — the template ships one (`VionIotLibraryTemplate.csproj:26`), a hand-rolled project may not |

#### The consumer surfaces — TestKit, DevHost, CLI

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 53 | THE SYSTEM SHALL let a test supply an `[InstantiationParameter]` value that is applied through the same JSON encode and decode path a configuration uses. | `LogicBlockTestContextBuilder.cs:129`–`:139`, `:266` | `InstantiationParameterGatingShould.ApplyTheParameterValueThroughTheBuilderAndResolveGates` | intended | a test that set the property directly would skip the decode path rows 5–9 govern, and so would not be testing what ships |
| 54 | WHEN a topology names instantiation-parameter values for an instance THE SYSTEM SHALL apply them before that block's `Configure` and resolve its gates against them. | `DevTopologyLoader.cs:79`–`:82`, `DevConfiguration.cs:103`, `DevLogicSystemInitializer.cs:420`–`:430` | `TopologyFilesShould.CarryInstantiationParametersFromTopologyFileThroughBuild`, `.RoundTripInstantiationParametersThroughTheEditorSave`, `ConfigTimeGatingShould.ShowExactlyTheIncludedComponentServices_ForTheTopologyParameter` | intended | the DevHost is the local stand-in for the cloud's config channel |
| 55 | THE SYSTEM SHALL resolve a DevHost block's definition view down to the live view for its chosen parameter values, dropping every excluded service, interface binding and contract binding. | `DevHostIntrospection.cs:456`, `:533`–`:540` | `ConfigTimeGatingShould.ResolveTheLiveViewAgainstTheParameterValue` | intended | the operator must see the member set the running block bound, not the maximum the type declares |
| 56 | THE SYSTEM SHALL overlay a DevHost instance's chosen parameter values on each parameter's introspected default when resolving that live view. | `DevHostIntrospection.cs:542`–`:568` (defaults first, then the overlay) | GAP | intended | an instance that overrides one of three parameters still needs the other two to resolve its gates |
| 57 | WHERE an `[IncludedWhen]` predicate cannot be parsed or evaluated while resolving a DevHost live view THE SYSTEM SHALL leave the member visible. | `DevHostIntrospection.cs:570`–`:587`; `wiring.js:127`–`:139` mirrors it client-side | GAP | intended | a deliberate fail-open and the stated exception in `sdk-surface-conventions.md` § 7 — the running block is the strict gate, so hiding a member the editor cannot judge would remove wiring the operator still needs |
| 58 | THE SYSTEM SHALL project each catalog block's `[InstantiationParameter]` set with its editor schema and default, and each gated interface and contract binding's predicate. | `LogicBlockDefinition.cs:45`–`:81`, `:89`–`:107`, `:218`, `:229`, `:236`–`:243`. ⚠ **The probe claim first recorded here was wrong** — a gated interface binding on a get-only property did *not* reach the catalog, and one with an explicit `Identifier` was listed under the wrong name; both fixed in amendment 2 and amendment 3, see the Drift checkpoints | `TopologyAuthoringShould.LogicBlockDefinition_FromType_CarriesInstantiationParametersAndGatePredicates` (parameter and contract halves; the interface half is a GAP) | intended | the metadata a topology-authoring client needs to refuse a wire onto an endpoint the chosen parameters remove |
| 59 | THE SYSTEM SHALL report each service's gate predicate in the plugin listing a consumer reads from a packed artifact. | `DalePluginInfo.cs:69`, `ListCommand.cs:83`, `CliListOutput.cs:45` | `ListCommandMappingTests.MapToCliOutput_CarriesServiceNameAndIncludedWhen`, `.MapToCliOutput_UngatedService_HasNullIncludedWhen`, `DalePluginInfoDeserializationTests.ParsePluginInfo_CurrentParserShape_ReadsServiceIncludedWhen` | intended | `dale list` is how a library author checks what they shipped without a cloud round trip |

#### Defect candidates

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 60 | ⚠ WHEN a `SetServicePropertyValue` request targets an `[InstantiationParameter]` property of a running block THE SYSTEM SHALL apply it, so the block reports a parameter value its bound member set does not match. | `LogicBlockBase.cs:900`–`:911` (no parameter or read-only test) → `ServiceBinder.cs:44`–`:59` (`binding.Setter` is non-null because `ReflectionHelper.HasPublicSetter` at `:256`–`:260`, consulted at `ServiceBuilderBase.cs:41`, reports an `init` accessor as a public setter); contradicts `InstantiationParameterAttribute.cs:12`–`:13` and `:55`–`:63`; **(probe)** `Count` 2 → 3 after `Build()` | GAP | ⚠ fix | the one `readOnly` whose value is structural: the gates already resolved, so the read-back honesty DALE044 spends two rules protecting is lost at runtime |
| 61 | ⚠ WHEN `[IncludedWhen]` is applied to a member of a type that is not a logic block THE SYSTEM SHALL report no diagnostic and SHALL ignore the gate. | `IncludedWhenPredicateAnalyzer.cs:45`–`:49` (returns before walking any member of a non-block type) against `InstantiationParameterAnalyzer.cs:42`–`:47` (walks every class); all three gate readers see only the block type's own properties (`DeclarativeServiceBinder.cs:38`, `DeclarativeInterfaceBinder.cs:66`, `DeclarativeContractBinder.cs:18`); **(probe, positive control in one compilation)** the same gate reports DALE043 on the block and nothing on a component | GAP | ⚠ fix | an author gating inside a component gets no build error and no runtime effect — the silence `sdk-surface-conventions.md` § 7 names, with the sibling analyzer already showing the shape of the fix |
| 62 | ⚠ WHERE a member's `[IncludedWhen]` predicate is the empty string THE SYSTEM SHALL report the member as ungated in the introspection JSON and SHALL then fail every block initialization. | `DeclarativeServiceBinder.cs:73`, `ContractMetaData.cs:43`, `FunctionInterfaceMetaData.cs:43` (all `string.IsNullOrEmpty`, so nothing is recorded) against `InclusionGate.cs:37` (only `null` short-circuits, so `""` reaches `Predicate.Parse`); **(probe)** the definition view reports `includedWhen: null`, the live bind throws `PredicateSyntaxException: predicate is empty` | GAP | ⚠ fix | three components disagree about what an empty gate means, and the artifact that travels to the cloud describes a member the runtime refuses to bind at all |
| 63 | ⚠ WHEN more than one supplied parameter fails to resolve or to decode THE SYSTEM SHALL report only the first failure. | `LogicBlockBase.cs:576`–`:597` (both throws are inside the loop) | GAP | ⚠ fix | this repo settled on collect-then-report for config validation (`DevTopologyLoader.cs:104`–`:123`, and `RejectDuplicateBlockIds` from VION-66); an operator fixing three typos one host restart at a time is the cost |
| 64 | ⚠ THE SYSTEM SHALL emit a gated member's predicate into the introspection JSON without checking that it parses or that its references resolve. | `InclusionGate.cs:37`–`:40` (the mode check precedes `Parse`), `LogicBlockIntrospection.cs:288`; **(probe)** `Count >>> 2` and `Missing >= 2` both reach `services[].includedWhen` | GAP | ⚠ park | a block whose gate cannot parse fails *every* activation, so `dotnet pack` accepting it ships a known-dead artifact — but making the pack path parse-check is a behavior change to the pack gate, not an area-local fix — **fixed by amendment 4b; the check was then made syntactic in amendment 5, see the drift checkpoints** |
| 65 | ⚠ WHEN a block that is already configured receives a second configuration THE SYSTEM SHALL re-apply the parameter values and re-run the binders over the existing bindings, so a gate that flips open adds its member and a gate that flips closed leaves the previously bound member in place. | `LogicBlockBase.cs:169`–`:194` (no already-initialized guard), `:552`–`:554` re-entered against the same `ServiceBinder`; **(probe)** `Count` 1 → 2 adds `Point2`, and `Count` 2 → 1 leaves `Point2` bound while the property reads 1 | GAP | ⚠ fix | the documented contract is that changing a parameter re-activates and recycles the block (`InstantiationParameterAttribute.cs:55`–`:58`); accepting the message instead leaves an instance whose reported configuration and bound shape disagree, with nothing to notice it |
| 66 | ⚠ WHEN a DevHost topology names an instantiation parameter that does not resolve, or a value that does not decode, THE SYSTEM SHALL report the host as started and leave that block uninitialized. | `DevLogicSystemInitializer.cs:425`–`:430` sends asynchronously, so row 3's and row 5's throw never reaches the `catch` at `:435`–`:439`; **(probe)** "Development host started successfully", every property of the block null, one `[EXCEPTION CAUGHT] Message: InitializeLogicBlock` in the log, and a service list computed from the *defaults* that disagrees with the nothing the block bound | GAP | ⚠ fix (loader half) + park | the SDK's fail-closed guarantee is real and the DevHost swallows it; the operator's only signal is a log line they have to go looking for |
| 67 | ⚠ THE SYSTEM SHALL throw `KeyNotFoundException` when a sender routes a message to an interface binding this instance's gates excluded. | `examples/Vion.Examples.Gating/README.md:54`–`:59` states exactly this, naming `LogicBlockBase.GetFunctionByIdentifier`; that member does not exist in `LogicBlockBase.cs`, and row 33 (`:366`–`:381`) is the behavior | GAP — `ChargingStationBlockShould` maps only included points, per the README's own workaround | ⚠ fix | the one caveat the gating showcase's README puts in a block quote is false, and it tells an author to design around a hazard that was closed |

**Failure sketches.**

- **60** — a block declares `[ServiceProperty(Title = "Ladepunkte")] [InstantiationParameter] public int PointCount { get; init; } = 1;`
  with two components gated `PointCount >= 2` and `>= 3`. The operator configures `PointCount = 2`,
  the binders bind `Point1` and `Point2`, and the block runs. A `SetServicePropertyValueRequest` for
  `PointCount = 3` — from a dashboard that ignores `schema.readOnly`, a `dale dev` control call, or a
  test — reaches `HandleSetServicePropertyValueRequest` (`:900`), which looks the member up by
  identifier and calls `_serviceBinder.SetPropertyValue` (`:908`). That method (`ServiceBinder.cs:44`)
  requires only `binding.Setter != null`, and the setter exists because `HasPublicSetter`
  (`ReflectionHelper.cs:256`–`:260`) reports an `init` accessor as a public setter. The write lands.
  From then on `PointCount` reads 3, `InclusionGate.BuildParameterContext` would answer 3, and every
  persistence and introspection read reports 3 — while `_serviceBinder` still holds exactly the two
  components bound at 2. The block is the one thing the design forbids: an instance whose parameter
  value and member set disagree, with no error anywhere.
- **61** — an author writes a component
  `public class ChargePoint { [ServiceProperty] public bool Active { get; set; } [IncludedWhen("PointCount >= 2")] public Meter SubMeter { get; } = new(); }`
  because gating on the block worked. `IncludedWhenPredicateAnalyzer.AnalyzeBlock` (`:45`) returns at
  once — `ChargePoint` does not inherit `LogicBlockBase` — so the build is clean. At bind,
  `DeclarativeServiceBinder.BindPropertyBasedServices` (`:38`) enumerates only the *block* type's
  properties, and the nested binder (`:78`–`:100`) reads no gate at all, so the gate does nothing.
  The positive control run this session: the identical gate on the block's own scalar reports
  `error DALE043 … this member is not gateable`, on the component nothing at all, in one compilation.
  `InstantiationParameterAnalyzer` reports DALE044 for exactly this misplacement of the *other*
  attribute (`InstantiationParameterAnalyzerTests.DeclaredOnAComponentType_ReportsDALE044`), so the
  hole is in one half of a matched pair.
- **62** — `[IncludedWhen("")]` on a component. `DeclarativeServiceBinder.cs:73` tests
  `string.IsNullOrEmpty(includedWhen)` and does not register it, so the definition view reports
  `services[].includedWhen: null` — the wire word for *unconditional*. `InclusionGate.IsIncluded`
  (`:37`) short-circuits on `predicate is null` only, so at bind `""` reaches `Predicate.Parse` and
  throws `PredicateSyntaxException: predicate is empty`. `dotnet pack` succeeds, `dale list` shows an
  ungated member, the cloud stores an ungated member, and every activation of the block fails.
- **63** — a payload carrying `Kount = 2`, `Modell = "Plus"` and `Regio = "EU"` (three transposed
  identifiers) reports `Instantiation parameter 'Kount' does not resolve …` and stops. The operator
  corrects it, restarts, and learns about `Modell`. The loop three rows above it in the same repo
  (`DevTopologyLoader.cs:104`–`:123`) collects every mapping error and throws once with all of them,
  which is the shape VION-66 settled on.
- **65** — a host sends `InitializeLogicBlock` twice, the second with `PointCount` lowered from 2 to
  1. `ApplyInstantiationParameters` (`:192`) writes 1; `Configure` (`:194`) re-runs the three binders
  against the same `ServiceBinder`; `DeclarativeServiceBinder` skips `Point2` this time but never
  removes the registration the first pass made. The block then reports `PointCount = 1`, publishes
  `Point2`'s service properties, and answers `SetServicePropertyValue` on them. Verified this session
  in both directions. Nothing in this repo sends a second one — which is exactly why the message is
  accepted without a guard.
- **66** — `duo-plus.topology.json` with `"Kount": 2` instead of `"ChargePointCount": 2`.
  `DevTopologyLoader.Build` accepts it (it validates instance names, types and interface-mapping
  compatibility, never parameter identifiers); `DevLogicSystemInitializer` spawns the actor, `SendTo`s
  the initialize message (`:425`) and returns. The throw happens later, inside the actor, and the
  surrounding `catch` (`:435`) is long gone. `host.StartAsync()` completes, the DevHost logs
  "Development host started successfully", and `GetAllProperties` returns
  `{"ChargePointCount":null,"Active":null,"Power":null}` while `GetConfiguration` lists the services
  the *definition* view computed from the C# defaults. The only trace is one
  `[EXCEPTION CAUGHT] Message: InitializeLogicBlock` line. Reproduced this session for both the
  unresolvable-identifier and the undecodable-value case.
- **67** — an author reading `examples/Vion.Examples.Gating/README.md:54` is told that a topology
  mapping to a gated-out interface "throws `KeyNotFoundException` at runtime the moment the sender
  routes a message to the missing endpoint (`LogicBlockBase.GetFunctionByIdentifier`)" and that
  "interface mappings are **not** skip-and-warned for a gated-out target". `GetFunctionByIdentifier`
  is absent from `LogicBlockBase.cs`, and `:366`–`:381` skips and warns. The gap was recorded in the
  design, the gap was closed, and the README was not updated — so a consumer designs their topology
  matrix around a restriction that no longer exists.

### Step 4 — every existing test mapped

**19 tests in the rewrite**, across 2 files.

| File | Tests | Rows |
| --- | --- | --- |
| `Vion.Dale.Sdk.Test/Configuration/ConfigTimeStructuralGatingShould.cs` | 17 | 1, 3, 5, 13, 14, 16, 20, 21, 23, 25, 26, 31, 33, 34, 35, 36, 38, 40, 41 |
| `Vion.Dale.Sdk.TestKit.Test/InstantiationParameterGatingShould.cs` | 2 | 2, 17, 53 |

Mapped and deliberately **not** rewritten (D2, D4, D5, and the pilot's cross-area rule):

| File | Tests | Rows | Owner |
| --- | --- | --- | --- |
| `Vion.Dale.Sdk.Generators.Test/IncludedWhenPredicateAnalyzerTests.cs` | 14 methods (4 rows) | 12, 15, 29, 44–49 | `ANLZ` |
| `Vion.Dale.Sdk.Generators.Test/InstantiationParameterAnalyzerTests.cs` | 12 | 47, 51, 52 | `ANLZ` |
| `Vion.Dale.Sdk.Test/Introspection/LogicBlockIntrospectionShould.cs` (gating row only) | 1 | 39 | `INTRO` |
| `Vion.Dale.Sdk.Test/Configuration/Services/ContractCarriedServiceRelationsShould.cs` (gating fixture) | fixture only (`:192`–`:198`, `:457`) | 14, 24 | `BIND` |
| `Vion.Dale.DevHost.Test/ConfigTimeGatingShould.cs` | 2 | 54, 55 | `CTRL` |
| `Vion.Dale.DevHost.Test/TopologyFilesShould.cs` (gating rows only) | 2 | 54 | `SCEN` |
| `Vion.Dale.DevHost.Test/TopologyAuthoringShould.cs` (gating row only) | 1 | 58 | `CTRL` |
| `Vion.Dale.Cli.Test/Commands/ListCommandMappingTests.cs` + `Models/DalePluginInfoDeserializationTests.cs` (gating rows only) | 4 | 59 | `CLI` |
| `examples/Vion.Examples.Gating/Vion.Examples.Gating.Test/ChargingStationBlockShould.cs` | 8 methods (16 cases) | 24, 25, 26, 27, 53 | Tier C |

**Unmapped tests: 0.** Every test in the rewrite maps to a row. Two properties of the rewrite are
non-conforming rather than unmapped, and are Phase B's debt:

- `ConfigTimeStructuralGatingShould.cs:441` reaches `LogicBlockBase._serviceBinder` by reflection and
  `:334` reaches `_interfaces` the same way; `InstantiationParameterGatingShould.cs:72` reaches
  `_serviceBinder` again. `testing-conventions.md` § 7 names the first as this area's debt and does
  not know about the other two. The seam is already on the wire: `SendBindLogicBlockServices`
  (`LogicBlockBase.cs:1019`–`:1027`) sends `BindLogicBlockServices` carrying the bound service map,
  and both the TestKit context and a `Mock<IActorContext>` observe it — so the bound set is assertable
  through a message the block already emits, with no new member on the SUT (§ 7's second half).
- Every test name in both files is non-conforming to `testing-conventions.md` § 12 —
  `BindOnlyIncludedComponentServices_ForACountParameter` (underscore-separated),
  `LeaveAGatedOutContractPropertyNull` and thirteen others (articles).
  `scripts/test-style-lint.ps1` gates them the moment they cite ids.

**What the rewrite will do to the two classes:**

| Was | Now |
| --- | --- |
| `ConfigTimeStructuralGatingShould` (17), one class holding binder gating, definition-mode emission, parameter application, the message-path guards and persistence | split along the page's sections, each file mirroring its SUT path per § 13's file-layout rule: parameter application and the runtime write (rows 1–12, 60), gate evaluation (13–23), what a gate removes (24–30), the message paths (31–33), persistence (34–37), the wire (38–43) |
| the three reflection sites | asserted through `BindLogicBlockServices` or the introspection result |
| 8 fixture blocks declared at file scope in the test namespace (`:27`–`:175`) | one `TestHelpers/` fixture set (§ 13), shared with the TestKit class rather than duplicated |
| `InstantiationParameterGatingShould` (2) | kept as the TestKit's own two rows (53, 2), reusing the shared fixtures |

### The emitted shape, read from the producer (`testing-conventions.md` § 4)

Rows 38–43 and 62 are introspection-JSON claims, so the shape was read out of
`Vion.Dale.LogicBlockParser` — the binary `Vion.Dale.Sdk.targets` runs at `dotnet pack` — rather than
described from the source. The example projects reference a **published** SDK package and so cannot
show a local change; the probe is a throwaway `netstandard2.1` plugin that `ProjectReference`s the SDK
in this working tree. Expectations were derived from the sources first, then checked off against the
emitted file:

| Member | Declared | Emitted |
| --- | --- | --- |
| `PointCount` | `[ServiceProperty(Minimum = 1, Maximum = 3)] [InstantiationParameter] int { get; init; } = 1` | `schema: {type: integer, format: int32, minimum: 1, maximum: 3, readOnly: true}`, `runtime: {instantiationParameter: true, default: 1}` |
| `Model` | `[InstantiationParameter] enum { get; init; } = Bricco` | `schema.enum: ["Bricco","Moka"]`, `runtime.default: "Bricco"` — the member name, not the ordinal |
| `Region` | `[InstantiationParameter] string? { get; init; } = "EU"` | `schema.type: ["string","null"]`, `runtime.default: "EU"` |
| `NullableCount` | `[InstantiationParameter] int? { get; init; }` | `schema.type: ["integer","null"]`, `runtime.default: null` — row 42 |
| `Point1` (ungated component) | no gate | `services[].includedWhen: null`, its property **and** its measuring point present |
| `Point2` (gated component) | `[IncludedWhen("PointCount >= 2")]` | `services[].includedWhen: "PointCount >= 2"`, full member set — row 21 |

**Post-fix read for row 62** (amendment 2, review). The table above was taken before the fixes; the one
row whose *wire* shape a fix changed is the empty gate, so it was re-read from a freshly packed probe
plugin rather than reasoned about:

| Member | Declared | Emitted, after the fix |
| --- | --- | --- |
| `Point2` | `[IncludedWhen("")]` | `services[].includedWhen: ""` — before the fix the key was `null`, the wire word for *unconditional* |
| `Point1` | no gate | `services[].includedWhen: null` — unchanged, and the control that shows `""` is not simply how the field serializes |

The in-repo assertions pinning the same shape are
`ConfigTimeStructuralGatingShould.EmitTheFullMemberSetWithPredicatesInDefinitionMode`,
`.EmitTheParameterRuntimeMarkerDefaultAndReadOnly` and `.EmitTheEnumParameterDefaultAsItsMemberName`;
none of them covers the measuring-point half of row 26 or the nullable default of row 42.

### What the probes ran

Every row marked **(probe)** was executed this session against the working tree, never inferred.
Four throwaway harnesses, all outside the repo — the fourth added in amendment 5:

1. **A console over `Vion.Dale.DevHost` and `Vion.Dale.Sdk.TestKit`** — rows 4, 6–9, 11, 18, 19, 22,
   30, 39, 58, 60, 65, and the DevHost boots of row 66. This is where the two most surprising results
   came from: the runtime write on a parameter succeeding, and reconfiguration being additive.
2. **A `netstandard2.1` plugin published and read through `Vion.Dale.LogicBlockParser`** — the emitted
   shape above, rows 38–43.
3. **The same plugin with the analyzer wired in** (an `OutputItemType="Analyzer"` project reference)
   — row 61's positive control. Worth recording as a trap: a plain `ProjectReference` to
   `Vion.Dale.Sdk` runs **no** analyzer, so the first build of the probe reported no diagnostic for a
   declaration DALE043 rejects outright. A negative about an analyzer needs a positive control in the
   same compilation (`docs/process-journal.md`, 2026-08-26 · VION-81 hop 1) — and here it needed one
   before the compilation could produce any diagnostic at all.

4. **A fourth plugin, in amendment 5** — a `netstandard2.1` `ProjectReference` probe over the working
   tree's `Vion.Dale.Sdk`, declaring one gated component service under
   `[IncludedWhen("Count >= 2 && Missing >= 1")]` and published, then read through
   `Vion.Dale.LogicBlockParser` — the tool `Vion.Dale.Sdk.targets` runs on `dotnet pack`. This is the
   pack-path proof row 62's precedent asked for and row 64's fix never had (re-run at amendment 6, so
   the line numbers below are HEAD's):

   ```
   fail: Program[0]
         Failed to process logic block GateProbe.ProbeBlock
         System.InvalidOperationException: Member 'Gated' on logic block 'GateProbe.ProbeBlock' has an [IncludedWhen] gate "Count >= 2 && Missing >= 1" that cannot be resolved against the block's [InstantiationParameter] properties: 'Missing' is not an [InstantiationParameter] of this block. The gate would fail every activation.
            at Vion.Dale.Sdk.Configuration.InclusionGate.EnsureResolvable(String predicate, Object logicBlock, String memberName) in C:\_gh\dale-sdk\Vion.Dale.Sdk\Configuration\InclusionGate.cs:line 100
            at Vion.Dale.Sdk.Configuration.Services.DeclarativeServiceBinder.BindPropertyBasedServices(...) in ...\DeclarativeServiceBinder.cs:line 61
            ...
            at Vion.Dale.LogicBlockParser.Program.RunParser(String[] args) in C:\_gh\dale-sdk\Vion.Dale.LogicBlockParser\Program.cs:line 149
   EXITCODE=1
   ```

   The negative control, on the same probe with `d12bc37`'s `InclusionGate` restored: **exit code 0**,
   and the emitted artifact carries the dead gate —
   `Gated -> 'Count >= 2 && Missing >= 1'`. That is the placeholder evaluator's short-circuit,
   observed on the path that ships.

One reading-derived hypothesis was recorded here as **falsified** by probe 1, and the falsification was
itself wrong. The reading — `DevConfigurationBuilder.GetAllLogicInterfaces` skips a property whose
`CanWrite` is false, so every get-only property-based interface binding is absent from the DevHost
catalog — was **correct**. The probe's "get-only" property was settable by the time the catalog read it,
so it never reproduced the shape it was built to test, and the pass wrote the true reading off on the
strength of it. A test on the committed `{ get; } = new()` shape is red on the pre-amendment-2 code.
Fixed in amendment 2 (discovery) and amendment 3 (naming); see the Drift checkpoints, and
`docs/process-journal.md` for the lesson, which is the opposite of the one first drawn here.

### Step 5 — self-check

- **A1 attributes**: 2 enumerated, **both visited** — `[InstantiationParameter]` in rows 1–12, 34,
  40–43, 51–53, 60, 63, 65, 66; `[IncludedWhen]` in rows 13–33, 38–39, 44–50, 55, 57–59, 61, 62, 64,
  67. Neither carries a named parameter beyond `Predicate`, which is row 14's subject.
- **A2 diagnostics**: 2 descriptors, **both visited**; 15 rules enumerated, **15 visited** (rows
  44–49, 51). Row 47 covers the two re-declaration rules together because they share a helper and a
  message; row 51 covers the seven `InstantiationParameterAnalyzer` rules the analyzer reports from
  one method. Row 50 covers the rule all of them share — no evaluation at compile time.
- **A3 gate model**: 6 member groups enumerated, **all visited**. `BindingMode`'s two members are
  rows 21–23; `InclusionGate`'s three methods are rows 13–20; the two metadata `IncludedWhen`
  properties are row 39; `ServiceBinder`'s pair is row 38.
  `LogicInterfaceExtensions.WithIncludedWhen` (`:30`) has no row of its own — it is the one-line
  setter `DeclarativeInterfaceBinder.cs:249` calls to reach row 39's annotation, and no behavior
  distinguishes it from that annotation.
- **A4 gate sites**: 3 binder sites plus 2 persistence sites enumerated, **all 5 visited** (rows
  24–26, 35–36).
- **A5 wire fields**: 4 enumerated, **all visited** (rows 38–42), each read from the packed artifact.
- **A6 PublicApi manifest**: 119 types scanned, 2 in this area, both visited (A1's rows).
  `BindingMode`'s absence is Q7 and deliberately not a row — no consumer-observable behavior turns on
  it.
- **A7 config message**: 1 field, visited (rows 1–11).
- **A8 TestKit**: 1 method, visited (row 53); its non-gating ergonomics recorded out of scope.
- **A9 consumers**: 11 walked, **10 with rows** (54–59, 66); `wiring.js` is Tier C (D5) and carries no
  criterion — its behavior appears in row 57's evidence as the client mirror of the DevHost's
  fail-open.
- **A10 persistence**: 3 sites, all visited (rows 34–37).
- **Rows**: 67 total — **59 `intended`**, **7 ⚠ `fix`** (60–63, 65, 66, 67), **1 ⚠ `park`** (64), 0
  `out-of-spec`. **35 rows carry `GAP`**; 32 have a test today.

---

## Drift checkpoints

> One line per divergence discovered during implementation:
> `YYYY-MM-DD: <what changed and why>`. Never inline in a spec page. A checkpoint that fixes a
> CLASS bug states the sibling sweep (done / N/A / handed off).

- 2026-09-02: The brief lists "the named parameters of the two attributes (each is a row candidate)"
  as an anchor kind. Counted this session: `[InstantiationParameter]` declares **none** — it is a bare
  marker whose every knob is read off the paired `[ServiceProperty]` — so that set is exactly one,
  `IncludedWhenAttribute.Predicate`. The row candidates the brief expected there are in A2 instead:
  2 descriptors carrying **15** rules.
- 2026-09-02: The brief says the TestKit's parameter surface is "in scope where it is gating
  semantics, out where it is test ergonomics — say so per row". Recorded per D3 and in A8: one row in
  (53); the selector overload and the builder's missing `[InstantiationParameter]` check out.
- 2026-09-02: `testing-conventions.md` § 7 names **one** reflection site in this area's suite
  (`ConfigTimeStructuralGatingShould.cs:441`). There are **three** — `:441` and `:334` in that file,
  and `InstantiationParameterGatingShould.cs:72`. All three are Phase B's debt, and § 7's list is
  corrected in the same PR.
- 2026-09-02: RFC 0016's R11 records that interface mappings to a gated-out endpoint are *not*
  skip-and-warned. That gap is closed in the code (row 33) and still open in the gating example's
  README (row 67). The RFC is deleted by this pass, so the correction lands on the README.
- 2026-09-02: `AC-GATE-007.1` (an excluded interface binding is not bound) was minted and then
  **withdrawn**: no mutation reddens it that does not also redden `AC-GATE-008.3`, because the only
  way to observe the absence of an interface binding is that a message routed to it arrives nowhere.
  Merged into `AC-GATE-008.3`, whose text now carries the never-bound clause. The skill's rule for an
  AC no mutation can reach — reword, merge or drop — applied at the point implementation showed it.
- 2026-09-02: `AC-GATE-002.10` (a rejected configuration applies none of its values) was **added**
  during T-005. Collect-then-report made decode-then-apply nearly free, and applying nothing is what
  keeps a rejected configuration from leaving a block whose parameters half moved — visible because a
  development host keeps such a block alive (the `_findings.md` entry).
- 2026-09-02: `AC-GATE-005.9` was narrowed. It first said an empty predicate is reported as a gate
  **and** fails initialization; the second half is `AC-GATE-005.6` (a predicate that does not parse),
  and the two redden under different mutations. The criterion now claims only the reporting half.

### Amendment 2 — after the completeness critic and `/vion-code-review`

- 2026-09-02: **Row 58's probe claim was wrong, and the row with it.** Phase A recorded that a get-only
  property-based interface binding reaches the development host's catalog with its predicate, on the
  strength of a scratch probe. The probe's "get-only" property was settable by the time the catalog
  read it — the reading that produced the hypothesis (`GetAllLogicInterfaces` skips `!CanWrite`) was
  right, and the probe that "falsified" it did not reproduce the shape. A test on the committed
  `{ get; } = new()` shape is red on the pre-fix code. The catalog now discovers interface bindings by
  the binder's own rule, which never asks for a setter (M2, `AC-GATE-012.6`). Recorded at length
  because the Phase A journal line drew the opposite lesson from this same probe.
- 2026-09-02: `AC-GATE-003.3` was true at one seam only. Persistence has two discovery doors —
  service-binding and opt-in — and only the first skipped parameters, so a parameter also marked
  `[Persistent]` was captured and restored *over* the configured value the gates had resolved against.
  Both doors now skip, and DALE044 refuses the combination so the author is told rather than silently
  ignored (M1, `AC-GATE-011.8`).
- 2026-09-02: `AC-GATE-004.1` was reworded to cover a block whose configuration *failed*, and
  `AC-GATE-003.5` added. A failed configuration spends the instance, which was already true and right,
  but the refusal pointed the operator back at "re-activate the configuration" — the thing that had
  just failed — and restore was the one teardown handshake that did not answer an uninitialised
  instance, so a fail-closed block hung its own reclamation (M3, M6).
- 2026-09-02: `AC-GATE-007.8` added as the mirror of `AC-GATE-007.7`. A null service-bearing component
  is omitted from the definition view because its members are read off the object; a null interface
  binding is not, because an endpoint's identity is type-level. The two were one row in Phase A and
  are two behaviors (M7).
- 2026-09-02: `AC-GATE-002.9`'s implementation was keyed on the wrong count — the *total* failures, not
  the decode failures — so one unresolvable identifier beside one bad value dropped the only actionable
  cause. The criterion's text was already right; the code now matches it.
### Amendment 6 — after the review of amendment 5

- 2026-09-02: **The new design's load-bearing claim was unverified.** Three sites — the
  `EnsureResolvable` doc comment, the `Vion.Dale.Sdk.csproj` link comment and the amendment-5
  checkpoint — said the two parsers are held to one grammar by `PredicateConformanceVectorTests`. That
  test feeds every vector to the **linked** parser only; nothing in this repo ran a vector through
  `Vion.Contracts`' `Predicate`. So the `parsed.Ast is null` branch, which turns a Contracts-accepted
  gate into a hard `dotnet pack` refusal, rested on a guarantee the repo did not hold.
  `Vion.Dale.Sdk.Test/Shared/PredicateParserShould.cs` now runs **all 21 parse cases and all 44 eval
  cases** of the vendored vector through both `Predicate.TryParse` and `PredicateParser.Parse` and
  fails on any accept/reject disagreement, and walks the linked tree of every eval case to assert its
  reference set equals that case's context keys — the property `EnsureResolvable` actually depends on.
  **The two agree on every vector**; there was nothing to report. The vector is *linked* from
  `Vion.Dale.Sdk.Generators.Test/Predicates/`, not copied, so the repo still holds one vendored file.
  All three claim sites now name this test.
- 2026-09-02: **The walk exposes one seam so the cross-check is not a second implementation.**
  `InclusionGate.ReferencedNames(PredicateNode)` is what both `EnsureResolvable` and
  `ExtractEveryNameEachEvalVectorResolves` call. A private walk plus a copy in the test would have
  proven the copy.
- 2026-09-02: **`BoolRefNode` was the one walked node kind nothing exercised.** No `[InstantiationParameter] bool`
  and no bare-reference gate existed in the repo, so deleting that `case` sent a legitimate gate to the
  walk's `default:` and refused it, with nothing red. `GatedBoolParameterBlock` (`bool Enabled`,
  `[IncludedWhen("Enabled")]`) is the fixture; `IntrospectGateOverBareBooleanReference` pins the
  definition view and `BindOnBareBooleanReference` the two live outcomes. The conformance cross-check
  reaches the same case independently — the vector carries bare `DirectMeasurement` and
  `!DirectMeasurement` rows — so both redden under the deletion.
- 2026-09-02: **A parser disagreement was speaking with the wrong voice.** It threw the operator-facing
  "cannot be resolved against the block's `[InstantiationParameter]` properties", which blames the
  author's parameters for a defect in one of the two parsers. It has its own message now, naming the
  cause.
- 2026-09-02: **Four journal lines prescribed the fix instead of recording what happened**, against the
  journal header's "record what happened, not what should change — the fix is the retro's job", and two
  recurrences were missing their `(second ask)` marker, which is what `process-metrics.md` counts.
  All six amendment-5 lines rewritten to what was felt; a seventh added for the criterion-cited-for-
  behavior-it-does-not-state recurrence, which had no line at all and is this pass's third occurrence.
- 2026-09-02: **The scorecard claimed a paste it had not made.** It declared "every line below pasted
  from the terminal" and then carried `` `cleanupcode applied changes` on 9 files `` — a sentence
  `scripts/cleanup-code.ps1` does not print. Replaced with the script's real output, and the file count
  reconciles with the commit's.
- 2026-09-02: Two smaller corrections. `components.js`'s merge comment claimed all four problem
  producers share `{ mappingIndex, kind, message }`; they share `{ kind, message }` and differ on the
  index (`mappingIndex`, `pairingIndex`, or none), which is what decides footer-only, so it says that
  now. And `RefuseIntrospectingCompoundGateReferencingUnknownName`'s third assertion looked for
  `"Missing"` in a message that already contains the predicate — it asserts the reason clause instead.
- 2026-09-02: The *What the probes ran* list was numbered 1, 2, 4, 3 after amendment 5 inserted probe 4
  above probe 3. Reordered, and the pack-path stack re-run so its line numbers are HEAD's.

### Amendment 5 — after the review of amendment 4/4b

- 2026-09-02: **Row 64's fix was unsound, and `AC-GATE-006.4` over-claimed for one amendment.**
  `EnsureResolvable` checked a gate's references by *evaluating* the predicate against typed
  placeholders. The evaluator short-circuits and the placeholders are each type's smallest value, so
  `Count >= 2 && Missing >= 1` returned `False` and `Count <= 5 || Missing >= 1` returned `True`
  without either ever reaching `Missing` — a gate naming an undeclared parameter inside any compound
  predicate passed introspection and still failed every activation, which is the hole row 64 was fixed
  to close. Reference extraction is **syntactic** now: `Vion.Dale.Sdk.Generators`'
  `PredicateParser.cs` + `PredicateSyntax.cs` are **linked** into `Vion.Dale.Sdk`
  (`<Compile Include=… LinkBase="Shared"/>`, the precedent at
  `Vion.Dale.Sdk.Generators.csproj:44`–`:51` running the other way), `InclusionGate` walks the tree's
  `PredicateRef` nodes for every referenced name and refuses any the block does not declare as an
  `[InstantiationParameter]`, and nothing is evaluated. `BuildResolutionProbe` is deleted. The link was
  the brief's first choice and it held: both files carry only `System.*` usings, and
  `dotnet build Vion.Dale.Sdk` succeeds with 0 warnings, so the tokenizer fallback was not needed.
  `Vion.Contracts`' `Predicate.Parse` stays the syntax authority and runs first; the linked parser is
  the tree source. **Amendment 6 made that pairing checkable**: `PredicateConformanceVectorTests` runs
  the vector through the *linked* parser only, so the "one grammar" claim rested on nothing this repo
  holds — `Vion.Dale.Sdk.Test/Shared/PredicateParserShould.cs` now runs every vector through both, and
  the three sites that made the claim name it instead. Known consequence of the link, recorded and
  accepted: the linked types are `internal`, but `GenerateDocumentationFile` documents them anyway, so
  the shipped `Vion.Dale.Sdk.xml` gained **8** `Vion.Dale.Sdk.Generators.Predicates.*` entries (plus
  one `InclusionGate` member whose signature names `PredicateNode`) out of 650. No surface moves —
  `git diff d12bc37 -- docs/snapshots/` is empty and the PublicApi manifest carries no `Predicate`
  entry — and the generated API reference is built from the manifest, not from the XML.
- 2026-09-02: **Consequence carried, and a 4b sentence retracted.** A predicate whose literal type does
  not match its parameter is **no longer** refused at introspection — it is DALE043's, which is what
  the page's prose has said all along; the 4b drift entry's closing sentence ("a type mismatch inside a
  predicate also fails to resolve and is refused here as well as by DALE043") was true only of the
  placeholder evaluator and is withdrawn. No test asserted it. `AC-GATE-006.5` ("SHALL NOT evaluate a
  gate … while introspecting") is now literally true rather than nearly true, and is kept; the page's
  paragraph under it is rewritten from "checked against placeholder values" to the syntactic reading,
  with both reasons — short-circuiting *and* a null default — stated.
- 2026-09-02: **The editor half of the catalog-default fix was dead code, and the Tier 2 table recorded
  an observation of it.** `wiring.js` exported `missingParameterValueProblems` and nothing in
  `wwwroot/` imported it; the footer problem list composed `problemsOf` + `gatedOutMappingProblems` +
  `pairingProblems` only. It is in that list now. The Tier 2 row is replaced by the re-run above — see
  that table for what the browser actually returned. Journalled on its own line: an unobserved Tier 2
  row is worse than a missing one, because it reads as evidence.
- 2026-09-02: **Amendment 4's own correction was not applied, and it named a mutation that cannot
  redden its test.** The amendment-3 sentence and mutation row it said were "both corrected" still
  stood; both are annotated now. And the guard is
  `propertyValue == null && mode == BindingMode.Live`, so dropping the `mode` conjunct makes the skip
  *unconditional* — `SkipInterfaceBindingHoldingNullWhenConfiguring` asserts an empty Live set and
  stays green under it, while `DescribeInterfaceBindingHoldingNullWhenIntrospecting` reddens. Removing
  the guard outright is the mutation the Live test catches. Both measured, both ledger rows corrected.
- 2026-09-02: **`AC-GATE-007.8` was cited for more than its test asserted.** The criterion says the
  endpoint *and its gate* are reported; `DescribeInterfaceBindingHoldingNullWhenIntrospecting` asserted
  only the endpoint's presence. It asserts the gate now — which made it byte-identical to
  `ConfigGatingMetadataShould.ReportGatedInterfaceBindingHoldingNull` (same fixture, same act, same
  assertion), so that duplicate is **deleted**. The gate it named is not another script but this test:
  the mutation "the endpoint's gate is not recorded" (`ApplyMetadata` passing `null`) reddens the
  surviving test, run and observed. `AC-GATE-007.8`'s gate half is over-determined with
  `AC-GATE-010.2`'s two tests, which the same mutation also reddens.
- 2026-09-02: **Two criteria were cited for behavior they did not state — the same defect as amendment
  4's, in the same commit.** `AC-GATE-012.8` covered only an unresolvable *identifier* while
  `RefuseTopologyCarryingUndecodableParameterValue` and `ReportUnknownIdentifierAndUndecodableValueTogether`
  cite it for undecodable *values*; the criterion now covers both, with its `MODIFIED` delta line.
  `AC-GATE-005.9` had been reworded on the page in 4b with no delta line at all, so page and delta
  disagreed; its `MODIFIED` line is added. `spec-trace` checks that an id exists, not that it says the
  right thing — this is now the third occurrence, and the retro reads for repeats.
- 2026-09-02: **The pack-path refusal had never been run end to end.** A `netstandard2.1` probe plugin
  carrying `[IncludedWhen("Count >= 2 && Missing >= 1")]`, published and read through
  `Vion.Dale.LogicBlockParser` — the tool `dotnet pack` runs — is refused with exit code 1 (pasted in
  *What the probes ran*, probe 4). The same probe against `d12bc37`'s `InclusionGate` exits **0** and
  writes the gate into the artifact, which is the row-64 harm demonstrated rather than argued.
- 2026-09-02: **Four style-gate hits were hiding behind the `Vion.Dale.DevHost.Test/` exemption.** The
  script was run once with that entry removed locally: one article name this pass authored
  (`LogicBlockDefinition_FromType_NamesEndpointsAsTheBinderResolvesThem` →
  `…NamesEndpointsByBinderRule`), one it authored with no Triple-A markers, and one it extended with
  both faults (`CarryInstantiationParametersFromTheTopologyFileThroughBuild` →
  `CarryInstantiationParametersFromTopologyFileThroughBuild`). All four fixed; the exemption is
  restored unchanged, so the committed script still skips the project the CTRL pass owns. Both
  behaviour-table and mutation-ledger references to the renamed tests were updated.
- 2026-09-02: **`ServeTheGenericTopologySchema` asserted a field name, not a type.** It string-matched
  `"instantiationParameters"`, which passes on the pre-widening schema that refused JSON null. It parses
  the served document and asserts the value types contain `"null"` now — reverting the widening reddens
  it, measured.

### Amendment 4 and 4b — after the review of amendment 3, and the operator's ledger decision

**Amendment 4.**

- 2026-09-02: **The Live-mode null-component test could not redden.** It asserted on the bound *service*
  map, and the fixture declares no service member, so the assertion held in every world — dropping the
  `mode` conjunct left it green. There was no seam for the bound *interface* set, so one was added
  (`LogicBlockBase.BoundInterfaceIdentifiers()`, internal, a method because a property there is woven as
  observable state and warns). Both halves are now separate tests over one fixture.
  **Corrected in amendment 5** — this entry named the wrong mutation for the wrong test, and its claim
  that the amendment-3 entry and checkpoint sentence had been corrected was false; neither had been
  touched, and both are annotated now. The guard is `propertyValue == null && mode == BindingMode.Live`,
  so the two mutations separate: removing the guard entirely reddens
  `SkipInterfaceBindingHoldingNullWhenConfiguring` (the Live half — the endpoint binds in a configured
  instance), while dropping the `mode` conjunct makes the skip *unconditional* and reddens
  `DescribeInterfaceBindingHoldingNullWhenIntrospecting` instead (the endpoint leaves the definition
  view); the Live test asserts an empty set and stays green under it. Both measured this session.
- 2026-09-02: **M2's naming fix covered the property half only.** A class-level binding was minted as the
  interface name while the binder resolves an explicit `Identifier` there too — the committed
  `ConsumerManagerBlock` (`Identifier = "Fleet"`) is exactly that shape. Third correction to this row.
- 2026-09-02: `AC-GATE-004.1`'s recording still missed the **deferred** completion: when
  `LinkRuntimeActors` arrives after the configuration message, the announcement and `Ready()` run from
  that arm. One helper now wraps both arms.
- 2026-09-02: **Specified behavior had changed without its spec for one amendment.**
  `PersistentExcludedParameter_NoDiagnostic` cited a criterion about plain setters, `AC-GATE-011.8` did
  not list `[Persistent]` among DALE044's triggers, and `AC-GATE-012.6` said predicates were projected,
  not how endpoints are named. `spec-trace` checks that an id exists, not that it says the right thing,
  so the citations passed the gate while pointing at the wrong text. Carried as `MODIFIED
  AC-GATE-011.8`, `ADDED AC-GATE-011.11` and `ADDED AC-GATE-012.10`.
- 2026-09-02: the "Config-time structural gating showcase" summary was orphaned onto `GatedSignalPoint`
  by an insertion anchored above `[LogicBlock]` — the **same mistake** the journal already records for
  this pass. Re-anchored; the recurrence has its own journal line, since the retro reads for repeats.

**Amendment 4b — ledger rows fixed in-pass** (operator's decision: three fixed, one narrowed).

- 2026-09-02: **Row 64.** The definition view now refuses a gate that could never work — one that does
  not parse, or that names something the block does not declare as an `[InstantiationParameter]`
  (`AC-GATE-006.4`). Checked at the one site all three binders share. **Deviation from the brief,
  recorded:** the brief said the parsed tree "appears to expose the referenced identifiers — verify".
  It does not — `Predicate` exposes only `Parse`, `TryParse`, `Evaluate` and `ToString` (verified
  against the 3.7.0 assembly), and there is no AST to walk. Resolution is therefore checked by
  evaluating against **placeholder** values of each declared parameter's own type, never the
  instance's, and discarding the result. ~~That satisfies the brief's actual constraint — its reason
  for "do not evaluate" was that a null default would fail closed and produce a false positive, which
  placeholders remove — and `AC-GATE-006.5` states it. One consequence the brief did not anticipate: a
  type mismatch inside a predicate also fails to resolve and is refused here as well as by DALE043.~~
  **Superseded in amendment 5.** It did *not* satisfy the constraint: the evaluator short-circuits, so
  the check never reached an undeclared name inside any compound predicate. The right answer was the
  brief's own alternative, which this entry stopped one step short of — `Vion.Contracts` exposes no
  AST, but the analyzer's own `PredicateParser` does, and it links. Resolution is syntactic now, and
  the type-mismatch sentence is withdrawn with the evaluator that produced it. The message still says
  the gate "cannot be resolved" rather than claiming which fault it is.
- 2026-09-02: Row 64's fix made `AC-GATE-005.9` unobservable as written — an empty predicate does not
  parse, so it is now refused rather than reported ungated. The criterion is **modified** to the
  surviving claim (an empty predicate is a gate that cannot be resolved, not an absent one), and its
  three tests assert the refusal at each of the three recording sites. They needed three fixtures: the
  binders run interface, then contract, then service, so one block carrying all three only ever shows
  the first refusal.
- 2026-09-02: The refusal was invisible where it matters. `Configure` is reached by reflection, so
  `dale build` / `dotnet pack` would have printed "Exception has been thrown by the target of an
  invocation." `LogicBlockIntrospection` now rethrows the real exception with its stack preserved. Two
  `BIND`-area tests asserted the wrapper and now assert the reason — a better contract, and this pass
  changed it, so this pass carries them.
- 2026-09-02: **Row 66, decode half.** The topology loader now decodes each value with the block's own
  rule, through one shared internal helper both call so loader and block cannot drift
  (`InstantiationParameterCodec`). The ledger row is rewritten to the actor half only.
- 2026-09-02: **M8.** `DefinitionParameter.DefaultKnown` (`AC-GATE-012.11`), and the editor gained a
  problem of its own kind for a gate reading a parameter whose default is known-null with no value
  chosen — that instance fails closed at `Configure`, so failing open on it was wrong. An *unknown*
  default still fails open.
- 2026-09-02: **M9.** The topology schema accepts `null` for a nullable parameter (`AC-GATE-012.12`);
  both committed copies widened. The model and loader already passed null through, so only the schema
  refused it.

**Tier 2 evidence** (recorded here because amendment 3's table was lost to a script that wrote twice
from a stale variable — the observation was made, the record was not kept; re-run and captured).
A live SmokeHost on the `gated-station` topology:

| Question | Answer from the running host |
| --- | --- |
| Get-only gated endpoints in the catalog at all? | `Point2Signal [PointCount >= 2] ZeroOrOne`, `ReserveSignal [Reserve >= 1] ZeroOrOne` — amendment 1 listed none |
| Under the binder's names? | yes — the fallbacks `Point2Endpoint_ISignalSink` / `ReserveEndpoint_ISignalSink` are absent |
| Editor offers `Point2Signal` at `PointCount = 2`? | yes; at `1`, gated out |
| Parameter defaults, with known/unknown split | `PointCount default=1 known=true`, `Reserve default=null known=true` |
| ~~Known-null default, no value chosen~~ | ~~`Station.Reserve has no default; supply a value — a gate reads it, and the block fails to start without one`~~ |
| ~~…and with a value chosen~~ | ~~no problem reported~~ |

**Withdrawn in amendment 5 — these two rows recorded an observation that had not been made.** The
string is the one `wiring.js`'s `missingParameterValueProblems` produces, but nothing in `wwwroot/`
imported that function, so no page could render it: it was read out of the source and written into the
table as if it had come off a screen. This is the pass's most serious evidence error. The export is
wired into the editor's footer problem list now (`components.js`), and the case was re-run on a live
SmokeHost — the rows below are what the browser returned:

| Question | Answer from the running host |
| --- | --- |
| Known-null default, no value chosen (`⛁ → Manage/edit… → gated-station → ✎ edit`, `Reserveleistung` cleared) | `⚠ 1 issue(s)` · `Station.Reserve has no default; supply a value — a gate reads it, and the block fails to start without one` — read back out of `.topo-problems` |
| …and with `Reserveleistung` set to `1` again | `.topo-problems` absent from the page |

The editor was closed with **cancel**; `git status Vion.Dale.DevHost.SmokeHost/` is empty, so the
committed fixture is unchanged.

### Amendment 3 — after the review of amendment 2

- 2026-09-02: **M7 shipped a null dereference.** Relaxing the binder's null check let a null component
  reach the interface factory, which hands the implementation to the generated `RegisterInstance` —
  a `ConditionalWeakTable` keyed by it. The amendment-2 test was green only because its fixture bound
  the *sink* side of a contract, and the generator emits an extension class for the *sender* side only.
  The fixture now binds the sender side and is red on the pre-fix `ArgumentNullException`; the factory
  gained a `Describe` path that registers the endpoint without the dispatch registration a null cannot
  satisfy. ~~The Live-side half of the guard (`mode == BindingMode.Live`) has its own test now too — it
  had none, so dropping the mode check would have passed.~~ **False — corrected in amendment 5:** no
  such test existed when this was written, and dropping the `mode` conjunct does not redden the Live
  assertion at all. See the amendment-4 entry above for what each mutation actually reddens.
- 2026-09-02: **M2 fixed discovery and left naming wrong.** The catalog listed get-only bindings but
  still minted `{Property}_{Interface}` unconditionally, while the binder honours an explicit
  `Identifier`. A binding declared with one was therefore listed under a name the block never answers
  to, so a topology authored from the catalog wired an endpoint the block skips with a warning — worse
  than the omission it replaced, because it looks wired. `GetAllLogicInterfaces` and `MultiplicityOf`
  now mint by the binder's rule. This is the second correction to the same row: the pass got the shape
  right only after being told twice.
- 2026-09-02: `AC-GATE-004.1` over-claimed for one amendment. `_configured` is set before contract
  mapping, persistence initialisation and `Ready()`, but the failure was recorded only around the first
  three steps — so a throw from any later one spent the instance and refused the retry with no reason.
  The whole configuration phase is now one recorded unit, extracted as `ApplyConfiguration` so the guard
  is visible rather than a sixty-line `try`.
- 2026-09-02: DALE044's new `[Persistent]` rule refused `[Persistent(Exclude = true)]` — the documented
  opt-out, which asks for exactly what a parameter already gets — with a message whose reason is false
  for it. Narrowed to the opt-in, reading its named argument the way the `WriteOnly` rule reads its own.
- 2026-09-02: **A GATE-authored class in an exempt project conforms anyway.** `ConfigTimeGatingShould`
  is this pass's own file, so the exemption's rationale ("cites without rewriting") does not cover it;
  it is at §12/§13 like the rest of the pass's suite. The exemption stays for the files the pass only
  cites. The same rule applies to the DevHost tests amendments 2 and 3 added.
- 2026-09-02: `AC-GATE-012.9`'s omission half was pinned on the in-memory DTO, which no mutation of the
  projection can redden — the object holds null either way. It asserts the serialized document now, so
  removing the `WhenWritingNull` condition reddens it, which is what the mutation ledger claims.

- 2026-09-02: **Vendored paths are exempt from the reference sweep.** The pass edited the RFC citation
  out of both `predicate-conformance.json` copies, whose headers say "VENDORED — do not edit here": the
  edit would have been lost on the next re-sync and read as local drift in the meantime. Both reverted.
  The sweep's rule is this repo's *own* living docs, skills and comments; a vendored file's provenance
  is upstream's record even where the annotation around it is ours.

---

## Spec delta (to distill)

> The machine-readable change, one line per id. Grammar:
> `<OP> <ID> -> docs/specs/config-gating.md : <EARS text>`. Every id is greppable in the target after
> distill; a criterion no test can reach carries its `GAP:` marker on the declaring line here and on
> the page.

### The parameter value channel

- ADDED AC-GATE-001.1 -> docs/specs/config-gating.md : WHEN a logic block is configured THE SYSTEM SHALL apply the configuration's `[InstantiationParameter]` values to their properties before `Configure` runs.
- ADDED AC-GATE-001.2 -> docs/specs/config-gating.md : WHEN a configuration supplies no value for a declared `[InstantiationParameter]` THE SYSTEM SHALL leave the property at its C# initializer default.
- ADDED AC-GATE-001.3 -> docs/specs/config-gating.md : WHEN a configuration supplies one identifier more than once THE SYSTEM SHALL apply the last of those values.
- ADDED AC-GATE-001.4 -> docs/specs/config-gating.md : THE SYSTEM SHALL apply an `[InstantiationParameter]` value whatever the property's accessor shape, including an `init`-only one.
- ADDED AC-GATE-001.5 -> docs/specs/config-gating.md : THE SYSTEM SHALL apply an `[InstantiationParameter]` declared on a base logic-block class to the derived instance.

### Parameter resolution and decoding

- ADDED AC-GATE-002.1 -> docs/specs/config-gating.md : WHEN a configuration names an identifier that is not an `[InstantiationParameter]` property of the block THE SYSTEM SHALL fail block initialization with an `InvalidOperationException` naming that identifier and the block type.
- ADDED AC-GATE-002.2 -> docs/specs/config-gating.md : THE SYSTEM SHALL match a configuration's parameter identifier to the property name case-sensitively.
- ADDED AC-GATE-002.3 -> docs/specs/config-gating.md : WHEN a supplied value cannot be decoded into its parameter's type THE SYSTEM SHALL fail block initialization with an `InvalidOperationException` naming that identifier, the block type and the decode error.
- ADDED AC-GATE-002.4 -> docs/specs/config-gating.md : THE SYSTEM SHALL decode a parameter value from the shared JSON-scalar form, taking an enum from its member-name string and an integer kind from a JSON number, and SHALL refuse a value of any other JSON shape.
- ADDED AC-GATE-002.5 -> docs/specs/config-gating.md : WHEN a supplied enum value names no member of the declared enum THE SYSTEM SHALL fail block initialization.
- ADDED AC-GATE-002.6 -> docs/specs/config-gating.md : WHEN a supplied integer value lies outside the declared property type's range THE SYSTEM SHALL fail block initialization.
- ADDED AC-GATE-002.7 -> docs/specs/config-gating.md : WHEN a supplied value is JSON null THE SYSTEM SHALL fail block initialization for a parameter whose declared type is not nullable, and SHALL set the property to null for one whose type is nullable.
- ADDED AC-GATE-002.8 -> docs/specs/config-gating.md : WHEN more than one supplied parameter fails to resolve or to decode THE SYSTEM SHALL name every one of them in the failure.
- ADDED AC-GATE-002.9 -> docs/specs/config-gating.md : WHEN exactly one supplied parameter fails to decode THE SYSTEM SHALL carry that decode failure as the initialization failure's inner exception.
- ADDED AC-GATE-002.10 -> docs/specs/config-gating.md : WHEN any supplied parameter fails to resolve or to decode THE SYSTEM SHALL apply none of that configuration's values.

### Runtime immutability

- ADDED AC-GATE-003.1 -> docs/specs/config-gating.md : WHEN a set-value request names an `[InstantiationParameter]` property THE SYSTEM SHALL refuse the write and leave the property unchanged.
- ADDED AC-GATE-003.2 -> docs/specs/config-gating.md : WHEN the system refuses such a write THE SYSTEM SHALL answer the requester with the property's unchanged value.
- ADDED AC-GATE-003.3 -> docs/specs/config-gating.md : THE SYSTEM SHALL exclude every `[InstantiationParameter]` from persistence discovery.
- ADDED AC-GATE-003.4 -> docs/specs/config-gating.md : WHEN restoring persistent data that names a member this instance does not carry THE SYSTEM SHALL leave the block's state unchanged and complete the restore.
- ADDED AC-GATE-003.5 -> docs/specs/config-gating.md : WHEN a block whose configuration failed is asked to restore persistent data, to stop, or for its persistent-data snapshot THE SYSTEM SHALL answer each request, restoring nothing and reporting an empty snapshot.

### Re-configuration

- ADDED AC-GATE-004.1 -> docs/specs/config-gating.md : WHEN a block whose configuration already ran, whether it succeeded or failed, receives a second configuration THE SYSTEM SHALL refuse it with an `InvalidOperationException` that names the earlier failure where there was one, and SHALL leave the bound member set unchanged.

### Gate evaluation

- ADDED AC-GATE-005.1 -> docs/specs/config-gating.md : THE SYSTEM SHALL treat a member carrying no `[IncludedWhen]` as part of every configured instance.
- ADDED AC-GATE-005.2 -> docs/specs/config-gating.md : WHEN a member carries `[IncludedWhen]` THE SYSTEM SHALL include it in the configured instance only while its predicate evaluates true against that instance's `[InstantiationParameter]` values.
- ADDED AC-GATE-005.3 -> docs/specs/config-gating.md : THE SYSTEM SHALL build a predicate's evaluation context from the block's `[InstantiationParameter]` properties and no other member.
- ADDED AC-GATE-005.4 -> docs/specs/config-gating.md : THE SYSTEM SHALL encode each parameter into that context in the shared JSON-scalar form, so an enum parameter compares against its member name and an integer parameter against a number.
- ADDED AC-GATE-005.5 -> docs/specs/config-gating.md : THE SYSTEM SHALL build that context from each parameter's current value, so a configured value and an unsupplied parameter's C# default are both what the gates see.
- ADDED AC-GATE-005.6 -> docs/specs/config-gating.md : WHEN a gate's predicate does not parse THE SYSTEM SHALL fail block initialization.
- ADDED AC-GATE-005.7 -> docs/specs/config-gating.md : WHEN a gate's predicate references a name the parameter context does not carry THE SYSTEM SHALL fail block initialization.
- ADDED AC-GATE-005.8 -> docs/specs/config-gating.md : WHEN a gate's predicate references a parameter whose value is null THE SYSTEM SHALL fail block initialization.
- ADDED AC-GATE-005.9 -> docs/specs/config-gating.md : THE SYSTEM SHALL report a declared `[IncludedWhen]` predicate as its member's gate whatever the predicate's text, so an empty one is reported as a gate rather than as an absent one.
- MODIFIED AC-GATE-005.9 -> docs/specs/config-gating.md : THE SYSTEM SHALL treat a declared `[IncludedWhen]` predicate as its member's gate whatever the predicate's text, so an empty one is a gate that cannot be resolved rather than an absent one.

### The two binding modes

- ADDED AC-GATE-006.1 -> docs/specs/config-gating.md : WHEN a block is introspected THE SYSTEM SHALL bind its full declared member set regardless of gates and record each gated member's predicate.
- ADDED AC-GATE-006.2 -> docs/specs/config-gating.md : WHILE binding that definition set THE SYSTEM SHALL evaluate no predicate.
- ADDED AC-GATE-006.3 -> docs/specs/config-gating.md : WHEN a block is configured for a running instance THE SYSTEM SHALL evaluate every gate and bind only the included members.

### What a gate removes

- ADDED AC-GATE-006.4 -> docs/specs/config-gating.md : WHEN a block is introspected and one of its gates carries a predicate that does not parse, or that names something the block does not declare as an `[InstantiationParameter]`, THE SYSTEM SHALL refuse the introspection naming the member and the predicate.
- ADDED AC-GATE-006.5 -> docs/specs/config-gating.md : THE SYSTEM SHALL NOT evaluate a gate against the block's own parameter values while introspecting, so a parameter whose declared default is null does not make its gate a refusal.
- ADDED AC-GATE-007.2 -> docs/specs/config-gating.md : WHEN a gated contract binding is excluded THE SYSTEM SHALL leave its property null and never construct the contract.
- ADDED AC-GATE-007.3 -> docs/specs/config-gating.md : WHEN a gated service-bearing component is excluded THE SYSTEM SHALL bind neither its service properties nor its measuring points.
- ADDED AC-GATE-007.4 -> docs/specs/config-gating.md : THE SYSTEM SHALL leave an excluded component's own instance untouched, so block code still reaches a non-null object.
- ADDED AC-GATE-007.5 -> docs/specs/config-gating.md : THE SYSTEM SHALL bind the block's own root service unconditionally.
- ADDED AC-GATE-007.6 -> docs/specs/config-gating.md : THE SYSTEM SHALL bind a class-implemented interface unconditionally.
- ADDED AC-GATE-007.7 -> docs/specs/config-gating.md : WHEN a gated service-bearing component property holds null THE SYSTEM SHALL omit that member from the definition view, reporting neither the member nor its gate.
- ADDED AC-GATE-007.8 -> docs/specs/config-gating.md : WHEN a gated property-based interface binding holds null THE SYSTEM SHALL still report that endpoint and its gate in the definition view.

### The runtime message paths

- ADDED AC-GATE-008.1 -> docs/specs/config-gating.md : WHEN a configuration maps a contract this instance excluded THE SYSTEM SHALL skip that mapping and keep the block running.
- ADDED AC-GATE-008.2 -> docs/specs/config-gating.md : WHEN a configuration links an interface this instance excluded THE SYSTEM SHALL skip that link and keep the block running.
- ADDED AC-GATE-008.3 -> docs/specs/config-gating.md : WHEN a message is routed to a property-based interface binding this instance excluded THE SYSTEM SHALL deliver it nowhere and keep the block running, that binding never having been bound.

### Gates and persistence

- ADDED AC-GATE-009.1 -> docs/specs/config-gating.md : THE SYSTEM SHALL capture the `[Persistent]` members of an included component and none of an excluded one, resolving the gates against the same parameter values the configuration used.
- ADDED AC-GATE-009.2 -> docs/specs/config-gating.md : WHEN a later configuration excludes a component an earlier one included THE SYSTEM SHALL stop capturing that component's persistent state.

### The wire

- ADDED AC-GATE-010.1 -> docs/specs/config-gating.md : THE SYSTEM SHALL report a gated component service's predicate on that service, and report null for an ungated one.
- ADDED AC-GATE-010.2 -> docs/specs/config-gating.md : THE SYSTEM SHALL report a gated contract binding's and a gated interface binding's predicate as an annotation on that binding.
- ADDED AC-GATE-010.3 -> docs/specs/config-gating.md : THE SYSTEM SHALL report an `[InstantiationParameter]` property as read-only in its schema whatever its C# accessors are.
- ADDED AC-GATE-010.4 -> docs/specs/config-gating.md : THE SYSTEM SHALL report an `[InstantiationParameter]` property with a runtime instantiation-parameter marker and the declaring block's default value, JSON-scalar-encoded.
- ADDED AC-GATE-010.5 -> docs/specs/config-gating.md : THE SYSTEM SHALL report a null default for an `[InstantiationParameter]` whose declared default is null.
- ADDED AC-GATE-010.6 -> docs/specs/config-gating.md : THE SYSTEM SHALL publish the `Minimum` and `Maximum` declared on an `[InstantiationParameter]`'s paired `[ServiceProperty]` in its schema, and SHALL apply a configured value outside them.

### Authoring diagnostics

- ADDED AC-GATE-011.1 -> docs/specs/config-gating.md : WHEN `[IncludedWhen]` is applied to a logic-block class THE SYSTEM SHALL report DALE043.
- ADDED AC-GATE-011.2 -> docs/specs/config-gating.md : WHEN `[IncludedWhen]` is applied to a method THE SYSTEM SHALL report DALE043.
- ADDED AC-GATE-011.3 -> docs/specs/config-gating.md : WHEN `[IncludedWhen]` is applied to a member that is neither a property-based interface binding, a contract binding, nor a service-bearing component THE SYSTEM SHALL report DALE043.
- ADDED AC-GATE-011.4 -> docs/specs/config-gating.md : WHEN `[IncludedWhen]` or `[InstantiationParameter]` is re-declared on an `override` or `new` member THE SYSTEM SHALL report a diagnostic.
- ADDED AC-GATE-011.5 -> docs/specs/config-gating.md : WHEN an `[IncludedWhen]` predicate does not parse, is qualified, or references a property that is not an `[InstantiationParameter]` of the same block THE SYSTEM SHALL report DALE043.
- ADDED AC-GATE-011.6 -> docs/specs/config-gating.md : WHEN an `[IncludedWhen]` predicate compares a parameter against a literal of another type, or names an enum member unquoted, THE SYSTEM SHALL report DALE044.
- ADDED AC-GATE-011.7 -> docs/specs/config-gating.md : THE SYSTEM SHALL report no diagnostic for an `[IncludedWhen]` predicate that parses and type-checks, whatever that predicate would evaluate to.
- ADDED AC-GATE-011.8 -> docs/specs/config-gating.md : WHEN an `[InstantiationParameter]` is declared on a type that is not a logic block, without a paired `[ServiceProperty]`, combined with `WriteOnly`, on a type outside bool, enum, an integer kind and string, with a computed getter, or is assigned in the declaring block's own code outside its constructor or an object initializer, THE SYSTEM SHALL report DALE044.
- ADDED AC-GATE-011.9 -> docs/specs/config-gating.md : THE SYSTEM SHALL report no diagnostic for an `[InstantiationParameter]` declared with a plain public setter.
- MODIFIED AC-GATE-011.8 -> docs/specs/config-gating.md : WHEN an `[InstantiationParameter]` is declared on a type that is not a logic block, without a paired `[ServiceProperty]`, combined with `WriteOnly`, combined with `[Persistent]`, on a type outside bool, enum, an integer kind and string, with a computed getter, or is assigned in the declaring block's own code outside its constructor or an object initializer, THE SYSTEM SHALL report DALE044.
- ADDED AC-GATE-011.11 -> docs/specs/config-gating.md : THE SYSTEM SHALL report no diagnostic for an `[InstantiationParameter]` combined with `[Persistent(Exclude = true)]`.
- ADDED AC-GATE-012.11 -> docs/specs/config-gating.md : THE SYSTEM SHALL report on each catalog parameter whether its default was read from an instance, so a default of null that was read is distinguishable from one that was never read.
- ADDED AC-GATE-012.12 -> docs/specs/config-gating.md : THE SYSTEM SHALL accept a JSON null in a topology's instantiation-parameter values for a parameter whose declared type is nullable.
- ADDED AC-GATE-012.10 -> docs/specs/config-gating.md : THE SYSTEM SHALL name each catalog interface binding the way the binder resolves it — the binding's explicit identifier where it declares one, otherwise the interface name for a class-level binding and the property name joined to the interface name for a property-based one.
- ADDED AC-GATE-011.10 -> docs/specs/config-gating.md : WHEN `[IncludedWhen]` is applied to a type that is not a logic block, or to a member of one, THE SYSTEM SHALL report DALE043.

### The consumer surfaces

- ADDED AC-GATE-012.1 -> docs/specs/config-gating.md : THE SYSTEM SHALL let a test supply an `[InstantiationParameter]` value that is applied through the same encode and decode path a configuration uses.
- ADDED AC-GATE-012.2 -> docs/specs/config-gating.md : WHEN a topology names instantiation-parameter values for an instance THE SYSTEM SHALL apply them before that block's `Configure`.
- ADDED AC-GATE-012.3 -> docs/specs/config-gating.md : THE SYSTEM SHALL resolve a development host's definition view down to the live view for an instance's chosen parameter values, dropping every excluded service, interface binding and contract binding.
- ADDED AC-GATE-012.4 -> docs/specs/config-gating.md : THE SYSTEM SHALL overlay an instance's chosen parameter values on each parameter's reported default when resolving that live view.
- ADDED AC-GATE-012.5 -> docs/specs/config-gating.md : WHERE an `[IncludedWhen]` predicate cannot be resolved while resolving that live view THE SYSTEM SHALL leave the member visible. GAP: no committed fixture declares a gate the live view cannot resolve; CTRL's pass owns the development host's fail-open surface.
- ADDED AC-GATE-012.6 -> docs/specs/config-gating.md : THE SYSTEM SHALL project each catalog block's `[InstantiationParameter]` set with its editor schema and default, and each gated interface and contract binding's predicate.
- ADDED AC-GATE-012.7 -> docs/specs/config-gating.md : THE SYSTEM SHALL report each service's gate predicate in the plugin listing read from a packed artifact.
- ADDED AC-GATE-012.8 -> docs/specs/config-gating.md : WHEN a topology names an instantiation parameter that is not an `[InstantiationParameter]` property of the instance's block type THE SYSTEM SHALL refuse the topology, naming every such parameter.
- MODIFIED AC-GATE-012.8 -> docs/specs/config-gating.md : WHEN a topology names an instantiation parameter that is not an `[InstantiationParameter]` property of the instance's block type, or supplies a value that will not decode into that parameter's type, THE SYSTEM SHALL refuse the topology, naming every such parameter.
- ADDED AC-GATE-012.9 -> docs/specs/config-gating.md : THE SYSTEM SHALL carry each instance's chosen instantiation-parameter values in the development host's configuration output, omitting the field for an instance that chose none.

---

## Tasks

> One-commit tasks, each tagged with at least one AC id. Ephemeral — they live and die with this
> change doc. The PR's per-task commits are the completion record.

- `T-001` (all): flip the doc `in-flight`, mint the ids above, record the consumer-visible note and the runtime relay note.
- `T-002` (`AC-GATE-003.1`, `.2`): refuse a set-value request naming an `[InstantiationParameter]`; answer with the unchanged value.
- `T-003` (`AC-GATE-011.10`): widen `IncludedWhenPredicateAnalyzer` to every class, mirroring `InstantiationParameterAnalyzer`'s reach.
- `T-004` (`AC-GATE-005.9`): one rule for a declared predicate at every site that records or resolves one.
- `T-005` (`AC-GATE-002.8`, `.9`): collect every failing parameter and report them together.
- `T-006` (`AC-GATE-004.1`): refuse a second configuration.
- `T-007` (`AC-GATE-012.8`): refuse a topology naming an unknown instantiation parameter, collecting them all.
- `T-008` (`AC-GATE-008.3`): correct the gating example's README to the skip-and-warn contract.
- `T-009` (—): open `docs/specs/_findings.md` with the two parked findings.
- `T-010` (`AC-GATE-001.*`–`AC-GATE-010.*`): rewrite the area's suite to `testing-conventions.md` §9–17, on the `BindLogicBlockServices` seam.
- `T-011` (`AC-GATE-011.*`, `AC-GATE-012.*`): cite the analyzer, development-host and CLI tests that already prove their criteria.
- `T-012` (all): author `docs/specs/config-gating.md` and distill every line above into it.
- `T-013` (—): absorb RFC 0016, run the reference sweep, correct `testing-conventions.md` § 7.

---

## The test → mutation list

Every criterion the pass added or fixed, with the one mutation that reddens its test and nothing else.
Criteria the rewrite pinned without changing behaviour are proven by the same discipline; the rows
below are the ones whose red run is the defect proof.

| Test | Mutation | Result |
| --- | --- | --- |
| `RefuseWriteToInstantiationParameter`, `AnswerRefusedWriteWithUnchangedValue` | the guard removed — i.e. the pre-fix code | red: `PointCount` reads 3 after a write refused by contract |
| `ApplyWriteToOrdinaryServiceProperty` | the guard made unconditional (`if (true)`) | red: an ordinary service property stops accepting writes |
| `GateOnAComponentMember_ReportsDALE043` | `ReportMisplacedGates` removed | red at the component member while its twin on the block still fires |
| `GateOnAComponentClass_ReportsDALE043` | the type-level report removed | red |
| `ReportEmptyGateOnComponentService` | `DeclarativeServiceBinder` back to `IsNullOrEmpty` | red: `IncludedWhen` reads null |
| `ReportEmptyGateOnContractBinding` | `ContractMetaData` back to `IsNullOrEmpty` | red: the annotation key is absent |
| `ReportEmptyGateOnInterfaceBinding` | `FunctionInterfaceMetaData` back to `IsNullOrEmpty` | red: the annotation key is absent |
| `NameEveryFailingParameter` | the throw moved back inside the apply loop | red: only the first identifier is named |
| `ApplyNoValueWhenAnyParameterFails` | the apply moved back inside the loop | red: `PointCount` holds a value from a rejected configuration |
| `CarrySingleDecodeFailureAsInnerException` | the inner exception dropped | red |
| `RefuseSecondConfiguration`, `KeepBoundMembersOnRefusedSecondConfiguration` | the `_configured` guard removed | red: no exception, and `Point3` joins the bound set |
| `RefuseTopologyNamingUnknownInstantiationParameters` | the loader check removed; separately, reduced to the first error | red both ways |
| `AcceptTopologyNamingDeclaredInstantiationParameters` | the declared-set lookup inverted | red |
| `BindExactlyTheIncludedMembers` (3 rows) | the gate check removed from `DeclarativeServiceBinder` | red at rows 1 and 2 |
| `ResolveEnumParameterByMemberName` (3 rows) | the context encoded with `(int)` instead of the shared codec | red |
| `IntrospectWithoutEvaluatingAnyPredicate` | `IsIncluded` parses before the mode check | red: introspection throws |
| `BindRootServiceWithEveryGateClosed` | `CreateService(type.Name)` deleted | red |
| `DeliverToGatedInterfaceEndpointOnlyWhileIncluded` (2 rows) | the gate check removed from `DeclarativeInterfaceBinder` | red at the excluded row |
| `DeliverToClassImplementedInterfaceWithEveryGateClosed` | `BindClassBasedInterfaces` deleted | red |
| `CaptureInterfaceOnlyComponentStateByGateRatherThanBinding` (2 rows) | the included set inferred from binder keys instead of the gate | red at the included row |

**Amendment 2.**

| Test | Mutation | Result |
| --- | --- | --- |
| `CaptureNoInstantiationParameterMarkedPersistent` | the opt-in discovery skip removed | red: `_direct.PointCount` joins the snapshot |
| `LeaveInstantiationParameterUnchangedOnRestore` | the opt-in discovery skip removed | red: the restore writes 3 over the configured 2 |
| `PersistentParameter_ReportsDALE044` | the `[Persistent]` rule removed | red |
| `LogicBlockDefinition_FromType_CarriesGetOnlyInterfaceBindingWithItsGate` | the `CanWrite` filter restored | red: the endpoint is absent from the catalog entirely |
| `NameEarlierFailureWhenRefusingAfterFailedConfiguration` | `_configurationFailure` never recorded (the single-message refusal) | red: the message names the re-activation contract, not the failure |
| `AnswerRestoreOnInstanceWhoseConfigurationFailed` | the `IsInitialized` guard removed from the restore handler | red: `PersistentData not initialized` |
| `ReportEmptySnapshotOnInstanceWhoseConfigurationFailed` | the `IsInitialized` guard removed from the snapshot handler | red |
| `ReportGatedInterfaceBindingHoldingNull` | the null skip made unconditional again | red: the endpoint and its gate are absent |
| `CarrySingleDecodeFailureBesideUnresolvedIdentifier` | the inner exception keyed on `failures.Count` instead of `decodeFailures` | red: inner is null |
| `CarryChosenParameterValuesInTheConfigurationOutput` | `InstantiationParameters` dropped from the configuration output projection | red |
| `OmitParameterValuesForInstanceThatChoseNone` | the `WhenWritingNull` condition removed | red — **after amendment 3**: the assertion moved to the serialized document, since the in-memory DTO holds null either way and no mutation of the projection could reach it |

The restore guard M6 added is a "leave it unchanged" assertion in three places, so the mutation that
matters for it is the *inverted* one — a guard that skipped the restore entirely. That is caught by
`LogicBlockTestContextShould.RestorePersistentState` and `.RestoreMultiplePersistentValues`
(`Vion.Dale.Sdk.TestKit.Test`), which assert a restored value does arrive; without them, three
unchanged-assertions would all pass a block that never restores anything.

**Amendment 3.**

| Test | Mutation | Result |
| --- | --- | --- |
| `ReportGatedInterfaceBindingHoldingNull` (re-pointed to the sender side) | the `Describe` path removed, so a null implementation reaches the factory's extension registration | red: `ArgumentNullException (Parameter 'key')` through three `TargetInvocationException`s |
| ~~`SkipInterfaceBindingHoldingNullWhenConfiguring`~~ | ~~the `mode == BindingMode.Live` conjunct dropped from the null guard~~ | **withdrawn in amendment 5** — this test did not exist in amendment 3, and the mutation reddens `DescribeInterfaceBindingHoldingNullWhenIntrospecting`, not it |
| `LogicBlockDefinition_FromType_NamesEndpointsTheWayTheBinderDoes` | `InterfaceIdentifierFor` replaced by the unconditional `{Property}_{Interface}` | red: `PrimarySink` absent, the fallback name present |
| `NameFailureFromLaterConfigurationSteps` | the recorded phase narrowed back to parameter/Configure/BuildThrottlers | red: the refusal carries no reason for a `Ready()` throw |
| `PersistentExcludedParameter_NoDiagnostic` | the `Exclude` named-argument check removed | red: DALE044 on a correct declaration |

**Amendment 4 and 4b.**

| Test | Mutation | Result |
| --- | --- | --- |
| `SkipInterfaceBindingHoldingNullWhenConfiguring` (now on the bound-interface seam) | the whole null skip removed | red: the endpoint binds in a configured instance — the assertion the amendment-3 version made could not reach this |
| `DescribeInterfaceBindingHoldingNullWhenIntrospecting` | the `Describe` path removed | red |
| `DescribeInterfaceBindingHoldingNullWhenIntrospecting` | the `mode == BindingMode.Live` conjunct dropped, making the null skip unconditional | red — **corrected in amendment 5**: amendment 4 pinned this mutation on `SkipInterfaceBindingHoldingNullWhenConfiguring`, which stays green under it |
| `LogicBlockDefinition_FromType_NamesEndpointsByBinderRule` | the class-level mint reverted to `iface.Name` | red at the `Fleet` assertion |
| `NameFailureFromDeferredCompletion` | the deferred arm's `RecordingConfigurationFailures` wrapper removed | red — and no other test moves |
| `PersistentNotExcludedParameter_ReportsDALE044` | the `Exclude` check widened to skip any `[Persistent]` | red |
| `RefuseIntrospectingGateOutsideGrammar`, `RefuseIntrospectingGateReferencingUnknownName` | `EnsureResolvable` removed from the three binder sites | red |
| `IntrospectGateOverNullDefaultedParameter` | the resolution probe replaced by the instance's own values | red — the false positive `AC-GATE-006.5` exists to prevent |
| `RefuseEmptyGateOnEveryMemberKindThatRecordsOne` (3 rows) | each recording site reverted to `IsNullOrEmpty` in turn | red at that row — the member reads ungated and introspection succeeds |
| `RefuseTopologyCarryingUndecodableParameterValue`, `ReportUnknownIdentifierAndUndecodableValueTogether` | the loader's decode call removed | red |
| `CarryNullThroughForNullableParameter` | the schema widening reverted | red at parse |
| `LogicBlockDefinition_FromType_SaysWhetherEachDefaultWasRead` (2 rows), `…ReportsKnownNullDefaultAsKnown` | `DefaultKnown` hard-coded false | red |
| `wiring-missing-parameter.test.mjs` (4 cases) | the `defaultKnown` guard dropped | red at the unknown-default case |

**Amendment 5.**

| Test | Mutation | Result |
| --- | --- | --- |
| `RefuseIntrospectingCompoundGateReferencingUnknownName` (5 rows: `&&`, `\|\|`, nested parentheses, `!`, membership) | the tree walk skipped (`CollectReferences` not called) | red at all 5 — and red at all 5 against `d12bc37`'s evaluating implementation, which is the defect proof |
| `RefuseIntrospectingGateReferencingUnknownName` | the same | red — 6 red in total, and no other test in the class moves |
| `DescribeInterfaceBindingHoldingNullWhenIntrospecting` (gate assertion added) | the endpoint's gate not recorded (`ApplyMetadata` passing `null`) | red — over-determined with `AC-GATE-010.2`'s `ReportInterfaceBindingGateAsAnnotation`, which the same mutation also reddens |
| `SkipInterfaceBindingHoldingNullWhenConfiguring` | the whole null skip removed | red: the endpoint binds in a configured instance |
| `ServeTheGenericTopologySchema` (type assertion added) | the schema widening reverted to `["boolean", "integer", "string"]` | red |
| `wiring-missing-parameter.test.mjs` + the editor's footer list | `missingParameterValueProblems` dropped from `components.js`'s `problems` computed | the string cannot reach a page — which is the state amendment 4b shipped, and what the withdrawn Tier 2 row claimed to have seen |

`ReportGatedInterfaceBindingHoldingNull` is **deleted**, not re-pointed: its mutation
(the `Describe` path removed) is already carried by
`DescribeInterfaceBindingHoldingNullWhenIntrospecting` in the amendment-4 table, and after the gate
assertion above the two tests were identical.

**Amendment 6.**

| Test | Mutation | Result |
| --- | --- | --- |
| `AgreeWithRuntimeParserOnEveryParseVector` | the linked grammar widened to accept an unquoted enum member (`Mode == Eco`) — the `ExpectedLiteral` throw replaced by a string literal | red: 1 disagreement, and neither eval test moves |
| `IntrospectGateOverBareBooleanReference` | the `BoolRefNode` case deleted from the walk | red: a bare-reference gate reaches the walk's `default:` and a legitimate block is refused |
| `ExtractEveryNameEachEvalVectorResolves` | the same | red — the vector's own bare `DirectMeasurement` rows reach the deleted case, so the two tests catch it independently |
| `BindOnBareBooleanReference` (2 rows) | the gate check removed from `DeclarativeServiceBinder` | red at the `false` row |

`AgreeWithRuntimeParserOnEveryEvalVector` has no mutation of its own: every eval predicate is inside
the grammar by construction, so it is the drift alarm for a *future* divergence rather than a guard
over today's behaviour. It is kept because the `parsed.Ast is null` branch fires on exactly that.

**Names above that no longer grep**, checked mechanically over the whole ledger in amendment 5. Each
row stays as the record of the amendment that ran it; this is where it resolves to today:

| In the ledger | Today |
| --- | --- |
| `IntrospectWithoutEvaluatingAnyPredicate` | `InclusionGateShould.IntrospectWithoutDecidingAnyGate` (renamed for §12) |
| `CarryChosenParameterValuesInTheConfigurationOutput` | `ConfigTimeGatingShould.CarryChosenParameterValuesInConfigurationOutput` (renamed for §12) |
| `LogicBlockDefinition_FromType_NamesEndpointsTheWayTheBinderDoes` | `…_NamesEndpointsByBinderRule` (renamed twice: amendment 4 widened it to the class-level half, amendment 5 dropped the article) |
| `ReportEmptyGateOnComponentService`, `…OnContractBinding`, `…OnInterfaceBinding` | one `[DynamicData]` test, `ConfigGatingMetadataShould.RefuseEmptyGateOnEveryMemberKindThatRecordsOne` — 4b changed the behaviour from *report* to *refuse*, so the three tests became three rows |
| `ReportGatedInterfaceBindingHoldingNull` | `GatedMemberBindingShould.DescribeInterfaceBindingHoldingNullWhenIntrospecting` (deleted as a duplicate in amendment 5, above) |

**Over-determined criteria.** `AC-GATE-005.6` (a predicate that does not parse fails initialization) is
enforced twice — `DALE043` refuses it at build time and the evaluator refuses it at bind. Both guards
are mutated and both redden their own test (`RefuseGateOutsideGrammar` for the runtime half,
`PredicateOutsideTheGrammar_ReportsDALE043` for the compile-time half), and the criterion is kept
because the analyzer is suppressible and absent from projects that do not wire the pack. The same is
true of `AC-GATE-005.7` and its `ReferenceToANonParameterProperty_ReportsDALE043` twin.

---

## Scorecard (pass 3 — attempt 1, six amendments, three reverts)

| Measure | Value |
| --- | --- |
| Gates (build/test/lint/trace/style/self-tests/cleanup/CI) | green locally after amendment 6 — every line below is a paste, including the ones whose numbers did not move. (Amendment 3 composed its numbers, amendment 4 carried them stale, and amendment 5 wrote a cleanup line the script does not print; all three are the same defect from a different angle.) <br>`Build succeeded.` · `0 Warning(s)` · `0 Error(s)` — zero `DALE` warnings in the SDK's own build. <br>`dotnet test Vion.Dale.Sdk.sln` — 27 projects, `Failed:     0` on all 27. Largest: `Passed:   455, Skipped:     4, Total:   459` (`Vion.Dale.Sdk.Test`), `Passed:   382` (`…Generators.Test`), `Passed:   367` (`…Modbus.Tcp.Test`), `Passed:   254` (`…DevHost.Test`). <br>`node --test` in `Vion.Dale.DevHost.Web`: `# tests 41` · `# pass 41` · `# fail 0`. <br>`spec-lint: OK` <br>`spec-trace: OK - 172 id(s) all referenced by tests (3 traced page(s); 3 GAP id(s) awaiting tests: AC-EMIT-002.4, AC-EMIT-009.4, AC-GATE-012.5)` <br>`test-style-lint: OK - 200 cited test(s) conform (88 file(s) in exempt projects skipped)` <br>`run-script-tests: OK - 4 self-test(s) passed; 7 script(s) exempt` <br>`scripts/cleanup-code.ps1 -Changed`: `Cleanup scoped to 62 changed .cs file(s).` then `cleanupcode applied changes - review with 'git diff' and commit:` / `  modified vs HEAD (a local run also lists edits you made yourself):` and a `git diff HEAD --stat` ending `8 files changed, 308 insertions(+), 27 deletions(-)`. <br>devhost-smoke Tier 1: `Passed:    27, Skipped:     0, Total:    27`. Tier 2 was re-run live in amendment 5 for the missing-parameter case; amendment 6 changes one comment in `components.js` and no SPA behaviour. CI not yet run. <br>**Reconciling the counts.** Cleanup's `8 files` is its own view and this commit carries **9**: the script's `$excludePaths` drops `Vion.Dale.DevHost.Web/wwwroot/**`, so the one-comment `components.js` edit is in the commit and never in that stat. Its `308 insertions(+), 27 deletions(-)` is a snapshot taken when it ran, before this scorecard row and the amendment-6 checkpoint were written; the commit's own stat reads `9 files changed, 320 insertions(+), 37 deletions(-)`. The script's stat is `git diff HEAD` and so, as its own line says, lists the author's uncommitted edits alongside its rewrites — it is a style verdict, not a change census. `172 ids` is unchanged from amendment 5 — amendment 6 minted no criterion, it cited two that already existed. `200 cited` is amendment 5's 198 plus the two new `InclusionGateShould` tests; the three `PredicateParserShould` tests cite no id by design, since they hold a design premise rather than prove a criterion. |
| Completeness-critic misses | **7 fixed** (M1 persistence's second door · M2 the catalog's get-only skip · M3 the refusal after a failed configuration · M6 restore on a fail-closed instance · M7 a null interface binding in the definition view · the `AC-GATE-002.9` count · row 58's probe claim), **2 stated** (M4, M5), **2 parked** (M8, M9). By sweep: 4 from the state-interaction sweep (M3, M6, M7 and row 58's shape), 2 from the consumer sweep (M2, M4), 3 from the statement sweep (M1, M5, `AC-GATE-002.9`). |
| Evidence errors found in review | **14**, all corrected or annotated in place. Found before amendment 5 (5): (1) row 58's "(probe)" claim — a get-only binding reaching the catalog, when the probe's property was settable; (2) the same claim restated in row 58's evidence cell; (3) and again in the Step 4 paragraph; (4) the amendment-2 ledger entry naming a mutation that could not redden its test; (5) the amendment-3 scorecard's gate numbers, composed rather than read. Found in the review of amendment 4/4b (6): (6) **the Tier 2 row for the known-null default — an observation that had not been made**, since nothing imported the function that produces the string; the pass's most serious evidence error, and the one with its own journal line; (7) the amendment-3 mutation row pinning "the `mode` conjunct dropped" on `SkipInterfaceBindingHoldingNullWhenConfiguring`, a test that did not exist then and stays green under it; (8) the amendment-4 row restating the same false pairing; (9) amendment 4's claim that (7)'s entry and its checkpoint sentence were "both corrected" — neither had been touched; (10) the 4b sentence claiming a predicate type mismatch is refused at introspection, true only of the placeholder evaluator that amendment 5 removed; (11) the amendment-4 scorecard's gate numbers, carried from amendment 3 rather than re-run — the same class as (5), recurring. Found in the review of amendment 5 (3): (12) **three sites claiming the two predicate parsers are "pinned to one grammar by `PredicateConformanceVectorTests`"** — that test runs the vector through the linked parser only, so the claim named a guarantee this repo did not hold, and the `parsed.Ast is null` refusal rested on it; (13) the amendment-5 scorecard declaring "every line below pasted from the terminal" while carrying `` `cleanupcode applied changes` on 9 files ``, a sentence `cleanup-code.ps1` does not print — the paste rule broken inside the sentence asserting it; (14) `components.js`'s merge comment saying all four problem producers share `{ mappingIndex, kind, message }`, when two of the four carry no `mappingIndex` at all. **14 total.** |
| Mutation evidence | complete — every added or fixed criterion has its named mutation above, run and observed, with one stated exception: `AgreeWithRuntimeParserOnEveryEvalVector` is a drift alarm for a future divergence and no mutation of today's code reddens it, which is said on its ledger row rather than papered over. Four over-determined criteria stated (`AC-GATE-005.6`, `AC-GATE-005.7`, and `AC-GATE-007.8`'s gate half against `AC-GATE-010.2`'s two tests) with each guard mutated. Two false pairings from earlier amendments are withdrawn rather than left standing — see rows (7)–(9) above. |
| Operator corrections (table + PR) | table: 0 overrides, 8 ⚠ rows classified as recommended with 5 decisions attached; amendment 2: 11 review items, all applied; PR: not yet opened |
| Parked findings | opened with 4 rows; **3 fixed in-pass** at the operator's word in amendment 4b (the unchecked predicate on the pack path, the catalog's unknown-versus-null default, the topology schema's refusal of null) and the fourth **narrowed** to the actor half. The first of those three was re-implemented in amendment 5 — 4b's version passed introspection for any compound predicate. |
| Cost | 2 sessions · Opus · 6 amendments (2, 3, 4, 4b, 5, 6) · 3 reverts (the whitespace-polluting reference sweep; the two vendored-file edits) |
