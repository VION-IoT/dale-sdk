---
slug: tkit-pass
status: proposed           # proposed | in-flight | parked | archived
blocked-on: none           # for parked docs: what's blocking + ref
areas: TKIT
author: spec-pass session (TKIT, attempt 1)
created: 2026-09-05
updated: 2026-09-05
supersedes: none           # path of a superseded change doc, or none
---

# TKIT area pass — the five test kits as one contract

> Change doc — one in-flight change. The `Spec delta` below is distilled into the current-truth
> pages under `docs/specs/` in the implementation PR, then this doc is archived
> (`pwsh scripts/spec-change.ps1 archive <slug>`). Process: `docs/spec-process.md`. Never put
> change narrative inline in a spec page — it lives here.

## At a glance

### Summary

The thirteenth SDD area pass: the five packable test kits — the core kit, the two I/O kits and the
two Modbus kits — become one current-truth spec page, `docs/specs/testkit.md`, with an id-cited test
suite across their five MSTest projects. This is the pass that empties `docs/rfcs/`: RFC 0001
(virtual time, Implemented) and RFC 0002 (handler test kit, Draft) are absorbed and deleted, and the
directory goes with them.

### Spec implications

Creates `docs/specs/testkit.md` (`trace: enforced`), the area's first page. It states what a block
author calls, what each helper refuses and with which exception and message, what every default is
(`Times.Once()`, tolerance `0`, the `2026-01-01T00:00:00Z` anchor, `EmissionPolicyMode.Off`,
`TimeProvider.System` on the TCP harnesses), what the builder's five phases do, what `AdvanceTime`
and `FlushPendingActions` each drive, and what the two Modbus fakes stand in for.

Cited, never re-minted: `block-lifecycle.md` (the five phases the builder drives), `emission.md`
(`AC-EMIT-001.3/.4` — the policy override), `config-gating.md` (`AC-GATE-012.1`),
`contracts.md`/decision `0081` (the binder rules the kit diverges from), `modbus.md` (the client and
queue the fakes stand in for), `io.md` (the four faces). `scenarios.md:20`'s unnamed deferral — "the
TestKit's virtual time and block lifecycle are other pages'" — resolves to this page.

Touches `docs/testing-conventions.md` § 7 (a stale sentence, corrected in-pass),
`docs/spec-process.md` and `CLAUDE.md` (the `docs/rfcs/` sweep).

### Decisions

- `D1` — **Page name `docs/specs/testkit.md`, area code `TKIT`.** One page for five packages: they
  ship as one contract (four of the five exist only to extend the core kit's
  `LogicBlockTestContext<T>`), and a page per package would state the same builder five times.
  The roster (`docs/spec-process.md:35`) reserves `TKIT` for "TestKit surfaces (virtual time)"; the
  page is wider than virtual time and the code is unchanged.
- `D2` — **Spine:** construction and lifecycle → the recording context and the verify family → the
  raise helpers → time → the two Modbus kits → the surface → the test discipline. That is the order
  an author meets the machinery, and it is the coordinator's recommendation in the brief (point 2),
  adopted unchanged.
- `D3` — **The harness is the five existing MSTest projects; no new project.** The kit is the SUT,
  driven through a fixture block (`SampleLogicBlock` / `SampleProviderLogicBlock` — the ideal-echo
  recipe of `docs/simulator-authoring.md`), never through a runtime, a broker, a device or the
  DevHost. The three uncited projects (Digital, Analog, Rtu) are rewritten to
  `docs/testing-conventions.md` § 9–17 where a rewrite is needed to cite.
- `D4` — **A change reaching a signature, a default, an exception type, a message shape or a package
  reference is a `propose` row, not a `fix`** — the kits are published packages with 126 consumer
  `.cs` files in `logic-block-libraries` alone. A fix *inside* a helper that leaves every existing
  assertion's outcome unchanged is a `fix` (brief point 4), and every such row below names the
  reader search that proved no consumer depends on today's behaviour.
- `D5` — **RFC 0002 is absorbed as a park, not implemented.** A handler test kit is a new
  `[PublicApi]` type family — a change doc's worth of work, and outside a pass's size guard. The
  page states what the kits do *not* offer and why the SDK's own handler suites roll their own; the
  ledger carries the design. This is the one question whose answer changes the pass's size, and it
  is Reviewer's question 7 below.

### Reviewer's questions

1. **(ratified — cite)** The ownership split. Rows that cite instead of mint: the lifecycle the
   builder drives (`AC-LIFE-*`), emission policy (`AC-EMIT-001.3/.4`), gating (`AC-GATE-012.1`), the
   binder's inclusion and discovery rules (`AC-BIND-*`, decision `0081`), the Modbus client and queue
   (`AC-MODB-*`), the I/O faces (`AC-IO-*`).
   **OUTCOME:** (pending classification)

2. **(decide-and-document)** The page name and spine — `D1`, `D2`.
   **OUTCOME:** (pending classification)

3. **(decide-and-document)** The harness — `D3`.
   **OUTCOME:** (pending classification)

4. **(propose-and-wait)** The six `propose` rows below — **7, 22, 44, 47, 80, 144**. Each is a
   surface change with readers outside this repository (a new member, an exception type, a default,
   a rendered message, or a discovery rule), and each names the reader search that found no
   dependant. Accepting a row makes it a `fix` in Phase B; declining makes it a ledger line. My
   recommendation is on each row: **7 accept** (unwrap the reflection exception), **22 decline —
   state it** (widening discovery would make a test's graph depend on what else is loaded),
   **44 accept** (pin the invariant culture; no rendered string changes), **47 decline — keep and
   document** (deleting two helpers is unreachable under `sdk-surface-conventions.md` § 3),
   **80 decline** (`WithTimeProvider` already serves it, 33 consumer sites), **144 decline — state
   the default** (changing it moves what 81 consumer sites measure).
   **OUTCOME:** (pending classification)

5. **(decide-and-document)** The observables the brief flagged, each classified on its mechanism
   below. Row numbers re-read against the finished table, not carried from the brief: the clock
   landing after a throwing action → **91** (`fix`); `Create<T>` without an `ILogger` ctor → **5, 6,
   7** (`fix`, `fix`, `propose`); `AssertCount` on `Between` and `AtMostOnce` → **42, 43** (`fix`,
   on probe **P2**'s `Times.Validate` result); both TCP harness ctors' wall-clock default → **144**
   (`propose`, recommend decline); the fake client's delays without a `Clock` → **139, 140**
   (`intended`, `fix`); `Verify*Changed`/`Verify*Emitted` identical → **47** (`propose`, recommend
   decline); the three test csprojs' parallelisation → **refuted, Drift checkpoint 3** — no row; the
   file/type name mismatch → **178** (`fix`); the missing package id in `set-version.ps1` → **176**
   (`fix`); the two downstream suites without the switch → **177** (`park`); the seven ledger lines →
   **21** (BIND 191, `park`), **22** (BIND 193, `propose`), **151** (MODB queue age, `fix`), **72**
   (IO 41 non-finite value, `fix`), **76** (IO 60 different suites, `fix`), **51** (IO 61
   `messageKind`, `fix`), **64** (IO 63 empty identity, `fix`); the stale conventions sentence →
   **183** (`fix`); the helpers no consumer calls → **41, 48, 52, 53, 55, 104, 150** (all
   `intended`, stated as the surface — a delete-don't-deprecate proposal is unreachable per `D4`);
   Moq in the signature → **172** (`intended`, stated).
   **OUTCOME:** (pending classification)

6. **(decide-and-document)** `FlushPendingActions`' 554 consumer call sites are legacy, not unmet
   demand: `EnergyManagerCycleHarness.cs:31-38` names `AdvanceTime` as the fix and says in as many
   words "This is NOT an SDK/TestKit defect". Row 82 is `intended` with the rule stated; no new
   driver is proposed.
   **OUTCOME:** (pending classification)

7. **(propose-and-wait — the one question that changes the pass's size)** RFC 0002 as a park (`D5`),
   or implemented in this pass? Recommendation: **park**. Its four gaps (`0002:9-12`) are all live,
   and its `:12` is stale by three replications — the IO pass's `HandlerHarness`/`ContractHarness`
   and `Vion.Dale.Sdk.Test/TestHelpers/LifecycleHarness.cs:149`'s `RecordingActorContext`. The root
   cause is one line: `LogicBlockTestContext<TLogicBlock> where TLogicBlock : LogicBlockBase`
   (`LogicBlockTestContext.cs:37-38`) cannot host a handler, so the extraction RFC 0002 proposes
   (`ActorTestContextBase`) is a refactor of a published generic type.
   **OUTCOME:** (pending classification)

---

## Full design

### What this area is

Five NuGet packages, all `net10.0`, all `IsPackable=true`, **35 public types = 35 `[PublicApi]`
marks, 0 `[InternalApi]`, 3590 LOC across 35 `.cs` files**:

```
$ for d in Vion.Dale.Sdk.TestKit Vion.Dale.Sdk.DigitalIo.TestKit Vion.Dale.Sdk.AnalogIo.TestKit \
           Vion.Dale.Sdk.Modbus.Rtu.TestKit Vion.Dale.Sdk.Modbus.Tcp.TestKit; do … done
Vion.Dale.Sdk.TestKit:            files=13 loc=1460 PublicApi=11 InternalApi=0
Vion.Dale.Sdk.DigitalIo.TestKit:  files=5  loc=205  PublicApi=4  InternalApi=0
Vion.Dale.Sdk.AnalogIo.TestKit:   files=5  loc=215  PublicApi=4  InternalApi=0
Vion.Dale.Sdk.Modbus.Rtu.TestKit: files=4  loc=398  PublicApi=3  InternalApi=0
Vion.Dale.Sdk.Modbus.Tcp.TestKit: files=8  loc=1312 PublicApi=13 InternalApi=0
                                  ------  --------  -----------
                                  35      3590      35
```

The core kit is the substrate: `LogicBlockTestContext<TLogicBlock>` is an `IActorContext` that
records what a block sends and hosts a `FakeTimeProvider`. The other four kits are extension-method
packages over it — the two I/O kits add raise and verify helpers per face, the RTU kit adds response
simulation, and the TCP kit adds a fake proxy, two harnesses and a synchronous request queue.

### Anchor inventory

This area's machine-readable surface is the `[PublicApi]` mark and the manifest it feeds.

| Anchor kind | Instances | Read by |
|---|---|---|
| `[PublicApi]` on a type | **35** (11 + 4 + 4 + 3 + 13) | `docs/snapshots/publicapi-manifest.json`; `DALE014` **only where the analyzer is referenced — no kit references it** (row 171) |
| `[InternalApi]` on a type | **0** | — |
| `[assembly: PublicApiNamespace(…)]` | **5** — one per kit, its own root namespace (`Vion.Dale.Sdk.TestKit/PublicApiConfig.cs:3` and four twins) | **nothing** — the declaration has no reader in any kit (row 171, probes **P3**/**P5**) |
| Manifest rows in a `*.TestKit` namespace | **35** of 121 | `.github/workflows/publish.yml`'s regenerate-and-diff step |
| `const` / `static readonly` declarations | **5** — `Constants.LogicBlockId` `:11`, `Constants.LogicBlockName` `:13`, `FakeModbusTcpServerProxy.RegisterBufferSize` `:20` and `BitBufferSize` `:22` (both private), `LogicBlockTestContext.DefaultAnchor` `:42-48` | rows 1, 18, 64, 122 |
| Timeouts | **0** — no `Timeout`, no `CancellationTokenSource` with a delay anywhere in the five kits | row 84 |
| `throw new` sites | **53** (11 Tcp, 42 the rest) | rows throughout |
| `InternalsVisibleTo` grants into the area | **2** — `Vion.Dale.Sdk.csproj:23` (`…TestKit`) and `:25` (`…TestKit.Test`) | rows 19, 129 |
| Private-field reaches (`GetPrivateField`) | **3** — `_serviceBinder` (`Builder:446`), `_interfaces` (`Builder:486`), `_timerCallbacks` (`LogicBlockTimerExtensions.cs:75`) | rows 16, 17, 86 |

The `[assembly: PublicApiNamespace]` row is the anchor-kind-with-no-reader shape the skill's step 2
asks for: the target is declared in all five kits and walked by nobody.

### Probes run this session

Every probe below ran against the tree at `spec-pass/tkit-a1`'s base commit (`1067807`), on
`net10.0`, Moq **4.20.72** (`typeof(Times).Assembly.GetName().Version` → `4.20.72.0`). Fixture
shapes are named because a probe is evidence only for the shape it ran.

| Probe | Fixture shape | What it read | Result |
|---|---|---|---|
| **P1** | `Times` values passed to the shipped `TimesExtensions.AssertCount(count, "probe")` | which forms it recognises | `AtMostOnce()` throws a raw `ArgumentOutOfRangeException: length ('-1') must be a non-negative value` at **every** count; `Between(1,3,Inclusive)` throws `TestKitVerificationException` at **every** count including 1, 2 and 3; `Between(…,Exclusive)` likewise at 2 |
| **P2** | the same values, `times.Validate(count)` beside `AssertCount` | whether Moq's own predicate agrees | **61 of 70 cells agree**; all 9 disagreements are the two broken forms above. `Times.Validate(int)` is `public Boolean Validate(Int32)` on 4.20.72 |
| **P3** | a bare `public class ZzProbeUnmarked` in `Vion.Dale.Sdk.TestKit` **and** the same shape in `Vion.Dale.Sdk/Core` | whether `DALE014` reaches a kit | control fires (`warning DALE014 … 'Vion.Dale.Sdk.Core'`); the kit is **silent** and the build succeeds |
| **P4** | the analyzer added to all five kit csprojs, `dotnet build … --no-incremental` over the solution | what the analyzer would say about the kits as they stand | **0 DALE diagnostics**, build succeeded |
| **P5** | P4's tree plus one unmarked public type per kit | whether the addition actually arms the gate | `DALE014` fires in **all five** kits |
| **P6** | a block scheduling a throwing action at +5 s and a recording one at +8 s, `AdvanceTime(10s)` | where the clock lands when an action throws | exception propagates; **clock lands at +5 s, not +10 s**; the +8 s action stays queued and a following `AdvanceTime(Zero)` does not reach it |
| **P7** | blocks with no `ILogger` ctor / abstract / throwing ctor | what `Create<T>` raises | `MissingMethodException: Constructor on type 'X' not found.` (both the no-ctor and the abstract case); `TargetInvocationException` wrapping the author's own exception |
| **P8** | `VerifyAnalogOutputSet` over eleven value/tolerance pairs on an echo block | the tolerance comparison's domain | `NaN`/±∞ against themselves fail at any tolerance; **tolerance `NaN` and tolerance `-1` reject an exact match**; tolerance `+∞` accepts any finite value; `-0.0` vs `0.0` passes |
| **P9** | zero-delay, equal-deadline, exact-deadline and re-entrant drives | dispatch order and the reentrancy guards | `AdvanceTime(Zero)` fires both zero-deadline actions in insertion order; a delta landing exactly on a deadline fires it; both `AdvanceTime` and `FlushPendingActions` refuse re-entry with `InvalidOperationException` |
| **P10** | a block with no `[Timer]` | the timer helpers' refusals | `FireTimer` → `…Available timers: (none).`; `GetTimerInterval` → the same first sentence **without** the available-timers tail |
| **P11** | builder knobs on an echo block | what each knob refuses | `WithPersistentValue` for a non-persistent property is **silently accepted**; `WithInstantiationParameter` for a plain property is refused **by the block**, not the kit; all three null guards fire |
| **P12** | `WithServices` vs `WithTimeProvider` both registering a clock | which clock the block and the context see | `WithServices`' clock wins in DI and **leaves `ctx.TimeProvider` unchanged** — block reads 2030, `AdvanceTime` drives 2026; `WithTimeProvider` binds both and moves the anchor |
| **P13** | `Build()` with and without auto-start | what the recording holds afterwards | auto-start: **0** recorded; `WithoutAutoStart`: **2**, both `BindLogicBlockServices` |
| **P14** | `VerifyContractMessageSent<SetAnalogOutput>` with four `messageKind` values | what `messageKind` filters | `"AnalogOutput"`, `"TotalNonsense"`, `""` and `null!` all pass identically; the argument reaches only the failure string |

Consumer counts, `git grep -o … | wc -l` in `C:\_gh\logic-block-libraries` at `3cd8324a`:

```
files matching Vion.Dale.Sdk.*TestKit|LogicBlockTestHelper|LogicBlockTestContext
    152 (all file kinds)   126 (*.cs only)
FlushPendingActions 554 · CreateTestContext 318 · WithLogicInterfaceMapping 237 ·
VerifySendStateUpdate 205 · AdvanceTime 144 · LogicBlockTestHelper.Create 139 ·
FakeModbusTcpHarness 81 · VerifySendCommand 35 · WithTimeProvider 33 ·
ClearRecordedMessages 23 · VerifyContractMessageSent 14 · VerifySendRequest 11 ·
RaiseSetReceived 10 · FakeModbusTcpClientProxy 8 · RaiseInputChanged 6 ·
VerifyDigitalOutputSet 5 · WithoutAutoStart 4 · VerifyServicePropertyEmitted 3 ·
WithPersistentValue 3 · SynchronousRequestQueue 2 · TestActorReference 1 ·
ModbusResponseBuilder 1 · SimulateReadResponse 1
zero: InitializeForTest · VerifyServicePropertyChanged · WithServices ·
      WithInstantiationParameter · FireTimer · GetTimerInterval · VerifyLogContains ·
      GetSentMessagesOfTypePublic · GetContractMessages · CreateWithLogger ·
      BuiltServiceProvider · AssertCount · VirtualNow · VerifyModbusReadSent ·
      VerifyAnalogOutputSet · MaxQueuedAge · Times.Between · Times.AtMostOnce ·
      MissingMethodException
$ git grep -n -E 'Throws.*AdvanceTime|AdvanceTime.*Throws'    -> (no matches)
```

The last four zeroes are what makes rows 41–43, 79, 100 and 7 `fix`-shaped rather than
`park`-shaped: no consumer catches the exception, names the `Times` form, sets the age, or reads the
clock after a throwing drive.

---

## Drift checkpoints

- `2026-09-05`: **The brief's "39 citations" is right and my first count of 40 was not.** A bare grep
  for `AC-[A-Z]+-[0-9.]+` over `Vion.Dale.Sdk.TestKit.Test` returns 40; the fortieth is a prose
  mention in a comment (`EmissionPolicyShould.cs:375`). The gated form
  (`[TestProperty("spec", "AC-…")]`) is **39**, plus **24** in `Vion.Dale.Sdk.Modbus.Tcp.TestKit.Test`
  = **63**. The table's counts use the gated form throughout.
- `2026-09-05`: **The brief's consumer file count of 152 is across all file kinds, not `.cs`.**
  `git grep -l … -- '*.cs'` at `3cd8324a` is **126**; `git grep -l …` over every kind is **152**.
  Both are recorded above; the page's prose uses 126, because a `.md` naming a helper is not a
  caller.
- `2026-09-05`: **REFUTED — the brief's parallelisation asymmetry does not exist.** The brief says
  Digital, Analog and Rtu "declare neither `IsTestProject` nor `IsPackable=false` … so those three
  suites run parallelised while the other two do not". The premise is read correctly off the three
  csprojs, but the conclusion does not hold: the MSTest 4.0.2 package's props set both properties,
  and MSBuild evaluates every `<PropertyGroup>` before any `<ItemGroup>` condition, so
  `Directory.Build.props:41-43`'s conditional `AssemblyAttribute` applies to all five.
  `dotnet msbuild -getProperty:IsPackable,IsTestProject` returns `false`/`true` for **all five**, and
  the generated `obj/**/*.AssemblyInfo.cs` carries `DoNotParallelize` in **all five** (`grep -c` →
  `1` each). There is no row and no fix; the *real* asymmetry those three csprojs carry is the
  missing `Microsoft.NET.Test.Sdk` reference, which is row 141.
- `2026-09-05`: **`scripts/sweep-residue-lint.tests.ps1:86` does not depend on the real
  `docs/rfcs/`.** It creates `docs/rfcs/0001-old.md` inside a temp tree to exercise the out-of-scope
  regex at `sweep-residue-lint.ps1:38` (`^docs/(rfcs|retro|changes/archive|snapshots)/…`). Both files
  stay untouched: the exclusion is a defensive rule that stays correct with the directory gone, and
  removing `rfcs` from the regex would redden the script's own self-test for no gain. Stated so the
  sweep's third "live mention" is closed by reading rather than by editing.
- `2026-09-05`: **The brief's "six raise helpers all of one shape" is six across two kits, three
  each.** There is no `IDigitalInputProviderExtensions` and no analog twin, and that is correct, not
  a gap: an input provider face carries `Drive` and no event (`io.md:53`), so there is nothing to
  raise on it. The six are `RaiseInputChanged`, `RaiseOutputChanged` and `RaiseSetReceived` per kit.

---

## Behavior table

**187 rows**, numbered 1–187 with no hole and no duplicate
(`grep -oE '^\| [0-9]+ \|' … | grep -oE '[0-9]+'` walked). Numbers are **allocation order, not table
order**: rows 186 and 187 were minted by the self-check below, after the first 185 were written, and
sit in the groups they belong to rather than at the end. `⚠` marks a `fix`, `park` or `propose` rec —
**32 of them**, one per such rec — and each has a two-line failure sketch under its group; a
`Note N` sketch under a group belongs to an `intended` row whose shape is worth a sentence. Evidence
is `file:line` read this session. `Test today` names the existing test or `GAP`.

Paths are abbreviated: `Ctx` = `Vion.Dale.Sdk.TestKit/LogicBlockTestContext.cs`, `Bld` =
`Vion.Dale.Sdk.TestKit/LogicBlockTestContextBuilder.cs`, `Helper` =
`Vion.Dale.Sdk.TestKit/LogicBlockTestHelper.cs`, `Timers` =
`Vion.Dale.Sdk.TestKit/LogicBlockTimerExtensions.cs`, `Times` =
`Vion.Dale.Sdk.TestKit/TimesExtensions.cs`, `DigExt` =
`Vion.Dale.Sdk.DigitalIo.TestKit/LogicBlockTestContextExtensions.cs`, `AnaExt` =
`Vion.Dale.Sdk.AnalogIo.TestKit/LogicBlockTestContextExtensions.cs`, `RtuExt` =
`Vion.Dale.Sdk.Modbus.Rtu.TestKit/IModbusRtuExtensions.cs`, `Fake` =
`Vion.Dale.Sdk.Modbus.Tcp.TestKit/FakeModbusTcpClientProxy.cs`, `Queue` =
`Vion.Dale.Sdk.Modbus.Tcp.TestKit/SynchronousRequestQueue.cs`, `SrvProxy` =
`Vion.Dale.Sdk.Modbus.Tcp.TestKit/FakeModbusTcpServerProxy.cs`.

### A. Construction and the builder's five phases

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 1 | THE SYSTEM SHALL construct a logic block for a test from its `ILogger` constructor, supplying a Moq `ILogger` mock. | `Helper:27-32` | `LogicBlockTestContextShould.CreateLogicBlockWithFactoryMethod` | intended | the entry point 139 consumer call sites use |
| 2 | THE SYSTEM SHALL offer a construction form returning the block and the logger mock together. | `Helper:39-45` | `.CreateLogicBlockWithLoggerMock` | intended | the form a log assertion needs |
| 3 | THE SYSTEM SHALL create a fresh `ILogger` mock per construction. | `Helper:17-20`, `:30`, `:42` | GAP | intended | two blocks never share a recorded log |
| 4 | THE SYSTEM SHALL supply the logger mock as the block's only constructor argument, so a block taking any other constructor cannot be created this way. | `Helper:31` (`Activator.CreateInstance(typeof(T), loggerMock.Object)`) | GAP | intended | the shape row 7 is about |
| 5 | ⚠ WHEN a block declares no single-`ILogger` constructor THE SYSTEM SHALL refuse construction with a message naming the constructor the helper requires. | `Helper:31`; probe **P7**: `MissingMethodException: Constructor on type 'NoLoggerCtorBlock' not found.` | GAP | fix | today a raw BCL exception names the type and not the requirement; `MissingMethodException` has 0 consumer readers |
| 6 | ⚠ WHEN the requested block type is abstract THE SYSTEM SHALL refuse construction with the same message. | probe **P7**: abstract block → `MissingMethodException: Constructor on type 'AbstractBlock' not found.` | GAP | fix | same site, same shape; the sibling of row 5 |
| 7 | ⚠ WHEN a block's own constructor throws THE SYSTEM SHALL surface that exception rather than a reflection wrapper. | probe **P7**: `TargetInvocationException` wrapping `NotSupportedException("ctor boom")` | GAP | propose | changes the exception type a caller catches; 0 consumer sites name `TargetInvocationException`, but this is the one of the three that a consumer could plausibly be catching |
| 8 | THE SYSTEM SHALL create a builder from any `LogicBlockBase`, and SHALL offer a one-call form that builds it immediately. | `LogicBlockBaseExtensions.cs:14-18`, `:24-29` | `.InitializeLogicBlockForTest` | intended | `CreateTestContext` has 318 consumer sites, `InitializeForTest` 0 in the first consumer and all 42 in the three uncited kit suites |
| 9 | WHEN a builder is created for a null block THE SYSTEM SHALL refuse it, naming the block argument. | `Bld:53`; probe **P11** | GAP | intended | a guard with a named parameter |
| 10 | THE SYSTEM SHALL drive a built block through five phases in order — initialize, link the runtime actors, restore persistent state, set the linked interfaces, start. | `Bld:208-216` | GAP | intended | the whole of what `Build()` guarantees; the phases themselves are `AC-LIFE-*`, cited |
| 11 | THE SYSTEM SHALL initialize a block under a fixed logic-block identity and name. | `Bld:261-262`, `Constants.cs:11,13` | GAP | intended | `"logic-block-id"` / `"LogicBlockName"` — the identity every recorded message carries |
| 12 | THE SYSTEM SHALL discover the block's service identifiers from the block class name and from every property whose type carries service members or a service interface. | `Bld:377-406` | GAP | intended | what routes a property change to a service |
| 13 | THE SYSTEM SHALL discover a contract identifier for every writable property whose type carries a service-provider contract type, taking the identifier from the binding attribute where one is declared and from the property name otherwise. | `Bld:352-370` | GAP | intended | why a contract is mapped in a test without a configuration |
| 14 | THE SYSTEM SHALL run every service registration declared in the assembly of each such contract type. | `Bld:322-346` | GAP | intended | what makes a Modbus RTU contract resolvable in a test |
| 15 | THE SYSTEM SHALL register the context's virtual clock as the block's `TimeProvider`, and SHALL let a later registration replace it. | `Bld:298`; probe **P12** | GAP | intended | the default that makes a block's `UtcNow` deterministic |
| 16 | THE SYSTEM SHALL resolve a persistent value's storage key from the block's own service-property bindings, falling back to a direct key when the property is not a service property. | `Bld:444-464` | GAP | intended | what `WithPersistentValue` writes under |
| 17 | THE SYSTEM SHALL map each declared logic-interface mapping onto the block's own sender interface for that contract. | `Bld:484-516` | `.WithLogicInterfaceMapping_AllowsBareLambdaOnSingleSenderBlock` | intended | 237 consumer sites |
| 18 | THE SYSTEM SHALL address every mapped peer through a stand-in actor reference named after the peer's logic block. | `Bld:510`, `TestActorReference.cs:14-22` | GAP | intended | what a `VerifySendCommand(to:)` matches against |
| 19 | THE SYSTEM SHALL convert an enum persistent value to its integer form before restoring it, as the persistence system does. | `Bld:427-431` | `.RestorePersistentState` | intended | a restore that skipped it would fail to bind |
| 20 | ⚠ WHEN a persistent value is declared for a property the block does not persist THE SYSTEM SHALL accept it silently, restoring it under a direct key the block never reads. | `Bld:462-463`; probe **P11**: no throw | GAP | park | catching it needs the binder's own view of which properties are persistent, which is `BIND`'s (`AC-BIND-*`) and not one line here |
| 21 | ⚠ THE SYSTEM SHALL map a contract an inclusion gate would have excluded, reading no `[IncludedWhen]`. | `Bld:357-369` — the loop tests the contract-type attribute and `CanWrite` and nothing else | GAP | park | `_findings.md:224-228`, BIND row 191, classified `park` there. Decision `0081` (`inclusion-gates-resolve-at-bind.md:22`) names "runtime/DevHost/TestKit — evaluate each gate before binding and skip" as the rule; matching it means running the gate evaluator the binder owns, which is `BIND`'s surface, not a line here |
| 22 | ⚠ THE SYSTEM SHALL discover a contract's service registrations from the block under test, where a host discovers them from the plugin. | `Bld:326-345` — walks `typeof(TLogicBlock)`'s properties | GAP | propose | `_findings.md:229-233`, BIND row 193, classified `propose` there ("`TKIT`'s to decide"). A block whose contract needs a dependency the block does not itself hold is untestable; the escape hatch exists today and is `WithServices` (`Bld:145`), which has **0** consumer sites. Recommendation: **state it, do not change it** — widening discovery to the whole load context would make a test's service graph depend on what else is loaded |
| 23 | WHEN a mapping's target class implements more than one contract interface THE SYSTEM SHALL refuse the mapping, naming the class, the count, every candidate and the explicit form to use. | `Bld:227-248`, message at `:244-247` | `.WithLogicInterfaceMapping_ThrowsWhenInferredTypeIsClassWithMultipleContracts` | intended | the guard that turned a silent mis-route into a call-site failure |
| 24 | THE SYSTEM SHALL accept a mapping whose type argument is a contract interface without an ambiguity check. | `Bld:231-234` | `.WithLogicInterfaceMapping_AllowsExplicitGenericOnMultiSenderBlock` | intended | the explicit form is unambiguous by construction |
| 25 | WHEN the block declares no sender interface for a mapping's type THE SYSTEM SHALL refuse the build, naming the type. | `Bld:500-503` | GAP | intended | the second of the builder's two mapping refusals |
| 26 | WHEN a mapping is declared for an interface the block does not implement THE SYSTEM SHALL refuse it at the call site, naming the block, the interface and the delegating form. | `Bld:89-93` | GAP | intended | the third refusal, on the self-implementation overload |
| 27 | THE SYSTEM SHALL apply an instantiation parameter through the configuration payload's value channel, encoding it as the configuration path does. | `Bld:129-139` | `InstantiationParameterGatingShould.ApplyParameterValueThroughConfigurationPath` (cites `AC-GATE-012.1`) | intended | `config-gating.md:396-398` owns the rule; cited |
| 28 | WHEN an instantiation parameter names a property the block does not declare THE SYSTEM SHALL refuse it, naming the property and the block. | `Bld:132-133` | GAP | intended | the kit's own guard, ahead of the block's |
| 29 | WHEN a property selector is not a property access THE SYSTEM SHALL refuse it, naming the required shape. | `Bld:533`, `Ctx:433` | GAP | intended | one refusal text, declared twice — `Bld:518-534` and `Ctx:418-434` are the same private helper copied into both types, so the two messages can drift apart |
| 30 | THE SYSTEM SHALL start a built block by default, and SHALL clear every message the start produced. | `Bld:470-479`; probe **P13**: 0 recorded after `Build()` | `.AutoStartByDefault`, `.AutoStartClearsInfrastructureMessages` | intended | why a verify after `Build()` counts only what the test caused |
| 31 | WHEN auto-start is suppressed THE SYSTEM SHALL leave the messages the earlier phases produced in the recording. | `Bld:470-479` — the clear is inside the auto-start branch; probe **P13**: **2** recorded, both `BindLogicBlockServices` | GAP | intended | the asymmetry a `WithoutAutoStart` test meets; 4 consumer sites. Stated, not changed: clearing them would erase the very messages an initialization test is there to read |
| 32 | THE SYSTEM SHALL expose the service provider the builder composed. | `Ctx:97` (public getter, `internal set`) | `WithEmissionPolicyShould.RegisterOverrideWhenAskedForAttributePolicy` and its three siblings | intended | the observable rows 21 and 22 would be tested through |
| 33 | ⚠ THE SYSTEM SHALL document `BuiltServiceProvider` with an example a consumer cannot write, because the type it names is internal to the SDK. | `Ctx:91-96` names `EmissionPolicyForceMarker`; `EmissionPolicyForceMarker.cs:22` is `internal`, visible only to `Vion.Dale.Sdk.TestKit` and `…TestKit.Test` (`Vion.Dale.Sdk.csproj:23,25`) | `WithEmissionPolicyShould` (in-repo, so it compiles) | fix | a doc-comment defect: the example does not compile outside this repository. Reword to an example a consumer can run |
| 34 | THE SYSTEM SHALL return the builder from every knob so they chain. | `Bld:80`, `:111`, `:138`, `:148`, `:159`, `:176`, `:200` | `WithEmissionPolicyShould.ReturnBuilderForChaining` | intended | the fluent contract |
| 35 | WHEN a caller-owned clock is bound THE SYSTEM SHALL refuse a null one, naming the clock argument. | `Ctx:398`; probe **P11** | GAP | intended | the guard |
| 186 | THE SYSTEM SHALL gate the emission policy **off** unless a test turns it on, so every service-property and measuring-point assignment surfaces as a change. | `EmissionPolicyMode.cs:12-17` (the `Off` member is first and documented as the default), `Bld:47` (`_emissionPolicyMode = EmissionPolicyMode.Off`), `Bld:303-306` (the force marker is registered only for `FromAttributes`); probe **P12**: marker absent by default, present with `FromAttributes` | `WithEmissionPolicyShould.RegisterNoOverrideByDefault`, `.RegisterNoOverrideWhenTurnedOffExplicitly`, `.RegisterOverrideWhenAskedForAttributePolicy` | intended | a default with the weight of `Times.Once()`, and the one `emission.md:36-37` calls "the only way to exercise throttling deterministically" (`AC-EMIT-001.3/.4`, cited not re-minted). Minted by the self-check: the first pass over group A rowed `BuiltServiceProvider` and the chaining contract and left the knob itself unstated |

**⚠ 5** — a consumer writes `LogicBlockTestHelper.Create<MyBlock>()` for a block whose only ctor
takes `(TimeProvider, ILogger)`. Today: `MissingMethodException: Constructor on type 'MyBlock' not
found.` — true and useless. With the fix: a `TestKitVerificationException` naming the required
`ILogger` constructor and pointing at direct construction plus `CreateTestContext()`.

**⚠ 6** — the same call on an abstract block. Today: the identical `MissingMethodException`, which
sends the reader hunting for a missing ctor on a type that can never be constructed.

**⚠ 7** — a block whose ctor throws `ArgumentException("bad config")`. Today the test sees
`TargetInvocationException: Exception has been thrown by the target of an invocation.` and must open
`InnerException` to learn anything. Recommendation: rethrow the inner exception preserving its stack.

**⚠ 20** — `WithPersistentValue(lb => lb.Power, 42.0)` where `Power` carries no `[Persistent]`. Today
the value is stored under `_direct.Power`, the block restores nothing, and the test asserts against a
default it never set. Park: the check needs `BIND`'s view of which properties persist.

**⚠ 21** — a contract behind `[IncludedWhen("mode == 'x'")]` with the gate false. Today the builder
maps it, so the test drives a contract a host would not have bound. Park: BIND row 191's own class.

**⚠ 22** — a contract type whose `IConfigureServices` lives in an assembly the block does not
reference through a property. Today `Build()` throws from the DI resolution of a service nothing
registered. Recommendation: state it, and name `WithServices` as the supported answer.

**Note 31** — `WithoutAutoStart()` then `ctx.GetSentMessagesOfTypePublic<object>()`. Two
`BindLogicBlockServices` are already there. Stated rather than fixed.

**⚠ 33** — a consumer copies the `BuiltServiceProvider` doc example and gets CS0122.

### B. The recording context and the verify family

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 36 | THE SYSTEM SHALL record every message a block sends to an actor, to itself, or in reply to a sender, in send order. | `Ctx:491-536` | `.ClearRecordedMessages` | intended | the substrate every verify helper reads |
| 37 | THE SYSTEM SHALL answer any actor lookup with a stand-in reference of the requested name. | `Ctx:486-489` | GAP | intended | what lets a block's deferred init complete without a runtime |
| 38 | THE SYSTEM SHALL carry no headers on the context. | `Ctx:481-484` | GAP | out-of-spec | an interface member returning a constant with no branch behind it |
| 39 | THE SYSTEM SHALL record a message and queue its action under one lock, so a block whose I/O completes on a background thread records correctly. | `Ctx:55`, `:499-528`, `:438-444` | `.DrainEveryActionExactlyOnceWhenEnqueuedFromBackgroundThreads` | intended | the threading contract at `Ctx:29-35` |
| 40 | THE SYSTEM SHALL let a test clear the recording. | `Ctx:265-271` | `.ClearRecordedMessages` | intended | 23 consumer sites |
| 41 | THE SYSTEM SHALL count a recorded stream against an expected number of occurrences and SHALL default that expectation to exactly once. | `Ctx:147`, `:175`, `:196`, `:222`, `:242`, `:406`; `Times:12-45` | `VerifyServicePropertyEmittedShould.DefaultToTimesOnce` | intended | the default with the weight of a contract — every one of the twelve verify helpers |
| 42 | ⚠ THE SYSTEM SHALL accept every occurrence-count form the mocking library expresses. | `Times:16-39`; probe **P1**: `Times.AtMostOnce()` raises `ArgumentOutOfRangeException` at every count; `Times.Between(…)` fails at every count | GAP | fix | probe **P2**: `Times.Validate(int)` is public on 4.20.72 and agrees with the shipped code on **61 of 70** cells — the 9 disagreements are exactly these two broken forms. 0 consumer sites name either form, so no assertion's outcome changes |
| 43 | ⚠ WHEN an occurrence count does not match THE SYSTEM SHALL fail with the verification's own message, the expected count and the actual count. | `Times:43` — `$"{assertMessage}: Expected {times} but found {actualCount}."`; probe **P1** shows `AtMostOnce` never reaching it | GAP | fix | the message shape a consumer greps; row 42's fix is what makes it reachable for all forms |
| 44 | ⚠ THE SYSTEM SHALL render the counts in a failure message in the invariant culture. | `Times:43` — `{actualCount}` is an `int` interpolated with the ambient culture | GAP | propose | an `int` renders identically in every culture this repository is built on, so today's text is invariant by accident rather than by rule. Pinning it costs one `CultureInfo.InvariantCulture` and makes the rule true rather than lucky; it changes no rendered string |
| 45 | THE SYSTEM SHALL assert on a service property's recorded changes, filtered to that property. | `Ctx:142-157` | `.VerifyServicePropertyChangedOnlyCountsTargetProperty` | intended | 0 consumer sites for `Changed`, 3 for `Emitted` |
| 46 | THE SYSTEM SHALL assert on a measuring point's recorded changes, filtered to that measuring point. | `Ctx:191-206` | `.VerifyServiceMeasuringPointChanged` | intended | the measuring-point half; `emission.md`'s dual-annotation rule is what makes the two streams separate |
| 47 | ⚠ THE SYSTEM SHALL offer one assertion per stream, not two, for the same recorded messages. | `Ctx:142-157` and `Ctx:170-185` are byte-identical but for the message string; likewise `Ctx:191-206` / `Ctx:217-232` | `VerifyServicePropertyEmittedShould` ×4 | propose | four helpers over two streams. The `Emitted` doc (`Ctx:159-169`) states an intent — post-policy — the body does not implement, because with the policy off the two are the same stream and with it on the recorded stream is already post-policy. Recommendation: **keep all four and state the intent as documentation**, because `VerifyServicePropertyEmitted` has 3 consumer sites and `docs/specs/emission.md:36-37` builds on the vocabulary; deleting two is unreachable under `sdk-surface-conventions.md` § 3 |
| 48 | THE SYSTEM SHALL assert on a command, a request or a state update sent through a logic interface, optionally filtered to one mapped peer. | `Ctx:112-136`, `:401-416` | GAP | intended | `VerifySendStateUpdate` 205, `VerifySendCommand` 35, `VerifySendRequest` 11 consumer sites — the three most-used helpers, all unproven here |
| 49 | THE SYSTEM SHALL run a caller's per-message assertion against every matching message, not only the first. | `Ctx:150-156`, `:409-415` | GAP | intended | a two-message stream runs the assertion twice |
| 50 | THE SYSTEM SHALL assert on a contract message by data type and contract identifier. | `Ctx:234-244` | GAP | intended | what all nine I/O and Modbus verify helpers delegate to |
| 51 | ⚠ THE SYSTEM SHALL name the message kind a contract verification is about in its failure message and SHALL filter nothing by it. | `Ctx:238-243`; probe **P14**: `"AnalogOutput"`, `"TotalNonsense"`, `""` and `null!` all pass identically | GAP | fix | `_findings.md:522-526`, IO row 61 (whose `:242` is `:243` — corrected as absorbed). The argument is a label, and six kit call sites pass contract-type names that duplicate `AC-IO-001.3`'s stable identifiers with no gate holding them in step. Fix: state the parameter as a label in its own name and doc, so a reader stops expecting a filter |
| 52 | THE SYSTEM SHALL return recorded contract messages of a data type for a test that needs the message itself. | `Ctx:250-260` | GAP | intended | how the RTU kit finds a pending request; 0 consumer sites |
| 53 | THE SYSTEM SHALL return every recorded message of a requested type for assertions the typed helpers do not cover. | `Ctx:386-389` | GAP | intended | the escape hatch; 0 consumer sites |
| 54 | THE SYSTEM SHALL materialise a recorded query under the lock rather than returning a deferred one. | `Ctx:436-444` — comment states why | GAP | intended | a deferred query would enumerate outside the gate |
| 55 | THE SYSTEM SHALL verify that a logger mock recorded a message containing a given text at a given level. | `ILoggerMockExtensions.cs:18-26`, `:32-40` | GAP | intended | 0 consumer sites. `testing-conventions.md` § 15 forbids asserting on log calls in *this repository's* tests; the helper is what the kit offers a consumer who chooses otherwise |
| 56 | THE SYSTEM SHALL offer that verification for both a bare and a typed logger mock. | `ILoggerMockExtensions.cs:18`, `:32` | GAP | intended | the two mock shapes a block ctor takes |
| 57 | THE SYSTEM SHALL raise a single exception type for every assertion failure the kits produce. | `TestKitMockException.cs:10-15`; every `AssertCount` failure and eight `Unable to assert …` refusals | `ToleranceDefaultsShould.Reject_a_near_miss_at_the_default_tolerance` | intended | what a consumer catches |

**⚠ 42** — `ctx.VerifySendStateUpdate<MyState>(times: Times.AtMostOnce())` on a stream of one.
Today: `ArgumentOutOfRangeException: length ('-1') must be a non-negative value` — a crash inside the
assertion, not a verdict. `Times.Between(1, 3, Range.Inclusive)` on a stream of two fails a
verification that should pass.

**⚠ 43** — the same call reaches no `TestKitVerificationException` at all, so a consumer grepping
their failure text finds a BCL message that names no property.

**⚠ 44** — a build on a machine whose current culture renders integers with a group separator would
change the message text a consumer asserts on. Recommendation: pin the interpolation.

**⚠ 47** — no failure; a surface question. Four members where two would do, one pair carrying a doc
claim its body does not implement. Recommendation: keep, document.

**⚠ 51** — `ctx.VerifyContractMessageSent<SetDigitalOutput>("AnyStringAtAll")` passes.
`VerifyDigitalOutputSet` passes `"DigitalOutput"` at `DigExt:40`, which duplicates
`AC-IO-001.3`'s identifier and is never compared to it.

### C. The two I/O kits' raise and verify helpers

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 58 | THE SYSTEM SHALL raise an input face's change event with a given value. | `IDigitalInputExtensions.cs:20-34`, `IAnalogInputExtensions.cs:20-34` | `LogicBlockTestContextShould.RaiseDigitalInputChanged`, `.RaiseAnalogInputChanged` | intended | the digital and analog rows of one rule |
| 59 | THE SYSTEM SHALL raise an output face's change event with a given value. | `IDigitalOutputExtensions.cs:20-34`, `IAnalogOutputExtensions.cs:20-34` | GAP | intended | the confirmation half a block observes |
| 60 | THE SYSTEM SHALL raise an output provider face's command event with a given value. | `IDigitalOutputProviderExtensions.cs:20-34`, `IAnalogOutputProviderExtensions.cs:20-34` | `ProviderFacesShould.Confirm_the_value_a_digital_output_commanded` | intended | 10 consumer sites |
| 61 | THE SYSTEM SHALL offer no raise helper for an input provider face. | the two kits hold three `I*Extensions.cs` each; `io.md:53` — an input provider carries `Drive` and no event | `.Drive_the_value_a_digital_input_observes` (asserts the drive, does not raise it) | intended | the absence is the face's shape, not a gap — Drift checkpoint 5 |
| 62 | WHEN a raise helper is given a null face THE SYSTEM SHALL refuse it, naming the face argument. | six sites at `:22-25` | GAP | intended | the first of two guards each helper carries |
| 63 | WHEN a raise helper is given a face that is not the SDK's own implementation THE SYSTEM SHALL refuse it, naming the event it cannot raise. | six sites at `:27-30` | GAP | intended | why a mocked face cannot be driven this way |
| 64 | ⚠ THE SYSTEM SHALL deliver a raised message under an empty logic-block identity. | six sites at `:32` — `new LogicBlockContractId("", …Identifier)`; `LogicBlockContractBase.cs:115-120` drops a *write* on exactly that shape | GAP | fix | `_findings.md:527-532`, IO row 63. Harmless today because the receive direction reads no identity, and one line (`Constants.LogicBlockId` in place of `""`) makes the raise path carry what a host would. Area-local, signature-preserving, no consumer reads the identity |
| 65 | THE SYSTEM SHALL assert that an output face was set with a given value, optionally filtered to one face. | `DigExt:23-41`, `AnaExt:25-44` | `.RaiseDigitalInputChanged`, `.RaiseAnalogInputChanged` | intended | 5 consumer sites for the digital form |
| 66 | THE SYSTEM SHALL assert that an output provider confirmed a given value, optionally filtered to one face. | `DigExt:51-69`, `AnaExt:55-77` | `ProviderFacesShould.Confirm_once_per_command` | intended | the confirmation half |
| 67 | THE SYSTEM SHALL assert that an input provider drove a given value, optionally filtered to one face. | `DigExt:79-97`, `AnaExt:88-107` | `.Drive_the_value_a_digital_input_observes` | intended | the drive half |
| 68 | WHEN a verify helper is given a face that is not the SDK's own implementation THE SYSTEM SHALL refuse it, naming the assertion it cannot make. | six sites — `DigExt:34,62,90`, `AnaExt:37,67,100` | GAP | intended | eight `Unable to assert …` messages in total across the kits |
| 69 | THE SYSTEM SHALL treat an omitted face as "any face of this kind" and an omitted value as "any value". | `DigExt:29-38,40`, `AnaExt:32-41,43` | `.RaiseAnalogInputChangedMultipleTimes`, `ProviderFacesShould.Write_nothing_until_a_command_arrives` | intended | the shape a `Times.Never()` assertion needs |
| 70 | THE SYSTEM SHALL compare a digital value for equality, offering no tolerance. | `DigExt:40`, `:68`, `:96` — `m.Value == value.Value` | GAP | intended | a truth value has no near miss |
| 71 | THE SYSTEM SHALL compare an analog value within an inclusive tolerance defaulting to zero, so an omitted tolerance means exact equality. | `AnaExt:28,58,91` (default `0`), `:43,75,106` (`Math.Abs(m.Value - value.Value) <= tolerance`) | `ToleranceDefaultsShould.Match_an_exact_value_at_the_default_tolerance` ×3, `.Admit_a_difference_exactly_equal_to_the_tolerance` ×3 | intended | the default that used to be unsatisfiable |
| 72 | ⚠ THE SYSTEM SHALL be unable to assert a non-finite analog value, because the difference comparison is false for a non-number and for either infinity against itself. | `AnaExt:43,75,106`; probe **P8**: `NaN`/`NaN` and `±∞`/`±∞` fail at tolerance `0` **and** at tolerance `1e9`/`∞` | GAP | fix | `_findings.md:513-517`, IO row 41. `io.md`'s `AC-IO-007.2` admits all of them and they reach the wire bit-exact, so a block that legitimately writes one cannot be asserted. Fix: compare bit-equal first, then within tolerance — signature-preserving, and it changes no currently-passing assertion's outcome (probe **P8** row `-0.0 vs 0.0` still passes) |
| 73 | ⚠ WHEN an analog verification is given a non-finite tolerance THE SYSTEM SHALL reject every value, including an exact match. | probe **P8**: `1.0` vs `1.0` at tolerance `NaN` → `TestKitVerificationException` | GAP | fix | row 72's sibling, which the ledger never named — a tolerance meant to widen the bound closes it completely. Fix: refuse a non-finite tolerance at the call site with a named message |
| 74 | ⚠ WHEN an analog verification is given a negative tolerance THE SYSTEM SHALL reject every value, including an exact match. | probe **P8**: `1.0` vs `1.0` at tolerance `-1` → `TestKitVerificationException` | GAP | fix | the second sibling: a negative tolerance is silently *narrower* than exact equality, which no caller can mean. Same one-site guard as row 73 |
| 75 | THE SYSTEM SHALL accept any finite value at an infinite tolerance. | probe **P8**: `1.0` vs `99.0` at tolerance `+∞` passes | GAP | intended | the bound row 73's guard must not break; stated so the guard is written for non-finite *and negative*, not for "not finite" alone |
| 76 | ⚠ THE SYSTEM SHALL prove the same verify-helper rules in both I/O kits' suites. | `ToleranceDefaultsShould` exists only under `Vion.Dale.Sdk.AnalogIo.TestKit.Test/`; the digital suite has 6 tests to the analog suite's 11 | `ToleranceDefaultsShould` (analog only) | fix | `_findings.md:518-521`, IO row 60. The digital verify helpers' negative sweep — a value that does not match must fail — is unproven. Fix: the rewrite gives both suites the same rules, with the value type as the row |

**⚠ 64** — no failure today; a latent one. A future helper that raised through the *write* path
(`LogicBlockContractBase`'s send) would have every message silently dropped, because
`LogicBlockContractBase.cs:115-120` drops a write whose logic-block id is empty.

**⚠ 72** — a heat-pump block writes `double.NaN` on an analog output to report an unavailable
setpoint. `ctx.VerifyAnalogOutputSet(block.Setpoint, double.NaN)` fails with "Expected Once but found
0", and no tolerance rescues it.

**⚠ 73** — a test computes its tolerance (`tolerance: expected * 0.01` where `expected` is `NaN`) and
every assertion in the file fails with a message about counts, naming nothing about the tolerance.

**⚠ 74** — `tolerance: -0.5` from a sign slip. Every value is rejected, including the exact one, and
the failure says "Expected Once but found 0".

**⚠ 76** — a change to the digital comparison that broke the negative half would be caught by
nothing.

### D. Time

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 77 | THE SYSTEM SHALL anchor a test context's virtual clock at `2026-01-01T00:00:00Z`. | `Ctx:42-48`, `:99-106`; probe **P13** | GAP | intended | the anchor a block reads on construction |
| 78 | THE SYSTEM SHALL expose the virtual clock as a `TimeProvider` a block can be constructed with, and the current virtual time as a plain instant. | `Ctx:81`, `:86-89` | `.TimeProvider_SharedBetweenBlockAndContextViaWithTimeProvider` | intended | 33 consumer `WithTimeProvider` sites |
| 79 | THE SYSTEM SHALL offer a construction form taking the anchor, and SHALL offer no builder knob that reaches it. | `Ctx:103-106` (public, no caller — `Bld:37` uses the parameterless form) | GAP | intended | RFC 0001 `:36` proposed `WithVirtualNow(DateTime anchor)`; it never shipped. Row 80 is the proposal |
| 80 | ⚠ THE SYSTEM SHALL let a test choose the anchor through the builder. | RFC 0001 `:36`; `Bld:37` constructs the context with the default anchor and never replaces it | GAP | propose | a new `[PublicApi]` member is a proposal by definition (brief point 4). Today's workaround is `WithTimeProvider(new FakeTimeProvider(anchor))` (probe **P12**: the anchor becomes the caller's), which 33 consumer sites already use — so the gap is real but already served. Recommendation: **do not add it**; state the workaround on the page and let RFC 0001's unshipped member die with the RFC |
| 81 | WHEN a caller-owned clock is bound THE SYSTEM SHALL use it for both the block's time reads and the context's deadlines. | `Bld:173-177`, `Ctx:396-399`, `Bld:298`; probe **P12** | `.TimeProvider_SharedBetweenBlockAndContextViaWithTimeProvider` | intended | RFC 0001 `:9-12`'s rule, absorbed |
| 82 | ⚠ WHEN a service registration replaces the clock THE SYSTEM SHALL give the block that clock and SHALL keep driving deadlines on its own, so the two diverge. | `Bld:298` then `Bld:309-312` (last registration wins); probe **P12**: block reads `2030-06-01`, `VirtualNow` reads `2026-01-01` | GAP | fix | the builder's own comment (`Bld:295-297`) documents the DI half and not the divergence. `WithServices` has **0** consumer sites, so no assertion changes. Fix is the comment and the page, not the code: making `WithServices` rebind the context clock would give one knob two jobs |
| 83 | THE SYSTEM SHALL advance the virtual clock by a requested amount and dispatch every action whose deadline the advance reaches, in deadline order. | `Ctx:284-332`, `:450-477` | `.AdvanceTime_FiresActionsInDeadlineOrderRegardlessOfInsertOrder` | intended | 144 consumer sites; the deadline-accurate driver |
| 84 | THE SYSTEM SHALL set the clock to each action's own deadline before running it. | `Ctx:315-318` | `.AdvanceTime_FiresActionAtItsDeadline` | intended | an action's `UtcNow` reads the time it was scheduled for |
| 85 | THE SYSTEM SHALL dispatch actions sharing a deadline in the order they were queued. | `Ctx:459` — the strict `d < nextDeadline` scan keeps the lowest index; probe **P9** | GAP | intended | the tie-break, stated at `Ctx:446-449` |
| 86 | THE SYSTEM SHALL dispatch an action queued during the advance whose deadline the advance still reaches. | `Ctx:303` (the loop re-scans) | `.AdvanceTime_CascadesActionsScheduledByOtherActionsWithinTheWindow` | intended | RFC 0001 open question 1's answer, absorbed |
| 87 | THE SYSTEM SHALL leave an action whose deadline lies beyond the advance queued. | `Ctx:459` (`d <= target`) | `.AdvanceTime_DoesNotFireActionWhoseDeadlineIsBeyondNewVirtualNow` | intended | what makes a self-rescheduling tick fire once per interval |
| 88 | THE SYSTEM SHALL dispatch an action whose deadline the advance reaches exactly. | `Ctx:459` (`<=`); probe **P9** | GAP | intended | the boundary a `AdvanceTime(interval)` cycle driver depends on |
| 89 | THE SYSTEM SHALL run an action whose deadline has already passed at the current virtual time rather than moving the clock backwards. | `Ctx:315-318`, reason at `:308-314` | `.DrainEveryActionExactlyOnceWhenEnqueuedFromBackgroundThreads` | intended | the forward-only clamp; the underlying clock throws rather than rewinding |
| 90 | THE SYSTEM SHALL leave the clock at the requested target when the queue is empty or exhausted. | `Ctx:326`, reason at `:323-325` | `.AdvanceTime_VirtualNowReflectsTargetAfterAdvance` | intended | what makes an advance on an empty queue not a no-op |
| 91 | ⚠ WHEN a dispatched action throws THE SYSTEM SHALL propagate the exception, leave every remaining action queued, and land the clock at the requested target. | `Ctx:299-331` — the `finally` at `:328-331` resets only the dispatch flag, so the landing at `:326` is skipped; probe **P6**: `AdvanceTime(10s)` over a throwing action at +5 s leaves the clock at **+5 s** and the +8 s action unreachable by a following `AdvanceTime(Zero)` | GAP | fix | RFC 0001 `:42` promised "Exceptions propagate; remaining due actions stay queued" and the code keeps two thirds of it. `Ctx:323-325`'s own comment states the landing as the rule. `git grep -E 'Throws.*AdvanceTime\|AdvanceTime.*Throws'` in the first consumer → **no matches**, so no assertion's outcome changes |
| 92 | WHEN an advance is asked to move the clock backwards THE SYSTEM SHALL refuse it, naming the direction. | `Ctx:286-289` | `.AdvanceTime_ThrowsOnNegativeDelta` | intended | `ArgumentOutOfRangeException`, the only one in the area |
| 93 | THE SYSTEM SHALL treat a zero advance as a dispatch of everything already due, leaving the clock where it was. | `Ctx:297`, `:459`; probe **P9**: both zero-deadline actions ran, clock unmoved | GAP | intended | the edge value that makes `SendToSelf`'s enlistment reachable |
| 94 | WHEN an advance is called from inside a dispatched action THE SYSTEM SHALL refuse it, naming the supported alternative. | `Ctx:291-295`; probe **P9** | GAP | intended | RFC 0001 open question 2's answer, absorbed |
| 95 | THE SYSTEM SHALL run every action queued at the moment of a flush, ignoring their deadlines and the clock. | `Ctx:351-379`, doc at `:334-349` | `.FlushPendingActions_StillDrainsRegardlessOfDeadline` | intended | 554 consumer sites — reviewer's question 6 |
| 96 | THE SYSTEM SHALL make a flush a single pass, deferring an action queued during it to the next flush. | `Ctx:360-365` (snapshot-and-clear), doc at `:345-349` | `.FlushPendingActions_DoesNotLoopWhenActionReschedulesItself` | intended | what stopped the unbounded drain |
| 97 | THE SYSTEM SHALL snapshot and clear the queue as one step, so an action queued between the two is not lost. | `Ctx:358-365`, comment states the defect it fixed | `.DrainEveryActionExactlyOnceWhenEnqueuedFromBackgroundThreads` | intended | the background-thread contract |
| 98 | WHEN a flush is called from inside a dispatched action THE SYSTEM SHALL refuse it. | `Ctx:353-356`; probe **P9** | GAP | intended | the guard is shared with row 94, so an advance inside a flushed action is refused too |
| 99 | THE SYSTEM SHALL leave the clock untouched during a flush. | `Ctx:351-379` — no clock write | `.FlushPendingActions_StillDrainsRegardlessOfDeadline` | intended | the whole difference from an advance; `EnergyManagerCycleHarness.cs:29-38` is the consumer that learned it |
| 100 | THE SYSTEM SHALL enlist an action a block schedules with no delay so a flush or an advance runs it. | `Ctx:499-515`, comment at `:505-509` | `.FlushPendingActions_ExecutesImmediateInvokeSynchronizedAction`, `.AdvanceTime_FiresImmediateInvokeSynchronizedAction` | intended | without it an immediate schedule is silently lost |
| 101 | THE SYSTEM SHALL stamp a delayed action's deadline from the virtual clock at the moment it is scheduled. | `Ctx:525` | `.AdvanceTime_FiresActionAtItsDeadline` | intended | what makes a delay mean anything |
| 102 | THE SYSTEM SHALL fire a block's timer callback by identifier, out of band of the virtual clock. | `Timers:21-33` | `.FireTimerByIdentifier`, `.FireTimerMultipleTimes` | intended | RFC 0001's opt-in decision, absorbed: an advance never fires a timer |
| 103 | THE SYSTEM SHALL fire a timer callback selected by a method-call expression. | `Timers:40-45`, `:84-92` | `.FireTimerByExpression` | intended | the type-safe form |
| 104 | THE SYSTEM SHALL report a timer's configured interval, by identifier or by expression. | `Timers:50-60`, `:66-71` | `.GetTimerInterval`, `.GetTimerIntervalByExpression` | intended | 0 consumer sites |
| 105 | WHEN a timer identifier is not registered THE SYSTEM SHALL refuse, naming the identifier and every registered timer. | `Timers:25-30`; probe **P10**: `…Available timers: (none).` | `.ThrowWhenFiringUnknownTimer` | intended | the message a consumer greps |
| 106 | ⚠ WHEN an interval is asked for a timer that is not registered THE SYSTEM SHALL refuse with the same message the firing helper gives. | `Timers:54-57` — the message stops at the identifier; probe **P10** | GAP | fix | two refusals for one condition, one of them missing the half that helps. Signature-preserving, message-only, 0 consumer sites |
| 107 | WHEN a selector expression is not a method call THE SYSTEM SHALL refuse it, naming the required shape. | `Timers:91` | GAP | intended | the expression guard |
| 108 | THE SYSTEM SHALL declare no timeout anywhere in the five kits. | `grep -rn 'Timeout\|CancellationTokenSource' Vion.Dale.Sdk*.TestKit --include=*.cs` → only `OperationTimeoutException` construction at `Fake:182,191` | GAP | intended | the anchor-kind row: a kit that never waits on wall time has nothing to time out |

**⚠ 80** — no failure; an absent member. RFC 0001 proposed it, it never shipped, and 33 consumer
sites already do the same thing through `WithTimeProvider`.

**⚠ 82** — a test writes `.WithServices(s => s.AddSingleton<TimeProvider>(myClock))` expecting
`ctx.AdvanceTime` to move `myClock`. It does not: the block reads `myClock` and the context drives
its own, so every deadline assertion silently misses.

**⚠ 91** — a cycle driver calls `AdvanceTime(ControlInterval)`; one cycle throws. The test catches
it, then advances again — and the second advance reaches nothing, because the clock never left the
first failure's deadline.

**⚠ 106** — `GetTimerInterval("OnTick")` on a block whose timer is `OnPeriod` says only that
`'OnTick'` is not registered, where `FireTimer` would have listed `'OnPeriod'`.

### E. The Modbus RTU kit

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 109 | THE SYSTEM SHALL deliver a successful read response to the pending request's callback through the same message path production uses. | `RtuExt:32-48` | `IModbusRtuExtensionsShould.InvokeSuccessCallbackOnSimulatedReadResponse` | intended | the byte conversion runs real SDK code |
| 110 | THE SYSTEM SHALL deliver a read failure to the pending request's callback, classifying the outcome from the exception unless one is given. | `RtuExt:58-79`, `:135-147` | `.InvokeErrorCallbackOnSimulatedReadError` | intended | mirrors what `ModbusRtuHandler` stamps |
| 111 | THE SYSTEM SHALL deliver a successful and a failed write response the same way. | `RtuExt:87-102`, `:112-132` | `.InvokeSuccessCallbackOnSimulatedWriteResponse`, `.InvokeErrorCallbackOnSimulatedWriteError` | intended | the write half |
| 112 | THE SYSTEM SHALL stamp a simulated receipt's completion instant and timestamp from the test's virtual clock. | `RtuExt:44-45`, `:75-76`, `:98-99`, `:128-129` | GAP | intended | what makes a round-trip assertable |
| 113 | THE SYSTEM SHALL carry no publish instant on an outcome the handler decides before publishing. | `RtuExt:151-154` | GAP | intended | expired, dropped and invalid never reach the wire |
| 114 | THE SYSTEM SHALL answer the most recent matching pending request. | `RtuExt:178`, `:194` (`candidates[^1]`) | `.MatchReadResponseByStartingAddressWhenMultipleReadsArePending` | intended | the rule two pending reads make visible |
| 115 | THE SYSTEM SHALL filter pending requests by address when one is given and consider all of them otherwise. | `RtuExt:170`, `:185` | `.MatchReadResponseByStartingAddressWhenMultipleReadsArePending` | intended | the optional filter |
| 116 | THE SYSTEM SHALL leave a request answerable again after it has been answered. | `RtuExt:169`, `:184` read the recording, which no simulation clears | GAP | intended | two `SimulateReadResponse` calls answer the same request twice, delivering two receipts for one read. Stated rather than changed: the recording is the test's to clear (`ClearRecordedMessages`, 23 consumer sites) and consuming it here would break the "answer the last one" rule row 114 states |
| 117 | WHEN no matching request is pending THE SYSTEM SHALL refuse the simulation, naming the request kind, the filter and what the block must do first. | `RtuExt:172-176`, `:187-192` | `.ThrowWhenSimulatingReadResponseWithNoPendingRequest` and three siblings | intended | four tests, one message shape per direction |
| 118 | WHEN a simulation is given a contract that is not the SDK's own implementation THE SYSTEM SHALL refuse it. | `RtuExt:156-164` | GAP | intended | the cast guard the four simulations share |
| 119 | THE SYSTEM SHALL assert that a read or a write request was sent, optionally filtered by contract, address and quantity. | `Vion.Dale.Sdk.Modbus.Rtu.TestKit/LogicBlockTestContextExtensions.cs:21-44`, `:53-68` | `LogicBlockTestContextShould.VerifyModbusReadSentWithAllFilters` and six siblings | intended | 0 consumer sites |
| 120 | THE SYSTEM SHALL build response bytes in big-endian order for floats, shorts, unsigned shorts, ints and doubles. | `ModbusResponseBuilder.cs:16-111` | `ModbusResponseBuilderShould.EncodeSingleFloatAsFourBigEndianBytes` and five siblings | intended | the wire order `ByteOrder.MsbToLsb` means |
| 121 | THE SYSTEM SHALL pack booleans one bit per coil, least-significant bit first, into one byte per eight. | `ModbusResponseBuilder.cs:117-130` | `.PackBoolsLsbFirstWithinEachByte`, `.AllocateOneByteForEveryEightBools` (6 rows) | intended | the Modbus coil packing |
| 122 | THE SYSTEM SHALL return an empty array for an empty input to every builder. | `ModbusResponseBuilder.cs:18`, `:119-120` | `.ReturnEmptyArrayForEmptyInput` | intended | the edge value, already swept |

**Note 116** — no failure; a shape a reader should know. A test that simulates twice without clearing
gets two receipts for one read, and a `Times.Once()` receipt assertion fails with a count of 2.

### F. The Modbus TCP kit

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 123 | THE SYSTEM SHALL hold register and coil contents per unit and address in raw wire bytes, so the SDK's own conversion runs against them. | `Fake:22-30`, `:386-393` | `FakeModbusTcpClientProxyShould.DecodeUInt32_FromHoldingRegisters_WithMsbToLsb_AndMswToLsw` | intended | the whole point of faking at the proxy layer |
| 124 | THE SYSTEM SHALL let a test pre-populate a single register or a contiguous range in each of the four tables. | `Fake:94-132` | `.DecodeUInt32_FromHoldingRegisters_WithMsbToLsb_AndMswToLsw` | intended | the arrange surface |
| 125 | WHEN register bytes are not a whole number of registers THE SYSTEM SHALL refuse them, naming the alignment rule. | `Fake:378-384`, called at `:105`, `:118`, `:323` | GAP | intended | one register is two bytes |
| 126 | THE SYSTEM SHALL answer a read of an address nothing pre-populated with zeros. | `Fake:395-411`, comment at `:407` | GAP | intended | what an uninitialised real register returns |
| 127 | THE SYSTEM SHALL record every read, write and connection attempt in order, with its unit, address and quantity or payload. | `Fake:236`, `:249`, `:255-330`, exposed at `:47-62` | `.RecordReadInHistory_WithAddressAndQuantity`, `.HandleSequenceOfReadsAndWrites_InOrder` | intended | the assertion surface `VerifyReadSent` reads |
| 187 | THE SYSTEM SHALL assert against the recorded histories that a read, a write, a connection attempt or a disconnect happened, each filtered by the fields its history carries and counted against the same occurrence-count default. | `FakeModbusTcpClientProxyExtensions.cs:23-36` (`VerifyReadSent`), `:47-61` (`VerifyWriteSent`), `:70-78` (`VerifyConnectAttempted`), `:85-89` (`VerifyDisconnectCalled`) — all four end in `(times ?? Times.Once()).AssertCount(…)` | `FakeModbusTcpClientProxyShould.VerifyReadAndWriteAndConnect_WithSugarHelpers`, `.VerifyConnectionSequence_OnIpChange_WithSugarHelpers` | intended | the Modbus block author's verify surface, and the one verify family that reads a history rather than the recorded messages — so rows 41–44's `Times` rules reach it too. Minted by the self-check: the first pass rowed the histories and not the helpers over them |
| 128 | THE SYSTEM SHALL record a write's payload as a defensive copy, so a reused production buffer cannot alter it afterwards. | `Fake:315`, `:326-328`, comments at `:314`, `:325` | GAP | intended | the SDK reuses internal buffers |
| 129 | ⚠ THE SYSTEM SHALL record a multiple-coil write as one byte per coil, where a single-coil write records the wire's own two values. | `Fake:277-283` (`0x01`/`0x00` per coil) against `Fake:269` (`0xFF`/`0x00`); `WriteEvent`'s doc at `:476-478` says "the raw wire-format payload (MSB-first per register)" | GAP | fix | the doc claims a wire format the multiple-coil arm does not produce — a real FC15 packs coils into bits. A consumer asserting `expectedBytes` on a multiple-coil write writes bytes no wire carries. Fix the **doc**, not the encoding: the one-byte-per-coil form is what makes `expectedBytes` readable, and changing it would break every existing `VerifyWriteSent(expectedBytes:)` on a coil write |
| 130 | THE SYSTEM SHALL answer a coil or discrete-input read with the Modbus bit packing, least-significant bit first. | `Fake:413-428` | `.ReadCoilsAsBools` (server side) | intended | the read direction, correctly packed |
| 131 | THE SYSTEM SHALL queue faults per operation kind, unit and address, consuming one per matching operation in the order they were queued. | `Fake:204-226`, `:353-360` | `.StackMultipleFaults_DrainFifo`, `.RecoverFromInjectedFault_OnNextReadAtSameAddress` | intended | the "fail, fail, recover" sequence |
| 132 | THE SYSTEM SHALL match a read's fault by the read's starting address, not by every address the read covers. | `Fake:139-146`, doc states it; `:256` passes `startingAddress` | GAP | intended | a Modbus exception applies to the whole request |
| 133 | THE SYSTEM SHALL record an operation before it consumes its fault, so a failed attempt is still verifiable. | `Fake:255-256` and its seven siblings — the history add precedes `AdvanceClockAndThrowIfFaultEnqueued` | `.SurfaceInjectedModbusException_OnWrite_AndStillRecordTheAttempt` | intended | what makes a reconnect target assertable on failure |
| 134 | THE SYSTEM SHALL queue connection failures separately from operation faults, consuming one per attempt. | `Fake:35`, `:199-202`, `:238-241` | `.SurfaceConnectionFailure_AsErrorCallback_AndStillRecordTheAttempt` | intended | connections are not keyed by unit or address |
| 135 | THE SYSTEM SHALL record a connection attempt with its target before deciding its outcome. | `Fake:236` precedes `:238` | `.SurfaceConnectionFailure_AsErrorCallback_AndStillRecordTheAttempt` | intended | doc at `:194-198` states it |
| 136 | THE SYSTEM SHALL offer a timeout fault that fires immediately, because a synchronous queue cannot wait for one. | `Fake:174-192`, doc at `:176-179` | `.SurfaceOperationTimeout_OnRead_AsErrorCallback` | intended | observably identical from the block's side |
| 137 | THE SYSTEM SHALL report whether it is connected, and SHALL leave that unchanged when a connection attempt fails. | `Fake:86`, `:243` (set only after the failure check), `:250` | `.RecordConnectAsync_OnFirstOperation` | intended | the state the real wrapper's lazy connect reads |
| 138 | THE SYSTEM SHALL consume a configured delay of virtual time on every read, write and connection attempt, on the fault path too. | `Fake:74-80`, `:237`, `:353-360`, comment at `:350-352` | GAP | intended | a device that times out took time to do so |
| 139 | WHEN a delay is configured and no clock is set THE SYSTEM SHALL refuse the operation, naming the clock property and the harness. | `Fake:362-376`, refusal at `:369-373` | GAP | intended | the refusal exists and is unproven; the guard is right |
| 140 | ⚠ WHEN a delay is configured as zero or negative THE SYSTEM SHALL consume no virtual time and require no clock. | `Fake:364-367` (`delay <= TimeSpan.Zero` returns) | GAP | fix | zero is the default and must not need a clock; a **negative** delay is silently ignored, which is the edge value beside it. Fix: refuse a negative delay at the setter, keep zero free — signature-preserving, 0 consumer sites set either |
| 141 | THE SYSTEM SHALL wire the fake proxy and the synchronous queue into the real client, leaving every layer between them real SDK code. | `FakeModbusTcpHarness.cs:71-84`, comment at `:65-70` | `ModbusLinkPolicyShould` (cites `AC-MODB-*`) | intended | `modbus.md`'s `AC-MODB-018.2` GAP names this registration order as its only net |
| 142 | THE SYSTEM SHALL let the harness's own registrations win over the SDK's by registering them last. | `FakeModbusTcpHarness.cs:78-80` | GAP | intended | last-registration-wins for `GetRequiredService<T>` |
| 143 | THE SYSTEM SHALL register the harness's clock before the SDK's, so the SDK's conditional registration keeps it. | `FakeModbusTcpHarness.cs:75-77`, comment states it | GAP | intended | `AC-MODB-018.2`'s other half |
| 144 | ⚠ THE SYSTEM SHALL default a harness with no clock argument to the real system clock. | `FakeModbusTcpHarness.cs:45`, `:49` — both non-clock ctors pass `TimeProvider.System` | GAP | propose | 9 of the first consumer's 10 harness constructions use the parameterless form, so every receipt they take is stamped from wall time. Changing the default to a fresh `FakeTimeProvider` would make those 9 deterministic and would change what `RoundTrip` reads in each — a behaviour change with 81 consumer sites behind it. Recommendation: **state the default and add no ctor**; the clock-taking ctor exists and is the documented answer |
| 145 | WHEN a harness is given a null proxy or a null clock THE SYSTEM SHALL refuse construction, naming the argument. | `FakeModbusTcpHarness.cs:59-63`, `FakeModbusTcpServerHarness.cs:53` | GAP | intended | the guards |
| 146 | THE SYSTEM SHALL dispose the client and the composed container when the harness is disposed. | `FakeModbusTcpHarness.cs:87-91`, `FakeModbusTcpServerHarness.cs:72-76` | `FakeModbusTcpServerHarnessShould.Cleanup` | intended | a per-test harness leaks nothing |
| 147 | THE SYSTEM SHALL run each enqueued request synchronously on the calling thread, routing its callbacks through the dispatcher production uses. | `Queue:149-166`, doc at `:14-19` | `.DriveAFullServerCycleWithoutSockets`, `ModbusLinkPolicyShould` | intended | why a flush drains a Modbus callback |
| 148 | WHEN the queue is held THE SYSTEM SHALL buffer enqueued requests instead of running them, and SHALL run them in order on a drain. | `Queue:41`, `:151-154`, `:134-142` | GAP | intended | the documented seam for putting virtual time between enqueue and execution |
| 149 | THE SYSTEM SHALL buffer again, rather than run, a request enqueued during a drain while the hold is still set. | `Queue:136-137` (snapshot-and-clear first), doc at `:129-133` | GAP | intended | the single-pass rule, mirroring the context's flush |
| 150 | THE SYSTEM SHALL report the number of requests the hold is buffering. | `Queue:50-53` | GAP | intended | 0 consumer sites |
| 151 | ⚠ THE SYSTEM SHALL refuse a maximum queued age of zero or a negative duration, as the real queue does. | `Queue:56` is a plain auto-property; `RequestQueue.cs:53-59` and `LogicBlockModbusTcpClient.cs:162-168` both refuse it; `IRequestQueue.cs:22` documents the refusal as the interface's contract | GAP | fix | `_findings.md:427-430`, MODB's second-opinion item 7. The two shipped `IRequestQueue` implementations disagree: the fake keeps `-1 s` and expires everything. The guard is four lines copied from `RequestQueue.cs:53-59`; `MaxQueuedAge` has **0** consumer `.cs` sites |
| 152 | THE SYSTEM SHALL read the maximum queued age at execution, so a change reaches a request already buffered. | `Queue:140`, `:157` | GAP | intended | `IRequestQueue.cs:23-26`'s rule, honoured |
| 153 | THE SYSTEM SHALL discard buffered requests on disposal without running them. | `Queue:124-127` | GAP | intended | a harness disposed while held drops what it holds. Stated: running them on disposal would fire callbacks into a torn-down container |
| 154 | THE SYSTEM SHALL ignore the capacity and overflow policy an initialization declares. | `Queue:59-62`, doc at `:58` | GAP | intended | they do not apply to a synchronous executor; a test cannot exercise `modbus.md`'s overflow rules through this queue |
| 155 | WHEN a request is enqueued before initialization THE SYSTEM SHALL refuse it, naming the queue and the enabling step. | `Queue:144-147` | GAP | intended | the accumulator guard |
| 156 | THE SYSTEM SHALL wire the fake server proxy into the real server, so extent validation and conversion run real SDK code. | `FakeModbusTcpServerHarness.cs:59-68` | `.DriveAFullServerCycleWithoutSockets` | intended | the server mirror of row 141 |
| 157 | THE SYSTEM SHALL hand the same server out through a factory for a factory-injected block. | `FakeModbusTcpServerHarness.cs:67`, `:78-91` | `.HandOutTheServerThroughTheFactory` | intended | the injection shape |
| 158 | THE SYSTEM SHALL offer a master-side view whose methods name the Modbus client surface, encoding typed values big-endian independently of the SDK's converter. | `FakeModbusTcpServerClient.cs:33-126`, comment at `:8-13` | `.RoundTripRawHoldingRegisterWrites`, `.ReadCoilsAsBools` | intended | so a conversion bug cannot cancel itself out |
| 159 | THE SYSTEM SHALL make that master view reachable only through the harness. | `FakeModbusTcpServerClient.cs:19` — an `internal` ctor on a `[PublicApi]` type | GAP | intended | the one manifest row a consumer cannot construct |
| 160 | THE SYSTEM SHALL hold full-range register and bit buffers regardless of the declared extents, and SHALL validate every simulated access against those extents. | `SrvProxy:20-30`, `:253-260` | `.RejectClientAccessOutsideTheDeclaredExtents` | intended | the declared extents are the contract, the buffer is the storage |
| 161 | WHEN a simulated client access lies outside the declared extents THE SYSTEM SHALL refuse it with the Modbus illegal-data-address exception a real master receives. | `SrvProxy:253-260` | `.RejectClientAccessOutsideTheDeclaredExtents` | intended | mirroring the wire |
| 162 | WHEN the fake server is not listening THE SYSTEM SHALL refuse every simulated client request, naming the enabling step. | `SrvProxy:245-251`, called from all six simulate methods | `.TrackListeningStateOnTheFakeProxy` | intended | the state guard |
| 163 | THE SYSTEM SHALL check a holding-register write's alignment before it checks that the server is listening. | `SrvProxy:114-120` — the alignment guard precedes `EnsureListening()` | `.RejectOddLengthRawHoldingRegisterWrites` | intended | an odd payload on a stopped server raises the alignment error, not the listening one. Stated: the alignment check is about the argument and the listening check about the state, and an argument guard first is the ordinary shape |
| 164 | THE SYSTEM SHALL record the extents a start declares and SHALL keep them when the server stops. | `SrvProxy:61-71` | `.TrackListeningStateOnTheFakeProxy` | intended | a restart re-declares them |
| 165 | THE SYSTEM SHALL stamp the last client write from its own clock, defaulting to the real system clock. | `SrvProxy:41`, `:127`, `:155` | `.SetLastClientWriteAtOnClientWrites` | intended | the third wall-clock default in the area, after row 144's two |
| 166 | THE SYSTEM SHALL let a test set the connection count and the last-write instant directly, to shape the diagnostics the server reports. | `SrvProxy:49`, `:55` | GAP | intended | what makes `modbus.md`'s diagnostics assertable |
| 167 | THE SYSTEM SHALL stop listening when disposed. | `SrvProxy:98-101` | GAP | intended | disposal is a stop |
| 168 | THE SYSTEM SHALL let a test seed the register buffers before the server is enabled. | `SrvProxy:74-95` — the buffer accessors carry no listening guard | `.AllowSeedingDefaultsBeforeEnable` | intended | the asymmetry with row 162: the buffers are reachable, the simulations are not |

**⚠ 129** — a consumer writes
`proxy.VerifyWriteSent(kind: WriteEventKind.MultipleCoils, expectedBytes: [0b00000101])` from the
`WriteEvent` doc's "raw wire-format payload" and it never matches; the recorded bytes are
`[0x01, 0x00, 0x01]`.

**Note 139** — no failure; the guard is correct and untested. `ResponseDelay` set with no `Clock` throws
a message naming both properties and the harness.

**⚠ 140** — `proxy.ResponseDelay = TimeSpan.FromMilliseconds(-5)` from a subtraction. Today: silently
no delay at all, and the round-trip assertion the delay was for reads zero.

**⚠ 144** — no failure; a default. `new FakeModbusTcpHarness()` stamps every receipt from wall time,
so a `RoundTrip` assertion is a wall-clock bound (`testing-conventions.md` § 16).

**⚠ 151** — `harness.Queue.MaxQueuedAge = TimeSpan.FromSeconds(-1)` is accepted, and every request
drained afterwards expires. The real queue refuses the same assignment.

**Note 153** — no failure; a shape. `using var harness = …; harness.Queue.Hold = true;` and the test
ends: the buffered requests are dropped and their callbacks never run.

**Note 163** — no failure; a guard order. Stated so a reader is not surprised by an `ArgumentException`
where they expected `InvalidOperationException`.

### G. Surface, packaging and gates

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 169 | THE SYSTEM SHALL ship five packable test kits, each targeting the runtime the tests run on and none of them the runtime a block ships to. | the five csprojs — `net10.0`, `IsPackable=true` | GAP | intended | the reason a kit may use `net10.0` where the SDK is `netstandard2.1` |
| 170 | THE SYSTEM SHALL mark every public type in each kit as published surface, and SHALL classify none as internal. | 35 `[PublicApi]`, 0 `[InternalApi]`; manifest carries all 35 | GAP | intended | the whole surface is the contract |
| 171 | ⚠ THE SYSTEM SHALL judge each kit's own declarations with the Dale analyzers, so an unmarked public type draws a diagnostic in the kit's build. | no kit csproj references `Vion.Dale.Sdk.Generators`; probes **P3** (kit silent, `Vion.Dale.Sdk` control fires), **P4** (analyzer added → 0 diagnostics), **P5** (analyzer added → `DALE014` fires in all five) | GAP | fix | the `[assembly: PublicApiNamespace]` declaration in all five kits has **no reader** today, so the manifest diff is the only gate on a quarter of it. Two lines per csproj, copied from `Vion.Dale.Sdk.DigitalIo.csproj:43`; probe **P4** proves the kits already satisfy every rule, so nothing else changes |
| 172 | THE SYSTEM SHALL carry the mocking library and the testable time provider in its published signature. | `Vion.Dale.Sdk.TestKit.csproj:23-24`; `Times?` on twelve verify helpers, `Mock<ILogger>` on three, `FakeTimeProvider` on `Ctx:81` and `Bld:173` | GAP | intended | every xunit consumer inherits both; the nine downstream suites are the proof it works |
| 173 | THE SYSTEM SHALL pin no test framework in any kit, so a consumer chooses their own. | no `MSTest` or `xunit` reference in the five kit csprojs | GAP | intended | `testing-conventions.md` § 1's two sides; the nine downstream consumers are xunit.v3 |
| 174 | THE SYSTEM SHALL reach the SDK's internal surface through two grants and three private-field reads. | `Vion.Dale.Sdk.csproj:23,25`; `Bld:446`, `:486`, `Timers:75` | GAP | intended | the honest statement of what a kit costs the SDK |
| 175 | THE SYSTEM SHALL read a block's bound service map through the SDK's own seam rather than through reflection. | `LogicBlockBase.cs:642` (`internal IReadOnlyCollection<string> BoundInterfaceIdentifiers()`); no `BindingFlags` in any of the five kit test projects | `Vion.Dale.Sdk.Test/Configuration/Interfaces/InterfaceBindingShould.cs:31` and 11 siblings | intended | row 183 is the stale sentence that still says otherwise |
| 176 | ⚠ THE SYSTEM SHALL clear every kit's package from the local cache when a release bumps the references. | `scripts/set-version.ps1:251-266` lists four kit ids and not `Vion.Dale.Sdk.Modbus.Tcp.TestKit` | `scripts/run-script-tests.ps1` | fix | one line in a script this repository owns, gated by `run-script-tests`. A stale cached Tcp kit is exactly the shape `docs/releasing.md` exists to prevent |
| 177 | ⚠ THE SYSTEM SHALL let a same-PR kit change be proven locally against every downstream consumer suite. | `grep -rln DaleLocalSource --include=*.csproj` names the seven examples and neither `templates/vion-iot-library/VionIotLibraryTemplate.Test` nor `libraries/Vion.Diagnostics/Vion.Diagnostics.Test` | GAP | park | adding the switch to two more projects touches the template's shipped content (a `dale new` output would carry it) and the first-party library lane. Bigger than a kit pass; `docs/releasing.md` is the owner |
| 178 | ⚠ THE SYSTEM SHALL name a type after the file that declares it. | `TestKitMockException.cs:10` declares `TestKitVerificationException` | GAP | fix | a rename of the file, not the type; no reference moves and the manifest row is unchanged |
| 179 | THE SYSTEM SHALL declare no test-framework attribute or assertion in a kit, so a kit compiles into any consumer's suite. | no `Assert` in the five kits; every failure is a thrown `TestKitVerificationException` | GAP | intended | why the kit throws rather than asserting |

**⚠ 171** — a public type added to a kit today carries no mark, draws no diagnostic, and reaches the
manifest diff only after CI regenerates it — which auto-commits rather than failing (`io.md:326-330`).

**⚠ 176** — `pwsh scripts/set-version.ps1 -Version X.Y.Z -Scope references` after a release leaves
the previous `Vion.Dale.Sdk.Modbus.Tcp.TestKit` in `~/.nuget/packages`, so the next local build of a
consumer resolves the old kit against the new SDK.

**⚠ 177** — a signature change to the core kit cannot be proven against the template's own test
project without publishing first.

**⚠ 178** — no failure; a navigation cost. `TestKitVerificationException` is unfindable by filename.

### H. Test discipline

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 180 | THE SYSTEM SHALL test each kit from its own MSTest project, driving the kit through a fixture logic block and never through a runtime, a broker, a device or the development host. | the five `*.TestKit.Test` projects; `SampleLogicBlock.cs` / `SampleProviderLogicBlock.cs` in four of them | all 182 | intended | `D3` |
| 181 | THE SYSTEM SHALL leave the suites that prove another area's criteria in place, cited to that area. | 39 `AC-EMIT`/`AC-GATE`/`AC-LIFE` citations in `Vion.Dale.Sdk.TestKit.Test`, 24 `AC-MODB` in `…Modbus.Tcp.TestKit.Test` | `EmissionPolicyShould` (24), `ModbusLinkPolicyShould` (12), `ModbusLinkDiagnosticsShould` (12) and four more | intended | 63 of the 182 tests prove other pages |
| 182 | ⚠ THE SYSTEM SHALL hold every test that cites a criterion to the gated naming and structure rules. | `scripts/test-style-lint.ps1:59-60` exempts nothing in these five projects; 119 of the 182 are uncited today | `scripts/test-style-lint.ps1` | fix | every test this pass cites is gated from that commit. `ProviderFacesShould.Write_nothing_until_a_command_arrives` carries no `// Act` marker and both `ProviderFacesShould` classes use snake-case names — the rewrite `D3` names |
| 183 | ⚠ THE SYSTEM SHALL name `EmissionPolicyShould.cs:233` as this repository's one reflection precedent only while that is true. | `testing-conventions.md:123-125` says it reaches `LogicBlockBase._serviceBinder` by `BindingFlags.NonPublic`; `EmissionPolicyShould.cs:225` now reads `context.VerifyServicePropertyEmitted(lb => lb.Slow, times: Times.Exactly(2))`, and `grep -rn 'BindingFlags\|GetPrivateField\|_serviceBinder'` over all five kit test projects is empty | GAP | fix | a stale convention line in a doc this repository owns. The EMIT pass (`0f2d35a`) discharged the debt and `LogicBlockBase.BoundInterfaceIdentifiers()` (`:642`) is the seam that replaced it (row 175) |
| 184 | THE SYSTEM SHALL keep the introspection suite that lives in the Modbus TCP kit's test project without citing it here. | `ModbusDiagnosticsIntrospectionShould.cs` — 10 tests, 0 citations, and it constructs no TestKit type | GAP | out-of-spec | it proves `Vion.Dale.Sdk.Modbus.Tcp.Diagnostics` against introspection; it belongs to `MODB` and `INTRO`, and citing it is their call, exactly as `io.md:361-365` decided about these kits |
| 185 | ⚠ THE SYSTEM SHALL declare the same test-platform reference in every kit test project. | `Vion.Dale.Sdk.TestKit.Test` and `…Modbus.Tcp.TestKit.Test` reference `Microsoft.NET.Test.Sdk` 18.0.1; Digital, Analog and Rtu do not | GAP | park | the three run green today under the MSTest platform alone (`dotnet test` passes), so this is a consistency question about the test-platform migration, not a defect this pass created. The brief's parallelisation claim about the same three csprojs is refuted in Drift checkpoint 3 |

**⚠ 182** — `pwsh scripts/test-style-lint.ps1` reddens the moment a `ProviderFacesShould` test carries
a citation, on the snake-case name and the missing `// Act`.

**⚠ 183** — a reader follows `testing-conventions.md` § 7 to `EmissionPolicyShould.cs:233` looking for
the precedent and finds an ordinary `Verify` call.

**⚠ 185** — none; the three suites pass. A consistency row for the retro.

---

## Unmapped tests

Tests in scope that map to no row above. All ten are in one file.

| Test | Disposition |
|---|---|
| `ModbusDiagnosticsIntrospectionShould.EmitTheLinkSummaryAsAStructWithEveryFieldOfTheSummary` | keep, uncited — row 184 |
| `.EmitTheConnectionSummaryAsAStruct` | keep, uncited — row 184 |
| `.EmitTheLinkStateAsItsMemberNames` | keep, uncited — row 184 |
| `.LabelEveryLinkSummaryFieldSoItReadsWithoutAProjection` | keep, uncited — row 184 |
| `.LabelEveryConnectionSummaryField` | keep, uncited — row 184 |
| `.RouteAnEnumFieldsAuthoredTitleToPresentationWhileItsSchemaTitleKeepsTheTypeIdentity` | keep, uncited — row 184 |
| `.CarryTheEnumLabelsAndSeveritiesOfAFieldsEnumTypeAlongsideTheField` | keep, uncited — row 184 |
| `.KeepTheTemporalFieldFormatsThatDriveDateAndDurationRendering` | keep, uncited — row 184 |
| `.MapTheLinkStateToSeveritiesAndLabelsWhenPublishedAsAStatusPill` | keep, uncited — row 184 |
| `.MapTheConnectionStateAndTheOutcomeToSeveritiesAndLabels` | keep, uncited — row 184 |

No test in the five projects is a merge or delete candidate: the 47 core-kit tests, the 6 digital,
the 11 analog, the 25 Rtu and the 25 TCP kit-owned tests all map to a row, and the 63 that cite other
areas are `AC-EMIT`, `AC-GATE`, `AC-LIFE` and `AC-MODB` proofs that stay where they are.

---

## Self-check against the anchor inventory

| Question | Answer |
|---|---|
| `[PublicApi]` types visited by a row | **35 of 35**. `Constants` 11 · `EmissionPolicyMode` **186** · `ILoggerMockExtensions` 55–56 · `LogicBlockBaseExtensions` 8 · `LogicBlockTestContext` 36–57, 77–101 · `LogicBlockTestContextBuilder` 9–35 · `LogicBlockTestHelper` 1–7 · `LogicBlockTimerExtensions` 102–107 · `TestActorReference` 18 · `TestKitVerificationException` 57, 178 · `TimesExtensions` 41–44 · the six `I*Extensions` 58–64 · the three `LogicBlockTestContextExtensions` 65–76, 119 · `IModbusRtuExtensions` 109–118 · `ModbusResponseBuilder` 120–122 · `FakeModbusTcpClientProxy` 123–140 · `FakeModbusTcpClientProxyExtensions` **187** · `FakeModbusTcpHarness` 141–146 · `FakeModbusTcpServerClient` 158–159 · `FakeModbusTcpServerHarness` 156–157 · `FakeModbusTcpServerProxy` 160–168 · `SynchronousRequestQueue` 147–155 · the three enums and three records 127, 129 |
| Manifest rows in a `*.TestKit` namespace | **35 of 35**, one per type above |
| `[assembly: PublicApiNamespace]` declarations | **5 of 5**, all in row 171 |
| Private-field reaches | **3 of 3** — rows 16, 17, 102 |
| `const` / `static readonly` | **5 of 5** — rows 11, 77, 160 |
| Timeouts | **0**, stated as row 108 rather than left silent |
| Which observables have **neither** a row nor a criterion? | **Asked, and it found two.** Walking the 35 manifest rows against the table rather than counting rows against types found `EmissionPolicyMode` covered only through the tests that exercise it, and `FakeModbusTcpClientProxyExtensions` covered only through the histories it reads — the knob and the four helpers themselves were unstated. Both are now rows **186** and **187**, and their numbers are out of table order because they were minted after the first 185 were written. Three observables remain with no row, each deliberate: `IActorContext.Headers` (row 38, `out-of-spec`); `FakeModbusTcpClientProxy.Dispose` (`Fake:334-337`, an empty body with a comment saying why — no observable); and the three history properties' *liveness* (`Fake:47-62` return the backing lists, so a held reference grows) — the last is stated in row 127's prose rather than minted, because no consumer can observe the difference without casting away `IReadOnlyList<T>` |
| Reviewer's questions describing an observable with no row | None. Question 5's list is a row index; question 7's RFC 0002 mints no row because the kits offer nothing to state — it is a park by name in the consolidation map |

---

## Spec delta (to distill)

_(minted in Phase B, after the operator's classification)_

---

## Relay notes for the PR body

_(written as each change lands in Phase B)_

---

## Tasks

_(written in Phase B, after the operator's classification)_
