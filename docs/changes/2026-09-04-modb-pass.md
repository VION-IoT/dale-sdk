---
slug: modb-pass
status: proposed           # proposed | in-flight | parked | archived
blocked-on: none           # for parked docs: what's blocking + ref
areas: MODB
author: spec-pass (MODB, attempt 1)
created: 2026-09-04
updated: 2026-09-04
supersedes: none           # path of a superseded change doc, or none
---

# MODB area pass — the Modbus protocol bindings

> Change doc — one in-flight change. The `Spec delta` below is distilled into the current-truth
> pages under `docs/specs/` in the implementation PR, then this doc is archived
> (`pwsh scripts/spec-change.ps1 archive <slug>`). Process: `docs/spec-process.md`. Never put
> change narrative inline in a spec page — it lives here.

## At a glance

### Summary

The tenth SDD area pass turns `Vion.Dale.Sdk.Modbus.Core`, `.Tcp` and `.Rtu` — 86 source files, 73
public types, 30 `[PublicApi]` marks, 388 tests in 25 files, none citing a criterion — into one
current-truth spec page. It absorbs and deletes RFC 0007 (Modbus-TCP server) and RFC 0018 (per-block
socket lifetime), leaving three files in `docs/rfcs/`. What a logic-block author gets from a Modbus
binding — the 36 reads and writes, the receipt, the link policy, the queue, the conversion, the
hosted server, the RTU contract, the diagnostics — is stated once, with the code as evidence.

### Spec implications

Creates **`docs/specs/modbus.md`** (`D1`), `trace: enforced`, minting `AC-MODB-*`. Nothing else in
`docs/specs/` changes except `_findings.md`, which gains a `## MODB` section for the `park` rows.

Rows another page owns are **cited, never re-minted**: the generic `[ServiceProviderContractBinding]`
and `ServiceProviderHandlerBase`'s arms (`AC-BIND-*`), `[assembly: DaleSharedAssembly]` loading
(`AC-PLUG-*`), the emission of the diagnostics as service properties (`AC-EMIT-*`), the stop hook's
promise (`AC-LIFE-012.*`), the DevHost's stand-ins (`AC-SCEN-*`, `AC-CTRL-*`), the analyzer pack's
reach (`AC-ANLZ-018.2`), and architecture decisions `0118` / `0119` / `0014` / `0023` / `0037`–`0039`
/ `0139` by number.

Outside `docs/specs/`: `CLAUDE.md:37`'s "five files remain" becomes three (`0001`, `0002`, `0009`);
`.claude/skills/modbus-smoke/SKILL.md:12-13` carries a claim this pass disproves (row 165).

### Decisions

- `D1` — the page is **`docs/specs/modbus.md`**, the brief's suggested path. "Modbus" is the
  published vocabulary a consumer greps for; `modbus-bindings` would name the folder layout instead.
- `D2` — the spine is the coordinator's recommendation verbatim (Reviewer's question 2): client
  surface · link policy · queue · conversion · server · RTU · diagnostics · DI and socket lifetime ·
  test discipline. It orders the page the way a block author meets the surface.
- `D3` — **both RFCs are absorbed and deleted this pass.** Every implemented clause becomes a
  criterion with the code as evidence; every deferred or open item becomes a ledger line with an
  owner. `docs/rfcs/README.md` names neither, so the sweep is `CLAUDE.md:37` plus the two files'
  own cross-references.
- `D4` — the harness the rewritten suite moves to: a **fake at the proxy seam**
  (`IModbusTcpClientProxy`) plus `SynchronousRequestQueue` where the criterion is about the link
  policy or the queue; Moq stays only where the collaborator genuinely has no state
  (`IActorDispatcher`, `IModbusRtuRequestFactory`'s callers). The three real-socket suites keep
  their sockets, are serialised against each other, and never run beside the smoke skill.
- `D5` — **the diagnostics the SDK owns are this page's** (decision `0118`): what each field of
  `ModbusLinkSummary` and `ModbusTcpConnectionSummary` records and when it moves. Their *emission*
  as `[ServiceProperty]` is `EMIT`'s and is cited.
- `D6` — **no analyzer judges these three projects.** None carries the analyzer `ProjectReference`
  that `Vion.Dale.Sdk.DigitalIo` and `.AnalogIo` do, so `AC-ANLZ-018.2` puts every declaration here
  outside the pack. The page states it; nothing is minted for an enforcement that does not exist.
- `D7` — the brief's phrase *"`ANLZ`'s Known non-conforming code"* names no section of
  `docs/specs/analyzers.md`. The citation used instead is **`AC-ANLZ-018.2`**, which states the rule
  the brief was reaching for. (Drift checkpoint.)

### Reviewer's questions

1. **(ratified — cite)** The ownership split. Rows that cite instead of mint: the binding and the
   provider-face base (`AC-BIND-*`), the shared-assembly loading (`AC-PLUG-*`), the emission of the
   diagnostics (`AC-EMIT-*`), the stop hook's promise (`AC-LIFE-012.*`), the DevHost's stand-ins
   (`AC-SCEN-*`, `AC-CTRL-*`), the decisions by number.
   **OUTCOME:** (pending classification)

2. **(decide-and-document)** The page name and spine — `D1` and `D2` above.
   **OUTCOME:** (pending classification)

3. **(decide-and-document)** Both RFCs absorbed and deleted — `D3`. RFC 0007's three deferred items
   (Q7 array overloads `:357`, Q2 multi-unit maps `:352`, Q3 a consumer validator hook `:353`) and
   RFC 0018's three open items (§ 8.1 the reuse-knob repro, § 8.3 factory-created clients, § 8.4 the
   `FluentModbus` question) become ledger lines; § 8.4's owner is the retro. RFC 0018 § 9's
   corrected claim is **row 57**, citing `AC-LIFE-012.*`. RFC 0007 `:369`'s "optional but
   recommended `examples/` server-role block" is satisfied by `ModbusTcpSimServer.cs`; its `:368`
   acceptance bar is **row 166**.
   **OUTCOME:** (pending classification)

4. **(propose-and-wait)** The 125-register protocol limit — **row 78**. The SDK enforces it nowhere
   (no such literal in the three projects; the only cap is `ushort.MaxValue` at
   `ModbusDataConverter.cs:36`), and the TCP example refuses quantity 126 before the wire
   (`ModbusTcpDebugClientReadShould.cs:302-311`). **The reader was searched:** the largest quantity
   any of the first consumer's 84 files reads in one call is **40 registers**
   (`ParameterGroupLength`), and the largest count-based read is 12 floats = 24 registers
   (`PhaseFloatCount`) — no consumer call approaches 125. Recommendation: **refuse in the client
   before the wire**, mirroring the example, since no fielded call is affected and the alternative
   is a device-dependent answer the page cannot state.
   **OUTCOME:** (pending classification)

5. **(decide-and-document)** The harness — `D4`. `testing-conventions.md` § 12 applies to **twelve**
   of the 25 files, not the brief's ten (the two factory suites also import Moq — Drift checkpoint).
   The TestKit already proves the fake-at-the-proxy-seam shape works for the link policy
   (`Vion.Dale.Sdk.Modbus.Tcp.TestKit.Test/ModbusLinkPolicyShould.cs`). Ports read at the call site:
   `ModbusTcpServerIntegrationShould` and `ReuseAddressTcpClientProviderShould` bind ephemeral
   loopback ports; `ModbusTcpServerProxyShould`'s `FluentModbus` server binds one too — none of the
   three binds 5000 or 15020, so the collision the brief guards against is with the smoke skill's
   host, not between the suites.
   **OUTCOME:** (pending classification)

6. **(decide-and-document)** The diagnostics — `D5`. Rows 135–145 state what each field records.
   **OUTCOME:** (pending classification)

7. **(decide-and-document)** The XML docs of `IModbusClient` (1045 lines) and
   `IModbusTcpClientWrapper` (1752 lines) as the consumer's contract. Every promise is a row.
   **Four promises the code does not keep** were found in the first read: rows 15 (`Invalid` and
   `Cancelled` are documented as counted and are not), 57 (queued requests are documented as
   cancelled on dispose and are silently abandoned), 62 (`Disconnect` while disabled is skipped and
   the doc does not say so), 153 (the architecture's shared-assembly note says an unregistered
   handler never resolves; the runtime resolves `ModbusRtuHandler` through `ActivatorUtilities`).
   **OUTCOME:** (pending classification)

8. **(propose-and-wait)** Wire and surface changes. Rows recommending `propose` rather than `fix`
   because a public signature, a receipt shape, a link-policy constant, a DI lifetime, the server's
   wire behaviour, the unit-id aliasing, or a TestKit-substituted seam moves: rows **17**, **44**,
   **48**, **57**, **78**, **113**, **150**, **151** — the eight `propose` recs. Each names its
   reader in *Why*.
   **OUTCOME:** (pending classification)

9. **(decide-and-document)** RFC 0018 § 8.3 — **row 150**, a `propose`. **The reader was searched:**
   `ILogicBlockModbusTcpClientFactory` has **zero call sites** in the first consumer's
   `Ecocoach.LogicBlockLibraries/**`, so no fielded block creates clients through the factory today.
   `ILogicBlockModbusTcpServerFactory` has the same shape (`ServiceCollectionExtensions.cs:34-35`)
   and is row 151.
   **OUTCOME:** (pending classification)

10. **(decide-and-document)** No analyzer judges these projects — `D6`, **row 154**, cited to
    `AC-ANLZ-018.2`. Nothing minted for an enforcement that does not exist.
    **OUTCOME:** (pending classification)

---

## Full design

### Anchor inventory

Enumerated mechanically this session. Every count below was taken from the tree at
`spec-pass/modb-a1` off `204f9c1`.

**Scope, counted.** 86 `.cs` files — `Modbus.Core` **40**, `Modbus.Tcp` **36**, `Modbus.Rtu` **10**
(the brief's counts hold). Per folder: Core `Client/` 1, `Conversion/` 8, `Diagnostics/` 5,
`Exceptions/` 13, `Server/` 9, `Validation/` 2, plus `ServiceCollectionExtensions.cs` and
`AssemblyAttributes.cs`; Tcp `Client/Implementation/` 4, `Client/LogicBlock/` 4, `Client/Request/`
16, `Diagnostics/` 3, `Server/Implementation/` 3, `Server/LogicBlock/` 4, plus
`ServiceCollectionExtensions.cs` and `PublicApiConfig.cs`; Rtu 10 flat.

**The client surface** — `IModbusClient` (`Client/IModbusClient.cs`, 1045 lines): **4 properties**
(`IsEnabled` `:60`, `DefaultOperationTimeout` `:70`, `MaxQueuedAge` `:81`, `Link` `:86`) and
**36 methods**, every one `void`, every one taking `IActorDispatcher` and a success callback:

| family | area | shapes | count |
|---|---|---|---|
| `ReadDiscreteInputs` / `ReadCoils` | bits | — | 2 |
| `ReadInputRegisters{Raw,AsShort,AsUShort,AsInt,AsUInt,AsFloat,AsLong,AsULong,AsDouble,AsString}` | input registers | 10 | 10 |
| `ReadHoldingRegisters{…same ten…}` | holding registers | 10 | 10 |
| `WriteSingleCoil` / `WriteMultipleCoils` | bits | — | 2 |
| `WriteSingleHoldingRegister` | holding registers | `short`, `ushort` overloads | 2 |
| `WriteMultipleHoldingRegisters{Raw,AsShort,AsUShort,AsInt,AsUInt,AsFloat,AsLong,AsULong,AsDouble,AsString}` | holding registers | 10 | 10 |

22 reads + 14 writes, matching `architecture/libraries/dale-sdk.md:184`. The **ten value shapes** are
`Raw` (bytes), `short`, `ushort`, `int`, `uint`, `float`, `long`, `ulong`, `double`, `string`. The
16-bit and `Raw` and `string` shapes take a `ushort quantity` (registers); the 32- and 64-bit shapes
take a `uint count` (values). `ILogicBlockModbusTcpClient : IModbusClient, IDisposable` adds 9
members (`QueueCapacity`, `QueueOverflowPolicy`, `QueuedRequestCount`, `ConnectionTimeout`, `Port`,
`IpAddress`, `ConnectBackoff`, `ConnectBackoffMax`, `Connection`, `Disconnect`);
`ILogicBlockModbusTcpServer : IDisposable` has 11 (`IsEnabled`, `ListenAddress`, `Port`, four
`*Count` extents, `IsListening`, `ConnectionCount`, `LastClientWriteAt`, two `Sync` overloads);
`IModbusRtu : IModbusClient` adds **nothing** — it is a marker carrying
`[ServiceProviderContractType("ModbusRtu")]` (`IModbusRtu.cs:48`).

**Enums, counted.** `ByteOrder` 2 · `WordOrder32` 2 · `WordOrder64` 4 (`ABCD`/`DCBA`/`CDAB`/`BADC`)
· `TextEncoding` 4 · `ModbusLinkState` 3 (`Unknown`/`Online`/`Faulted`) · `ModbusOutcome` **10**
(five link verdicts `Success`, `DeviceError`, `Timeout`, `TransportError`, `ProtocolError`; five
local-only `BackedOff`, `Expired`, `Dropped`, `Invalid`, `Cancelled` — the split is stated in the
type's own remarks at `ModbusOutcome.cs:9-13` and implemented by `ModbusLinkAccumulator.ReachedTheWire`
at `:141-144`) · `ModbusServerArea` 4 · `ModbusExceptionCode` **10** (`Ok`=0x00 through
`GatewayTargetDeviceFailedToRespond`=0x0B, with 0x07 and 0x09 unused) · `ModbusTcpConnectionState` 3
(`Disconnected`/`Connected`/`BackingOff`) · `QueueOverflowPolicy` 3 · `RequestDropReason` 2.

**Structs.** `ModbusReceipt` 5 fields · `ModbusLinkSummary` **18** `[StructField]`-annotated fields ·
`ModbusTcpConnectionSummary` **9** · `ModbusServerAreaExtents` 4 plus one method `Covers`.

**Exceptions, 16 in scope.** Core 13 (`InvalidBitQuantity`, `InvalidCount`, `InvalidServerAddress`,
`InvalidUnitIdentifier`, `Modbus`, `ModbusResponseAlignment`, `OperationTimeout`, `RequestExpired`,
`UnsupportedByteOrder`, `UnsupportedTextEncoding`, `UnsupportedWordOrder32`, `UnsupportedWordOrder64`,
plus the `ModbusExceptionCode` enum in the same folder), Tcp 4 (`LinkBackoff`, `RequestDropped`,
`IpAddressNotSet` at `ModbusTcpClientWrapper.cs:1216`, `ConnectionTimeout` at
`ModbusTcpClientProxy.cs:268`), Rtu 2 (`PendingRequestsLimitReached` at `ModbusRtuHandler.cs:664`,
`ServiceProviderContractMappingNotFound` at `:680`).

**The link policy's constants and transitions** (`ModbusTcpClientWrapper.cs` unless noted):
`ConnectFailuresBeforeBackoff` 2 (`:22`), `DefaultConnectBackoff` 1 s (`:24`),
`DefaultConnectBackoffMax` 30 s (`:26`); `ConnectionTimeout` 3 s and `Port` 502 set in the client's
constructor (`LogicBlockModbusTcpClient.cs:44-45`), `DefaultMaxQueuedAge` 30 s (`:17`),
`DefaultOperationTimeout` 1 s (`:261`), `QueueCapacity` 256 and `QueueOverflowPolicy.DropOldest`
(`:110`, `:118`). Transitions: the early return when connected and no reconnect is pending (`:170`),
`ThrowIfBackingOff` (`:176` called, `:215-231` defined — an elapsed backoff returns at `:225` and
lets the attempt through), `RecordConnectFailed` + `ArmConnectBackoff` (`:194-196`; nothing below
two consecutive failures at `:236-239`, then `Exponential(ConnectBackoff, failures - 2,
ConnectBackoffMax)` at `:241`, doubling and stopping *before* the multiply at `max/2` — the guard at
`:288` inside `:282-297`), success clearing the backoff (`:201-208`), `ClearConnectBackoff` (`:248`)
from `Port` (`:106`), `IpAddress` (`:125`) and re-enabling (`LogicBlockModbusTcpClient.cs:75`), and
`CloseSocketOnWireFault` (`:262-279` — only `Timeout`/`TransportError`/`ProtocolError` force a
reconnect).

**The refusals.** `ConvertCountToQuantity`: zero count (`ModbusDataConverter.cs:28`, thrown at
`:30`) and a register quantity above `ushort.MaxValue` (`:36-41`) — `count` is `uint`, so there is
no negative case. Unsupported orders and encodings: `:50` (`ByteOrder`), `:71` (`WordOrder32`),
`:154` (`WordOrder64`, inside the loop), `:169` and `:182` (`TextEncoding`). Bit quantity above the
bytes returned: `:197`. Unit id outside 0–255: `ModbusValidator.cs:13-16`. Response byte count not a
multiple of the value size: `ModbusValidator.cs:22-25`. Client-side knobs:
`MaxQueuedAge <= 0` (`LogicBlockModbusTcpClient.cs:133`), `ConnectBackoff <= 0` (`:198`),
`ConnectBackoff > ConnectBackoffMax` (`:203`), `ConnectBackoffMax < ConnectBackoff` (`:222`), a port
outside `IPEndPoint.MinPort..MaxPort` (`:153`), an unparseable IP address (`:169-177`).
Server-side: `ModbusServerAreaExtents.Covers` (`:43`), `ModbusRegisterAccessor.ValidateRange`
(`:214-217`), `ModbusBitAccessor.ValidateAddress` (`:54-57`), odd raw register bytes
(`ModbusRegisterAccessor.cs:56-59`), a port above `MaxPort` 65535
(`LogicBlockModbusTcpServer.cs:12`, `:86`), a config change while enabled (`:213-219`), and
`EnsureNotInSyncCallback` (`:222-231`, from `IsEnabled` `:38` and `Dispose` `:198`).

**The 125-register protocol limit is enforced nowhere in the SDK.** `grep -rn "125" ` over the three
projects returns no such literal; the only cap is `ushort.MaxValue`. Row 78.

**DI.** `AddDaleModbusCoreSdk` (`Modbus.Core/ServiceCollectionExtensions.cs:17`) — three transients
(`IBitConverterProxy`, `IModbusDataConverter`, `IModbusValidator`). `AddDaleModbusTcpSdk`
(`Modbus.Tcp/ServiceCollectionExtensions.cs:25`) — calls the Core one, then **two singletons** (the
client and server factories) and **seven transients** (`ILogicBlockModbusTcpClient`,
`IRequestFactory`, `IRequestQueue`, `IModbusTcpClientWrapper`, `IModbusTcpClientProxy`,
`ILogicBlockModbusTcpServer`, `IModbusTcpServerProxy`), then `TryAddSingleton(TimeProvider.System)`
at `:41`. `Modbus.Rtu`'s `DependencyInjection : IConfigureServices` (`ConfigureServices.cs:11`)
calls `AddDaleModbusCoreSdk()` and registers `IModbusRtuRequestFactory` as a **singleton** —
and registers **no handler**, unlike `DigitalIo` and `AnalogIo` (row 153).

**Assembly markers.** `[assembly: DaleSharedAssembly]` on Core (`AssemblyAttributes.cs:9`) and Rtu
(`AssemblyAttributes.cs:3`); Tcp deliberately carries none. `[assembly: PublicApiNamespace]` — Tcp
five (`PublicApiConfig.cs:3-7`), Rtu one (`:3`), **Core none**.

**The published surface.** 73 public types (Core **38**, Tcp 23, Rtu 12 — the brief said 37 for
Core; Drift checkpoint), **30** `[PublicApi]` marks (18/11/1), **2** `[InternalApi]`
(`ModbusLinkAccumulator`, `ModbusTcpConnectionAccumulator`), and exactly **30** in-scope rows in
`docs/snapshots/publicapi-manifest.json` (Core 18, Tcp 11, Rtu 1) — the marks and the manifest agree
one-for-one, which is the mechanism `docs/process-journal.md:71` records. Adding the two TestKits'
16 types and the five assembly rows gives the 51 rows naming Modbus.

**None of the three projects references the analyzer.** `grep -rl Vion.Dale.Sdk.Generators
--include=*.csproj` lists 19 projects, including `Vion.Dale.Sdk.DigitalIo` and
`Vion.Dale.Sdk.AnalogIo`, and none of the three Modbus ones. Row 154.

**Tests in scope, counted.** 388 `[TestMethod]`s in 25 files with 290 `[DataRow]`s — Core 74 in 8
files (39 rows), Tcp 222 in 14 (207 rows), Rtu 92 in 3 (44 rows). **Zero cite an `AC-` id**, and
`scripts/test-style-lint.ps1`'s exempt map is empty (`:59-60`, "Empty since the ANLZ pass retired the
last entry"), so every citation added here is gated from the first commit. **Twelve** of the 25
import Moq — the brief's ten plus `LogicBlockModbusTcpClientFactoryShould` and
`LogicBlockModbusTcpServerFactoryShould` (Drift checkpoint).

### Probes run this session

None. Every row's evidence is a read of the code at the cited `file:line`, of a committed scenario
file, of the PublicApi manifest, or of a consumer's call sites; three rows additionally rest on a
read of the private runtime at `C:\_gh\dale` (rows 153, 150, 151), cited there.

**Every `file:line` in this document was then re-resolved mechanically** — a script extracted all
citations, opened each file at the cited line, and printed what stands there. The first run found
**48 citations off by a fixed offset**: they had been taken from a `cat -n` over several files at
once, whose numbering runs continuously across the concatenation rather than restarting per file
(`ModbusLinkSummary`, `ModbusReceipt`, both connection-diagnostics types, `DeviceRequest`,
`ControlRequest`, `IRequestQueue`, `ILogicBlockModbusTcpServer`, `IModbusRegisterAccessor`, the two
factories, three exception types, and the Core DI extension). All 48 are corrected above, and the
audit now resolves every citation to the code its row claims. Reading several files in one dump is a
false economy this pass paid for once; only a per-file read yields citable line numbers.

### The reader reads that changed a row's class

Two claims the extraction would have got wrong from the SDK alone, both settled by opening the
reader:

- **Row 153.** `Vion.Dale.Sdk.Modbus.Rtu`'s `IConfigureServices` does not register
  `ModbusRtuHandler`, where `DigitalIo` and `AnalogIo` register theirs, and
  `architecture/libraries/dale-sdk.md`'s shared-assembly note says such handlers "never resolve".
  Read at the reader: the runtime spawns handler actors with
  `IActorSystem.CreateRootActorFromDi` (`C:\_gh\dale\Dale\Program.cs:216-234`), which resolves the
  receiver through `ActivatorUtilities.CreateInstance(scope.ServiceProvider, actorReceiverType)`
  (`Vion.Dale.ProtoActor/ActorSystem.cs:237`) — a type constructed this way need not be registered,
  only its constructor parameters must resolve, and `ModbusRtuHandler`'s are `TimeProvider` and
  `ILogger<>`. And `PluginLoader.RegisterSharedAssemblyServices`
  (`C:\_gh\dale\Dale\Plugin\PluginLoader.cs:301-317`) reports the unregistered-handler fault **only
  when the assembly declares no `IConfigureServices` at all** — Rtu declares one, so the branch is
  never reached. The row is therefore `intended` with the architecture prose flagged as a
  checkpoint, not a `fix`.
- **Rows 150 and 151.** RFC 0018 § 8.3's leak is real at the SDK
  (`ServiceCollectionExtensions.cs:28-29`, `:34-35`), but its blast radius is zero today: the first
  consumer calls neither factory anywhere in `Ecocoach.LogicBlockLibraries/**`.

---

## The behavior table

Recommendation legend: `intended` → spec · `fix` → defect, fixed in Phase B · `park` → ledger ·
`propose` → operator decides (implemented as `fix` if accepted, written to the ledger as `park` if
not) · `out-of-spec` → implementation shape.

### A. The shared client surface — `IModbusClient`

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 1 | THE SYSTEM SHALL offer the same 22 reads and 14 writes with identical signatures on `ILogicBlockModbusTcpClient` and `IModbusRtu`, so a device sold as TCP and as RTU is one block. | `IModbusClient.cs:104-1044` (36 methods); both interfaces derive from it (`ILogicBlockModbusTcpClient.cs:71`, `IModbusRtu.cs:49`) | `LogicBlockModbusTcpClientShould` (36 families) and `ModbusRtuShould` (36 families) | intended | the shared base is the whole reason `IModbusClient` exists (`architecture/libraries/dale-sdk.md:183-185`) |
| 2 | THE SYSTEM SHALL return from every read and write immediately and deliver the result later through the `IActorDispatcher` the call was given. | every method returns `void` and takes `IActorDispatcher dispatcher` (`IModbusClient.cs:107`); TCP enqueues (`LogicBlockModbusTcpClient.cs:280`), RTU sends a contract message (`ModbusRtu.cs:1101`) | `ArrayResultRequestShould.PassSuccessCallbackToDispatcherWhenOperationSucceeds` | intended | a logic block's actor thread must never block on a socket |
| 3 | THE SYSTEM SHALL hand a `ModbusReceipt` to both the success and the error callback of every device operation. | `IModbusClient.cs:108-109` and every sibling; `DeviceRequest.cs:87-90` builds it | `SingleResultRequestShould.PassErrorCallbackToDispatcherWhenOperationFails` | intended | decision `0119`; no compatibility overloads, `_` to ignore |
| 4 | THE SYSTEM SHALL stamp the receipt's instants at the moment the response or failure was observed, before the callback is handed to the block's mailbox. | `DeviceRequest.cs:73-90` (on the queue consumer); `ModbusRtuHandler.cs:256-259` (on the handler actor) | GAP | intended | it is what makes the receipt usable as the age of the value it accompanies |
| 5 | THE SYSTEM SHALL carry on the receipt the wall-clock `ReceivedAt`, the monotonic `ReceivedTimestamp`, the `RoundTrip`, the `QueuedWait` and the `ModbusOutcome`. | `ModbusReceipt.cs:40` | `ModbusRtuRequestFactoryShould.RecordEveryCompletedTransactionInTheLinkAccumulator` | intended | the two clocks are the wall-clock-steps-backwards guard (`ModbusReceipt.cs:18-21`) |
| 6 | THE SYSTEM SHALL split a transaction's latency into `QueuedWait` (before dispatch) and `RoundTrip` (dispatch to response), and SHALL include a connection handshake in `RoundTrip`. | `DeviceRequest.cs:46`, `:75`; `ModbusRtuReceiptBuilder.Build` (`ActorMessages.cs:133-138`); the handshake happens inside `EnsureClientIsConnectedAsync` (`ModbusTcpClientWrapper.cs:1106`) before the timeout starts | GAP | intended | `Connection.LastConnectDuration` is how a caller separates the two (`ModbusReceipt.cs:24-29`) |
| 7 | WHEN `IsEnabled` is `false` THE SYSTEM SHALL execute no read or write and SHALL invoke neither callback. | `LogicBlockModbusTcpClient.cs:274-278` and 35 siblings; `ModbusRtu.cs:1089-1093`, `:1123-1127` | `LogicBlockModbusTcpClientShould.NotEnqueueReadCoilsWhenClientIsDisabled` (×36), `ModbusRtuShould.SkipRequestWhenDisabled` | intended | it lets a block be configured field by field without half-set configuration failing (`IModbusClient.cs:56-59`) |
| 8 | WHEN `IsEnabled` becomes `false` THE SYSTEM SHALL still run the requests it has already accepted. | nothing in `LogicBlockModbusTcpClient.IsEnabled`'s setter (`:60-85`) touches the queue; `RequestQueue.ConsumeAsync` keeps draining | GAP | intended | documented at `IModbusClient.cs:58`; a disable is a configuration gate, not a cancel |
| 9 | THE SYSTEM SHALL default `IsEnabled` to `false`. | auto-property with no initialiser (`LogicBlockModbusTcpClient.cs:56`, `ModbusRtu.cs:89`, `LogicBlockModbusTcpServer.cs:32`) | GAP | intended | `IModbusClient.cs:54` states it; a client that polls before it is configured would fault its own link |
| 10 | WHEN an operation passes no `operationTimeout` THE SYSTEM SHALL use `DefaultOperationTimeout`, and a change to it SHALL NOT affect operations already accepted. | `operationTimeout ?? DefaultOperationTimeout` is captured into the request's closure at enqueue (`LogicBlockModbusTcpClient.cs:285`); RTU captures it into the message (`ModbusRtu.cs:1042`) | GAP | intended | `IModbusClient.cs:67-68` states it |
| 11 | THE SYSTEM SHALL default `DefaultOperationTimeout` to 1 second on Modbus TCP and to 5 seconds on Modbus RTU. | `LogicBlockModbusTcpClient.cs:261`; `ModbusRtu.cs:144` | GAP | intended | the transports' inherent difference, stated at `IModbusClient.cs:43-47` |
| 12 | THE SYSTEM SHALL measure `DefaultOperationTimeout` over the wire exchange only — from dispatch to the complete response — and not over the wait before dispatch, parameter validation or data conversion. | the timeout CTS is created *after* `EnsureClientIsConnectedAsync` and *after* `ValidateUnitIdentifier` (`ModbusTcpClientWrapper.cs:1100-1110`); RTU starts it at publish (`ModbusRtuHandler.cs:224`) | GAP | intended | `IModbusClient.cs:66-68`; the split is what makes `QueuedWait` meaningful |
| 13 | WHEN `MaxQueuedAge` is set to zero or a negative duration THE SYSTEM SHALL throw `ArgumentOutOfRangeException`. | `LogicBlockModbusTcpClient.cs:133-136`; `ModbusRtu.cs:120-123` | GAP | intended | `IModbusClient.cs:75` states it, and both implementations keep it |
| 14 | THE SYSTEM SHALL default `MaxQueuedAge` to 30 seconds on both transports, and SHALL treat `null` as no limit. | `LogicBlockModbusTcpClient.cs:17`, `:50`; `ModbusRtu.cs:127`; `RequestQueue.cs:45-52` maps `null` to `-1` ticks | GAP | intended | `IModbusClient.cs:35` |
| 15 | WHEN a transaction ends with `Invalid` or `Cancelled` THE SYSTEM SHALL record it as the last failure but SHALL NOT count it, so `Link` carries no total for either. | `ModbusLinkAccumulator.cs:60-93` has no `case` for either and no counter field; `:95-99` still sets `LastFailureAt`/`LastFailureOutcome`; `ModbusLinkSummary.cs:64-79` declares eight counters and no `InvalidCount`/`CancelledCount` | `ModbusRtuShould.RecordAValidationFailureAsInvalidInTheLinkSummary` (asserts the outcome, not a count) | ⚠ fix | `IModbusClient.cs:27-28` says a bad unit id is "counted there" and `ModbusLinkSummary.cs:20-22` says local outcomes "are still counted" — both false for exactly the two the sentence exists to cover (see sketch) |
| 16 | THE SYSTEM SHALL take the success callback of a read as required and the success callback of a write as optional. | reads declare `Action<T[], ModbusReceipt> successCallback` with no default (`IModbusClient.cs:108`); writes declare `Action<ModbusReceipt>? successCallback = null` (`:156`) | `VoidResultRequestShould.NotInvokeDispatcherWhenOperationSucceedsAndSuccessCallbackIsNull` | intended | a read with nowhere to put its value is a no-op; a fire-and-forget write is the normal case |
| 17 | THE SYSTEM SHALL name the register-count parameter `quantity` on the raw, 16-bit and string families and `count` on the 32- and 64-bit families, with `quantity` counting registers and `count` counting values. | `IModbusClient.cs:106` vs `:280`; `ConvertCountToQuantity` converts one to the other (`ModbusDataConverter.cs:26`) | GAP | ⚠ propose | one interface, two vocabularies for the same argument position — the reader is every consumer call site (the first consumer's 84 files pass both `1u` and `1`); renaming is source-breaking for named arguments (see sketch) |
| 18 | THE SYSTEM SHALL log every failure whether or not an error callback is supplied. | `DeviceRequest.Complete` logs before the callback null-check (`:92-107`); `ModbusRtuRequestFactory.HandleRequestFailed` the same (`:257-281`) | `ArrayResultRequestShould.NotInvokeDispatcherWhenOperationFailsAndErrorCallbackIsNull` | intended | `IModbusClient.cs:97-98` promises it on all 36 methods |
| 19 | THE SYSTEM SHALL default `byteOrder` to `MsbToLsb`, 32-bit `wordOrder` to `MswToLsw`, 64-bit `wordOrder` to `ABCD`, and `textEncoding` to `Ascii` on every method that takes them. | `IModbusClient.cs:231`, `:285`, `:369`, `:451` and every sibling | `ModbusRtuShould.ReadInputRegistersAsInt` (asserts the defaults reach the request) | intended | the Modbus standard's byte order is big-endian (`ByteOrder.cs:9-10`); a device that differs is the exception |
| 20 | WHEN a block calls a read or write on a client whose queue was never created THE SYSTEM SHALL throw `InvalidOperationException` synchronously. | `RequestQueue.RequireAccumulator` (`:165-168`) and `EnqueueCore` (`:172-175`); the queue is created only on the first `IsEnabled = true` (`LogicBlockModbusTcpClient.cs:78-84`) | `RequestQueueShould.ThrowExceptionWhenEnqueuingArrayResultRequestBeforeInitialization` | intended | unreachable through the client, whose `IsEnabled` gate (row 7) returns first — reachable through the public `IRequestQueue` the TestKit substitutes |

### B. The Modbus TCP link policy

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 21 | THE SYSTEM SHALL establish the TCP connection lazily, inside the first operation that needs one, and SHALL keep it for later operations. | `ExecuteReadOperationAsync` awaits `EnsureClientIsConnectedAsync` (`ModbusTcpClientWrapper.cs:1106`); `:170-174` returns early when connected | `ModbusTcpClientWrapperShould.ConnectWhenNotConnected`, `.NotReconnectWhenConnectionSettingsUnchanged` | intended | `ILogicBlockModbusTcpClient.cs:20-24`; a block never calls `Connect` |
| 22 | THE SYSTEM SHALL default `Port` to 502 and `ConnectionTimeout` to 3 seconds. | `LogicBlockModbusTcpClient.cs:44-45` | `LogicBlockModbusTcpClientShould.ApplyDefaultPortOnConstruction` | intended | the standard Modbus TCP port; the timeout is the one knob a slow gateway needs |
| 23 | WHEN `IpAddress` is not set THE SYSTEM SHALL fail the operation with `IpAddressNotSetException` and an `Invalid` receipt, without recording a connect attempt. | `ModbusTcpClientWrapper.cs:180-183` is reached *after* `ThrowIfBackingOff` and `DisconnectAsync` but *before* `RecordConnectAttempt` (`:186`); `ModbusOutcomeClassifier.cs:39` maps it to `Invalid` | GAP | intended | an unconfigured client is not evidence about a device, so it must not arm a backoff |
| 24 | WHEN `Port` is set outside 0–65535 THE SYSTEM SHALL throw `FormatException`. | `LogicBlockModbusTcpClient.cs:153-156` (`IPEndPoint.MinPort`/`MaxPort`) | `LogicBlockModbusTcpClientShould.ThrowExceptionWhenPortIsInvalid` | intended | `FormatException` rather than `ArgumentOutOfRangeException` because the value arrives from a configuration binder, like `IpAddress` |
| 25 | THE SYSTEM SHALL accept port 0 on the client. | `IPEndPoint.MinPort` is 0, so `:153` admits it | GAP | ⚠ park | a connect to port 0 is a guaranteed failure that arms a backoff; the first consumer's own `ModbusConnection` declares `Minimum = 1` (`…/Shared/ModbusConnection.cs:46`), so the consumer already refuses what the SDK admits — the fix is one boundary but it is the same question as the server's port 0 (row 112), so it goes to the ledger as one entry |
| 26 | WHEN `IpAddress` is set to null, empty, whitespace or a string `IPAddress.TryParse` rejects THE SYSTEM SHALL throw `FormatException`. | `LogicBlockModbusTcpClient.cs:169-177` | `LogicBlockModbusTcpClientShould.ThrowExceptionWhenIpAddressIsInvalid` | intended | the property is a `string?` because a configuration binder writes it |
| 27 | THE SYSTEM SHALL refuse a host name as `IpAddress`, accepting only a literal IPv4 or IPv6 address. | `IPAddress.TryParse` (`:174`) does not resolve names | GAP | intended | a gateway resolving DNS per connect is a failure mode the page should name rather than hide |
| 28 | WHEN `Port` or `IpAddress` is set to the value already in force THE SYSTEM SHALL do nothing — no reconnect, no backoff reset. | `ModbusTcpClientWrapper.cs:98-101` (`Port`), `:117-120` (`IpAddress`) | `ModbusTcpClientWrapperShould.NotReconnectWhenThePortAlreadyInForceIsSetAgain`, `.NotReconnectWhenTheIpAddressAlreadyInForceIsSetAgain` | intended | a consumer that re-applies its whole configuration on every edit would otherwise drop the socket on every unrelated edit (`:96-97`); the `modbus-link-policy` scenario's "no-op" steps are the live proof |
| 29 | WHEN `Port` or `IpAddress` changes THE SYSTEM SHALL mark a reconnect required and clear any armed connect backoff, so the next operation attempts a connection at once. | `ModbusTcpClientWrapper.cs:103-107`, `:122-126`; `ClearConnectBackoff` at `:248-256` | `ModbusTcpClientWrapperShould.ReconnectWhenPortChanged`, `.ReconnectWhenIpAddressChanged`; scenario `modbus-link-policy` steps "fix — put the port back" | intended | a corrected address must apply on the very next operation, not after the backoff |
| 30 | WHEN a disabled client is re-enabled THE SYSTEM SHALL clear any armed connect backoff. | `LogicBlockModbusTcpClient.cs:71-76` calls `ResetConnectBackoff(nameof(IsEnabled))` only on a false→true edge | GAP | intended | re-enabling is how a block says its configuration is complete again, so it supersedes the failures that armed the backoff |
| 31 | THE SYSTEM SHALL arm no connect backoff before the second consecutive failed connect. | `ModbusTcpClientWrapper.cs:236-239` (`consecutiveConnectFailures < ConnectFailuresBeforeBackoff` returns) | `ModbusTcpClientWrapperShould` has no backoff test; the TestKit's `ModbusLinkPolicyShould` proves it out of area | intended | a single failure is a transient the next request pays for anyway (`:21`) |
| 32 | WHEN the second consecutive connect fails THE SYSTEM SHALL wait `ConnectBackoff`, doubling it on each further consecutive failure up to `ConnectBackoffMax`. | `ModbusTcpClientWrapper.cs:241` (`doublings = failures - 2`), `Exponential` at `:282-297` | GAP | intended | the sequence from the defaults is 1 s, 2 s, 4 s, 8 s, 16 s, 30 s, 30 s… |
| 33 | THE SYSTEM SHALL stop doubling the backoff before the multiplication that would exceed `ConnectBackoffMax`, so a long run of failures cannot overflow. | `ModbusTcpClientWrapper.cs:288` (`ticks >= max.Ticks / 2` returns `max`) | GAP | intended | the guard is why an outage of hours does not produce a negative `TimeSpan` |
| 34 | WHEN `ConnectBackoff` equals `ConnectBackoffMax` THE SYSTEM SHALL wait a constant interval. | `Exponential` returns `max` on the first iteration when `unit == max` (`:288`) | scenario `modbus-link-policy` pins the backoff to a constant 5 s for exactly this reason | intended | `ILogicBlockModbusTcpClient.cs:157-161` states it as the way to get a constant wait |
| 35 | THE SYSTEM SHALL offer no value of `ConnectBackoff` that turns the backoff off. | `LogicBlockModbusTcpClient.cs:198-201` refuses `<= 0` | GAP | intended | `ILogicBlockModbusTcpClient.cs:160` states it; a client that retries a dead endpoint per request is the failure mode the policy exists to prevent |
| 36 | WHEN `ConnectBackoff` is set above `ConnectBackoffMax`, or `ConnectBackoffMax` below `ConnectBackoff`, THE SYSTEM SHALL throw `ArgumentOutOfRangeException` naming the other property. | `LogicBlockModbusTcpClient.cs:203-209`, `:222-228` | GAP | intended | the pair has to stay ordered, and the message says which one to move first |
| 37 | WHEN an operation is issued while a connect backoff is still running THE SYSTEM SHALL fail it immediately with `LinkBackoffException` and a `BackedOff` receipt, without contacting the device. | `ThrowIfBackingOff` (`ModbusTcpClientWrapper.cs:176`, `:215-231`); `ModbusOutcomeClassifier.cs:19` | scenario `modbus-link-policy` steps "fault — requests now fail fast", "and they keep being refused fast" | intended | it is what drains a queue during an outage instead of filling it |
| 38 | WHEN the armed backoff instant has passed THE SYSTEM SHALL let the next operation attempt a connection. | `ModbusTcpClientWrapper.cs:225-228` returns without clearing `_backoffUntilTicks` | scenario `modbus-link-policy` "recovery — no operator action on the client: it reconnects itself" | intended | the queue has one consumer, so "the next operation" is exactly one attempt; a successful connect then clears the backoff (`:202`) and a failure re-arms a longer one |
| 39 | THE SYSTEM SHALL carry on `LinkBackoffException` the endpoint, the consecutive-failure count and the instant of the next attempt. | `LinkBackoffException.cs:38-53` | `RequestFailureMessagesShould.NameTheUnreachableEndpointAndWhenTheClientWillTryItAgain` | intended | the block's only in-callback handle on why a request failed instantly |
| 40 | THE SYSTEM SHALL render every duration in a Modbus failure message in the invariant culture. | `RequestExpiredException.cs:46-50` and `LinkBackoffException.cs:47-52` pass `CultureInfo.InvariantCulture`; `RequestDroppedException.cs:50`, `:59` likewise | `RequestFailureMessagesShould.SayHowLongAnExpiredRequestWaitedAndAgainstWhichLimit` | intended | `sdk-surface-conventions.md` § 4; a gateway's culture is not the reader's |
| 41 | WHEN a connect times out THE SYSTEM SHALL report the timeout in seconds using the current culture. | `ConnectionTimeoutException.cs` — `ModbusTcpClientProxy.cs:275`, `$"…within {seconds} seconds."` on a `double` with no `IFormatProvider` | GAP | ⚠ fix | the one message in the area that breaks row 40's rule; on a comma-decimal gateway a 2.5 s timeout reads *"2,5 seconds"*, and it is the exact shape that failed the ANLZ pass's CI on a Linux runner (see sketch) |
| 42 | WHEN an established connection produces a `Timeout`, `TransportError` or `ProtocolError` THE SYSTEM SHALL close the socket, so the next operation reconnects. | `CloseSocketOnWireFault` (`ModbusTcpClientWrapper.cs:262-279`), called from both execute helpers (`:1117`, `:1123`, `:1154`, `:1160`) | `ModbusTcpClientWrapperShould.CloseTheSocketWhenAnOperationFaultsOnTheWire` | intended | a peer that dropped and came back is reached again without operator action, and a stray response cannot be read as the next transaction's answer |
| 43 | WHEN an operation fails with any outcome other than `Timeout`, `TransportError` or `ProtocolError` THE SYSTEM SHALL keep the socket open. | `ModbusTcpClientWrapper.cs:265-268` | `ModbusTcpClientWrapperShould.KeepTheSocketWhenAnOperationFailsForAReasonThatIsNotTheWire` | intended | a `DeviceError` proves the link works; dropping the socket over a refused register would be self-harm |
| 44 | THE SYSTEM SHALL classify any exception it does not recognise as `TransportError`. | `ModbusOutcomeClassifier.cs:43` | GAP | ⚠ propose | an `ObjectDisposedException` from a disposed proxy, or an `ArgumentOutOfRangeException` from a negative `DefaultOperationTimeout` (row 47), moves `Link.State` to `Faulted` and closes the socket though nothing touched the wire — the readers are the first consumer's four `Link.State` call sites; narrowing the default is a wire-visible reclassification (see sketch) |
| 45 | THE SYSTEM SHALL never retry an operation automatically. | no retry anywhere in `ModbusTcpClientWrapper`, `RequestQueue` or `DeviceRequest`; the only re-attempt is the next request's own connect | GAP | intended | `ILogicBlockModbusTcpClient.cs:56-58` — reads are re-polled by construction and repeating a write after a fault would write a pulse twice |
| 46 | THE SYSTEM SHALL report the socket's own state as `Disconnected`, `Connected` or `BackingOff`, separately from the device's `Link.State`. | `ModbusTcpConnectionState.cs`; `ModbusTcpConnectionAccumulator.ConnectionState` (`:164-172`) | scenario `modbus-healthy` "the socket is up" and `modbus-link-policy` "the second consecutive failed connect arms the backoff" | intended | a socket can be up while every request times out, and a device can be reachable across a socket re-established several times (`ModbusTcpConnectionSummary.cs:12-14`) |
| 47 | THE SYSTEM SHALL accept `ConnectionTimeout` and `DefaultOperationTimeout` of zero or a negative duration. | neither setter validates — `LogicBlockModbusTcpClient.cs:184-189` and `:261` | GAP | ⚠ fix | the two unguarded knobs among five; `ConnectionTimeout = 0` makes every connect a `ConnectionTimeoutException` that arms a backoff, and `DefaultOperationTimeout = 0` makes every operation an `OperationTimeoutException` that faults the link and closes the socket — the reader is the first consumer's `ModbusConnection`, which offers both as free-form `TimeSpan` commissioning fields (`…/Shared/ModbusConnection.cs:48-51`) (see sketch) |
| 48 | THE SYSTEM SHALL accept an `int` unit identifier on the bit operations of `IModbusTcpClientProxy` and a `byte` on the register operations. | `IModbusTcpClientProxy` — `ModbusTcpClientProxy.cs:69`, `:87`, `:105` take `int`; `:138`, `:156`, `:174`, `:190` take `byte`; the wrapper casts at `ModbusTcpClientWrapper.cs:402` and every register call site | GAP | ⚠ propose | one seam, two parameter types for one protocol field, on the interface the TestKit substitutes (`FakeModbusTcpClientProxy`) — the reader is `TKIT`; unifying on `int` is source-breaking for any other implementer (see sketch) |

### C. The Modbus TCP request queue

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 49 | THE SYSTEM SHALL execute the operations of one client strictly one at a time, in the order they were issued. | `RequestQueue.ConsumeAsync` awaits each `ProcessRequestAsync` in a single `await foreach` (`:195-198`), `SingleReader = true` (`:77`) | `RequestQueueShould.ExecuteRequestsInFifoOrder` | intended | `ILogicBlockModbusTcpClient.cs:27-28` — the underlying library cannot run concurrent operations on one connection |
| 50 | THE SYSTEM SHALL create the request queue the first time the client is enabled, and once only. | `LogicBlockModbusTcpClient.cs:78-84`; `RequestQueue.Initialize` throws when already initialised (`:60-63`) | `LogicBlockModbusTcpClientShould.InitializeRequestQueueWhenClientIsEnabled`, `.NotReinitializeRequestQueueAfterInitialization` | intended | the queue's capacity and policy are `Channel` construction arguments and cannot change afterwards |
| 51 | THE SYSTEM SHALL default `QueueCapacity` to 256 and `QueueOverflowPolicy` to `DropOldest`. | `LogicBlockModbusTcpClient.cs:110`, `:118` | `LogicBlockModbusTcpClientShould.InitializeRequestQueueWithConfiguredCapacityAndOverflowPolicy` | intended | a poll loop's freshest request is the one worth keeping |
| 52 | WHEN `QueueCapacity` or `QueueOverflowPolicy` is set after the queue exists THE SYSTEM SHALL ignore the write silently, leaving the value in force. | `LogicBlockModbusTcpClient.cs:109`, `:117` — `field = _requestQueueInitialized ? field : value` | `LogicBlockModbusTcpClientShould.PreventQueueCapacityChangeAfterInitialization`, `.PreventQueueOverflowPolicyChangeAfterInitialization` | ⚠ fix | a silent no-op on a public setter: the getter then returns a value the caller did not write, and a commissioner raising capacity on a congested client sees the number they typed reflected back with nothing changed — throwing `InvalidOperationException` is the shape the server's own `EnsureDisabled` already uses (`LogicBlockModbusTcpServer.cs:213-219`) (see sketch) |
| 53 | WHEN the queue is full THE SYSTEM SHALL apply `DropOldest`, `DropNewest` or `RejectNew` and SHALL complete the evicted request through its error callback with `RequestDroppedException` and a `Dropped` receipt. | `RequestQueue.Initialize` maps the three onto `BoundedChannelFullMode` (`:67-73`) and passes the drop callback (`:83`); `ModbusOutcomeClassifier.cs:20` | `RequestQueueShould.DropOldestRequestWhenQueueFull`, `.DropNewestRequestWhenQueueFull`, `.RejectNewRequestWhenQueueFull` | intended | `ILogicBlockModbusTcpClient.cs:99-102` |
| 54 | WHEN `QueueOverflowPolicy` is `DropNewest` THE SYSTEM SHALL evict the newest request already waiting, not the one being enqueued. | `BoundedChannelFullMode.DropNewest` (`RequestQueue.cs:69`) | `RequestQueueShould.DropNewestRequestWhenQueueFull` | intended | `QueueOverflowPolicy.cs:17` says so explicitly, and it is the counter-intuitive half of the enum |
| 55 | THE SYSTEM SHALL check `MaxQueuedAge` immediately before dispatch, so a value set now applies to requests already waiting. | `RequestQueue.ProcessRequestAsync` reads the property per request (`:217`); `DeviceRequest.ExecuteAsync` compares (`:363-369`) | GAP | intended | `IModbusClient.cs:77` and `IRequestQueue.cs:24-25`; the check lives in the request so every `IRequestQueue` implementation applies it |
| 56 | WHEN a request has waited strictly longer than `MaxQueuedAge` THE SYSTEM SHALL complete it through its error callback with `RequestExpiredException` and an `Expired` receipt, without contacting the device. | `DeviceRequest.cs:47-52` (`queuedWait > limit`) | GAP — the `modbus-link-policy` scenario's judge 3 says expiry is deliberately not asserted there | intended | a queue built up during an outage drains in microseconds instead of replaying minutes of dead work (`IModbusClient.cs:32-35`); the boundary is exclusive, so a wait exactly equal to the limit still runs |
| 57 | WHEN the client is disposed THE SYSTEM SHALL complete no request still waiting in the queue — no callback, no receipt, no link record. | `RequestQueue.Dispose` calls `Writer.TryComplete()` and `_cts.Cancel()` (`:235-236`) and never drains; `Channel.TryComplete` does not run the itemDropped callback for items already written | GAP | ⚠ propose | `ILogicBlockModbusTcpClient.cs:38-41` promises "requests already queued or in flight are cancelled" — the in-flight one is, the queued ones vanish. This is RFC 0018 § 9's corrected claim seen from the block's side: a `Stopping()` safe-baseline write is cut off *and silent*. The readers are the three consumer device blocks RFC 0018 § 9 names; draining with `RequestDroppedException(ClientDisposed)` changes what a fielded error callback sees during shutdown (see sketch) |
| 58 | WHEN a request is enqueued after the client is disposed THE SYSTEM SHALL complete it with `RequestDroppedException` whose `Reason` is `ClientDisposed`, and a `Cancelled` receipt. | `RequestQueue.EnqueueCore` (`:182-187`); `ModbusOutcomeClassifier.cs:21` | `RequestQueueShould.RejectNewRequestWhenQueueDisposed`, `RequestFailureMessagesShould.SayTheClientWasDisposed` | intended | `ILogicBlockModbusTcpClient.cs:38-40` |
| 59 | THE SYSTEM SHALL log a request dropped because the client was disposed at `Error`. | `DeviceRequest.Complete`'s quiet arm covers `BackedOff`, `Expired` and `Dropped` only (`:94`); `ClientDisposed` classifies as `Cancelled` and falls to `LogRequestFailed` (`:100`, declared `Level = LogLevel.Error` at `Request.cs:50`) | GAP (`testing-conventions.md` § 15 forbids asserting on a log call) | ⚠ fix | the comment two lines above the quiet arm names the case it misses — *"Congestion and **disposal** are expected under load and on shutdown; at Error they turn a single fault into a flood"* (`Request.cs:53-54`). Every queued request of every Modbus block logs an Error line on shutdown (see sketch) |
| 60 | THE SYSTEM SHALL exclude the request currently executing from `QueuedRequestCount`. | `RequestQueue.cs:39-42` returns `Reader.Count`, which excludes the item already read | `RequestQueueShould.ExcludeInFlightRequestFromQueuedCount` | intended | `ILogicBlockModbusTcpClient.cs:110-111` states it; a depth of 0 with one in flight is not an idle client |
| 61 | THE SYSTEM SHALL exempt a control operation from `MaxQueuedAge`, from the link summary and from the receipt. | `ControlRequest.ExecuteAsync` ignores its `maxQueuedAge` argument (`:35-50`) and takes no accumulator (`:22-32`) | GAP | intended | a disconnect that has waited a long time is exactly the one still worth doing (`ControlRequest.cs:10-13`) |
| 62 | WHEN `Disconnect` is called on a disabled client THE SYSTEM SHALL do nothing and invoke neither callback. | `LogicBlockModbusTcpClient.cs:243-247` | `LogicBlockModbusTcpClientShould.NotEnqueueDisconnectWhenClientIsDisabled` | ⚠ fix | consistent with row 7, but `ILogicBlockModbusTcpClient.cs:178-209` documents `Disconnect` as idempotent and says nothing about the gate — a block that disables then disconnects gets neither callback and no explanation. Doc-only fix (see sketch) |
| 63 | WHEN `Disconnect` runs on a client that is not connected THE SYSTEM SHALL complete successfully without touching the socket. | `ModbusTcpClientWrapper.DisconnectAsync` (`:155-159`) | `ModbusTcpClientWrapperShould.SkipDisconnectWhenNotConnected` | intended | `ILogicBlockModbusTcpClient.cs:202` calls it idempotent |
| 64 | THE SYSTEM SHALL re-establish the connection on the next read or write after a `Disconnect`. | `DisconnectAsync` leaves `_reconnectRequired` alone but `_clientProxy.IsConnected` goes false, so `:170` no longer short-circuits | GAP | intended | `ILogicBlockModbusTcpClient.cs:199` — this is what makes `Disconnect` usable for a rare-operation block |
| 65 | WHEN `MaxQueuedAge` is set to a negative duration on `IRequestQueue` directly THE SYSTEM SHALL read it back as `null`, meaning no limit. | `RequestQueue.cs:54` stores `value?.Ticks ?? -1` and `:49-51` maps any negative ticks to `null` | GAP | ⚠ fix | the `-1`-means-null encoding silently swallows every negative value; the client's own setter refuses them (row 13) but `IRequestQueue` is public and the TestKit substitutes it, so a test or a second implementation can set `-1 s` and be told it set "no limit" (see sketch) |

### D. Register-word conversion

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 66 | THE SYSTEM SHALL swap register bytes only when the requested `ByteOrder` differs from the host's endianness. | `ModbusDataConverter.cs:55-59` | `ModbusDataConverterShould.NotSwapBytesWhenByteOrderMatchesSystemEndianness`, `.SwapBytesWhenByteOrderDoesNotMatchSystemEndianness` | intended | the swap is a host-memory↔wire transform, so it is a no-op on a big-endian host |
| 67 | WHEN an unsupported `ByteOrder` or `WordOrder32` is passed THE SYSTEM SHALL throw `UnsupportedByteOrderException` or `UnsupportedWordOrder32Exception` before touching the buffer. | `ModbusDataConverter.cs:50-53`, `:71-74` | `ModbusDataConverterShould.ThrowExceptionWhenByteOrderIsUnsupported`, `.ThrowExceptionWhen32BitWordOrderIsUnsupported` | intended | an out-of-range cast enum value is a caller error the receipt reports as `Invalid` (`ModbusOutcomeClassifier.cs:35-37`) |
| 68 | WHEN an unsupported `WordOrder64` is passed with a non-empty buffer THE SYSTEM SHALL throw `UnsupportedWordOrder64Exception`; with an empty buffer it SHALL return silently. | the `default:` arm is inside the `for` loop (`ModbusDataConverter.cs:93-155`), unlike the pre-loop guards at `:50` and `:71` | `ModbusDataConverterShould.ThrowExceptionWhen64BitWordOrderIsUnsupported` (passes 8 bytes) | ⚠ fix | one of three sibling validators placed differently from the other two; an empty response with a bad word order is accepted where a one-value response is refused — hoist the guard above the loop (see sketch) |
| 69 | THE SYSTEM SHALL apply `ABCD` and `DCBA` only when the host's endianness differs, and `CDAB` and `BADC` always, in the direction the host requires. | `ModbusDataConverter.cs:97-153`; documented at `IModbusDataConverter.cs:75-82` | `ModbusDataConverterShould.Swap64BitMidBigEndianWordsToLittleEndianWhenSystemIsLittleEndian` and its three siblings | intended | the mid-endian orders are a real device shape (`WordOrder64.cs:27-30`) and are the half a host-endianness rule gets wrong |
| 70 | WHEN a buffer whose length is not a whole number of values is swapped THE SYSTEM SHALL throw `IndexOutOfRangeException`. | `ModbusDataConverter.cs:62-65` (`i += 2`, reads `i+1`), `:83-86` (`i += 4`), `:93` (`i += 8`) | GAP | intended | unreachable through a client, which validates response alignment first (rows 71, 76); reachable through the public `IModbusDataConverter` |
| 71 | WHEN a device's response byte count is not a multiple of the requested value size THE SYSTEM SHALL fail the operation with `ModbusResponseAlignmentException` and a `ProtocolError` receipt. | `ModbusValidator.cs:22-25`, called from all three typed read helpers (`ModbusTcpClientWrapper.cs:1031`, `:1057`, `:1084`; `ModbusRtu.cs:971`, `:980`, `:990`); `ModbusOutcomeClassifier.cs:27` | `ModbusValidatorShould.ThrowExceptionWhenResponseAlignmentIsInvalid` | intended | `MemoryMarshal.Cast` truncates a ragged tail silently (`ModbusDataConverter.cs:242`), so the check is what turns a short frame into a diagnosis instead of a wrong value |
| 72 | WHEN a unit identifier below 0 or above 255 is passed THE SYSTEM SHALL fail the operation with `InvalidUnitIdentifierException` and an `Invalid` receipt, without contacting the device. | `ModbusValidator.cs:13-16`; called first in both execute helpers (`ModbusTcpClientWrapper.cs:1100`, `:1139`) and in both RTU ones (`ModbusRtu.cs:1098`, `:1132`); `ModbusOutcomeClassifier.cs:33` | `ModbusValidatorShould.ThrowExceptionWhenUnitIdentifierIsInvalid`, `ModbusRtuShould.ValidateUnitIdentifier` | intended | it is the row `IModbusClient.cs:27` calls "a bad unit id" — the one local outcome the docs single out |
| 73 | THE SYSTEM SHALL accept unit identifiers 0 and 255. | `ModbusValidator.cs:13` refuses only `> byte.MaxValue or < 0` | `ModbusValidatorShould.NotThrowExceptionWhenUnitIdentifierIsValid` | intended | 0 addresses a gateway and 255 a direct TCP device — both are fielded (the first consumer's `ModbusConnection.cs:39-44` documents exactly this) |
| 74 | WHEN a count of zero is converted to a register quantity THE SYSTEM SHALL throw `InvalidCountException`. | `ModbusDataConverter.cs:28-31` | `ModbusDataConverterShould.ThrowExceptionWhenCountIsZero` | intended | reading zero values is a caller mistake, not an empty result |
| 75 | WHEN a count would require more than 65535 registers THE SYSTEM SHALL throw `InvalidCountException` naming the maximum count for that value width. | `ModbusDataConverter.cs:36-42` | `ModbusDataConverterShould.ThrowExceptionWhenConvertedQuantityExceedsMaximumValue` | intended | the message is the author's only handle on which of `count` and `quantity` overflowed |
| 76 | WHEN fewer bits are returned than the quantity requested THE SYSTEM SHALL fail the operation with `InvalidBitQuantityException` and a `ProtocolError` receipt. | `ModbusDataConverter.cs:196-200`; `ModbusOutcomeClassifier.cs:28` | `ModbusDataConverterShould.ThrowExceptionWhenQuantityExceedsAvailableBits` | intended | the device answering short is a protocol fault, not a caller error |
| 77 | THE SYSTEM SHALL ignore the padding bits of the last byte of a bit response, unpacking from the least significant bit of each byte. | `ModbusDataConverter.cs:204-226` | `ModbusDataConverterShould.ConvertBitsToBoolsTruncatingWhenQuantityIsLess`, `.ConvertBitsToBoolsWhenQuantityMatchesBits` | intended | the Modbus packing rule, and the one place an off-by-one silently inverts a coil |
| 78 | THE SYSTEM SHALL send a read or write of up to 65535 registers and let the device answer, enforcing no protocol quantity limit of its own. | no `125`, `123` or `2000` literal anywhere in the three projects; the only cap is `ushort.MaxValue` (`ModbusDataConverter.cs:36`) | the example refuses it one layer up — `ModbusTcpDebugClientReadShould.RejectAQuantityAboveTheModbusRegisterLimitWithoutTouchingTheWire` (`:302-311`) | ⚠ propose | the protocol allows 125 registers per FC3/FC4 and 123 per FC16; above that the device answers `IllegalDataValue` or drops the frame, and the block sees a `DeviceError` or a `Timeout` with no hint. **Readers searched:** the largest quantity in the first consumer's 84 files is 40 (see Reviewer's question 4) — refusing at 125 breaks nothing fielded (see sketch) |
| 79 | WHEN a string is encoded to an odd number of bytes THE SYSTEM SHALL append one zero byte so the payload aligns to a register boundary. | `ModbusDataConverter.cs:185-188` | `ModbusDataConverterShould.ConvertAsciiStringToBytesWithNullByteWhenLengthIsOdd`, `.ConvertUtf8StringToBytesWithNullByteWhenLengthIsOdd` | intended | documented at `IModbusDataConverter.cs:105-108`; the device receives a NUL the caller did not write |
| 80 | THE SYSTEM SHALL decode and encode text as ASCII, UTF-8, UTF-16 little-endian or UTF-16 big-endian, and SHALL throw `UnsupportedTextEncodingException` for any other value. | `ModbusDataConverter.cs:163-171`, `:176-183` | `ModbusDataConverterShould.ConvertBytesToAsciiString` and its three siblings, `.ThrowExceptionWhenTextEncodingIsUnsupported` | intended | four encodings cover every fielded register-map string; the refusal is what keeps a cast enum value from silently reading ASCII |
| 81 | THE SYSTEM SHALL read and write string bytes in their natural sequential order, applying no byte or word swap. | `ModbusTcpClientWrapper.cs:556-572` and `:944-957` call the converter without a swap; `ModbusRegisterAccessor.cs:164-165` states the reason in a comment | `ModbusRegisterAccessorShould.WriteStringsInNaturalWireOrder`, `.ReadStringsInNaturalWireOrder` | intended | RFC 0007's post-implementation review found the dropped swap inverted semantics on little-endian hosts; non-standard layouts go through `Raw` |
| 82 | THE SYSTEM SHALL encode an empty string as an empty byte array. | `Encoding.*.GetBytes("")` returns an empty array and `:185` leaves it | `ModbusDataConverterShould.ConvertEmptyStringToBytes` | intended | the client then writes zero registers, which the device answers as it chooses — the server's own accessor refuses it (row 92) |
| 83 | THE SYSTEM SHALL cast a byte buffer to a value array by reinterpreting its bytes, dropping any tail shorter than one value. | `ModbusDataConverter.cs:239-243` (`MemoryMarshal.Cast`) | `ModbusDataConverterShould.CastBytesToInt`, `.CastEmptyBytesToInt` | intended | the truncation is invisible, which is why row 71's alignment check runs first at every call site |
| 84 | THE SYSTEM SHALL convert a `bool` to the byte 1 or 0. | `ModbusDataConverter.cs:258-261` | GAP | intended | the single-coil write payload |
| 85 | THE SYSTEM SHALL divide by zero when a value width below two bytes is passed to the count conversion or the alignment check. | `ModbusDataConverter.cs:38` (`ushort.MaxValue / registersPerValue`, zero when `bytesPerCount < 2`); `ModbusValidator.cs:22` (`byteCount % bytesPerValue`) | GAP | ⚠ park | unreachable from any SDK call site — the only arguments are the constants 2, 4 and 8 — but `IModbusDataConverter` and `IModbusValidator` are public types a consumer can call, and the failure is a bare `DivideByZeroException` with no diagnosis. It is one guard in each, but it belongs with the surface question of rows 154–157 rather than on its own |

### E. The hosted Modbus TCP server

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 86 | THE SYSTEM SHALL serve all four register areas from one hosted server, bounded by four declared extents. | `LogicBlockModbusTcpServer.cs:46` passes `ModbusServerAreaExtents`; `CreateSnapshot` builds one accessor per area (`:202-211`) | `LogicBlockModbusTcpServerShould.ExposeAllFourAreasInTheSnapshot` | intended | RFC 0007 widened v1 to all four |
| 87 | THE SYSTEM SHALL treat an extent as a count of addresses starting at 0, so a map at an offset declares `offset + size`. | `ModbusServerAreaExtents.cs:10-13` (remarks) and `Covers` at `:43` | `ModbusServerAreaExtentsShould.CoverOffsetBasedMapsUpTo0X8000` | intended | the ST porting pipeline's `0x8000`-based maps port address-for-address (RFC 0007) |
| 88 | THE SYSTEM SHALL treat an extent of 0 as an area that is not served. | `Covers` compares against the count (`:43`); the default of each property is 0 | `ModbusServerAreaExtentsShould.RejectRangesOnUnservedAreas` | intended | `ILogicBlockModbusTcpServer.cs:100-101` states the default |
| 89 | THE SYSTEM SHALL decide coverage without overflowing, so address 65535 with a large quantity is refused rather than wrapping. | `ModbusServerAreaExtents.cs:43` — `startingAddress + (long)quantity <= extent`, the cast widening before the add | `ModbusServerAreaExtentsShould.DecideWhetherARangeIsCovered` | intended | the cast is the whole mechanism; without it the sum wraps and a request past the end reads as covered |
| 90 | THE SYSTEM SHALL treat a range of quantity 0 as not covered. | `ModbusServerAreaExtents.cs:43` (`quantity >= 1`) | `ModbusServerAreaExtentsShould.RejectZeroQuantity` | intended | a zero-length range has no addresses to be inside anything |
| 91 | WHEN a client requests a range outside the declared extent THE SYSTEM SHALL answer with an `IllegalDataAddress` Modbus exception. | `ModbusTcpServerProxy.ValidateRequest` (`:175-201`) wired as `RequestValidator` at `:91` | `ModbusTcpServerIntegrationShould.RejectOutOfExtentAccessWithIllegalDataAddress` (real socket) | intended | RFC 0007 Q3 resolved it into v1, auto-derived from the extents |
| 92 | WHEN a block accesses a snapshot outside the declared extent of an area THE SYSTEM SHALL throw `InvalidServerAddressException` naming the area, the address, the quantity and the extent. | `ModbusRegisterAccessor.ValidateRange` (`:214-217`), `ModbusBitAccessor.ValidateAddress` (`:54-57`), `InvalidServerAddressException.cs:20-22` | `ModbusRegisterAccessorShould.RejectAccessOutsideTheExtent`, `ModbusBitAccessorShould.RejectAccessOutsideTheExtent`, `LogicBlockModbusTcpServerShould.EnforceTheConfiguredExtentsInTheSnapshot` | intended | the block-side analog of the wire-side `IllegalDataAddress`, so both halves of the extent agree |
| 93 | WHEN a block writes an empty byte array or an empty string through a snapshot register accessor THE SYSTEM SHALL throw `InvalidServerAddressException`. | `ModbusRegisterAccessor.WriteRaw` passes `registerBytes.Length / 2` to `ValidateRange` (`:61`), whose `quantity == 0` arm throws (`:216`); `WriteAsString` routes through `WriteRaw` (`:172`) | GAP | ⚠ fix | the exception names an address problem for a payload problem — `IModbusRegisterAccessor.cs:263-265` documents `InvalidServerAddressException` as meaning "outside the declared extent", which an empty write is not. `ArgumentException`, as `WriteRaw`'s own odd-length arm already throws (`:56-59`), is the honest one (see sketch) |
| 94 | WHEN a block writes an odd number of raw register bytes THE SYSTEM SHALL throw `ArgumentException`. | `ModbusRegisterAccessor.cs:56-59` | `ModbusRegisterAccessorShould.RejectOddRawByteCounts` | intended | one register is two bytes, and half a register cannot go on the wire |
| 95 | THE SYSTEM SHALL execute a `Sync` callback synchronously on the caller's thread while holding the server lock, and SHALL make every access inside one callback atomic with respect to client requests. | `LogicBlockModbusTcpServer.cs:163-177`, `:180-194`; the lock is the proxy's (`ModbusTcpServerProxy.cs:82-85`, FluentModbus's own) | `LogicBlockModbusTcpServerShould.ExecuteSyncUnderTheProxyLock`, `.ReturnTheSyncCallbackResult` | intended | it is what makes read-modify-publish (echoing a client-written heartbeat) need no further coordination |
| 96 | THE SYSTEM SHALL offer `Sync` in an action form and a function form, the latter returning the callback's result. | `LogicBlockModbusTcpServer.cs:163`, `:180` | `LogicBlockModbusTcpServerShould.ReturnTheSyncCallbackResult` | intended | a block that only reads should not need a captured local |
| 97 | THE SYSTEM SHALL allow `Sync` while the server is disabled, and SHALL retain buffer contents across disable/enable cycles. | neither `Sync` overload checks `IsEnabled`; the buffers live on the FluentModbus server, not the listener (`IModbusTcpServerProxy.cs:12-15`) | `LogicBlockModbusTcpServerShould.AllowSyncWhileDisabled`, `ModbusTcpServerIntegrationShould.SurviveDisableEnableCycles` | intended | a block seeds default values before it starts listening |
| 98 | WHEN `IsEnabled` or `Dispose` is called from inside a `Sync` callback THE SYSTEM SHALL throw `InvalidOperationException` rather than deadlock. | `EnsureNotInSyncCallback` (`LogicBlockModbusTcpServer.cs:222-231`), from `IsEnabled` (`:38`) and `Dispose` (`:198`) | `LogicBlockModbusTcpServerShould.RejectDisablingFromInsideASyncCallback`, `.RejectEnablingFromInsideASyncCallback`, `.RejectDisposingFromInsideASyncCallback`, `.AllowDisablingAfterTheSyncCallbackReturns` | intended | stopping the listener joins request-handler tasks that may be waiting for the lock the callback holds — reproduced live during RFC 0007's review |
| 99 | WHEN a `Sync` callback nests a second `Sync` THE SYSTEM SHALL stop guarding the outer callback once the inner one returns, so a later `IsEnabled` or `Dispose` deadlocks instead of throwing. | `_inSyncCallback` is a `bool`, not a depth counter; the inner callback's `finally` sets it to `false` (`LogicBlockModbusTcpServer.cs:174`, `:191`) while the outer one still holds the re-entrant lock | GAP | ⚠ fix | the guard row 98 exists for is silently off for the rest of the outer callback, and the failure it prevents is a permanent actor-thread deadlock, not an exception — a depth counter, or saving and restoring the previous value, is the fix (see sketch) |
| 100 | THE SYSTEM SHALL accept requests for any unit identifier and echo the request's identifier in the response. | `ModbusTcpServerProxy` registers exactly unit 0 (`:52-56`) for FluentModbus's single-zero-unit filter, and aliases the buffer maps of ids 1–255 onto unit 0's arrays (`AliasAllUnitIdentifiersToUnitZero`, `:203-224`, called from the constructor at `:58`) | `ModbusTcpServerIntegrationShould.ServeAnyUnitIdentifier` (real socket) | intended | RFC 0007 Q1 — the spec's endpoint behaviour for directly connected servers, which every fielded master in the porting pipeline assumes |
| 101 | WHEN the underlying server library's per-unit buffer maps are not the shape the aliasing expects THE SYSTEM SHALL refuse to construct the server, naming the field it could not find. | `ModbusTcpServerProxy.cs:208-217` throws `NotSupportedException` | GAP | intended | a warn-and-degrade fallback to unit-0-only would look green in DevHost and TestKit paths while every fielded master breaks (`:210-213`) |
| 102 | WHEN a client sends a single-value write reported with quantity 0 THE SYSTEM SHALL validate it as a range of one address. | `ModbusTcpServerProxy.cs:198` (`Math.Max(quantity, (ushort)1)`) | `ModbusTcpServerProxyShould.TreatSingleWriteQuantityZeroAsOne` | intended | otherwise FC5/FC6 would be refused by row 90's zero-quantity rule |
| 103 | THE SYSTEM SHALL leave a function code it does not map to an area for the underlying server to answer. | `ModbusTcpServerProxy.cs:188-195` returns `OK` for an unmapped code | `ModbusTcpServerProxyShould.LeaveUnsupportedFunctionCodesToFluentModbus` | intended | `IllegalFunction` is the library's answer to give, and duplicating the mapping would be a second place to keep right |
| 104 | THE SYSTEM SHALL validate both the read range and the write range of a read-write-multiple-registers request against the holding-register extent. | `ModbusTcpServerProxy.cs:187` maps FC23 to holding registers; the library invokes the validator once per range | `ModbusTcpServerIntegrationShould.ValidateBothRangesOfReadWriteMultipleRegisters` (real socket) | intended | FC23 carries two independent ranges, and validating one is validating none |
| 105 | THE SYSTEM SHALL bind the listener with the address-reuse option, so a same-version redeploy rebinds a port whose previous socket is still lingering. | `ReuseAddressTcpClientProvider` (`:374-383`, `ExclusiveAddressUse = false` before `Start()`), injected at `ModbusTcpServerProxy.cs:109` with `leaveOpen: false` | `ReuseAddressTcpClientProviderShould.BindTheEndpoint_AndAcceptAConnection` (real socket) | intended | RFC 0018 Part B; the built-in provider sets no socket options, which is why the default bind hits `EADDRINUSE` |
| 106 | WHEN the listener cannot bind THE SYSTEM SHALL propagate the failure to the caller of `IsEnabled` and leave the server disabled. | `LogicBlockModbusTcpServer.cs:46` throws before `field = value` at `:55` | `LogicBlockModbusTcpServerShould.PropagateBindFailures` | intended | `ILogicBlockModbusTcpServer.cs:57-59` — the logic block decides whether a taken port is fatal |
| 107 | THE SYSTEM SHALL stop the listener repeatedly, up to three times with a 10 ms pause, while clients remain connected. | `ModbusTcpServerProxy.Stop` (`:115-130`) | GAP | intended | the library disposes request handlers without the server lock, so a handler accepted in the stop window survives the first `Stop`; the cost is up to 30 ms on the caller's thread, which is the block's actor thread |
| 108 | THE SYSTEM SHALL timestamp the most recent client write to any area, including a write that does not change the stored value. | `ModbusTcpServerProxy.cs:98-103` (`AlwaysRaiseChangedEvent`), `:239-247` (volatile UTC ticks), `:71-79` | `ModbusTcpServerIntegrationShould.TrackLastClientWriteAt` (real socket) | intended | a master cyclically re-writing an unchanged setpoint must still count as alive for communication surveillance |
| 109 | THE SYSTEM SHALL refuse a change to `ListenAddress`, `Port` or any extent while the server is enabled. | `EnsureDisabled` (`LogicBlockModbusTcpServer.cs:213-219`) from all six setters | `LogicBlockModbusTcpServerShould.RejectConfigurationChangesWhileEnabled`, `.AllowReconfigurationAfterDisabling` | intended | a port or address change is a rebind; the message says disable, reconfigure, re-enable |
| 110 | THE SYSTEM SHALL default `ListenAddress` to `0.0.0.0` and `Port` to 502. | `LogicBlockModbusTcpServer.cs:76`, `:94`, `:22` | `LogicBlockModbusTcpServerShould.DefaultToAnyAddressAndPort502` | intended | a logic-block-hosted server exists to be reached from the LAN (RFC 0007); a simulator that must not be sets loopback explicitly |
| 111 | WHEN `ListenAddress` is set to null, empty, whitespace or an unparseable address THE SYSTEM SHALL throw `FormatException`; when `Port` is outside 0–65535 it SHALL throw `FormatException`. | `LogicBlockModbusTcpServer.cs:67-70`, `:86-89` | `LogicBlockModbusTcpServerShould.RejectInvalidListenAddress`, `.RejectInvalidPort` | intended | same binder-facing choice as the client's (row 24) |
| 112 | THE SYSTEM SHALL accept port 0 on the server, binding an ephemeral port the block cannot discover. | `LogicBlockModbusTcpServer.cs:86` refuses only `< 0 or > MaxPort` | GAP | ⚠ park | same boundary as row 25 and the same one-line fix, and the server has the sharper consequence: `IsListening` reads `true` on a port nothing was told about. One ledger entry covers both |
| 113 | WHEN `Dispose` is called THE SYSTEM SHALL stop the listener and release the server, leaving `IsEnabled` reading `true`. | `LogicBlockModbusTcpServer.Dispose` (`:196-200`) calls `_proxy.Dispose()` and never touches `field`; the proxy's own `_disposed` guard is at `:158-163` | `LogicBlockModbusTcpServerShould.DisposeTheProxy` | ⚠ propose | a disposed server reports itself enabled, and setting `IsEnabled = false` afterwards calls `Stop()` on a disposed server, which the proxy swallows as a teardown race (`:226-237`). Readers: the 23 consumer files using `ILogicBlockModbusTcpServer`; making `Dispose` clear the flag changes what a block's own teardown observes (see sketch) |
| 114 | THE SYSTEM SHALL swallow and log a teardown race raised by the underlying server on `Stop` or `Dispose`, never throwing it. | `TryStopServer` (`ModbusTcpServerProxy.cs:226-237`), `Dispose` (`:165-172`) | GAP | intended | `ILogicBlockModbusTcpServer.cs:44-46` promises it; a block's teardown must not fail on a benign race |
| 115 | THE SYSTEM SHALL surface no event or callback to the logic block from a background thread; the block chooses its own cadence. | the only handlers are the proxy's two private ones (`ModbusTcpServerProxy.cs:239-247`), which write a timestamp and nothing else | `LogicBlockModbusTcpServerShould.PassDiagnosticsThrough` | intended | `ILogicBlockModbusTcpServer.cs:27-30`; it is the whole reason the surface is `Sync` and three properties rather than events |
| 116 | THE SYSTEM SHALL report `LastClientWriteAt` as a `DateTimeOffset?` while every client-side diagnostic instant is a `DateTime?` in UTC. | `ILogicBlockModbusTcpServer.cs:167` vs `ModbusLinkSummary.cs:59` and `ModbusTcpConnectionSummary.cs:48` | GAP | ⚠ park | one surface, two instant types, on properties a block publishes side by side; changing either is source-breaking for the 23 consumer files and for the DevHost's struct renderer, so it is the ledger's, not this pass's |

### F. Modbus RTU — the contract and its shared handler

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 117 | THE SYSTEM SHALL expose Modbus RTU as `IModbusRtu`, a service-provider contract type carrying the token `ModbusRtu`, adding no member to `IModbusClient`. | `IModbusRtu.cs:47-51` | `ModbusRtuShould.SendReadRequestToContractHandler` | intended | the block declares the binding as `IModbusRtu` and uses it through `IModbusClient` wherever the code should work for either transport; the binding itself is `AC-BIND-*`'s |
| 118 | THE SYSTEM SHALL route every Modbus RTU binding in a runtime through one shared handler actor. | `ModbusRtu.ContractHandlerActorName` is `nameof(ModbusRtuHandler)` (`:37`), a constant, so every instance names the same actor | `ModbusRtuHandlerShould.PublishRequestToMqttClient` | intended | it is why the pending limit, the sweep and the ordering are shared — an outcome of `Dropped` here may have been caused by another block (`IModbusRtu.cs:20-21`) |
| 119 | THE SYSTEM SHALL report `Link.QueueDepth` as 0 on Modbus RTU. | `ModbusRtu.cs:136` passes the literal 0 to `Snapshot` | GAP | intended | there is no depth this client can honestly claim as its own (`:131-132`); `ModbusLinkSummary.cs:90` says so on the field |
| 120 | THE SYSTEM SHALL start the expiry sweep when the handler's contract actors are first linked, and SHALL reschedule it about once a second thereafter. | `StartCheckExpiredRequestsLoop` from `OnContractActorsLinked` (`ModbusRtuHandler.cs:97`, `:590-598`); `_checkExpiredRequestsDelay` 1 s (`:29`); `CompleteExpiredRequests` reschedules first (`:601-603`) | `ModbusRtuHandlerShould.ScheduleExpirationCheckWhenLinked`, `.RescheduleExpirationCheckAfterRun`, `.NotRescheduleExpirationCheckOnSubsequentLink` | intended | a timed-out operation can therefore complete up to a second after its timeout (`IModbusRtu.cs:21-22`) |
| 121 | WHEN a published request's operation timeout has elapsed THE SYSTEM SHALL complete it with `OperationTimeoutException` and a `Timeout` receipt. | `ModbusRtuHandler.cs:611-633` (`utcNow < ExpiresAt` continues) | `ModbusRtuHandlerShould.CompleteExpiredReadRequestWithTimeout`, `.NotCompleteNonExpiredReadRequest` and the write pair | intended | the timeout is inclusive at the boundary — an instant exactly equal to `ExpiresAt` expires |
| 122 | WHEN the runtime-wide pending-request limit of 1000 is reached THE SYSTEM SHALL drop the arriving request with `PendingRequestsLimitReachedException` and a `Dropped` receipt, without publishing it. | `MaxPendingRequests` (`:25`); `CheckPendingRequestLimit` (`:493-505`, `>= MaxPendingRequests`); `PublishModbusGetRequest` (`:195-200`) | `ModbusRtuHandlerShould.SendLimitReachedReadResponseWhenPendingRequestLimitReached` and its write sibling | intended | the message names the shared handler so a block author does not hunt for a limit in their own code (`:670-672`) |
| 123 | WHEN a request arrives for a contract with no service-provider mapping THE SYSTEM SHALL complete it with `ServiceProviderContractMappingNotFoundException` and an `Invalid` receipt. | `ModbusRtuHandler.cs:202-206`, `:373-377` | `ModbusRtuHandlerShould.DropRequestWhenContractNotMapped` | intended | an unmapped binding is a configuration fault, not a device fault, so it must not move `Link.State` |
| 124 | WHEN the hop from the block to the handler took longer than `MaxQueuedAge` THE SYSTEM SHALL complete the request with `RequestExpiredException` and an `Expired` receipt, without publishing it. | `ModbusRtuHandler.cs:208-216`, `:379-385` | `ModbusRtuHandlerShould.SendExpiredReadResponseWhenRequestIsOlderThanMaxQueuedAge` and its write sibling | intended | publishing now would put a read nobody wants any more onto a bus shared with every other binding (`:211-212`) |
| 125 | THE SYSTEM SHALL start the operation timeout when the handler publishes the request, not when the block issued the call. | `ModbusRtuHandler.cs:224` (`expiresAt = publishedAt + request.OperationTimeout`) | GAP | intended | it is what makes `MaxQueuedAge` and `DefaultOperationTimeout` measure two different things on RTU (`IModbusRtu.cs:27-29`) |
| 126 | THE SYSTEM SHALL split an RTU receipt at the publish — the hop to the handler as `QueuedWait`, the wire exchange as `RoundTrip` — and SHALL report a `RoundTrip` of zero for a request that never reached the bus. | `ModbusRtuReceiptBuilder.Build` (`ActorMessages.cs:133-138`), clamping negatives to zero at `:141-144` | `ModbusRtuRequestFactoryShould.RecordEveryCompletedTransactionInTheLinkAccumulator` | intended | the split the type's own remarks state (`ActorMessages.cs:125-130`) |
| 127 | WHEN a response arrives for a correlation id with no pending request THE SYSTEM SHALL ignore it. | `ModbusRtuHandler.cs:155-159`, `:326-330` | `ModbusRtuHandlerShould.IgnoreReadResponseWhenNoPendingRequest`, `.IgnoreWriteResponseWhenNoPendingRequest` | intended | the expiry sweep removes the entry first, so a late answer to a timed-out request has nowhere to go |
| 128 | WHEN a response payload cannot be read THE SYSTEM SHALL complete the request with a `ProtocolError` receipt rather than a device error. | `ModbusRtuHandler.cs:180-184`, `:351-356` | `ModbusRtuHandlerShould.InvokeReadCallbackWithExceptionWhenResponsePayloadIsInvalid` and its write sibling | intended | the device answered; our reading of the answer failed |
| 129 | WHEN turning a successful device answer into the requested type fails THE SYSTEM SHALL correct the outcome to `Invalid` for a caller error and to `ProtocolError` otherwise, before it reaches the link summary. | `ModbusRtuRequestFactory.CompleteRead` (`:222-239`); `ModbusRtuOutcomeClassifier.ClassifyResponseFailure` (`:14-22`) | `ModbusRtuRequestFactoryShould.ReclassifySuccessAsProtocolErrorWhenArrayProcessResponseThrows`, `.ReclassifySuccessAsInvalidWhenArrayProcessResponseRejectsTheRequestedConversion` | intended | otherwise a bad `WordOrder64` would read as a healthy link and a successful transaction |
| 130 | WHEN the block's callback throws THE SYSTEM SHALL let the exception escape into the contract actor on Modbus RTU, while catching and logging it on Modbus TCP. | RTU calls `dispatcher.InvokeSynchronized` bare (`ModbusRtuRequestFactory.cs:161`, `:183`, `:243`, `:280`; `ModbusRtu.FailRequest` `:1178`) and `ModbusRtu.HandleContractMessage` invokes the callback bare (`:71`, `:75`); TCP wraps every one in `Request.TryInvokeCallback` (`Request.cs:35-45`) | `ArrayResultRequestShould.NotThrowExceptionWhenDispatcherInvocationFails` (TCP only) | ⚠ fix | one `IModbusClient` surface, two answers to the same block mistake — a throwing callback is contained on TCP and reaches the actor's message loop on RTU. The consumer's 210 `receipt.Outcome` call sites are all inside such callbacks (see sketch) |
| 131 | WHEN a request is rejected before it leaves the block THE SYSTEM SHALL complete it through the error callback with an `Invalid` receipt, so a caller never handles the same error both inline and later. | `ModbusRtu.FailRequest` (`:1167-1181`) | `ModbusRtuShould.InvokeErrorCallbackWhenValidatorThrows`, `.RecordAValidationFailureAsInvalidInTheLinkSummary` | intended | the promise stated at `:1143-1145`; TCP keeps it too, through the request's own catch |
| 132 | WHEN a logic block contract is linked a second time to a different service provider THE SYSTEM SHALL keep the first mapping and warn. | `ModbusRtuHandler.cs:86-93` | `ModbusRtuHandlerShould.NotOverwriteExistingServiceProviderMappingOnRelink`, `.StoreNewServiceProviderMappingForUnlinkedLogicBlock` | intended | a re-link that silently repointed a binding would move a device's traffic to another bus |
| 133 | WHEN an MQTT message arrives with an empty correlation id or an unrecognised topic THE SYSTEM SHALL ignore it and log. | `ModbusRtuHandler.cs:103-107`, `:118-120` | `ModbusRtuHandlerShould.IgnoreMqttMessageWhenCorrelationIdIsEmpty`, `.IgnoreUnknownMqttTopic` | intended | the handler shares its topic space with every other binding |
| 134 | THE SYSTEM SHALL hand every timed-out request and every limit-rejected request the same exception instance. | `ModbusRtuHandler.cs:41` and `:51` construct one `PendingRequestsLimitReachedException` and one `OperationTimeoutException` per handler and reuse them | GAP | out-of-spec | an allocation choice; the two types carry no per-request state and are never thrown, so nothing a block can observe differs — worth naming in the page's prose, not as a criterion |

### G. The link diagnostics the SDK owns (decision `0118`)

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 135 | THE SYSTEM SHALL accumulate every completed transaction of one client into a `ModbusLinkSummary` readable at any time without blocking the transaction updating it. | `ModbusLinkAccumulator.Record` and `.Snapshot` under one lock (`:56-139`); `LogicBlockModbusTcpClient.Link` (`:91`), `ModbusRtu.Link` (`:136`) | `ModbusRtuRequestFactoryShould.RecordEveryCompletedTransactionInTheLinkAccumulator` | intended | decision `0118` — the SDK owns protocol link diagnostics so no consumer hand-keeps counters |
| 136 | THE SYSTEM SHALL set `Link.State` to `Online` on `Success` and `DeviceError`, to `Faulted` on `Timeout`, `TransportError` and `ProtocolError`, and SHALL leave it unchanged on every locally decided outcome. | `ModbusLinkAccumulator.cs:62-93`; `ReachedTheWire` at `:141-144` | scenario `modbus-healthy` "the device answered", `modbus-link-policy` "the link faults on the first failed request" and "a local backlog is not a device fault" | intended | it is what lets a block distinguish a congested queue from a broken device — the first consumer reads `Link.State` at four sites and nothing else |
| 137 | THE SYSTEM SHALL leave `Link.State` unchanged by the passage of time, so a client polled once an hour that answered an hour ago still reads `Online`. | nothing in `ModbusLinkAccumulator` reads a clock | GAP | intended | only the caller knows its own poll cadence; a freshness rule is built from `LastContactAt` or the receipt's monotonic stamp (`ModbusLinkSummary.cs:25-27`) |
| 138 | THE SYSTEM SHALL record `LastFailureAt` and `LastFailureOutcome` for every non-successful outcome, local ones included. | `ModbusLinkAccumulator.cs:95-99` | GAP | intended | it is the half of row 15's promise the code does keep |
| 139 | THE SYSTEM SHALL update the round-trip extremes only from transactions that reached the wire. | `ModbusLinkAccumulator.cs:103-108`, guarded by `ReachedTheWire` | GAP | intended | a locally decided transaction carries `TimeSpan.Zero` and would drag `MinRoundTrip` to zero for the rest of the client's life (`:101-102`) |
| 140 | THE SYSTEM SHALL update `LastQueuedWait` and `MaxQueuedWait` from every outcome, including one that never reached the wire. | `ModbusLinkAccumulator.cs:110-111`, outside the `ReachedTheWire` guard | GAP | ⚠ fix | the sibling of row 139, and the asymmetry is unstated: an `Invalid` transaction (never queued, `QueuedWait` zero) resets `LastQueuedWait` to zero, so the field a block reads to see congestion is cleared by a bad unit id. `Expired` and `Dropped` genuinely waited and must keep contributing, so the guard is not `ReachedTheWire` but "was ever queued" (see sketch) |
| 141 | THE SYSTEM SHALL count transactions for the lifetime of the client instance and never reset them. | no reset path in `ModbusLinkAccumulator` | GAP | intended | `ModbusLinkSummary.cs:30`; a counter a consumer cannot reset is a counter two readers can compare |
| 142 | THE SYSTEM SHALL report the socket's connect attempts, failures, consecutive-failure run, last handshake duration, current backoff and next attempt in a `ModbusTcpConnectionSummary`. | `ModbusTcpConnectionAccumulator.Snapshot` (`:143-162`); `ModbusTcpConnectionSummary.cs:45-63` (nine fields) | scenario `modbus-link-policy` asserts `connectAttemptCount`, `nextAttemptAt`, `currentBackoff` | intended | separate from the link summary because it is about the transport rather than the device |
| 143 | THE SYSTEM SHALL reset `ConsecutiveConnectFailures` on a successful connect and on a configuration change, and SHALL never reset the two totals. | `RecordConnected` (`:81-92`), `ResetConnectBackoff` (`:132-140`); `_connectAttemptCount`/`_connectFailureCount` are only incremented | scenario `modbus-link-policy` "exactly one connect so far" | intended | `ModbusTcpConnectionSummary.cs:23-24` states the exception explicitly |
| 144 | WHEN an armed backoff's instant has passed THE SYSTEM SHALL report `State` as `Disconnected` while keeping `CurrentBackoff` and `NextAttemptAt` filled. | `ModbusTcpConnectionAccumulator.cs:150` compares the clock; `:147-149` states why | GAP | intended | the socket really is closed and the next request will attempt a connection, but a reader can still see what the client was waiting on |
| 145 | THE SYSTEM SHALL publish both summaries as service-property-legal flat readonly record structs with a title and a description on every field. | `ModbusLinkSummary.cs:55-91` (18 `[StructField]`), `ModbusTcpConnectionSummary.cs:45-63` (9) | `ModbusRtuShould` / `LogicBlockModbusTcpClientShould` do not assert it; the introspection of the annotations is `INTRO`'s | intended | the whole summary is one `[ServiceProperty]`; the emission is `AC-EMIT-*`'s and the annotation's journey to the wire is `AC-INTRO-*`'s — cited, not re-minted |

### H. Registration, packaging and the socket's lifetime (RFC 0018)

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 146 | THE SYSTEM SHALL register the Modbus Core services — the bit-converter proxy, the data converter and the validator — as transients. | `Modbus.Core/ServiceCollectionExtensions.cs:19-21` | `Vion.Dale.Sdk.Modbus.Core.Test/ServiceCollectionExtensionsShould.RegisterModbusTcpRelatedServices` | intended | they are stateless; a transient costs nothing and cannot be shared wrongly |
| 147 | THE SYSTEM SHALL register the Modbus TCP client and server, their wrappers, proxies, request factory and queue as transients, and their two factories as singletons. | `Modbus.Tcp/ServiceCollectionExtensions.cs:28-36` | `Vion.Dale.Sdk.Modbus.Tcp.Test/ServiceCollectionExtensionsShould.RegisterModbusTcpRelatedServices` | intended | one client owns one socket and one queue, so it must be per block; the factories hold only a provider |
| 148 | THE SYSTEM SHALL register `TimeProvider.System` only when the host has not already registered one. | `Modbus.Tcp/ServiceCollectionExtensions.cs:41` (`TryAddSingleton`) | GAP | intended | it keeps a test-supplied `FakeTimeProvider` and the full SDK's own registration authoritative (`:38-40`) |
| 149 | THE SYSTEM SHALL reclaim a constructor-injected Modbus client when its logic block's actor stops. | `ActorSystem.CreateRootActorFromDi` resolves the block from a per-actor scope (`Vion.Dale.ProtoActor/ActorSystem.cs:231-237`) which `Actor<T>` disposes on `Stopped` | `Vion.Dale.ProtoActor.Test` (out of area; `AC-LIFE-*` owns the scope) | intended | RFC 0018 Part A; the socket closes deterministically on redeploy and the block never self-disposes — cited to `LIFE`, not re-minted |
| 150 | THE SYSTEM SHALL resolve a factory-created Modbus client from the singleton factory's root provider, so the per-block scope does not reclaim it. | `LogicBlockModbusTcpClientFactory.cs:20-24` holds the `IServiceProvider` injected into a singleton (`ServiceCollectionExtensions.cs:28`), which is the root | `LogicBlockModbusTcpClientFactoryShould.CreateLogicBlockModbusTcpClientInstance` (asserts an instance is returned, not its scope) | ⚠ propose | RFC 0018 § 8.3, still open. One half-open socket per factory-created client per redeploy. **Reader searched:** `ILogicBlockModbusTcpClientFactory` has zero call sites in the first consumer's 84 files, so nothing fielded is affected today (see sketch) |
| 151 | THE SYSTEM SHALL resolve a factory-created Modbus server the same way. | `LogicBlockModbusTcpServerFactory.cs:20-24`; `ServiceCollectionExtensions.cs:34` | `LogicBlockModbusTcpServerFactoryShould.CreateLogicBlockModbusTcpServerInstance` | ⚠ propose | the same shape one file over, with a listener rather than a socket at stake; RFC 0018 named only the client. Reader: zero call sites in the consumer (see sketch) |
| 152 | THE SYSTEM SHALL mark `Vion.Dale.Sdk.Modbus.Core` and `Vion.Dale.Sdk.Modbus.Rtu` as shared assemblies and SHALL leave `Vion.Dale.Sdk.Modbus.Tcp` unmarked. | `Modbus.Core/AssemblyAttributes.cs:9`, `Modbus.Rtu/AssemblyAttributes.cs:3`; no such attribute in Tcp | `SharedAssemblyMarkerShould.MarkTheModbusCoreAssemblyAsSharedAcrossPlugins` | intended | `ModbusReceipt` and `ModbusOutcome` ride inside the RTU actor-message records, so they need one identity per gateway; Tcp declares no handler and no crossing type, so a copy per plugin is right. The loading itself is `AC-PLUG-*`'s |
| 153 | THE SYSTEM SHALL leave `ModbusRtuHandler` unregistered in the Modbus RTU assembly's `IConfigureServices`, and the runtime SHALL still construct it. | `Modbus.Rtu/ConfigureServices.cs:16-17` registers only the Core services and the request factory, where `Vion.Dale.Sdk.DigitalIo/ConfigureServices.cs` and `.AnalogIo`'s register their two handlers each; the runtime spawns handler actors with `ActivatorUtilities.CreateInstance` (`C:\_gh\dale\Dale\Program.cs:216-234` → `ActorSystem.cs:237`), which needs no registration, and `PluginLoader.RegisterSharedAssemblyServices` reports the unregistered-handler fault only for an assembly with no `IConfigureServices` at all (`C:\_gh\dale\Dale\Plugin\PluginLoader.cs:301-317`) | GAP | intended | the asymmetry is real and harmless, and it is worth stating precisely because `architecture/libraries/dale-sdk.md`'s shared-assembly note reads as though it were fatal — a checkpoint against that prose, not an edit from here |

### I. The published surface, and what no analyzer judges

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 154 | THE SYSTEM SHALL judge no declaration of the three Modbus projects with any `DALE` diagnostic. | none of `Vion.Dale.Sdk.Modbus.{Core,Tcp,Rtu}.csproj` carries the analyzer `ProjectReference` that 19 other projects do, including `Vion.Dale.Sdk.DigitalIo` and `.AnalogIo` | GAP (`AC-ANLZ-018.2` is the criterion; which projects reference it is a build-graph fact) | intended | `AC-ANLZ-018.2` states the rule; the page names the consequence — `[PublicApi]` marks and XML docs here are author discipline with no gate behind them (`D6`) |
| 155 | THE SYSTEM SHALL publish exactly the 30 `[PublicApi]`-marked types of the three assemblies into the API manifest. | 18 + 11 + 1 marks; `docs/snapshots/publicapi-manifest.json` carries 18 Core, 11 Tcp and 1 Rtu in-scope types | `SharedAssemblyMarkerShould` pins the assembly marker only | intended | `[PublicApi]` is the manifest gate and `[assembly: PublicApiNamespace]` only drives grouping (`docs/process-journal.md:71`) |
| 156 | THE SYSTEM SHALL expose 43 further public types the manifest does not carry, among them the data converter, the validator, the request, queue and factory interfaces, both server and client proxy interfaces and eleven exception types. | 73 public types against 30 marks; unmarked examples: `IModbusDataConverter`, `IModbusValidator`, `IBitConverterProxy`, `IRequest`, `IRequestQueue`, `IRequestFactory`, `IModbusTcpClientProxy`, `IModbusTcpClientWrapper`, `IModbusTcpServerProxy`, `OperationTimeoutException`, `InvalidUnitIdentifierException` | GAP | ⚠ park | `sdk-surface-conventions.md`'s surface minimalism against a real dependency: `TKIT` substitutes four of these seams and the two TestKits' 16 manifest types name them, so the set is deliberate in part and accidental in part. Sorting which is which is a surface review, not an area pass's `fix` |
| 157 | THE SYSTEM SHALL declare `IpAddressNotSetException` and `ConnectionTimeoutException` in the files of the types that throw them, in the `Client.Implementation` namespace. | `ModbusTcpClientWrapper.cs:1216`, `ModbusTcpClientProxy.cs:268` — both public, neither `[PublicApi]`, both in a file named for an internal class | GAP | ⚠ park | a block's error callback receives both, so they are consumer-facing types in an implementation namespace with no manifest row; moving them to `Exceptions/` is a namespace change, which is source-breaking — the ledger's |
| 158 | THE SYSTEM SHALL log a Modbus TCP multiple-coil write as executed before it is attempted, and a multiple-register write as executing after it has completed. | `ModbusTcpClientProxy.cs:125` calls `LogExecutedWriteOperation` where its five siblings call `LogExecutingWriteOperation`; `:197` calls `LogExecutingWriteOperation` where its five siblings call `LogExecutedWriteOperation` | GAP (`testing-conventions.md` § 15 forbids asserting on a log call) | ⚠ fix | two transposed calls in one file: FC15 never logs "Executing" and FC16 never logs "Executed", so a gateway log cannot pair the two writes that matter most for surveillance. One line each (see sketch) |
| 159 | THE SYSTEM SHALL name the Modbus Core dependency-injection test after the Modbus TCP services. | `Vion.Dale.Sdk.Modbus.Core.Test/ServiceCollectionExtensionsShould.cs` declares `RegisterModbusTcpRelatedServices`, identical to the Tcp project's own test name | — | ⚠ fix | the name is a claim about the wrong assembly; the rename round of Phase B has to touch it anyway (see sketch) |
| 160 | THE SYSTEM SHALL keep the Modbus TCP client wrapper's tests under `Client/LogicBlock/` while the wrapper lives under `Client/Implementation/`. | `Vion.Dale.Sdk.Modbus.Tcp.Test/Client/LogicBlock/ModbusTcpClientWrapperShould.cs` against `Vion.Dale.Sdk.Modbus.Tcp/Client/Implementation/ModbusTcpClientWrapper.cs` | — | ⚠ fix | `testing-conventions.md`'s file-layout rule is that a suite mirrors its subject's path; a reader looking for the link policy's tests looks in the wrong folder (see sketch) |

### J. Test discipline, the smoke tier and RFC 0007's acceptance bar

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 161 | THE SYSTEM SHALL prove the Modbus TCP link policy against a real client/server socket pair, because a stepped clock never elapses a backoff and a virtual round trip reads zero. | `examples/Vion.Examples.ModbusTcp/scenarios/modbus-link-policy.scenario.json` (real clock, `127.0.0.1:15020`); `scripts/smoke-modbus.ps1 -LocalSource` | the scenario itself, plus `Vion.Examples.ModbusTcp.IntegrationTest` running the same files in `dotnet test` | intended | the scenarios are Tier 1 proof, cited rather than re-proven |
| 162 | THE SYSTEM SHALL prove the healthy poll baseline — a connected socket, an `Online` link, four successful polls, π decoded from input registers 6–7, zero timeouts, zero transport errors, zero drops, one connect attempt and no armed backoff. | `modbus-healthy.scenario.json` steps and expectations, read from the file | the scenario | intended | the floor the link-policy scenario builds on |
| 163 | THE SYSTEM SHALL prove the link policy's transitions — one connect for a re-applied identical configuration, `Faulted` then `BackingOff` on an unreachable port, a named next attempt, fast refusals inside the window, recovery on a corrected port with no operator action, and `Online` under maximum cadence. | `modbus-link-policy.scenario.json`: `Faulted` six times, `Online` ten, `127.0.0.2` three, one `nextAttemptAt` | the scenario | intended | every assertion is a criterion's proof at Tier 1 |
| 164 | THE SYSTEM SHALL prove neither `MaxQueuedAge` nor the `Expired` outcome in the committed scenarios. | `modbus-link-policy.scenario.json` judge 3 says so, and no step names either | rows 55 and 56 are therefore GAP at Tier 1 and need a unit test | intended | the simulated server answers in under a millisecond, so no cadence this client can generate ages a request out |
| 165 | THE SYSTEM SHALL prove none of the link policy through the TestKit's fake client proxy. | `.claude/skills/modbus-smoke/SKILL.md:12-13` | disproved by `Vion.Dale.Sdk.Modbus.Tcp.TestKit.Test/ModbusLinkPolicyShould.cs`, which drives the whole backoff through `FakeModbusTcpClientProxy.EnqueueConnectFailure` (`FakeModbusTcpClientProxy.cs:199-201`, `:238-241`) and the fake's `FakeTimeProvider` clock | ⚠ fix | the fake substitutes the **proxy**, so the wrapper's real link policy runs above it; what the fake cannot prove is the socket's own behaviour — a refused connect's errno, a half-open socket, the reuse-address bind. The skill's sentence sends an author to a real socket for a criterion a virtual clock proves in milliseconds (see sketch) |
| 166 | THE SYSTEM SHALL keep the Modbus TCP server's consumer integration tests passing unchanged as the acceptance bar for the server surface. | RFC 0007 `:368`; `ModbusTcpServerIntegrationShould` (7 real-socket tests) | the suite | intended | the absorbed clause; its assignee is this page's test-discipline section |
| 167 | THE SYSTEM SHALL bind an ephemeral loopback port in each of its three real-socket suites, so none collides with another or with a fixed port. | `ModbusTcpServerIntegrationShould`, `ReuseAddressTcpClientProviderShould` and `ModbusTcpServerProxyShould` each bind a port the OS chooses; neither 5000 nor 15020 appears in any of the three | the suites pass in the ordinary `dotnet test` run | intended | the collision to guard against is with the smoke skill's host (ports 5000 and 15020), not between the suites — the brief's premise, corrected |
| 168 | THE SYSTEM SHALL cite no acceptance criterion from any Modbus test. | zero `AC-` tokens across the three test projects; `scripts/test-style-lint.ps1`'s exempt map is empty (`:59-60`) | — | ⚠ fix | the pass's own subject: every one of the 388 is rewritten to cite, and the style gate applies from the first commit |

---

## Failure sketches for the ⚠ rows

**Row 15 — `Invalid` and `Cancelled` are documented as counted and are not.**
A block reads `Link.ExpiredCount + Link.DroppedCount + Link.BackedOffCount` to see how much local
shedding is happening, believing the docs' "local outcomes … are still counted"; every request
refused for a bad unit id or lost to a disposal is invisible in every counter, so a
misconfigured unit id looks like a client doing nothing at all.
*Fix:* either add `InvalidCount` and `CancelledCount` to the summary and the accumulator, or correct
the two sentences to name the five outcomes that do have counters. The second is not source-breaking
and is the smaller claim; the first changes an 18-field published struct.

**Row 17 — `quantity` and `count` name the same argument position.**
A block author copies a working `ReadHoldingRegistersAsUShort(unit, addr, quantity: 4, …)` into a
32-bit read and the compiler rejects `quantity:`; worse, they pass 4 to `count` meaning registers and
silently read eight.
*Recommendation:* leave both names. The distinction is real — one counts registers, the other values
— and renaming either breaks every named-argument call site in the first consumer's 84 files. State
it on the page instead, per family.

**Rows 25 / 112 — port 0 is accepted on both the client and the server.**
A commissioning form leaves the port empty, the binder writes 0, and the client spends its life
arming backoffs against a port that cannot answer, while the server binds an ephemeral port and
reports `IsListening == true` on a number no one was told. The first consumer's own
`ModbusConnection` already declares `Minimum = 1` (`…/Shared/ModbusConnection.cs:46`), so the
consumer refuses what the SDK admits.
*(park)* One boundary per setter, but the client's and the server's are the same question and the
server's `Port` doc explicitly says "0-65535" — a ledger entry that decides both at once, with the
doc, is worth more than two independent one-line fixes.

**Row 41 — the connect-timeout message is culture-formatted.**
A gateway running under a comma-decimal culture reports *"The connection could not be established
within 2,5 seconds."*; a test that pins the message passes on the author's machine and fails on CI's
Linux runner — the exact shape that failed one test of 455 in the ANLZ pass.
*Fix:* `string.Format(CultureInfo.InvariantCulture, …)`, matching the area's three other message
builders.

**Row 44 — the classifier's default faults the link for a local failure.**
A block sets `DefaultOperationTimeout` from a commissioning field to `-5 s`; every operation throws
`ArgumentOutOfRangeException` from the timeout CTS, classifies as `TransportError`, closes the socket
and reports the device `Faulted` — the operator replaces a working device.
*Recommendation:* keep the default (an unrecognised exception genuinely might be transport) but
classify `ObjectDisposedException` as `Cancelled` and `ArgumentException`/`ArgumentOutOfRangeException`
as `Invalid`. It is a reclassification a block's error callback can see, so it is the operator's call.

**Row 47 — two unguarded timeout knobs beside three guarded ones.**
`ConnectionTimeout = TimeSpan.Zero` makes `Task.Delay(0)` win every race in
`ModbusTcpClientProxy.ConnectAsync`, so every connect raises `ConnectionTimeoutException`, two in a
row arm a backoff, and the client never reaches a device that is present and answering.
*Fix:* refuse `<= 0` on both setters with `ArgumentOutOfRangeException`, exactly as `ConnectBackoff`
and `MaxQueuedAge` already do.

**Row 48 — one proxy seam, two unit-identifier types.**
A second `IModbusTcpClientProxy` implementation (a bench, a recording proxy) has to write four
`int` overloads and four `byte` ones for one protocol field, and a `(byte)` cast in the wrapper turns
unit id 256 into 0 rather than reaching the validator — which cannot happen today only because
`ValidateUnitIdentifier` runs first.
*Recommendation:* unify on `int`. It is source-breaking for any implementer outside this repository;
inside it, only `FakeModbusTcpClientProxy` and `ModbusTcpClientProxy` implement it.

**Row 52 — a public setter that silently does nothing.**
An integrator raises `QueueCapacity` from 256 to 2048 on a congested client from the dashboard,
reads it back as 2048, and keeps losing requests at 256 forever.
*Fix:* throw `InvalidOperationException` from both setters once the queue exists, with the server's
own message shape ("… can only be changed while the client is disabled").

**Row 57 — a queued write vanishes on dispose.**
A device block writes a safe baseline from `Stopping()`; the actor's scope disposes the client, the
queue's writer is completed and its CTS cancelled, and the write is neither sent nor reported — no
callback, no receipt, no link record. RFC 0018 § 9 records this as the corrected claim, and three
fielded consumer blocks depend on it arriving.
*Recommendation:* drain the channel on `Dispose`, completing each remaining request through
`HandleRequestFailed(new RequestDroppedException(name))` as the disposed-enqueue path already does.
That is a new callback a fielded error handler will start receiving during shutdown, so it is the
operator's call. The alternative — correcting `ILogicBlockModbusTcpClient.cs:38-41` to say the
queued ones are dropped silently — documents a hole rather than closing it.

**Row 59 — shutdown logs an Error per queued request.**
A gateway with six Modbus blocks redeploys; each block's queue holds a handful of requests, and the
log pipeline takes tens of `Request 'ReadHoldingRegistersAsUShort' failed` lines at Error for an
ordinary restart — the flood the comment three lines up exists to prevent.
*Fix:* add `ModbusOutcome.Cancelled` to the quiet arm at `DeviceRequest.cs:94`.

**Row 62 — `Disconnect` on a disabled client is silently skipped.**
A block disables its client, calls `Disconnect(this, onDone, onError)` to release the socket, and
neither callback ever runs; the block waits forever on a completion that cannot arrive.
*Fix:* documentation. Add the gate to `ILogicBlockModbusTcpServer`'s sibling wording on
`ILogicBlockModbusTcpClient.cs:178-209` — the behaviour is right (row 7's rule), the promise is
incomplete.

**Row 65 — a negative `MaxQueuedAge` reads back as "no limit".**
A TestKit-based bench sets `queue.MaxQueuedAge = TimeSpan.FromSeconds(-1)` to force expiry, reads it
back as `null`, and every request runs however old it is — the opposite of what was asked, silently.
*Fix:* refuse a non-positive value in `RequestQueue`'s setter, matching the client's, so the `-1`
sentinel is reachable only from `null`.

**Row 68 — the 64-bit word-order guard is inside the loop.**
A device returns an empty register frame for a `ReadHoldingRegistersAsDouble` with a cast-invalid
`WordOrder64`; the swap returns silently, the cast yields an empty array, and the block's success
callback fires with no values instead of the `Invalid` receipt its two sibling orders produce.
*Fix:* hoist the enum check above the loop, as `SwapBytes` (`:50`) and `SwapWords(WordOrder32)`
(`:71`) already do.

**Row 78 — the SDK sends a request the protocol cannot honour.**
A block reads 200 holding registers; the device answers `IllegalDataValue` and the block sees a
`DeviceError` with no hint that the request, not the device, was wrong — or the device drops the
frame and the block sees a `Timeout` and blames the network.
*Recommendation:* refuse in the client before the wire — 125 registers for FC3/FC4, 123 for FC16,
2000 bits for FC1/FC2 — with `InvalidCountException` and an `Invalid` receipt, matching what
`Vion.Examples.ModbusTcp` already does one layer up. No call in the first consumer's 84 files reads
above 40 registers, so nothing fielded changes.

**Row 85 — a value width below two bytes divides by zero.**
A consumer resolving the public `IModbusDataConverter` to decode a byte-wide field calls
`ConvertCountToQuantity(4, 1)`; `registersPerValue` is 0, the overflow branch computes
`ushort.MaxValue / 0`, and the block sees a bare `DivideByZeroException` classified as
`TransportError` — a faulted link for an argument mistake.
*(park)* Two guards, one per method, but the real question is whether these two types should be
public at all (row 156); fixing the arithmetic without answering that hardens a surface the SDK may
not want.

**Row 93 — an empty write reports an address error.**
A block calls `WriteAsString(addr, "")` after a null-coalesce, and the exception says the access
"lies outside the declared extent of 64" — the author checks their extents, which are fine.
*Fix:* throw `ArgumentException` naming the empty payload, as `WriteRaw`'s odd-length arm already
does, and leave `InvalidServerAddressException` for what its name says.

**Row 99 — a nested `Sync` disarms the deadlock guard.**
A block factors register access into two helpers, each opening its own `Sync`, and calls one inside
the other; after the inner one returns, `IsEnabled = false` inside the outer callback no longer
throws — it calls `Stop()`, which joins request handlers waiting for the lock the callback still
holds, and the actor thread never returns.
*Fix:* make `_inSyncCallback` a depth counter, or save and restore its previous value in each
`finally`.

**Row 113 — a disposed server reports itself enabled.**
A block disposes its server in teardown and a later health check reads `IsEnabled == true` and
`IsListening == false`, which no documented state pairs; setting `IsEnabled = false` then calls
`Stop()` on a disposed server, logged as a teardown race.
*Recommendation:* have `Dispose` set the flag to `false` first. It changes what a block's own
teardown observes, and 23 consumer files use this interface, so it is the operator's call.

**Row 116 — two instant types on one surface.**
A block publishes `Connection.LastConnectedAt` (`DateTime?`) and `LastClientWriteAt`
(`DateTimeOffset?`) side by side; the DevHost renders one as a UTC instant and the other with an
offset, and a comparison between them needs a conversion the author did not expect.
*(park)* Changing either is source-breaking for the consumer's 23 files and for the DevHost's struct
renderer.

**Row 130 — a throwing callback is contained on TCP and escapes on RTU.**
A block's `receipt.Outcome` switch throws on an outcome it does not handle; on Modbus TCP the request
logs `Failed to invoke callback` and the queue keeps draining, on Modbus RTU the exception reaches
the contract actor's message loop. One `IModbusClient` surface, two answers.
*Fix:* wrap RTU's four `dispatcher.InvokeSynchronized` call sites and `HandleContractMessage`'s two
callback invocations the way `Request.TryInvokeCallback` wraps TCP's.

**Row 140 — a bad unit id clears the queued-wait gauge.**
A block watches `Link.MaxQueuedWait` to size its poll cadence; one request with a mistyped unit id
records a `QueuedWait` of zero as `LastQueuedWait`, and the field a dashboard shows as "how long
requests are waiting" reads zero while the queue is deep.
*Fix:* guard the two queued-wait writes on the outcome having been queued at all — every outcome
except `Invalid` — rather than on `ReachedTheWire`, which would wrongly exclude `Expired` and
`Dropped`.

**Rows 150 / 151 — a factory-created client or server is never reclaimed.**
A block that needs two endpoints calls `factory.Create()` in its constructor; the per-block scope
disposes the injected client and not the created one, so a same-version redeploy strands one
half-open socket (or one bound listener) per created instance until the process exits.
*Recommendation:* resolve the factory from the ambient block scope — register the factory as
`Transient` so it receives the scoped provider, or inject `IServiceScopeFactory`. It is a DI-lifetime
change on a published registration, and RFC 0018 § 8.3 left the choice open; no consumer call site
exists today.

**Row 156 — 43 public types the manifest does not carry.**
A consumer writing a bench discovers `IRequestQueue` and `IModbusTcpClientProxy` by IntelliSense,
builds against them, and finds no API-reference page and no compatibility promise; a later pass
narrows one and their build breaks with no migration note.
*(park)* Sorting the deliberate seams (the four the TestKit substitutes) from the accidental exposure
(the converter, the validator, eleven exceptions) is a surface review with a release note, not an
area pass's fix.

**Row 157 — two consumer-facing exceptions in an implementation namespace.**
A block author writes `catch (ConnectionTimeoutException)` and has to `using
Vion.Dale.Sdk.Modbus.Tcp.Client.Implementation` — a namespace whose name says not to.
*(park)* Moving them to `Exceptions/` is source-breaking for any consumer already catching them.

**Row 158 — two transposed log calls.**
A gateway log shows a multiple-coil write "Executed" twice and never "Executing", and a
multiple-register write "Executing" twice and never "Executed"; pairing a write with its completion
in a field log is impossible for exactly the two function codes a surveillance question asks about.
*Fix:* one identifier each at `ModbusTcpClientProxy.cs:125` and `:197`.

**Row 159 — the Core DI test is named after Tcp.**
A reader greps `RegisterModbusTcpRelatedServices`, finds two identical names in two assemblies, and
cannot tell which covers which registration list.
*Fix:* rename the Core one to say Core. The Phase B rename round touches it anyway.

**Row 160 — the wrapper's tests are in the wrong folder.**
A reader looking for the link policy's tests opens `Client/Implementation/` in the test project and
finds nothing; the 57 tests are one folder over under `Client/LogicBlock/`, beside the client's.
*Fix:* move the file to `Client/Implementation/ModbusTcpClientWrapperShould.cs`.

**Row 165 — the smoke skill says the TestKit can prove none of the link policy.**
An author changing `ArmConnectBackoff` reads the skill, concludes only a real socket can judge it,
and spends a minute of real clock per iteration — while
`Vion.Dale.Sdk.Modbus.Tcp.TestKit.Test/ModbusLinkPolicyShould.cs` proves the same transitions in
milliseconds on a virtual clock.
*Fix:* correct the sentence to name what the fake genuinely cannot prove — the socket's own
behaviour: a refused connect's errno, a half-open socket, the reuse-address bind, and a round trip
that is real time.

**Row 168 — no Modbus test cites a criterion.**
Nothing links the 388 tests to a stated rule, so `spec-trace` cannot tell a criterion with a proof
from one without.
*Fix:* the pass's own subject — the rewrite in Phase B.

---

## Unmapped tests

Tests in scope that map to no row above, and their Phase B disposition:

| Test | Why unmapped | Phase B |
|---|---|---|
| `ModbusDataConverterShould.CastShortToBytes`, `.CastIntToBytes`, `.CastLongToBytes` and their three `CastEmpty…` siblings | six methods over one `MemoryMarshal.Cast` rule (row 83) | merge into `[DataRow]`s of one criterion; the empty cases are rows of the same table |
| `ModbusDataConverterShould.CastBytesToShort`, `.CastBytesToInt`, `.CastBytesToLong` and their three `CastEmpty…` siblings | the reverse direction of the same rule | merge into the same criterion's rows |
| `ModbusDataConverterShould.GetBytesOfShort`, `.GetBytesOfUShort` | pin the `IBitConverterProxy` pass-through, not a Modbus rule | keep, uncited **premise tests** — they pin that the converter delegates rather than reimplements; `docs/spec-process.md` names the category |
| `ModbusExceptionShould.ReportADeviceCodeWhenConstructedWithOne`, `.ReportNoDeviceCodeWhenConstructedWithoutOne` | map to the `HasExceptionCode` distinction the classifier reads (`ModbusOutcomeClassifier.cs:25-26`) | cite the criterion row 42's cluster mints for `DeviceError` vs `ProtocolError` |
| `ModbusBitAccessorShould.ReadBitsPackedLeastSignificantFirst`, `.WriteAndClearBits`, `.LeaveNeighboringBitsUntouched` | the server-side half of row 77's packing rule | cite row 77's criterion; the packing rule is one rule with a client and a server call site |
| `ModbusRegisterAccessorShould.RoundTripEveryTypedPair`, `.RoundTripLongAcrossWordOrders`, `.RoundTripRawBytes`, `.RoundTripStringsPaddedToRegisterBoundary` | round-trip pins over the accessor family, mirroring rows 66–69 and 79–81 | keep, cited to the conversion criteria — a round trip is the only assertion that catches a swap bug cancelling itself out (RFC 0007's review found exactly that) |
| `ModbusRegisterAccessorShould.ReadUShortFromWireOrderBytes`, `.WriteUShortAsWireOrderBytes`, `.ReadIntHighWordFirstByDefault`, `.ReadIntLowWordFirstWhenRequested`, `.WriteIntLowWordFirstWhenRequested` | five methods over the byte/word-order defaults (row 19) applied server-side | merge into `[DataRow]`s under row 19's criterion |
| `LogicBlockModbusTcpClientShould` — 72 `Enqueue…WhenClientIsEnabled` / `NotEnqueue…WhenClientIsDisabled` methods | one pair per method family, all proving rows 7 and 2 | merge into two `[DataRow]`-driven criteria with the 36 families as rows |
| `ModbusTcpClientWrapperShould` — 36 per-family methods (`ReadDiscreteInputs` … `WriteMultipleHoldingRegistersAsString`) | one per family, all proving that the family reaches the proxy with its arguments intact (row 1) | merge into `[DataRow]`s of one criterion per direction |
| `ModbusRtuShould` — 36 per-family methods | the RTU mirror of the same | merge the same way |
| `ArrayResultRequestShould.HaveRequestName`, `.HaveRequestId` and the same pair in `SingleResultRequestShould` and `VoidResultRequestShould` | six methods pinning that a request carries a name and a fresh id | merge into one `[DataRow]` criterion over the three request kinds; the id's uniqueness is what correlates a log line to a request |
| `ModbusTcpClientWrapperShould.DisposeClientProxy`, `.NotThrowWhenClientProxyThrowsOnDisposal`, `.NotDisposeClientProxyWhenAlreadyDisposed` | the wrapper's disposal contract, which no row states | mint a criterion under section H — disposal is idempotent and swallows the proxy's failure |
| `LogicBlockModbusTcpClientShould.ReleaseManagedResourcesWhenDisposed`, `.ReleaseManagedResourcesOnlyOnce` | the client's disposal ordering (queue then wrapper) | cite the same criterion |
| `RequestQueueShould.NotThrowIfDisposedMultipleTimes` | idempotent disposal one layer down | cite the same criterion |
| `RequestQueueShould.ThrowExceptionWhenOverflowPolicyUnsupported` | the `NotSupportedException` for a cast-invalid `QueueOverflowPolicy` (`RequestQueue.cs:72`) | cite row 53's criterion, whose text covers the three policies and their refusal |
| `RequestQueueShould.ContinueProcessingQueuedRequestsAfterFailure`, `.ExecuteAllEnqueuedRequests`, `.ReportZeroQueuedCountWhenQueueEmpty`, `.DecrementQueuedCountAfterRequestsComplete` | the consumer loop's own resilience and the depth gauge | cite rows 49 and 60; `.ContinueProcessing…` mints one line under row 49 — one request's failure does not stop the consumer |
| `ModbusTcpClientWrapperShould.ValidateUnitIdentifier`, `.ThrowExceptionWhenUnitIdentifierInvalid`, `.ThrowExceptionOnTimeout`, `.ThrowExceptionOnCancellation`, `.PropagateProxyException` | the execute helpers' error paths | cite rows 72, 12 and 44 |
| `ModbusTcpClientWrapperShould.ConnectWithConfiguredSettings`, `.DisconnectBeforeReconnecting` | the connect sequence's shape | cite rows 21 and 29 |
| `ModbusRtuShould.DropMessageWhenLogicBlockIdIsEmpty` | a `LogicBlockContractBase` guard, not a Modbus rule | keep, re-cited to the `AC-BIND-*` id it proves |
| `ModbusRtuShould.InvokeReadResponseCallback`, `.InvokeWriteResponseCallback`, `.SendReadRequestToContractHandler`, `.SendWriteRequestToContractHandler` | the contract-message round trip | cite rows 117 and 2 |
| `SharedAssemblyMarkerShould.MarkTheModbusCoreAssemblyAsSharedAcrossPlugins` | maps to row 152 | cite row 152's criterion; the loading itself stays `AC-PLUG-*`'s |
| `ModbusTcpServerIntegrationShould.PublishInputRegistersToTheWire`, `.RoundTripTheVgtShapedCycle` | end-to-end wire fidelity over rows 86–104 | keep, cited to the server criteria — they are RFC 0007's acceptance bar (row 166) |

No test in scope is a deletion candidate: every one either maps to a row, folds into a `[DataRow]` of
one, or is a premise test named above. **Three families account for 144 of the 388** and collapse
into six criteria with 36 rows each.

---

## Self-check

**Against the anchor inventory.**

- **`IModbusClient`'s 4 properties:** `IsEnabled` rows 7–9, `DefaultOperationTimeout` rows 10–12 and
  47, `MaxQueuedAge` rows 13, 14, 55, 56, 65, `Link` rows 135–141. **All four visited.**
- **`IModbusClient`'s 36 methods:** rows 1, 2, 3, 7, 16, 17, 18, 19 cover every family; the ten value
  shapes are rows 66–83; the two bit families are rows 76, 77, 84. **All 36 visited as families**,
  which is the altitude a page states (the per-family tests become `[DataRow]`s).
- **`ILogicBlockModbusTcpClient`'s 9 added members:** `QueueCapacity`/`QueueOverflowPolicy` rows 51,
  52; `QueuedRequestCount` row 60; `ConnectionTimeout` rows 22, 47; `Port` rows 22, 24, 25, 28, 29;
  `IpAddress` rows 23, 26, 27, 28, 29; `ConnectBackoff`/`ConnectBackoffMax` rows 32–36;
  `Connection` rows 46, 142–144; `Disconnect` rows 61–64. **All nine visited.**
- **`ILogicBlockModbusTcpServer`'s 11 members:** `IsEnabled` rows 98, 106, 109, 113; `ListenAddress`
  rows 110, 111; `Port` rows 110–112; the four extents rows 86–90, 109; `IsListening` row 113;
  `ConnectionCount` row 107; `LastClientWriteAt` rows 108, 116; both `Sync` overloads rows 95–99.
  **All eleven visited.**
- **`IModbusRtu`:** rows 117–119 (it adds no member).
- **Enums:** `ByteOrder` 66–67 · `WordOrder32` 67 · `WordOrder64` 68–69 · `TextEncoding` 80 ·
  `ModbusLinkState` 136 · `ModbusOutcome` 15, 136, 138–140 (all ten values named across the four) ·
  `ModbusServerArea` 86, 92 · `ModbusExceptionCode` — **see the hole below** ·
  `ModbusTcpConnectionState` 46, 144 · `QueueOverflowPolicy` 53, 54 · `RequestDropReason` 53, 58.
- **Structs:** `ModbusReceipt` rows 3–6 · `ModbusLinkSummary` rows 135–141, 145 (its 18 fields:
  `State` 136–137, `LastContactAt` 136, `LastFailureAt`/`LastFailureOutcome` 138, the six wire
  counters 136, the three local counters 15, `LastRoundTrip`/`Min`/`Max` 139,
  `LastQueuedWait`/`MaxQueuedWait` 140, `QueueDepth` 60 and 119) · `ModbusTcpConnectionSummary` rows
  142–145 (all nine) · `ModbusServerAreaExtents` rows 87–90.
- **The 16 exceptions:** `InvalidCount` 74, 75 · `InvalidBitQuantity` 76 ·
  `ModbusResponseAlignment` 71 · `InvalidUnitIdentifier` 72, 73 · `UnsupportedByteOrder` 67 ·
  `UnsupportedWordOrder32` 67 · `UnsupportedWordOrder64` 68 · `UnsupportedTextEncoding` 80 ·
  `OperationTimeout` 12, 121 · `RequestExpired` 56, 124 · `Modbus` — **see the hole below** ·
  `InvalidServerAddress` 92, 93 · `LinkBackoff` 37, 39 · `RequestDropped` 53, 58 ·
  `IpAddressNotSet` 23, 157 · `ConnectionTimeout` 41, 157 · `PendingRequestsLimitReached` 122 ·
  `ServiceProviderContractMappingNotFound` 123.
- **The link policy's constants:** all six named in rows 22, 31, 32, 51.
- **DI:** rows 146–148, 150–153.
- **Assembly markers:** row 152 (`DaleSharedAssembly`), row 155 (`PublicApiNamespace` and the
  manifest).
- **The manifest:** rows 155, 156.

**Two holes, both filled here rather than left for the classification to find:**

- **`ModbusExceptionCode`'s ten values and `ModbusException`'s two constructors** had no row of their
  own — the classifier's `HasExceptionCode` split (`ModbusOutcomeClassifier.cs:23-26`) is the only
  observable, and it is stated inside row 42's *Why* rather than as a behaviour. It is a behaviour a
  consumer observes (a device's refusal is a `DeviceError` and keeps the socket; a code-less frame
  fault is a `ProtocolError` and closes it), so it becomes **row 169**:

  | # | Behavior (EARS) | Evidence | Test today | Rec | Why |
  |---|---|---|---|---|---|
  | 169 | WHEN the device answers with a Modbus exception code THE SYSTEM SHALL report `DeviceError` and keep the socket; when a frame or protocol fault carries no code it SHALL report `ProtocolError` and close the socket. | `ModbusException.cs:14` (the `0xFF` sentinel), `:26` (`HasExceptionCode`); `ModbusTcpClientProxy.ToSdkException` (`:233-242`); `ModbusOutcomeClassifier.cs:25-26`; `CloseSocketOnWireFault` (`ModbusTcpClientWrapper.cs:265`) | `ModbusExceptionShould.ReportADeviceCodeWhenConstructedWithOne`, `.ReportNoDeviceCodeWhenConstructedWithoutOne` | intended | a device that answers proves the link works, so refusing a register must not drop a healthy socket — the distinction the `0xFF` sentinel exists to preserve |

- **The reverse question, asked per anchor** — *which observables have neither a row nor a
  criterion?* — surfaced one more: **nothing stated what a block observes when the same `IsEnabled`
  value is set again on the server** while `EnsureNotInSyncCallback` has already run. Row 98's text
  covers the guard, row 109's covers configuration, and neither says the no-op still throws inside a
  callback. It becomes **row 170**:

  | # | Behavior (EARS) | Evidence | Test today | Rec | Why |
  |---|---|---|---|---|---|
  | 170 | WHEN `IsEnabled` is set to the value already in force THE SYSTEM SHALL do nothing — except inside a `Sync` callback, where it SHALL throw. | `LogicBlockModbusTcpServer.cs:38` (the guard) runs before `:39-42` (the equality check) | `LogicBlockModbusTcpServerShould.BeIdempotentOnRepeatedEnable`, `.NotStopWhenAlreadyDisabled` | intended | the guard is about where the call comes from, not about what it would do; `ILogicBlockModbusTcpServer.cs:62-63` says the same value is a no-op and `:72-74` says a call from inside a callback throws — both are true and the order is what makes them consistent |

**Against the PublicApi manifest.** All 30 in-scope manifest types are visited: Core's 18 across rows
1–19, 66–84, 92, 135–141, 145, 169; Tcp's 11 across rows 21–65, 86–116, 150, 151; Rtu's 1 across rows
117–119.

**Counts**, taken mechanically from this file at the commit below. **170 rows** — 168 in the ten
lettered sections plus rows 169 and 170 minted by this self-check. **56** carry `GAP` in the *Test
today* column. **31 are `⚠`**: **17 `fix`**, **8 `propose`**, **6 `park`**.

| rec | rows |
|---|---|
| `fix` | 15, 41, 47, 52, 59, 62, 65, 68, 93, 99, 130, 140, 158, 159, 160, 165, 168 |
| `propose` | 17, 44, 48, 57, 78, 113, 150, 151 |
| `park` | 25, 85, 112, 116, 156, 157 |

Every `⚠` row has a failure sketch; the sketch set and the `⚠` set were compared programmatically
and agree exactly. **22** unmapped-test entries cover **144 of the 388** tests, of which 144 fold
into `[DataRow]`s of criteria named above and none is a deletion candidate.

---

## Drift checkpoints

> One line per divergence discovered during implementation:
> `YYYY-MM-DD: <what changed and why>`. Never inline in a spec page. A checkpoint that fixes a
> CLASS bug states the sibling sweep (done / N/A / handed off).

- 2026-09-04: The brief's **72 public types (37/23/12)** is 73 — `Vion.Dale.Sdk.Modbus.Core` has
  **38**, enumerated in the anchor inventory. The `[PublicApi]` count (30, 18/11/1) and the manifest
  count (30 in-scope rows, 51 naming Modbus) both hold.
- 2026-09-04: The brief's **"Ten of the 25 files import Moq"** is twelve — the two factory suites
  (`LogicBlockModbusTcpClientFactoryShould`, `LogicBlockModbusTcpServerFactoryShould`) also import
  it, and the brief counts them among "the three DI/factory tests" without counting their Moq.
  `D4`'s harness rewrite therefore covers twelve files.
- 2026-09-04: The brief's citation *"`ANLZ`'s Known non-conforming code"* names no section of
  `docs/specs/analyzers.md`; the page has no such heading. `AC-ANLZ-018.2` ("judge the declarations
  of every project that references the analyzer assembly … and no others") is the criterion that
  states the rule, and is what row 154 cites. Recorded as `D7`.
- 2026-09-04: The brief's *"the three real-socket suites … never run beside … each other's ports"*
  rests on a premise the call sites refute: none of `ModbusTcpServerIntegrationShould`,
  `ReuseAddressTcpClientProviderShould` or `ModbusTcpServerProxyShould` binds a fixed port — each
  takes an ephemeral loopback port from the OS. The real constraint is against the smoke skill's host
  (5000 and 15020). Row 167 states it; the serialisation `D4` promises is kept anyway, because three
  suites accepting real connections in parallel is a flakiness source independent of port choice.
- 2026-09-04: The brief's *"the skill says the fake 'can prove none of' the link policy"* quotes
  `.claude/skills/modbus-smoke/SKILL.md:12-13` accurately, and the skill is wrong —
  `Vion.Dale.Sdk.Modbus.Tcp.TestKit.Test/ModbusLinkPolicyShould.cs` drives the whole backoff through
  the fake. Row 165, a `fix` on the skill file.
- 2026-09-04: `docs/specs/_findings.md` carries **no** `MODB` line and no Modbus line at all,
  confirming the brief. The neighbours it names — `ANLZ`'s analyzer-pack reach, `BIND`'s row 111
  (`[StructField]` targets, which touches the consumer's `ModbusConnection`) and `PLUG`'s
  shared-assembly rows — are cited by rows 154, 145 and 152 respectively and re-minted nowhere.
- 2026-09-04: RFC 0018 `:25` cites `ModbusTcpServerProxy.cs:105` for the pre-Part-B bind; the current
  file has the reuse-address call at `:109` and the explanatory comment at `:105-108`. The absorbed
  criterion (row 105) cites the current line.
- 2026-09-04: RFC 0018 § 3 `:66` asks for a **rebind-before-release regression** in
  `ModbusTcpServerIntegrationShould`; the suite's seven tests contain none, and § 9 `:132` records
  only "a focused provider test" (`ReuseAddressTcpClientProviderShould`, one test, which binds and
  accepts but does not rebind over a lingering socket). The deferred item is § 8.1's, and it goes to
  the ledger with the reuse-knob question rather than being written as an untestable-portably gate.

---

## Spec delta (to distill)

> Minted in Phase B, after the operator's classification. The consolidation map is written with it.

- _(none yet — Phase A produces the table only)_

---

## Relay notes for the PR body

> Written as each change lands in Phase B. The PR body quotes this section verbatim and nothing else.

- _(none yet — Phase A lands no production or test change)_

---

## Tasks

> One-commit tasks, each tagged with ≥1 AC id. Ephemeral — they live and die with this change doc.

- `T-000` (Phase A): the behavior table, the anchor inventory and the self-check — this commit.
