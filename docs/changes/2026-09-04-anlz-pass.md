---
slug: anlz-pass
status: in-flight          # proposed | in-flight | parked | archived
blocked-on: none           # for parked docs: what's blocking + ref
areas: ANLZ
author: spec-pass (ANLZ area pass, attempt 1)
created: 2026-09-04
updated: 2026-09-04
supersedes: none           # path of a superseded change doc, or none
---

# ANLZ area pass — the DALE diagnostic registry and the analyzers that report it

> Change doc — one in-flight change. The `Spec delta` below is distilled into the current-truth
> pages under `docs/specs/` in the implementation PR, then this doc is archived
> (`pwsh scripts/spec-change.ps1 archive <slug>`). Process: `docs/spec-process.md`. Never put
> change narrative inline in a spec page — it lives here.

## At a glance

### Summary

The ninth SDD area pass. Covers the `DALE` diagnostic registry shipped inside `Vion.Dale.Sdk`'s
package as `analyzers/dotnet/cs`, the thirty-six analyzers that report it, the shared symbol walk
they rest on, the severities and tags, the source generator's own untagged diagnostics, the
packaging that puts the pack into a consumer's build, and the whole
`Vion.Dale.Sdk.Generators.Test` suite — which this pass brings under
`scripts/test-style-lint.ps1` by retiring its exemption.

### Spec implications

Creates `docs/specs/analyzers.md` (`trace: enforced`), area code `ANLZ`. The page states the
registry as a contract (ids, one category, severities, tags, retirement, enabled by default), what
every analyzer can and cannot see, the diagnostics by family with the ids as the `[DataRow]`s of
their tests, the packaging and the wiring probe, and points at
`docs/testing-conventions.md` § 3 and `docs/sdk-surface-conventions.md` §§ 4–5 for the author
disciplines rather than restating them.

Rows the neighbouring pages already own are **cited, never re-minted**: `AC-GATE-011.1`–`011.11`
(the `[IncludedWhen]` / `[InstantiationParameter]` authoring diagnostics), `AC-EMIT-012.1`–`012.7`
(the emission knobs), `AC-LIFE-007.2` (the timer binder's refusals), `AC-INTRO-017.4` (the wiring
probe), `AC-INTRO-014.3`/`014.4` (endpoint identifiers), the `BIND` criteria naming a `DALE` id,
and `AC-SCEN-*` for `DALE046`.

### Decisions

- `D1` — Page path `docs/specs/analyzers.md`, title *"Analyzer diagnostics: what the SDK refuses at
  compile time"* — the flat one-noun page name the corpus already uses (`emission.md`,
  `introspection.md`), and the subject is the registry, not the analyzer classes.
- `D2` — Spine: the registry as a contract → what every analyzer sees → the diagnostics by family
  (contract and message shapes; timers; supported types; presentation hints; access flags; emission
  knobs; observability; public-API documentation; gating and instantiation; scenario wire) → the
  packaging and the wiring probe → a pointer to the test discipline. Ids are the `[DataRow]`s of a
  family's criterion, never one criterion per id.
- `D3` — The generator's three `SourceGenerator`-category pseudo-diagnostics
  (`LogicClassGeneratorDBG`/`INF`/`ERR`) are this page's to state as what they are: outside the
  registry, untagged, absent when the generator does not run. `LogicClassGeneratorDBG` has no call
  site at all and is deleted in this pass (row 148); whether `…ERR` deserves a registry id is
  question 3.
- `D4` — The harness stays `AnalyzerTestBase` + `TestAttributeStubs.cs`; no snapshot files; the
  wiring probe is not extended, and its build-shape hazard is stated rather than re-guarded
  (journal `:134`, `AnalyzerWiringShould.LeaveTheBuildOutputsOfItsDependencyGraphUntouched` is the
  standing guard).
- `D5` — `scripts/test-style-lint.ps1`'s `Vion.Dale.Sdk.Generators.Test/` exemption
  (`:53-54`) retires in Phase B; all 358 tests conform afterwards. **Ordered: citations first, then
  names.** The gate skips any method with no `AC-`/`SYS-` literal in its attribute block (`:70`), so
  retiring the exemption before the citations land would gate 93 of 358 and read as green.
- `D6` — Retired ids (`DALE006`, `DALE029`) stay retired; every new diagnostic this pass mints takes
  the next free id from `DALE047`. A new **Error**-severity firing shape is `propose`, never `fix`,
  because the first consumer suppresses nothing (verified: 0 `#pragma`, 0 `NoWarn`, 0 `.editorconfig`
  line naming a `DALE` id, no `TreatWarningsAsErrors` in either repository).
  **Overridden once, at the classification, on searched evidence:** `DALE048` (row 174) ships at
  `Error` because the shape it fires on — a blank `[ServiceProviderContractType]` token — was
  searched for in the consumer's eight tokens and in every analyzer-judged in-repo project and does
  not occur. `DALE047` (row 47) keeps the policy and ships at `Warning`. The rule stands for every
  future id; the override is the operator's to confirm.
- `D7` — `VisibleWhenPredicateAnalyzer` carries private copies of seven `AnalyzerHelper` members
  with identical bodies (`:221-369`). No observable differs today, so the row is `out-of-spec`; the
  duplication is named on the page as a drift hazard and collapsed only if the operator asks.

### Reviewer's questions

Classified by the coordinator (`sdk: sdd pass 7`) under the operator's standing delegation, on a
fresh-context second opinion that read every ⚠ row's mechanism at the call site **and searched the
readers** — the first consumer and every in-repo project the analyzers judge — for each shape a new
or widened diagnostic would fire on. The record is
`architecture/.claude/briefs/anlz-pass-phaseA-second-opinion.md`; the classification is
`amend-anlz-pass-1.md`.

1. **(ratified — cite)** The ownership split: rows whose *firing rule* a neighbouring page already
   states are cited there. Each neighbour's text was read before deciding; where a page says only
   that a diagnostic exists as the compile-time door (`contracts.md:22-25` lists nine ids and says
   they "are the analyzer area's and are named, never restated"), this page states the rule.
   **OUTCOME:** ratified as cited. `AC-GATE-011.*`, `AC-EMIT-012.1`–`012.7`, `AC-LIFE-007.2`,
   `AC-INTRO-017.4` and the `BIND`/`SCEN` citations are cited and never re-minted; the 93 existing
   citations stay, with an `ANLZ` id beside one only where the neighbour's text does not state what
   the test proves, marked over-determined.
2. **(decide-and-document)** Page name and spine — `D1`/`D2`.
   **OUTCOME:** accepted unchanged — `docs/specs/analyzers.md`, families as umbrellas with their ids
   as `[DataRow]`-shaped leaves.
3. **(propose-and-wait)** `LogicClassGeneratorERR` is an `Error`-severity diagnostic minted inline
   by the generator, outside the registry, with the message text as its own format string
   (`DiagnosticsExtensions.cs:33-44`; four call sites, `LogicClassGenerator.cs:659,662,677,680`).
   Giving it a registry id would make it configurable and suppressible in a consumer's
   `.editorconfig` like every other Dale rule. Reader: every consumer's build log.
   *Recommendation:* a ledger line owned by the retro — the four sites all fire on a missing or
   broken embedded template, which is a build of the SDK itself going wrong, not an authoring
   mistake. **OUTCOME:** recommendation accepted — the registry-id question becomes a ledger line
   owned by the retro. The question's *other half*, which no row carried, is a defect and is now
   **row 178, a `fix`**: passing the caller's message as the descriptor's `MessageFormat` means a
   brace in a template error breaks `Diagnostic.Create` inside the generator.
4. **(decide-and-document)** The harness premise. `AnalyzerTestBase` compiles against stubs read off
   disk (`Helpers/AnalyzerTestBase.cs:58-67`) and `testing-conventions.md` § 3 requires an analyzer
   test to reproduce the real compilation. Two suites use `CS0246`
   (`ServiceRelationAnalyzerTests`, `ScenarioWireTypeAnalyzerTests`) but only the first uses it as
   § 5's *pin* — a Dale diagnostic reported **beside** the compile error. Rows 44 and 141 say which
   families owe a pin now; the rest is a ledger line.
   **OUTCOME:** accepted. The discipline is stated on the page with § 3 cited. Row 170's pins land
   now, and the second opinion splits them: `ContractPropertyAnalyzer` and
   `IncludedWhenPredicateAnalyzer` resolve **by symbol** and owe § 5's pin, while
   `ContractMessageAnalyzer` compares the attribute's **strings** and owes the by-name pin instead —
   a test that the comparison still holds against an unresolved interface. The page records which
   families resolve which way; anything further is a ledger line.
5. **(decide-and-document → `fix`)** `D5`, the `test-style-lint` exemption.
   **OUTCOME:** accepted as `fix`, with two corrections. Rows 171 and 172 are **not rows** — they are
   Phase B mechanics under `D5` and are removed from the table. And they are **ordered: citations
   first, then names**, because `scripts/test-style-lint.ps1:70` checks only methods that already
   cite an id, so retiring the exemption before the citations land would gate 93 of 358. The gate
   itself enforces two things (an article token as a PascalCase word, a Triple-A marker); "verb form"
   and "no collaborator name" are `testing-conventions.md` § 12 prose applied by reading each name.
6. **(decide-and-document)** `AnalyzerReleases.Shipped.md` / `Unshipped.md` do not exist and `RS2008`
   is suppressed with the reason *"analyzer is internal (not shipped as a NuGet package)"*
   (`Vion.Dale.Sdk.Generators.csproj:17`, the `NoWarn` at `:19`). That reason is **false** — the DLL
   is packed to `analyzers\dotnet\cs\` at `Vion.Dale.Sdk.csproj:92` and reaches every consumer.
   § 4 does not decide release tracking.
   **OUTCOME:** accepted. Row 162 fixes the comment (the reason is that no release-tracking files
   exist, not that the analyzer does not ship); adding the tracking files is a ledger line owned by
   the retro. § 4 (`:103-134`) was re-read and decides next-free-id, never-reuse, one-id-two-
   severities, the `CompilationEnd` tag, the three-rule supported-type gate and § 5's real-build
   proof — and says nothing about release tracking, so the ledger line is the right home. The Phase A
   text pointed at "row 147", which is not this row; the pointer is corrected to **row 162**.
7. **(decide-and-document)** `D4`, the harness and the probe.
   **OUTCOME:** accepted unchanged — the existing harness, no snapshot files, the wiring probe not
   extended and its build-shape hazard stated.
8. **(propose-and-wait)** Eleven rows were marked `propose` with their readers named.
   **OUTCOME:** decided on searched evidence, five to `fix`, five to `park`, one to `intended`:
   **5** and **35** → `fix` (message text only; nothing in either repository pins the literals).
   **26** → `fix` at **Warning** through `DALE044`'s `effectiveSeverity` overload, row 6's precedent.
   **40** → `intended`; its Why was wrong — a component *is* a non-block class, so the `Error` is
   well aimed. **47** → `fix` as a new **`DALE047` at Warning**, with `BIND`'s one in-repo fixture
   taking a `#pragma`. **174** → `fix` as a new **`DALE048` at Error** — an **override of `D6`** on
   searched evidence (no blank token in the consumer's eight or in any analyzer-judged in-repo
   project), decided under the delegation and listed here for the operator.
   **17**, **152**, **173**, **175**, **176** → `park`, each with its owner and its home named.
9. **(decide-and-document)** The convention docs as the author's contract. `sdk-surface-conventions.md`
   § 4's two count sentences (`:110`, `:117`) are **true** — 44 live descriptors, 46 ids, two
   retired.
   **OUTCOME:** accepted. Rows 1–20 carry § 4 and § 5 as criteria with the code as evidence; the
   sentences that are author discipline stay where they are and the page points at them. Two
   sentences in those docs are corrected by this pass's own findings: `:259-262`'s project list
   (drift checkpoint) and § 5's implicit "only `ServiceRelationAnalyzerTests` pins it".
10. **(decide-and-document)** The 27 `#pragma warning disable DALE*` sites in other areas' fixtures
    and suites (14 files) are read, classified, and stated as a rule.
    **OUTCOME:** accepted, and the question's own two claims are **withdrawn as wrong**. It said the
    sites are classified "on rows 152–155" — they are on rows **165–167**. It said one
    (`ThrottleConfiguredShould.cs:58`) suppresses a diagnostic the fixture no longer violates, and
    pointed at "row 154": read at the site, `Vion.Dale.Sdk.Test/Core/ThrottleConfiguredShould.cs:57-63`
    suppresses `DALE038` over `[ServiceProperty(MinInterval = "1s", MinChange = "0.1", Immediate =
    true)]`, which `ImmediateIgnoresThrottleKnobsAnalyzer.cs:33-65` still reports — **not stale**;
    and row 154 is the examples' conditional reference, while the stale-suppression row is 166, which
    asserts the opposite. **None of the 27 is stale.** This pass adds three suppressions in other
    areas' fixtures — `LIFE`'s two `DALE005` (row 70) and `BIND`'s one `DALE047` (row 47) — each a
    drift checkpoint with the owning page untouched.
11. **(decide-and-document)** Ledger entries addressed to `ANLZ`, all four carried in section O:
    `_findings.md:30-35` (blank or colliding endpoint `Identifier =`) → row **173**;
    `:174-179` (a message struct declared beside its contract class) → row **47**;
    `:207-212` (`[ServiceProviderContractType]` token, emptiness and uniqueness) → rows **174**
    and **175**; `:219-223` (the generator's `Contract`-substring predicate) → row **176**. The two
    lines naming `DALE014` (`:97`, `:235`) are addressed to the retro and are cited, not taken.
    **OUTCOME:** accepted with these dispositions — **47** and **174** are fixed and their ledger
    entries close (`:174-179` entirely; `:207-212` is rewritten to carry only the uniqueness half,
    with the `CompilationEnd` obligation and the current-assembly boundary named). **173**, **175**
    and **176** stay as ledger lines, owners `ANLZ`, `ANLZ` and `ANLZ`.

---

## Full design

### Anchor inventory

Enumerated mechanically this session from `Vion.Dale.Sdk.Generators/Analyzers/` (39 `.cs` files:
`DaleDiagnostics.cs`, `AnalyzerHelper.cs`, `EmissionAttributeHelper.cs`, and 36 analyzers).

**The registry** — `DaleDiagnostics.cs`, one category `Vion.Dale.Usage` (`:11`), **44 live
descriptors** across ids `DALE001`–`DALE046`, every one passing the enabled flag **positionally**
as `true` (no `isEnabledByDefault:` named argument appears anywhere in the file). Two retired ids
leave comments where the descriptor was: `DALE006` (`:106-107`), `DALE029` (`:312-314`).

Severities: **25 `Error`** — 001, 002, 003, 005, 008, 009, 010, 011, 016, 017, 019, 020, 022, 023,
030, 034, 035, 036, 040, 041, 042, 043, 044, 045, 046. **18 `Warning`** — 004, 007, 012, 013, 014,
015, 018, 021, 024, 025, 026, 027, 028, 031, 032, 033, 037, 038. **1 `Info`** — 039 (`:475-480`).

`WellKnownDiagnosticTags.CompilationEnd` on four: 013 (`:187`), 014 (`:198`), 015 (`:209`), 045
(`:586`).

| id | reporter (`Analyzers/…Analyzer.cs`) | registration | severity |
|---|---|---|---|
| DALE001 | ContractProperty | `SymbolKind.Property` | Error |
| DALE002, DALE005 | TimerMethod | `SymbolKind.Method` | Error |
| DALE003 | ServiceElementType | `SymbolKind.Property` | Error |
| DALE004 | MeasuringPoint | `SymbolKind.Property` | Warning |
| DALE007 | PersistentProperty | `SymbolKind.Property` | Warning |
| DALE008 | ImmutableArrayServiceElement | `SymbolKind.Property` | Error |
| DALE009, DALE010, DALE011 | ContractMessage | `SymbolKind.NamedType` | Error |
| DALE012 | TimerMethod | `SymbolKind.NamedType` | Warning |
| DALE013, DALE014, DALE015 | PublicApiDocumentation | `RegisterCompilationAction` | Warning + `CompilationEnd` |
| DALE016 | StructServiceElement | `SymbolKind.Property` | Error |
| DALE017 | NullableString | `SymbolKind.Property` | Error |
| DALE018 | ImmutableArrayInitialization | `SymbolKind.Property` | Warning |
| DALE019 | MultipleAttributesFromSameBase | `SymbolKind.Property` | Error |
| DALE020 | MultiInterfaceConflict | `SymbolKind.NamedType` | Error |
| DALE021 | DecimalsOnNonNumeric | `SymbolKind.Property` | Warning |
| DALE022 | WriteOnlyTypeRestriction | `SymbolKind.Property` | Error |
| DALE023 | TriggerHintRequiresBool | `SymbolKind.Property` | Error |
| DALE024 | StatusIndicatorRequiresEnum | `SymbolKind.Property` | Warning |
| DALE025 | CrossFillConflict | `SymbolKind.Property` | Warning |
| DALE026 | LiteralGroupKey | compilation-start → `SyntaxKind.Attribute` | Warning |
| DALE027 | FormatOnNonTemporal | `SymbolKind.Property` | Warning |
| DALE028 | FormatSentinelTypeMismatch | `SymbolKind.Property` | Warning |
| DALE030 | ReadOnlyAndWriteOnlyExclusivity | `SymbolKind.Property` | Error |
| DALE031 | ObservableStructMemberDependency | `SyntaxKind.PropertyDeclaration` | Warning |
| DALE032 | ImportanceRequiresScalarType | `SymbolKind.Property` | Warning |
| DALE033 | StringFormatOnNonString | `SymbolKind.Property` | Warning |
| DALE034 | MinChangeWithoutChangeThreshold | compilation-start → `SymbolKind.Property` | Error |
| DALE035 | MinChangeUnparseable | `SymbolKind.Property` | Error |
| DALE036, DALE037 | MinIntervalInvalid | `SymbolKind.Property` | Error / Warning |
| DALE038 | ImmediateIgnoresThrottleKnobs | `SymbolKind.Property` | Warning |
| DALE039 | DeadbandWithoutThrottle | `SymbolKind.Property` | Info |
| DALE040 | WriteOnlyStructFieldTypeRestriction | `SymbolKind.Parameter` | Error |
| DALE041, DALE042 | VisibleWhenPredicate | `SymbolKind.NamedType` | Error |
| DALE043 | IncludedWhenPredicate | `SymbolKind.NamedType` | Error |
| DALE044 | IncludedWhenPredicate **and** InstantiationParameter | `SymbolKind.NamedType` + `OperationKind.SimpleAssignment` | Error |
| DALE045 | ServiceRelation | `RegisterCompilationAction` | Error **and** Warning (same id) |
| DALE046 | ScenarioWireType | `SymbolKind.NamedType` | Error |

Registration totals: 24 on `SymbolKind.Property` (23 top-level + `MinChangeWithoutChangeThreshold`'s
nested one), 7 on `NamedType`, 1 on `Method` (`TimerMethod`, which also registers on `NamedType`),
1 on `Parameter`, 1 top-level syntax-node action, 2 compilation-start actions, 2 whole-compilation
actions, 1 operation action. Every one of the 36 calls
`ConfigureGeneratedCodeAnalysis(GeneratedCodeAnalysisFlags.None)` then `EnableConcurrentExecution()`
as the first two statements of `Initialize`.

**Attribute targets vs readers.** The attributes these analyzers key off are matched by
fully-qualified metadata name (`AnalyzerHelper.cs:15-53`, `:58`/`:66`/`:74`). Two targets are
declared and read by no analyzer walk: `[IncludedWhen]` declares
`AttributeTargets.Property | Class | Method` and `IncludedWhenPredicateAnalyzer` reads all three
(row 118); `[StructField]` declares a parameter target and only
`WriteOnlyStructFieldTypeRestrictionAnalyzer` walks `SymbolKind.Parameter` — no analyzer judges a
`[StructField]` on any other target. Row 129.

**The generator's own diagnostics** — `DiagnosticsExtensions.cs`, category `SourceGenerator`, three
descriptors minted fresh per report, none tagged, none in the registry:
`LogicClassGeneratorDBG` (Info, **0 call sites**), `LogicClassGeneratorINF` (Info, 24 sites),
`LogicClassGeneratorERR` (Error, 4 sites) — all in `LogicClassGenerator.cs`, 28 calls total.

**Packaging** — `Vion.Dale.Sdk.csproj:34` (`ReferenceOutputAssembly="false" OutputItemType="Analyzer"`),
`:92` (the DLL packed to `analyzers\dotnet\cs\` **only if it exists**), `:81-82` (`PredicateParser.cs`
and `PredicateSyntax.cs` linked into the runtime assembly under `LinkBase="Shared"`),
`Vion.Dale.Sdk.Generators.csproj:19` (`NoWarn` `RS2008;CS8669`). Seventeen csprojs carry the analyzer
`ProjectReference`: **eight unconditionally** (`Vion.Dale.Sdk`, `.DigitalIo`, `.AnalogIo`,
`Vion.Dale.Sdk.Test`, `DevHost.Test`, `DevHost.SmokeHost`, the two `LogicBlockParser.Test.*Plugin`s)
and **nine under `Condition="'$(DaleLocalSource)' == 'true'"`** (all the examples). Four SDK
projects carry none: `Vion.Dale.Sdk.Http`, `.Modbus.Core`, `.Modbus.Tcp`, `.Modbus.Rtu`.
`Vion.Dale.Sdk.Generators.Test` references the generator project as a **plain library**
(`:22`), so its own fixtures are not judged by the analyzers.

**Tests** — `Vion.Dale.Sdk.Generators.Test/`: 43 `.cs` files, 40 with `[TestMethod]`s, **358
methods**, 31 `[DataRow]`s, no `[DataTestMethod]`. 93 methods in 9 files cite another page's ids.
Every one of the 36 analyzers has a dedicated `…AnalyzerTests.cs`.

### Probes run this session

A scratch console harness (`scratchpad/anlzprobe`) loaded
`Vion.Dale.Sdk.Generators/bin/Debug/netstandard2.0/Vion.Dale.Sdk.Generators.dll`, built
`CSharpCompilation`s from `Helpers/TestAttributeStubs.cs` plus a fixture source (and, where a row
says so, a second emitted assembly), and ran the named analyzer through `WithAnalyzers`. Each row
citing a probe records the fixture shape and the surface it read. The probe reads the **analyzer's
own** diagnostics, not a Metalama-hosted build — so it reproduces the unit-test compilation, not a
consumer's, except where a row explicitly uses a referenced assembly.

---

## The behavior table

Recommendation legend: `intended` → spec · `fix` → defect, fixed in Phase B · `park` → ledger ·
`propose` → operator decides (implemented as `fix` if accepted, written to the ledger as `park` if
not) · `out-of-spec` → implementation shape.

### A. The registry as a contract

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 1 | THE SYSTEM SHALL report every Dale authoring diagnostic under the single category `Vion.Dale.Usage`. | `DaleDiagnostics.cs:11`, sole `Category` const | GAP | intended | one category is what lets a consumer configure the whole rule set with one `.editorconfig` prefix |
| 2 | THE SYSTEM SHALL allocate diagnostic ids in the range `DALE001`–`DALE046`, of which 44 are live. | `DaleDiagnostics.cs` (44 descriptors enumerated above) | GAP | intended | `sdk-surface-conventions.md:110` states it; no gate holds it |
| 3 | WHEN a diagnostic is retired THE SYSTEM SHALL leave its id unused and never reassign it. | `DaleDiagnostics.cs:106-107` (`DALE006`), `:312-314` (`DALE029`) | GAP | intended | a consumer's `.editorconfig` or `#pragma` naming a retired id must never start configuring a different rule |
| 4 | THE SYSTEM SHALL enable every diagnostic by default, so a consumer that configures nothing receives all of them. | every descriptor passes `true` positionally, e.g. `DaleDiagnostics.cs:24`, `:602` | GAP | intended | the analyzer obligation is worthless if a rule ships off |
| 5 | WHEN a `[ServiceProperty]` or `[ServiceMeasuringPoint]` declares an unsupported type THE SYSTEM SHALL report `DALE003` naming every supported type, including `Guid`. | message at `DaleDiagnostics.cs:43` omits `Guid`; `AnalyzerHelper.cs:143` accepts it; probe P7 — `[ServiceProperty] Guid G` produced no `DALE003` | `ServiceElementTypeAnalyzerTests.Guid_ServiceProperty_NoDiagnostic` | ⚠ fix | the message is the author's only list of the supported set and it is short by one type the SDK ships (`architecture/libraries/dale-sdk.md:84-85`); message text only — nothing in either repository pins the literal |
| 6 | THE SYSTEM SHALL report `DALE045` as an `Error` for a declaration defect and as a `Warning` for an advisory finding, under one id. | `ServiceRelationAnalyzer.cs:335` vs `:344-346` (the `effectiveSeverity` overload) | `ServiceRelationAnalyzerTests.AnErrorAndAWarningCoexistOnOneCompilation` | intended | `sdk-surface-conventions.md:118-122` is the precedent; the consequence — `.editorconfig` moves both together — is the page's to state |
| 7 | THE SYSTEM SHALL tag `DALE013`, `DALE014`, `DALE015` and `DALE045` with `CompilationEnd` so they survive IDE live analysis. | `DaleDiagnostics.cs:187`, `:198`, `:209`, `:586` | GAP | intended | without the tag a whole-compilation diagnostic is dropped in the editor and only appears on a full build |
| 8 | THE SYSTEM SHALL report `DALE039` at `Info` severity, so it appears in a build log only at detailed verbosity and fails nothing. | `DaleDiagnostics.cs:479` | `DeadbandWithoutThrottleAnalyzerTests.ZeroIntervalWithMinChange_ReportsInfo` | intended | the one advisory-only id; an author declaring deadband-only is doing something legitimate |
| 9 | WHEN a consumer sets `dotnet_diagnostic.DALEnnn.severity` in `.editorconfig` THE SYSTEM SHALL honour it for every rule that id carries. | one descriptor per id; `DALE045`'s two severities share `DaleDiagnostics.DALE045_ServiceRelationDiscipline` | GAP | intended | the observable a consumer has to know before suppressing `DALE045`'s warning half |
| 10 | WHEN a source file carries `#pragma warning disable DALEnnn` THE SYSTEM SHALL suppress that diagnostic for the enclosed span. | 27 sites across 14 files in this repository, e.g. `Vion.Dale.Sdk.Test/TestHelpers/IntrospectionBlocks.cs:302` | the suppressed fixtures compile clean | intended | this is how a fixture declares a shape it violates on purpose |
| 11 | WHEN a `#pragma warning disable` names a retired id THE SYSTEM SHALL accept it silently and suppress nothing. | no descriptor for `DALE006`/`DALE029`; Roslyn does not diagnose an unknown-but-well-formed id in a `#pragma` | GAP | intended | the cost of the never-reuse rule, and the reason it is safe |
| 12 | THE SYSTEM SHALL report a whole-compilation diagnostic only for types declared in the compilation's own assembly. | `ServiceRelationAnalyzer.cs:50-52`; `PublicApiDocumentationAnalyzer.cs:61` | `ServiceRelationAnalyzerTests.AServiceLessTypeThatIsNotAComponentOfALogicBlock_NoDiagnostic` | intended | a referenced library's declarations are its own build's business |
| 13 | THE SYSTEM SHALL analyse no compiler-generated code. | `GeneratedCodeAnalysisFlags.None` in all 36 `Initialize` methods | GAP | intended | `sdk-surface-conventions.md:160-166` records that flipping it changes nothing — cited, not re-probed |
| 14 | THE SYSTEM SHALL run its analyzers concurrently. | `EnableConcurrentExecution()` in all 36 | GAP | out-of-spec | a performance choice with no observable except through a data race, and every analyzer's state is either local or built once at compilation start |
| 15 | WHEN the SDK assembly is not referenced THE SYSTEM SHALL report no `DALE` diagnostic. | attributes matched by full name (`AnalyzerHelper.cs:58`) never resolve; `MinChangeWithoutChangeThresholdAnalyzer.cs:33-38` returns early by name lookup | GAP | intended | the analyzers ship inside `Vion.Dale.Sdk`, so this is only reachable through a stray `analyzers/` reference — and the answer is silence |
| 16 | THE SYSTEM SHALL match every attribute it keys off by fully-qualified metadata name, so an alias or a `using` rename does not hide a declaration. | `AnalyzerHelper.cs:58-82` (`ToDisplayString(FullyQualifiedFormat)`) | GAP | intended | an author writing `[SP]` for `[ServiceProperty]` through a `using SP = …` is judged identically |
| 17 | WHEN a member carries a **preset** attribute derived from a Dale attribute THE SYSTEM SHALL judge it as it judges the base attribute. | probe R2 — `[Kilowatts] List<double>` (`class Kilowatts : ServicePropertyAttribute`) produced **no** `DALE003` and **no** `DALE008`, while `[ServiceProperty]` on the identical type produced both. `AnalyzerHelper.cs:58`/`:66` compare the attribute class's own full name; only `MultipleAttributesFromSameBaseAnalyzer.cs:76-87` walks the base chain | `MultipleAttributesFromSameBaseAnalyzerTests.TwoSubclassesOfServiceProperty_ReportsDiagnostic` (the one rule that walks it) | ⚠ park | a preset-annotated member is judged by **every rule but `DALE019`** — 43 diagnostics go blind — while the runtime honours the preset (`Vion.Dale.Sdk.Test/Core/AttributeInheritanceShould.cs`). Widening the match lights up diagnostics on every fielded preset-annotated declaration (see sketch) |
| 18 | THE SYSTEM SHALL report each diagnostic at the location of the attribute that declares the offending knob, where a member may carry two. | `EmissionAttributeHelper.cs:59-62`; `MultipleAttributesFromSameBaseAnalyzer.cs:69` | `MinIntervalInvalidAnalyzerTests.DualAnnotatedBothIntervalsBad_ReportsOnEachAttribute` | intended | two squiggles on one identifier say nothing about which stream each is about |
| 19 | THE SYSTEM SHALL report the presentation and cross-fill diagnostics at the property's own location rather than the attribute's. | `CrossFillConflictAnalyzer.cs:56`, `DecimalsOnNonNumericAnalyzer.cs:62`, `StringFormatOnNonStringAnalyzer.cs:55` | the `_ReportsDiagnostic` tests assert the id, not the span | out-of-spec | inconsistent with row 18 but unobservable in the message; a squiggle position is not a contract |
| 20 | THE SYSTEM SHALL not fail a build for any `Warning` or `Info` diagnostic unless the consumer opts in. | 18 `Warning` + 1 `Info` descriptors; no `TreatWarningsAsErrors` in this repository or in `logic-block-libraries` | GAP | intended | the severity choice is the whole consumer contract for a new rule (`D6`) |

### B. The shared walk — what every analyzer can and cannot see

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 21 | THE SYSTEM SHALL judge only **public, non-static** properties where a rule walks a type's members, matching the declarative binders' `BindingFlags.Public \| Instance`. | `AnalyzerHelper.cs:261-273` | `IncludedWhenPredicateAnalyzerTests.GateAndParameterDeclaredOnABaseClass_ResolveOnTheLeaf_NoDiagnostic` | intended | the walk exists to agree with the binder; a member the binder never binds is not the analyzer's to judge |
| 22 | THE SYSTEM SHALL walk a type's base chain up to but not including `object` where a rule walks a type's members. | `AnalyzerHelper.cs:263` | `IncludedWhenPredicateAnalyzerTests.GateAndParameterDeclaredOnABaseClass_ResolveOnTheLeaf_NoDiagnostic` | intended | a block's members are its own and its bases' |
| 23 | THE SYSTEM SHALL yield the most-derived declaration of a shadowed property name first. | `AnalyzerHelper.cs:263-272` (self before base); `IncludedWhenPredicateAnalyzer.cs:119` (`seen.Add`) | `IncludedWhenPredicateAnalyzerTests.RegatedNewShadow_ReportsDALE043` | intended | the gate and the parameter are read off one declaration, and `new` picks the derived one |
| 24 | THE SYSTEM SHALL never judge a field, a method or an event through the shared property walk. | `AnalyzerHelper.cs:265` (`OfType<IPropertySymbol>()`) | GAP | intended | every service-element attribute targets a property; a field carrying one is unread by every walk |
| 25 | WHEN a service-element attribute is declared on a non-public or a static property THE SYSTEM SHALL still report the per-property diagnostics, though no binder binds the member. | probe Q10 — `[ServiceProperty(MinInterval="nonsense")]` on `static`, `internal` and `public` properties all produced `DALE036`; symbol actions carry no accessibility filter | GAP | intended | a per-property symbol action sees the declaration whatever its accessibility, and warning about a dead knob costs nothing |
| 26 | WHEN a rule walks a type's members THE SYSTEM SHALL be blind to a non-public or static property, so an `internal` `[InstantiationParameter]` is invisible to the gate that names it. | probe Q6 — `internal [InstantiationParameter][ServiceProperty] bool Hidden` with `[IncludedWhen("Hidden")]` produced `DALE043` *"service 'instantiation-parameters' has no service property 'Hidden'"* and no diagnostic on `Hidden` itself | GAP | ⚠ fix | the author is told the gate is wrong when the parameter is; a new `DALE044` message on the parameter, reported at **Warning** through the `effectiveSeverity` overload (row 6's precedent) (see sketch) |
| 27 | WHEN a property is an explicit interface implementation THE SYSTEM SHALL not walk it. | `AnalyzerHelper.cs:267` — an explicit implementation's `DeclaredAccessibility` is `Private` | GAP | intended | the binder cannot bind it either |
| 28 | THE SYSTEM SHALL treat a type as a logic block when `Vion.Dale.Sdk.Core.LogicBlockBase` appears anywhere in its base chain, and not when it *is* that type. | `AnalyzerHelper.cs:275-286` (starts at `type.BaseType`) | `IncludedWhenPredicateAnalyzerTests.ValidGateOnAComponent_NoDiagnostic` | intended | the base class itself declares no author's block |
| 29 | THE SYSTEM SHALL judge a logic block declared as a `class`; a logic block cannot be declared as a `record` or a `struct`. | `AnalyzerHelper.cs:275-286` (no `TypeKind` guard) with the callers' own guards — `IncludedWhenPredicateAnalyzer.cs:45`, `VisibleWhenPredicateAnalyzer.cs:41`, `ServiceRelationAnalyzer.cs:187`, `InstantiationParameterAnalyzer.cs:43`. Probe R1: `record RecordBlock : LogicBlockBase` is `CS8864` (*"Records may only inherit from object or another record"*), and Roslyn reports `TypeKind = Class` for a record anyway, so the guards and the helper agree | GAP | intended | the guard reads as a narrowing and is not one — the language refuses the only shape it could exclude |
| 30 | THE SYSTEM SHALL treat a property's type as a service-provider contract when the type itself or any of its interfaces carries `[ServiceProviderContractType]`. | `AnalyzerHelper.cs:88-106` | `ContractPropertyAnalyzerTests.ConcreteContractTypeWithoutSetter_ReportsDiagnostic` | intended | mirrors `DeclarativeContractBinder.IsContractType` for both the interface-typed and the concrete-typed shape |
| 31 | THE SYSTEM SHALL accept as a service-element type: `bool`, `string`, `byte`, `short`, `ushort`, `int`, `uint`, `long`, `float`, `double`, `DateTime`, `TimeSpan`, `Guid`, any enum, any flat readonly record struct, `ImmutableArray<T>` of the same, and `T?` of any value type. | `AnalyzerHelper.cs:115-150` (`Guid` at `:143`) | `ServiceElementTypeAnalyzerTests.SupportedTypes_NoDiagnostic`, `.Guid_ServiceProperty_NoDiagnostic` | intended | the supported set is the whole rich-type contract |
| 32 | THE SYSTEM SHALL reject `decimal`, `sbyte` and `ulong` as service-element types. | probe P7 — all three produced `DALE003`; `AnalyzerHelper.cs:137-139` omits them | `ServiceElementTypeAnalyzerTests.Decimal_WasAllowedNowRejected_ReportsDiagnostic`, `.SByte_ReportsDiagnostic`, `.ULong_ReportsDiagnostic` | intended | `decimal` is excluded by the rich-types spec; the two integer kinds have no wire representation |
| 33 | THE SYSTEM SHALL detect a record struct loaded from metadata through its synthesized `Deconstruct` method when `IsRecord` reports false. | `AnalyzerHelper.cs:191-204`, `:439-450` | `CrossAssemblyReadonlyRecordStructTests.RoslynMetadataReader_RecordStruct_IsRecordFalse_HasDeconstructMarker` | intended | the same struct from source and from a reference must be judged the same, and Roslyn does not say so |
| 34 | THE SYSTEM SHALL accept `TimeSpan` and `Guid` as flat record-struct field types. | `AnalyzerHelper.cs:229` | `StructServiceElementAnalyzerTests.ValidFlatReadonlyRecordStruct_NoDiagnostic` | intended | the field whitelist follows the member whitelist |
| 35 | WHEN a struct used as a service element is not a flat readonly record struct THE SYSTEM SHALL report `DALE016` naming the allowed field types, including `TimeSpan` and `Guid`. | message at `DaleDiagnostics.cs:149` says *"primitive, enum, string, or nullable-of-those parameters only"*; `AnalyzerHelper.cs:229` also accepts `TimeSpan` and `Guid` | `StructServiceElementAnalyzerTests.ReadonlyRecordStructWithNonFlatField_ReportsDiagnostic` | ⚠ fix | same shape as row 5, one level in — the message under-states the set the code accepts; message text only |
| 36 | THE SYSTEM SHALL treat a flat record struct with no positional constructor parameters as **not** flat, so a parameterless struct used as a service element is rejected. | `AnalyzerHelper.cs:214-218` returns `false` when no constructor has parameters | GAP | intended | a struct with no positional members carries no value the wire format can build |

### C. Contract and message shapes — `DALE001`, `009`, `010`, `011`, `019`, `020`, `025`, `045`

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 37 | WHEN a property typed as a service-provider contract has no setter THE SYSTEM SHALL report `DALE001`. | `ContractPropertyAnalyzer.cs:33-44` | `ContractPropertyAnalyzerTests.ContractPropertyWithoutSetter_ReportsDiagnostic` | intended | the bind-time refusal's compile-time door; `contracts.md` names the id |
| 38 | THE SYSTEM SHALL accept a setter of any accessibility for `DALE001`, including a `private set` declared on a base class. | `ContractPropertyAnalyzer.cs:39` (`SetMethod != null`) | `ContractPropertyAnalyzerTests.ContractPropertyWithPrivateSetter_NoDiagnostic` | intended | matches the binder after `BIND` row 206; journal `:246` records the shape the binder used to refuse and the analyzer never did |
| 39 | WHEN a contract-typed property with no setter is declared on an **interface** THE SYSTEM SHALL report `DALE001`, whose suggested remedy is a compile error there. | probe Q2 — `interface IHolder { IThing Held { get; } }` produced `DALE001` with the message *"Add at least a private setter: { get; private set; }"* | GAP | ⚠ fix | `MeasuringPointAnalyzer.cs:48-51` and `ImmutableArrayInitializationAnalyzer.cs:51-54` both guard `TypeKind.Interface` for exactly this reason; `DALE001` is the odd one out (see sketch) |
| 40 | WHEN a contract-typed property with no setter is declared on a class that is not a logic block THE SYSTEM SHALL report `DALE001`. | probe Q2 — a plain `class Poco { public IThing Held { get; } }` produced `DALE001` | GAP | intended | `DeclarativeContractBinder` binds a block's **components**, which are ordinary non-block classes, so the `Error` is well aimed there; a class no block holds is judged too, and warning about a declaration that could become a component costs nothing |
| 41 | WHEN a contract-typed property with no setter is declared `abstract` THE SYSTEM SHALL report `DALE001`. | probe Q2 — `abstract class AbstractHolder { public abstract IThing Held { get; } }` produced `DALE001` | GAP | intended | an override cannot add an accessor the base does not declare, so the abstract declaration is the actionable one — `MeasuringPointAnalyzer.cs:45-47` states the same reasoning |
| 42 | WHEN a `[LogicBlockContract]`'s `BetweenInterface` or `AndInterface` does not start with `I` THE SYSTEM SHALL report `DALE009` naming which one. | `ContractMessageAnalyzer.cs:47-63` | `ContractMessageAnalyzerTests.BetweenInterfaceMissingPrefix_ReportsDiagnostic`, `.AndInterfaceMissingPrefix_ReportsDiagnostic` | intended | the generator silently skips a non-conforming name |
| 43 | WHEN a `[Command]`, `[StateUpdate]` or `[RequestResponse]` nested in a contract names a `From` or `To` that is neither of the contract's two interfaces THE SYSTEM SHALL report `DALE010`. | `ContractMessageAnalyzer.cs:89-115` | `ContractMessageAnalyzerTests.CommandFromMismatch_ReportsDiagnostic`, `.CommandToMismatch_ReportsDiagnostic` | intended | the generator silently skips a mismatched value |
| 44 | THE SYSTEM SHALL check `From`/`To` only when both interface names are present, which the attribute's `required` members guarantee. | `ContractMessageAnalyzer.cs:89`, `:103`; probe P9 — omitting `AndInterface` is `CS9035`, so the shape is not expressible | `ContractMessageAnalyzerTests.ValidCommandFromTo_NoDiagnostic` | out-of-spec | the null guards are unreachable for a well-formed contract; the type system already refuses the case they exist for |
| 45 | WHEN a `[RequestResponse]`'s `ResponseType` is not a struct nested in the same contract class THE SYSTEM SHALL report `DALE011`. | `ContractMessageAnalyzer.cs:140-153` | `ContractMessageAnalyzerTests.ResponseTypeNotInSameContract_ReportsDiagnostic` | intended | violating it produces broken generated code |
| 46 | THE SYSTEM SHALL judge only the message structs **nested in** the contract class. | `ContractMessageAnalyzer.cs:66-69` (`type.GetTypeMembers()`) | `ContractMessageAnalyzerTests.NoLogicBlockContractAttribute_NoDiagnostic` | intended | the generator reads only nested structs, so the walk matches it |
| 47 | WHEN a `[Command]`, `[StateUpdate]` or `[RequestResponse]` struct is declared beside its contract class rather than nested THE SYSTEM SHALL report a diagnostic on the struct. | `ContractMessageAnalyzer.cs:66` never reaches it; `_findings.md:174-179` (`BIND` row 46); confirmed absent — no `DALE` id covers the shape | GAP | ⚠ fix | the ledger entry addressed to this pass, closed by a new `DALE047` at **Warning**. Consumer searched: none (all 53 message structs nested). One in-repo hit — `Vion.Dale.Sdk.Test/Configuration/Interfaces/GeneratedContractSurfaceShould.cs:235-237`, `BIND`'s deliberate stray struct — takes a `#pragma` (see sketch) |
| 48 | WHEN two attributes deriving from one platform base appear on a single property THE SYSTEM SHALL report `DALE019` once per attribute. | `MultipleAttributesFromSameBaseAnalyzer.cs:67-72` | `MultipleAttributesFromSameBaseAnalyzerTests.TwoSubclassesOfServiceProperty_ReportsDiagnostic` | intended | both squiggles say which two to choose between |
| 49 | THE SYSTEM SHALL accept `[ServiceProperty]` and `[ServiceMeasuringPoint]` on one property. | probe Q3 — the pair produced no `DALE019`; `MultipleAttributesFromSameBaseAnalyzer.cs:22-28` lists them as distinct bases | `MultipleAttributesFromSameBaseAnalyzerTests.ServicePropertyPlusMeasuringPoint_NoDiagnostic` | intended | dual annotation is the cross-fill rule's whole premise |
| 50 | THE SYSTEM SHALL treat `[ServiceProperty]`, `[ServiceMeasuringPoint]`, `[Presentation]` and `[StructField]` as the four platform bases for `DALE019`. | `MultipleAttributesFromSameBaseAnalyzer.cs:22-28` | GAP | intended | the four attributes an author can subclass into a preset |
| 51 | WHEN a class implements two interfaces declaring one property name with conflicting `Unit` values THE SYSTEM SHALL report `DALE020`. | `MultiInterfaceConflictAnalyzer.cs:69-102` | `MultiInterfaceConflictAnalyzerTests.TwoInterfacesConflictOnUnit_ReportsDiagnostic` | intended | the cascade rule has no way to pick a winner |
| 52 | THE SYSTEM SHALL suppress `DALE020` when the implementing class declares its own `[ServiceProperty]` or `[ServiceMeasuringPoint]` on that property. | `MultiInterfaceConflictAnalyzer.cs:93-97`, `:129-133` | `MultiInterfaceConflictAnalyzerTests.ConflictSuppressedByExplicitOverride_NoDiagnostic` | intended | the explicit attribute *is* the resolution |
| 53 | WHEN the implementing property carrying the resolving attribute is declared on a **base class** THE SYSTEM SHALL suppress `DALE020` as it does for a class-level declaration. | probe R3 — `class OnBase : BaseHolder, IA, IB` (the resolving `[ServiceProperty(Unit = "kW")]` on `BaseHolder`) produced `DALE020`; `class OnSelf : IA, IB` with the identical attribute on itself produced none. `MultiInterfaceConflictAnalyzer.cs:93` uses `type.GetMembers(name)`, own members only, unlike `AnalyzerHelper.EnumerateProperties` | GAP | ⚠ fix | the resolution the author already wrote is not seen, so a resolved conflict fails the build as unresolved with the remedy already in place (see sketch) |
| 54 | THE SYSTEM SHALL report `DALE020` only for a non-abstract class. | `MultiInterfaceConflictAnalyzer.cs:40-43` | GAP | intended | an abstract base has not finished choosing its interfaces |
| 55 | THE SYSTEM SHALL read a `Unit` only from the attribute's written named arguments, so two preset attributes that set it in their constructors do not conflict. | `MultiInterfaceConflictAnalyzer.cs:115-126` and the comment there | GAP | intended | a stated limitation with a stated reason; presets that share a unit are not conflicting |
| 56 | WHEN one property carries `[ServiceProperty]` and `[ServiceMeasuringPoint]` setting the same `Title`, `Description`, `Unit`, `Minimum` or `Maximum` to different values THE SYSTEM SHALL report `DALE025` once per conflicting field. | `CrossFillConflictAnalyzer.cs:21-23`, `:48-78` | `CrossFillConflictAnalyzerTests.ConflictingTitle_ReportsDiagnostic`, `.ConflictingUnit_ReportsDiagnostic` | intended | the cross-fill rule needs one source of truth per field |
| 57 | THE SYSTEM SHALL treat an empty or unset string field as absent for `DALE025`, so one attribute carrying the value and the other leaving it blank is not a conflict. | `CrossFillConflictAnalyzer.cs:85` (`!string.IsNullOrEmpty`) | `CrossFillConflictAnalyzerTests.SingleAttributeOnly_NoDiagnostic` | intended | that is cross-fill working, not failing |
| 58 | WHEN a `[ServiceRelation]` is declared on a class without `[LogicBlockContract]` THE SYSTEM SHALL report `DALE045` as an `Error`. | `ServiceRelationAnalyzer.cs:86-98` | `ServiceRelationAnalyzerTests.RelationOnANonContractClass_ReportsError` | intended | the relation's two sides are the contract's two interfaces |
| 59 | WHEN a `[ServiceRelation]`'s `RelationType` or `OutwardsInterface` is empty or whitespace THE SYSTEM SHALL report `DALE045` as an `Error`. | `ServiceRelationAnalyzer.cs:120-123`, `:144-147` | `ServiceRelationAnalyzerTests.EmptyRelationType_ReportsError`, `.WhitespaceOutwardsInterface_ReportsError` | intended | `RelationType` is the stable cloud-API key |
| 60 | WHEN one contract declares the same `RelationType` twice THE SYSTEM SHALL report `DALE045` as an `Error` on the duplicate only. | `ServiceRelationAnalyzer.cs:124-131` | `ServiceRelationAnalyzerTests.DuplicateRelationTypeOnOneContract_ReportsErrorOnTheDuplicate` | intended | duplicate rows crash the cloud activation projection on its composite primary key |
| 61 | WHEN an `OutwardsInterface` is neither the contract's `BetweenInterface` nor its `AndInterface` THE SYSTEM SHALL report `DALE045` as an `Error`. | `ServiceRelationAnalyzer.cs:148-154` | `ServiceRelationAnalyzerTests.OutwardsInterfaceNamingNeitherSide_ReportsError` | intended | the bind-time throw's compile-time door |
| 62 | WHEN one `RelationType` appears on two contracts in a compilation THE SYSTEM SHALL report `DALE045` as a `Warning` on each. | `ServiceRelationAnalyzer.cs:158-179`, `:342-351` | `ServiceRelationAnalyzerTests.TheSameRelationTypeOnTwoContracts_ReportsWarningOnBoth` | intended | legitimate for contract versioning, a collision when both are wired between the same two services |
| 63 | THE SYSTEM SHALL exclude a `RelationType` whose declaration was already reported as an `Error` from the cross-contract collision check. | `ServiceRelationAnalyzer.cs:132-142` (only the non-duplicate arm records) | `ServiceRelationAnalyzerTests.ABrokenDeclarationIsNotCountedForTheCrossContractCollision` | intended | one defect, one message |
| 64 | WHEN a logic block declares a public property whose type implements a relation-bearing contract interface but carries no service surface THE SYSTEM SHALL report `DALE045` as a `Warning`. | `ServiceRelationAnalyzer.cs:184-211` | `ServiceRelationAnalyzerTests.AServiceLessComponentOnARelationBearingContract_ReportsWarning` | intended | the endpoint wires normally yet emits no relation half — the silent case the warning exists for |
| 65 | THE SYSTEM SHALL resolve a relation-bearing interface both by symbol and by the base-list identifier written in source. | `ServiceRelationAnalyzer.cs:236-266`, `:269-293` | `ServiceRelationAnalyzerTests.AServiceLessComponentNamingAnUnresolvedContractInterface_ReportsWarning` (with `CS0246`) | intended | `sdk-surface-conventions.md:136-155`'s worked example — a generated contract interface is an error type in a real build |
| 66 | WHEN the logic block that declares a service-less relation-bearing component is defined in a **referenced** assembly THE SYSTEM SHALL report nothing. | `ServiceRelationAnalyzer.cs:50-52` (current-assembly filter) and `:192` (`type.GetMembers()`, own members only) | GAP | park | the same current-assembly boundary row 12 states as intended; a library author who inherits a block from a shared base gets the warning in the base's own build, not in theirs — widening it means judging a referenced assembly's declarations, which is a design decision, not a fix |

### D. Timers — `DALE002`, `DALE005`, `DALE012`

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 67 | WHEN a `[Timer]` method is not `void` or takes parameters THE SYSTEM SHALL report `DALE002` naming both faults. | `TimerMethodAnalyzer.cs:48-65` | `TimerMethodAnalyzerTests.TimerWithReturnTypeAndParameters_ReportsDiagnostic` | intended | over-determined with the binder's own refusal — `AC-LIFE-007.2` (`block-lifecycle.md:209`) states the runtime half |
| 68 | THE SYSTEM SHALL judge a `[Timer]` method of any accessibility. | `TimerMethodAnalyzer.cs:32` registers on `SymbolKind.Method` with no filter | GAP | intended | the binder walks `Public \| NonPublic` (`DeclarativeTimerBinder.cs:137`), so the analyzer must too |
| 69 | WHEN a `[Timer]`'s interval is zero or negative THE SYSTEM SHALL report `DALE005`. | `TimerMethodAnalyzer.cs:68-75` | `TimerMethodAnalyzerTests.TimerWithZeroInterval_ReportsDiagnostic`, `.TimerWithNegativeInterval_ReportsDiagnostic` | intended | over-determined three ways — the analyzer, `TimerAttribute`'s constructor (`TimerAttribute.cs:19-22`) and the binder |
| 70 | WHEN a `[Timer]`'s interval is `NaN` or infinity THE SYSTEM SHALL report `DALE005`. | probe Q5 — `[Timer(0.0 / 0.0)]` and `[Timer(1.0 / 0.0)]` produced no diagnostic; `TimerMethodAnalyzer.cs:71` (`intervalValue <= 0`), `TimerAttribute.cs:19` (same comparison) | GAP | ⚠ fix | the binder throws for exactly these two values (`DeclarativeTimerBinder.cs:94-101`), so all three compile-time guards let through what the runtime refuses (see sketch) |
| 71 | WHEN a `[Timer]`'s interval is `-0.0` THE SYSTEM SHALL report `DALE005`. | probe Q5 — `[Timer(-0.0)]` produced `DALE005` rendering *"[Timer(-0)]"* | GAP | intended | `-0.0 <= 0` holds; the edge value beside row 70 |
| 72 | WHEN two `[Timer]` methods **on one class** resolve to the same identifier THE SYSTEM SHALL report `DALE012` on the second. | `TimerMethodAnalyzer.cs:104-120` | `TimerMethodAnalyzerTests.DuplicateExplicitIdentifier_ReportsDiagnostic` | intended | last-write-wins in the callback dictionary is a silent bug |
| 73 | THE SYSTEM SHALL resolve a `[Timer]`'s identifier to its explicit second constructor argument, or to the method name when none is given. | `TimerMethodAnalyzer.cs:93-101` | `TimerMethodAnalyzerTests.ExplicitIdentifierMatchesOtherMethodName_ReportsDiagnostic` | intended | the collision an author cannot see is name-against-explicit |
| 74 | WHEN a `[Timer]` method on a base class and one on a derived class resolve to the same identifier THE SYSTEM SHALL report `DALE012`. | probe P3 — `Base.BaseTick` and `Derived.DerivedTick` both `[Timer(…, "tick")]` produced **no** diagnostic, while a same-class pair produced one; `TimerMethodAnalyzer.cs:85` uses `type.GetMembers()`, not `AnalyzerHelper.EnumerateProperties`'s base walk | GAP | ⚠ fix | the binder collects `[Timer]` methods across the whole base chain (`DeclarativeTimerBinder.cs:130-146`), so both reach the dictionary and one silently never ticks — the exact bug `DALE012` exists to catch (see sketch) |
| 75 | THE SYSTEM SHALL treat an `override` and the virtual it overrides as one timer. | `DeclarativeTimerBinder.cs:139` (`definitions.Add(method.GetBaseDefinition())`) | GAP | intended | the constraint any base-chain widening of row 74 must satisfy, or it invents a false positive on every override |

### E. Service-element types — `DALE003`, `008`, `016`, `017`, `018`

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 76 | WHEN a `[ServiceProperty]` or `[ServiceMeasuringPoint]` property's type is outside the supported set THE SYSTEM SHALL report `DALE003`. | `ServiceElementTypeAnalyzer.cs:31-49` | `ServiceElementTypeAnalyzerTests.CustomClassType_ReportsDiagnostic` | intended | an unsupported type cannot be represented as a `TypeRef` |
| 77 | WHEN a service-element property is an array or a mutable generic collection THE SYSTEM SHALL report `DALE008`. | `ImmutableArrayServiceElementAnalyzer.cs:58-87` (`T[]`, `List`, `IList`, `ICollection`, `IEnumerable`, `IReadOnlyList`, `IReadOnlyCollection` in `System.Collections.Generic`) | `ImmutableArrayServiceElementAnalyzerTests.ListOfDouble_ReportsDiagnostic` | intended | Dale value semantics require immutability |
| 78 | WHEN a service-element property is a collection type outside that seven THE SYSTEM SHALL report `DALE003` alone. | `ImmutableArrayServiceElementAnalyzer.cs:74-83` (no `Dictionary`, `HashSet`, `ISet`) | GAP | intended | `DALE003` already names the type as unsupported; `DALE008`'s "use `ImmutableArray<T>`" is not the right advice for a map |
| 79 | WHEN one declaration violates more than one supported-type rule THE SYSTEM SHALL report each independently. | probe Q9 — `[ServiceProperty] List<NotFlat>` produced `DALE003` **and** `DALE008` | `ServiceElementTypeAnalyzerTests.ListOfDouble_ReportsDiagnostic` + `ImmutableArrayServiceElementAnalyzerTests.ListOfDouble_ReportsDiagnostic` | intended | `sdk-surface-conventions.md:126-130` — the supported-type gate is three rules, and adding a type to one and not the others ships a type the build rejects |
| 80 | THE SYSTEM SHALL peel `Nullable<T>` and `ImmutableArray<T>` before judging a struct for `DALE016`. | `StructServiceElementAnalyzer.cs:66-76` | `StructServiceElementAnalyzerTests.NullableRegularStruct_ReportsDiagnostic`, `.ImmutableArrayOfRegularStruct_ReportsDiagnostic` | intended | the offender is the element type, not the wrapper |
| 81 | THE SYSTEM SHALL exempt `DateTime`, `TimeSpan` and `Guid` from `DALE016`. | `StructServiceElementAnalyzer.cs:95-98` | `StructServiceElementAnalyzerTests.Guid_NoDiagnostic`, `.SystemValueTypes_NoDiagnostic` | intended | the `v0.8.1` follow-up fix (`architecture/libraries/dale-sdk.md:84-85`) — exempted in **both** `DALE003` and `DALE016` |
| 82 | WHEN a service-element property is typed `string` in a nullable-disabled context THE SYSTEM SHALL report `DALE017`. | `NullableStringAnalyzer.cs:35-47` (`NullableAnnotation.None`) | `NullableStringAnalyzerTests.NullableDisabled_StringProperty_ReportsDiagnostic` | intended | the compiler cannot tell intent, and the wire format has to |
| 83 | WHEN a service-element property is an `ImmutableArray<T>` auto-property with no initializer THE SYSTEM SHALL report `DALE018`. | `ImmutableArrayInitializationAnalyzer.cs:30-91` | `ImmutableArrayInitializationAnalyzerTests.ImmutableArrayWithoutInitializer_ReportsDiagnostic` | intended | a default `ImmutableArray<T>` throws at access |
| 84 | THE SYSTEM SHALL exempt from `DALE018` an interface member, an abstract property, an expression-bodied property, and a property with an explicit `get` accessor. | `ImmutableArrayInitializationAnalyzer.cs:51-88` | `.InterfaceMember_…_NoDiagnostic`, `.AbstractProperty_…_NoDiagnostic`, `.ImmutableArrayExpressionBodiedProperty_NoDiagnostic`, `.ImmutableArrayWithBlockBodiedGetter_NoDiagnostic` | intended | none of the four can carry a property initializer, and an explicit getter is opaque |
| 85 | THE SYSTEM SHALL read the accessor list off each declaring syntax reference, so a partial property split across files is judged on whichever part declares it. | `ImmutableArrayInitializationAnalyzer.cs:62-88` (`foreach` over `DeclaringSyntaxReferences`, returning on the first exempting shape) | GAP | intended | the loop returns on any exempting declaration, which is the safe direction for a partial |
| 86 | WHEN a `[ServiceMeasuringPoint]` that is not also a `[ServiceProperty]` has a public setter THE SYSTEM SHALL report `DALE004`. | `MeasuringPointAnalyzer.cs:32-57` | `MeasuringPointAnalyzerTests.MeasuringPointWithPublicSetter_ReportsDiagnostic` | intended | measuring points are read-only metrics |
| 87 | THE SYSTEM SHALL exempt an interface declaration from `DALE004`, and keep reporting an abstract one. | `MeasuringPointAnalyzer.cs:43-51` and the comment there | `MeasuringPointAnalyzerTests.InterfaceMeasuringPointWithSetter_NoDiagnostic` | intended | the remedy is a compile error on an interface; a public abstract setter propagates to overrides that cannot narrow it |
| 88 | WHEN a `[Persistent]` property has no setter and does not set `Exclude` THE SYSTEM SHALL report `DALE007`. | `PersistentPropertyAnalyzer.cs:31-50` | `PersistentPropertyAnalyzerTests.PersistentGetOnly_ReportsDiagnostic`, `.PersistentExcludeTrue_NoDiagnostic` | intended | persistence silently skips a read-only property |

### F. Presentation hints — `DALE021`, `023`, `024`, `026`, `027`, `028`, `032`, `033`

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 89 | THE SYSTEM SHALL judge a `[Presentation]` hint on any property, whether or not it also carries `[ServiceProperty]` or `[ServiceMeasuringPoint]`. | probe Q4 — `[Presentation(UiHint="trigger")] int` with no service attribute produced `DALE023`; `[Presentation(Decimals=2)] string` produced `DALE021` | GAP | intended | a presentation hint on an unpublished member is inert either way, and the diagnostic is still the right advice; stating it is what stops the next reader assuming a service-attribute guard |
| 90 | WHEN `[Presentation(Decimals = …)]` is written on a non-numeric property THE SYSTEM SHALL report `DALE021`. | `DecimalsOnNonNumericAnalyzer.cs:41-64` | `DecimalsOnNonNumericAnalyzerTests.DecimalsOnString_ReportsDiagnostic` | intended | the hint is silently ignored |
| 91 | THE SYSTEM SHALL distinguish an unwritten `Decimals` from an explicit `Decimals = 0`. | `DecimalsOnNonNumericAnalyzer.cs:41-54` and the comment there | `DecimalsOnNonNumericAnalyzerTests.DecimalsUnset_NoDiagnostic` | intended | `int.MinValue` is the unset sentinel and `GetNamedArgument<int>` would return `0` for both |
| 92 | THE SYSTEM SHALL treat `decimal`, `sbyte` and `ulong` as numeric for `DALE021` though `DALE003` rejects them as service-element types. | `DecimalsOnNonNumericAnalyzer.cs:75-77` vs `AnalyzerHelper.cs:137-139` | GAP | intended | the two rules answer different questions, and `DALE003` already names the type |
| 93 | WHEN `[Presentation(UiHint = "trigger")]` is written on a property that is not a writable `bool` THE SYSTEM SHALL report `DALE023` naming which half failed. | `TriggerHintRequiresBoolAnalyzer.cs:36-52` | `TriggerHintRequiresBoolAnalyzerTests.TriggerOnNonBool_ReportsDiagnostic`, `.TriggerOnReadOnlyBool_ReportsDiagnostic` | intended | the dashboard commits `true` on click |
| 94 | THE SYSTEM SHALL match the `trigger` hint case-sensitively, so `UiHint = "Trigger"` draws no diagnostic and is not a trigger. | probe Q4 — `[Presentation(UiHint = "Trigger")]` on an `int` produced no `DALE023`; `TriggerHintRequiresBoolAnalyzer.cs:37` | GAP | park | the same casing the renderer matches on, so the analyzer agrees with the consumer; a "did you mean `trigger`?" diagnostic is a new id and a new vocabulary question — `UiHints` is an open constant set |
| 95 | WHEN `[Presentation(StatusIndicator = true)]` is written on a property whose type is not an enum or nullable enum THE SYSTEM SHALL report `DALE024`. | `StatusIndicatorRequiresEnumAnalyzer.cs:37-58` | `StatusIndicatorRequiresEnumAnalyzerTests.StatusIndicatorOnNonEnum_ReportsDiagnostic`, `.StatusIndicatorOnNullableEnum_NoDiagnostic` | intended | status mappings are silently absent otherwise; the descriptor records that this id replaces the retired `DALE006` |
| 96 | WHEN `[Presentation(Format = …)]` is written on a property that is not `DateTime` or `TimeSpan` THE SYSTEM SHALL report `DALE027`. | `FormatOnNonTemporalAnalyzer.cs:38-62` | `FormatOnNonTemporalAnalyzerTests.FormatOnString_ReportsDiagnostic` | intended | the renderer consumes `Format` only for the temporal types |
| 97 | WHEN `Format` is the `relative` sentinel on a non-`DateTime` property, or the `humanize` sentinel on a non-`TimeSpan` property, THE SYSTEM SHALL report `DALE028`. | `FormatSentinelTypeMismatchAnalyzer.cs:61-78` | `FormatSentinelTypeMismatchAnalyzerTests.RelativeOnTimeSpan_ReportsDiagnostic`, `.HumanizeOnDateTime_ReportsDiagnostic` | intended | the renderer falls back to the default formatter |
| 98 | WHEN a property is neither `DateTime` nor `TimeSpan` and carries a sentinel format THE SYSTEM SHALL report both `DALE027` and `DALE028`. | `FormatOnNonTemporalAnalyzer.cs:53` and `FormatSentinelTypeMismatchAnalyzer.cs:61` judge independently | GAP | intended | the same shape row 79 states for the supported-type gate |
| 99 | WHEN `[Presentation(Importance = Primary \| Secondary)]` is written on a flat record struct or an `ImmutableArray<T>` THE SYSTEM SHALL report `DALE032`. | `ImportanceRequiresScalarTypeAnalyzer.cs:45-71` | `ImportanceRequiresScalarTypeAnalyzerTests.ImportancePrimaryOnRecordStruct_ReportsDiagnostic` | intended | a struct stringifies to `[object Object]` on a dashboard tile; the first consumer carries five prose references to this rule |
| 100 | THE SYSTEM SHALL not report `DALE032` for a type outside the supported service-element set. | `ImportanceRequiresScalarTypeAnalyzer.cs:61` (`IsSupportedServiceElementType && !IsScalarTileType`) | `ImportanceRequiresScalarTypeAnalyzerTests.ImportancePrimaryOnScalar_NoDiagnostic` | intended | `DALE003`/`008`/`016` already name it, and a second message adds nothing |
| 101 | WHEN `StringFormat` is set on a non-`string` member, or set to a reserved type-kind format (`date-time`, `duration`, `uuid`) on a `string` member, THE SYSTEM SHALL report `DALE033`. | `StringFormatOnNonStringAnalyzer.cs:43-58` | `StringFormatOnNonStringAnalyzerTests.StringFormatOnNonString_ReportsDiagnostic`, `.ReservedFormatValueOnString_ReportsDiagnostic` | intended | those three formats have dedicated CLR types |
| 102 | WHEN a property carries both emission attributes and only the `[ServiceMeasuringPoint]` sets a misplaced `StringFormat` THE SYSTEM SHALL report `DALE033`. | probe P2 — `[ServiceProperty][ServiceMeasuringPoint(StringFormat="ipv4")] int Dual` produced **no** diagnostic while the same measuring point alone produced one; `StringFormatOnNonStringAnalyzer.cs:36-37` reads `ServiceProperty ?? ServiceMeasuringPoint` | GAP | ⚠ fix | the exact first-attribute-wins shape the `EMIT` pass fixed in `EmissionAttributeHelper.GetEmissionAttributes` (`:33-52`) and journal `:156` says to sweep for — one site was left (see sketch) |
| 103 | WHEN a literal `[Presentation(Group = "…")]` key matches no constant in a `PropertyGroup`-named static class THE SYSTEM SHALL report `DALE026`. | `LiteralGroupKeyAnalyzer.cs:82-102` | `LiteralGroupKeyAnalyzerTests.UnknownLiteral_ReportsDiagnostic` | intended | almost always a typo, and suppressible for a deliberate one-off |
| 104 | THE SYSTEM SHALL accept a symbolic constant reference as a `Group` key without judging it. | `LiteralGroupKeyAnalyzer.cs:89-93` | `LiteralGroupKeyAnalyzerTests.ConstantReference_NoDiagnostic` | intended | the first consumer's whole convention rests on it (`Ecocoach.EnergyManagement.Common/PropertyGroups.cs:10-12`) |
| 105 | WHEN a literal `Group` key matches a constant in a `PropertyGroup` class in a **referenced** assembly THE SYSTEM SHALL report no `DALE026`. | probe Q1 — the literal `"acme.powertrain"` with the matching class in a referenced assembly produced `DALE026`; with the identical class in source it produced nothing. `LiteralGroupKeyAnalyzer.cs:42` uses `compilation.GetSymbolsWithName`, which searches source declarations only | `LiteralGroupKeyAnalyzerTests.LiteralMatchingPlatformConstant_NoDiagnostic` (`:24`) — passes because `Helpers/TestAttributeStubs.cs:137-149` declares `PropertyGroup` as a **source** file the harness injects (`AnalyzerTestBase.cs:35`), never as metadata | GAP | ⚠ fix | every consumer sees `Vion.Dale.Sdk.Core.PropertyGroup` as metadata, so `[Presentation(Group = "status")]` warns in a consumer's build for a key the platform ships — and is silent in the SDK's own. `sdk-surface-conventions.md:136-155` names this class of blind spot (see sketch) |

### G. Access and redaction flags — `DALE022`, `DALE030`, `DALE040`

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 106 | WHEN `[ServiceProperty(WriteOnly = true)]` is written on a member that is not `string` or `string?` THE SYSTEM SHALL report `DALE022`. | `WriteOnlyTypeRestrictionAnalyzer.cs:30-50` | `WriteOnlyTypeRestrictionAnalyzerTests.WriteOnlyOnInt_ReportsDiagnostic` | intended | the redaction sentinel is a string literal |
| 107 | WHEN `[StructField(WriteOnly = true)]` is written on a struct member that is not `string` or `string?` THE SYSTEM SHALL report `DALE040`. | `WriteOnlyStructFieldTypeRestrictionAnalyzer.cs:29-54` | `WriteOnlyStructFieldTypeRestrictionAnalyzerTests.WriteOnlyOnNonStringField_ReportsDiagnostic` | intended | the per-member analogue of `DALE022`; a non-string member is rejected by the contracts schema parser at upload |
| 108 | THE SYSTEM SHALL accept `string?` for both, nullability being an annotation on one `SpecialType`. | `WriteOnlyTypeRestrictionAnalyzer.cs:42`, `WriteOnlyStructFieldTypeRestrictionAnalyzer.cs:46` and the comment there | `WriteOnlyStructFieldTypeRestrictionAnalyzerTests.WriteOnlyOnNullableStringField_NoDiagnostic` | intended | the shape an author writes for an optional secret |
| 109 | WHEN `[ServiceProperty]` sets both `ReadOnly = true` and `WriteOnly = true` THE SYSTEM SHALL report `DALE030`. | `ReadOnlyAndWriteOnlyExclusivityAnalyzer.cs:32-45` | `ReadOnlyAndWriteOnlyExclusivityAnalyzerTests.BothFlagsTrue_ReportsDiagnostic` | intended | the flags hide opposite directions of the value flow |
| 110 | THE SYSTEM SHALL judge `[StructField(WriteOnly)]` only on a constructor parameter. | `WriteOnlyStructFieldTypeRestrictionAnalyzer.cs:26` (`SymbolKind.Parameter`) | GAP | intended | a positional record struct's members *are* its primary-constructor parameters |
| 111 | WHEN a `[StructField]` is declared on a target other than a parameter THE SYSTEM SHALL report nothing. | `WriteOnlyStructFieldTypeRestrictionAnalyzer.cs:26` is the only `SymbolKind.Parameter` registration; row 129 | GAP | park | needs the attribute's own `AttributeTargets` narrowed or a new id; `[StructField]` is a published attribute and narrowing it is source-breaking (`_findings.md:174-179`'s shape) |

### H. Emission knobs — `DALE034`–`DALE039` (rules cited from `emission.md`)

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 112 | THE SYSTEM SHALL validate each emission attribute a member declares, so a dual-annotated member's `[ServiceMeasuringPoint]` knobs are judged as its `[ServiceProperty]`'s are. | `EmissionAttributeHelper.cs:39-52`; all six analyzers loop it | `MinIntervalInvalidAnalyzerTests.DualAnnotatedBothIntervalsBad_ReportsOnEachAttribute` and six siblings | intended (cite `AC-EMIT-012.7`) | the `EMIT` pass's headline fix; this page states only that the six analyzers share one reader — the rules are `emission.md`'s |
| 113 | WHEN `MinChange` is set on a member whose type has no built-in or visible `IChangeThreshold<T>` THE SYSTEM SHALL report `DALE034`. | `MinChangeWithoutChangeThresholdAnalyzer.cs:45-76` | `MinChangeWithoutChangeThresholdAnalyzerTests.MinChangeOnBool_ReportsDiagnostic` | intended (cite `AC-EMIT-012.1`) | `emission.md` states the rule; this page states only that the compile-time door exists |
| 114 | THE SYSTEM SHALL collect the visible `IChangeThreshold<T>` implementations once per compilation, across the compilation's own assembly and every referenced assembly that references the SDK. | `MinChangeWithoutChangeThresholdAnalyzer.cs:31-42`; `EmissionAttributeHelper.cs:284-328` | `MinChangeCrossAssemblyThresholdAnalyzerTests.MinChangeWithThresholdInReferencedAssembly_NoDiagnostic` | intended | the one precomputed set built at compilation start; the foundation-library pattern depends on it |
| 115 | WHEN a type implementing `IChangeThreshold<T>` is added to a compilation THE SYSTEM SHALL see it, the set being built at compilation start over the whole compilation. | `MinChangeWithoutChangeThresholdAnalyzer.cs:31-40` (compilation start, not per-file) | `MinChangeCrossAssemblyThresholdAnalyzerTests.MinChangeWithoutThresholdAnywhere_ReportsDiagnostic` | intended | the state-interaction question a compilation-start precompute always raises, and the answer here is that the whole compilation exists by then |
| 116 | WHEN `MinChange` is unusable by a built-in numeric or `TimeSpan` deadband THE SYSTEM SHALL report `DALE035`, and SHALL never parse-check a custom-threshold type's token. | `MinChangeUnparseableAnalyzer.cs:32-59`; `EmissionAttributeHelper.cs:134-168` | `MinChangeUnparseableAnalyzerTests.UnparseableOnCustomType_NoDiagnostic` | intended (cite `AC-EMIT-012.2`) | a custom threshold's format is its own to define |
| 117 | WHEN `MinInterval` is not a usable duration THE SYSTEM SHALL report `DALE036`, and WHEN it is positive and below 1 ms THE SYSTEM SHALL report `DALE037`. | `MinIntervalInvalidAnalyzer.cs:38-65` | `MinIntervalInvalidAnalyzerTests.Unparseable_ReportsError`, `.BelowFloorMicroseconds_ReportsWarning` | intended (cite `AC-EMIT-012.3`, `012.4`) | two ids from one analyzer, error and warning |
| 118 | THE SYSTEM SHALL report nothing for a `MinInterval` at the tick-representation boundary, configuring a negative interval instead. | probe P6 — `MinInterval = "922337203685477.6"` produced no diagnostic while `"922337203685478"` produced `DALE036`. `EmissionAttributeHelper.cs:267` uses `asTicks > long.MaxValue`; the boundary double **equals** `long.MaxValue` as a double, so the cast wraps | `MinIntervalInvalidAnalyzerTests.TooLargeToRepresent_ReportsError` covers the value one ulp above | ⚠ park | the runtime's `DurationParser.cs:120-125` carries the **identical** comparison, so the analyzer agrees with the runtime exactly and a one-sided fix would flag a token the runtime accepts. Both sides move together, and the runtime half is `emission.md`'s (`AC-EMIT-*`) |
| 119 | THE SYSTEM SHALL accept `"-0"` as a `MinInterval`, treating it as the throttle-disabling sentinel. | probe P6 — `MinInterval = "-0"` produced no diagnostic; `EmissionAttributeHelper.cs:233` (`value < 0` is false for `-0.0`) | GAP | intended | it resolves to zero ticks, which is what the sentinel means; `IsDisablingSentinel` (`:183-191`) is spelled in ticks, not in text |
| 120 | THE SYSTEM SHALL reject exponent notation in a duration token. | probe P6 — `MinInterval = "1e3"` produced `DALE036`; `EmissionAttributeHelper.cs:214-223` splits at the first non-numeric character, leaving `"e3"` as an unknown unit | GAP | intended | the grammar is a number and a unit suffix, and `e` is not a unit |
| 121 | THE SYSTEM SHALL accept surrounding whitespace, a leading `+`, and an upper-case unit suffix in a duration token. | probe P6 — `" 5 ms "`, `"+5ms"` and `"5MS"` all produced no diagnostic; `EmissionAttributeHelper.cs:207`, `:217`, `:226` | GAP | intended | the analyzer mirrors `DurationParser` exactly, spelling included |
| 122 | WHEN `Immediate = true` is declared alongside a non-default `MinInterval` or any `MinChange` THE SYSTEM SHALL report `DALE038` naming the ignored knob. | `ImmediateIgnoresThrottleKnobsAnalyzer.cs:33-65` | `ImmediateIgnoresThrottleKnobsAnalyzerTests.ImmediateWithBothKnobs_ReportsDiagnostic` | intended (cite `AC-EMIT-012.5`) | `Immediate` bypasses both gates |
| 123 | THE SYSTEM SHALL compare the declared interval against the default as a **duration**, so `"250"` and `"250ms"` are the same knob. | `ImmediateIgnoresThrottleKnobsAnalyzer.cs:45-49`; `EmissionAttributeHelper.cs:174-177` | `ImmediateIgnoresThrottleKnobsAnalyzerTests.ImmediateWithDefaultIntervalWrittenWithoutUnit_NoDiagnostic` | intended | reporting a knob as ignored on the strength of how it was typed is the defect this closes |
| 124 | WHEN `MinChange` is set while `MinInterval` resolves to zero and `Immediate` is not set THE SYSTEM SHALL report `DALE039` as information. | `DeadbandWithoutThrottleAnalyzer.cs:34-58` | `DeadbandWithoutThrottleAnalyzerTests.ZeroIntervalWithMinChange_ReportsInfo`, `.ZeroIntervalMinChangeButImmediate_NoDiagnostic` | intended (cite `AC-EMIT-012.6`) | a valid deadband-only configuration, surfaced so the intent is explicit |

### I. Observability — `DALE031`

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 125 | WHEN a computed `[ServiceProperty]` or `[ServiceMeasuringPoint]` getter reads a **property** of a struct-typed field or property this type owns THE SYSTEM SHALL report `DALE031`. | `ObservableStructMemberDependencyAnalyzer.cs:41-130` | `ObservableStructMemberDependencyAnalyzerTests.ExpressionBodied_ReadsStructMember_ReportsDiagnostic` | intended | the aspect does not track struct-property reads, so the value is permanently stale with no `LAMA51xx` |
| 126 | THE SYSTEM SHALL not report `DALE031` for a struct **field** read, a method call, a `nameof`, a whole-value read, a reference-typed instance, a static, a local, a read through another object, a `readonly` field, a get-only property, or a field declared by a base type. | `ObservableStructMemberDependencyAnalyzer.cs:87-118`, `:169-214` | eleven `_NoDiagnostic` tests in that suite | intended | each exemption is a claim about Metalama's tracking, and journal `:165` records the one that was wrong (the field half) being narrowed |
| 127 | THE SYSTEM SHALL report `DALE031` for the `X?.Member` shape as well as `X.Member`. | `ObservableStructMemberDependencyAnalyzer.cs:136-164` | `.NullableFieldRootStructMemberRead_ReportsDiagnostic` | intended | a nullable struct is a `ConditionalAccessExpression`, not a `MemberAccessExpression`, and is the shape VION-81 was filed against |
| 128 | THE SYSTEM SHALL report `DALE031` at most once per (computed property, struct-valued member) pair. | `ObservableStructMemberDependencyAnalyzer.cs:72`, `:120-123` | GAP | intended | one dependency is one finding however many times it is read |
| 129 | THE SYSTEM SHALL report `DALE031` for a computed property carrying an emission attribute, whether or not the containing type is a logic block. | `ObservableStructMemberDependencyAnalyzer.cs:60-63` (`IsObservableServiceMember` only), `:66-69` (interface guard) | `.NonObservableComputedProperty_NoDiagnostic` | intended | the aspect weaves any service type, block or component |

### J. Public-API documentation — `DALE013`, `DALE014`, `DALE015`

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 130 | WHEN a `[PublicApi]` type has no `<summary>` documentation THE SYSTEM SHALL report `DALE013`. | `PublicApiDocumentationAnalyzer.cs:79-86` | `PublicApiDocumentationAnalyzerTests.DALE013_PublicApiWithoutSummary_ReportsWarning` | intended | the docs site's API reference is generated from these comments |
| 131 | WHEN a public type in a `[PublicApiNamespace]` carries neither `[PublicApi]` nor `[InternalApi]` THE SYSTEM SHALL report `DALE014`. | `PublicApiDocumentationAnalyzer.cs:88-92` | `.DALE014_PublicTypeInApiNamespace_WithoutAttribute_ReportsWarning` | intended | the manifest ratchet's only door |
| 132 | WHEN a `[PublicApiNamespace]` matches no public type THE SYSTEM SHALL report `DALE015`. | `PublicApiDocumentationAnalyzer.cs:95-106` | `.DALE015_StaleNamespace_ReportsWarning` | intended | a stale namespace declaration silently stops asking for marks |
| 133 | THE SYSTEM SHALL judge only types declared in source, never a referenced assembly's. | `PublicApiDocumentationAnalyzer.cs:61-64` | `.DALE014_TypeInNonConfiguredNamespace_NoDiagnostic` | intended | row 12's boundary |
| 134 | THE SYSTEM SHALL judge a **nested** public type for `DALE013` and `DALE014` as it judges a top-level one. | probe P4 — a nested `[PublicApi]` type with no `<summary>` produced no `DALE013` and a nested unmarked public type produced no `DALE014`, while the top-level control produced one. `PublicApiDocumentationAnalyzer.cs:109-123` walks `ns.GetTypeMembers()` and never recurses into a type's own type members | GAP | ⚠ fix | a public nested type is public API, appears in the manifest, and is documented by the docs generator — and neither rule has ever asked about one (see sketch) |
| 135 | WHEN two `[PublicApiNamespace]` declarations are prefixes of one another THE SYSTEM SHALL credit each with the types it actually matches. | probe P5 (4 runs) — with `[PublicApiNamespace("Api")]` and `[PublicApiNamespace("Api.Sub")]` and one public type in `Api.Sub`, `DALE015` reported `"Api.Sub"` as stale every run. `PublicApiDocumentationAnalyzer.cs:67-73` records only the **first** matching prefix | GAP | ⚠ fix | a namespace with types is reported stale, and the remedy the message suggests — delete the declaration — silently drops the ratchet for that namespace (see sketch) |
| 136 | THE SYSTEM SHALL match a `[PublicApiNamespace]` against a namespace and every namespace beneath it. | `PublicApiDocumentationAnalyzer.cs:67` (`ns == pn \|\| ns.StartsWith(pn + ".")`) | GAP | intended | one declaration covers a subtree, which is why row 135's overlap is reachable at all |
| 137 | WHEN a `[PublicApi]` type's XML documentation is not generated THE SYSTEM SHALL report `DALE013` for every such type. | `PublicApiDocumentationAnalyzer.cs:81-82` reads `GetDocumentationCommentXml()`, which returns the comment whether or not the project emits a documentation file | GAP | intended | the rule is about the comment in source, not the build's doc-file setting |

### K. Gating, instantiation and visibility — `DALE041`–`DALE044` (rules cited from `config-gating.md`)

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 138 | WHEN a `[Presentation(VisibleWhen = …)]` predicate does not parse or resolve THE SYSTEM SHALL report `DALE041`, and WHEN it violates the type discipline `DALE042`. | `VisibleWhenPredicateAnalyzer.cs:61-80` | 23 tests in `VisibleWhenPredicateAnalyzerTests` | intended (cite `config-gating.md`) | the predicate dialect is `GATE`'s; this page states only that the analyzer parses and type-checks and never evaluates |
| 139 | WHEN a `[Presentation(VisibleWhen = …)]` is declared on an **abstract** logic block whose compilation contains no concrete subclass THE SYSTEM SHALL report nothing. | probe Q7 — an abstract block with `VisibleWhen = "NoSuchProperty"` produced no diagnostic; adding a concrete subclass (Q7b) produced `DALE041`. `VisibleWhenPredicateAnalyzer.cs:41` (`block.IsAbstract` guard); `IncludedWhenPredicateAnalyzer.cs:45` has no such guard | GAP | ⚠ fix | a library of abstract base blocks — the first consumer's shape — gets none of its own predicates validated in its own build, and the two predicate analyzers disagree about the same declaration (see sketch) |
| 140 | WHEN one component type is held by two properties of a block THE SYSTEM SHALL validate its annotated members against the first holder only. | `VisibleWhenPredicateAnalyzer.cs:100-111`, `:143-146` (`seen` keyed by property symbol) | GAP | park | the predicate resolves against the holder's own service map, and two holders of one type have identical maps, so no reachable outcome differs today — it differs only if a future rule makes the holder's identity matter |
| 141 | WHEN an `[IncludedWhen]` gate does not parse, names a qualified reference, resolves to no `[InstantiationParameter]`, sits outside the gateable set, or re-declares a base gate THE SYSTEM SHALL report `DALE043`. | `IncludedWhenPredicateAnalyzer.cs:128-181` | 14 cited tests in `IncludedWhenPredicateAnalyzerTests` | intended (cite `AC-GATE-011.1`–`011.5`, `011.7`, `011.10`) | `config-gating.md:305-335` states each firing rule; citing, never re-minting |
| 142 | WHEN an `[InstantiationParameter]` violates its discipline, or a referencing `[IncludedWhen]` predicate has a type error, THE SYSTEM SHALL report `DALE044`. | `IncludedWhenPredicateAnalyzer.cs:276-279`; `InstantiationParameterAnalyzer.cs:140`, `:212` | 13 cited tests in `InstantiationParameterAnalyzerTests` | intended (cite `AC-GATE-011.4`, `011.6`, `011.8`, `011.9`, `011.11`) | the one id with two reporters — this page states that fact; the rules are `GATE`'s |
| 143 | THE SYSTEM SHALL report `DALE044` for an assignment to an `[InstantiationParameter]` outside its declaring type's constructor or object initializer. | `InstantiationParameterAnalyzer.cs:119-143` (the one operation action) | `InstantiationParameterAnalyzerTests.InCodeAssignmentOutsideConstructor_ReportsDALE044` | intended (cite `AC-GATE-011.8`) | the one non-symbol registration in the family |
| 144 | THE SYSTEM SHALL judge an assignment only where the assigning code is in the parameter's own declaring type. | `InstantiationParameterAnalyzer.cs:129-132` | GAP | intended | another block writing your parameter is that block's business, and the platform writes it from outside by design |
| 145 | WHEN an `[IncludedWhen]` is declared on a type or member that is not part of a logic block THE SYSTEM SHALL report `DALE043` saying the gate is never evaluated. | `IncludedWhenPredicateAnalyzer.cs:51`, `:58-78` | `IncludedWhenPredicateAnalyzerTests.GateOnAComponentMember_ReportsDALE043`, `.GateOnAComponentClass_ReportsDALE043` | intended (cite `AC-GATE-011.10`) | all three of `[IncludedWhen]`'s declared targets are read, on a block and off it |

### L. Scenario wire — `DALE046`

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 146 | WHEN a `[ScenarioWire]` `Inbound` or `Outbound` type is not a value struct whose members are all representable leaves or nested readonly record structs of the same THE SYSTEM SHALL report `DALE046` naming the member path. | `ScenarioWireTypeAnalyzer.cs:33-146` | `ScenarioWireTypeAnalyzerTests.ADelegateInsideANestedWireStruct_ReportsDiagnostic` | intended (cite `scenarios.md`) | the codec builds the wire value from the struct's positional constructor |
| 147 | WHEN the wire type is an unresolved error type THE SYSTEM SHALL report nothing. | `ScenarioWireTypeAnalyzer.cs:56-59` | `ScenarioWireTypeAnalyzerTests.AnUnresolvedWireType_NoDiagnostic` (with `CS0246`) | intended | the compile error already says it; a second message would mislead |
| 148 | WHEN both directions declare an unrepresentable type THE SYSTEM SHALL report `DALE046` twice. | `ScenarioWireTypeAnalyzer.cs:43-68` | `ScenarioWireTypeAnalyzerTests.BothDirectionsUnrepresentable_ReportsTwice` | intended | two declarations, two findings |
| 149 | THE SYSTEM SHALL judge the widest non-copy positional constructor of a wire struct. | `ScenarioWireTypeAnalyzer.cs:109-117`, `:200-203` | `ScenarioWireTypeAnalyzerTests.AMultiFieldStructWithEnumNestedNullableAndTimestamp_NoDiagnostic` | intended | the codec reflects over the positional constructor, and a copy constructor is not one |
| 150 | WHEN one struct type appears twice in a wire type's member graph THE SYSTEM SHALL judge only its first occurrence. | `ScenarioWireTypeAnalyzer.cs:104-107` (`visited` is never released) | GAP | park | a second occurrence of a struct already judged representable is representable; a second occurrence of one already judged *un*representable is unreachable, because the first occurrence returned. Guarding it needs a per-path visited set, and the outcome differs for no shape I could construct |

### M. Packaging, the pack, the wiring probe, the generator's own diagnostics

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 151 | THE SYSTEM SHALL ship the analyzers inside the `Vion.Dale.Sdk` package under `analyzers/dotnet/cs`, so a consumer that references the package receives them. | `Vion.Dale.Sdk.csproj:92`; `templates/vion-iot-library/VionIotLibraryTemplate/VionIotLibraryTemplate.csproj:23` | GAP | intended | the one delivery path for every out-of-repo consumer |
| 152 | WHEN the generator assembly is absent at pack time THE SYSTEM SHALL produce a package with no analyzers and no warning. | `Vion.Dale.Sdk.csproj:92` (`Condition="Exists(…)"`) | GAP | ⚠ park | a consumer of that package silently loses all 44 diagnostics; the assertion belongs in the post-pack artifact gate (`scripts/verify-packed-assembly-versions.ps1`, `publish.yml:72-93`), which is the release process's, not this area's (see sketch) |
| 153 | THE SYSTEM SHALL judge the declarations of `Vion.Dale.Sdk`, `.DigitalIo`, `.AnalogIo`, `Vion.Dale.Sdk.Test`, `Vion.Dale.DevHost.Test`, `Vion.Dale.DevHost.SmokeHost` and the two `LogicBlockParser.Test.*Plugin` projects in an ordinary build of this repository. | eight unconditional `OutputItemType="Analyzer"` references, enumerated above | `AnalyzerWiringShould.RunTheDaleAnalyzersOverTheIoProjects` for two of them | intended | the eight are the first consumers, and a new diagnostic that fires in-repo shows there first |
| 154 | THE SYSTEM SHALL judge the examples' declarations only under `-p:DaleLocalSource=true`. | the nine example csprojs' `ItemGroup Condition="'$(DaleLocalSource)' == 'true'"`, e.g. `examples/Vion.Examples.Emission/Vion.Examples.Emission/Vion.Examples.Emission.csproj:28-32` | GAP | intended | the default build judges them with the **published** analyzers, which is why `testing-conventions.md` § 1 says an example cannot prove a same-PR analyzer change |
| 155 | THE SYSTEM SHALL judge no declaration in `Vion.Dale.Sdk.Http`, `.Modbus.Core`, `.Modbus.Tcp` or `.Modbus.Rtu`. | none of the four carries the reference | GAP | intended | `sdk-surface-conventions.md:259-262` names it as known non-conforming code with the remedy and the reason not to do it as a drive-by |
| 156 | THE SYSTEM SHALL judge no declaration in `Vion.Dale.Sdk.Generators.Test` itself. | `Vion.Dale.Sdk.Generators.Test.csproj:22` — a plain `ProjectReference`, no `OutputItemType` | GAP | intended | the analyzer test project's fixtures are deliberately illegal, and this is why none of them needs a `#pragma` |
| 157 | THE SYSTEM SHALL fail a build of `Vion.Dale.Sdk.DigitalIo` or `.AnalogIo` with `DALE046` when the analyzer-wiring probe is linked in. | `AnalyzerWiringShould.cs:66-77`; `AnalyzerWiring/ScenarioWireWiringProbe.cs` | `AnalyzerWiringShould.RunTheDaleAnalyzersOverTheIoProjects` (cites `AC-INTRO-017.4`) | intended | an analyzer that is referenced is not necessarily running (`testing-conventions.md:54-59`) |
| 158 | THE SYSTEM SHALL keep the probe out of an ordinary build. | `AnalyzerWiringShould.cs:80-88` | `AnalyzerWiringShould.KeepTheProbeOutOfAnOrdinaryBuild` | intended | the probe is a gate only as long as it is invisible the rest of the time |
| 159 | THE SYSTEM SHALL send the probe's child build outputs to a disposable directory, leaving this repository's own build outputs untouched. | `AnalyzerWiringShould.cs:90-124`, `:135-142` | `AnalyzerWiringShould.LeaveTheBuildOutputsOfItsDependencyGraphUntouched` | intended | release 0.11.1 is what happens without it (journal `:140`); the CI-shape hazard is stated, not re-guarded (`D4`) |
| 160 | THE SYSTEM SHALL report the source generator's own findings under the `SourceGenerator` category with ids `LogicClassGeneratorINF` and `LogicClassGeneratorERR`, untagged and outside the registry. | `DiagnosticsExtensions.cs:20-45`; `LogicClassGenerator.cs` — 24 `LogInfo`, 4 `LogError` calls | GAP | intended | they vanish when the generator does not run, and no `.editorconfig` `DALE` prefix reaches them |
| 161 | THE SYSTEM SHALL not report `LogicClassGeneratorDBG` in any build. | `DiagnosticsExtensions.cs:7` is the only occurrence of `LogDebug` in the SDK; zero call sites | GAP | ⚠ fix | a public helper minting a descriptor nothing reports — surface minimalism, delete-don't-deprecate (`sdk-surface-conventions.md` § 1) (see sketch) |
| 162 | THE SYSTEM SHALL suppress `RS2008` in the analyzer project. | `Vion.Dale.Sdk.Generators.csproj:19`, with the reason *"analyzer is internal (not shipped as a NuGet package)"* at `:17` | GAP | ⚠ fix | the stated reason is false — the DLL is packed to `analyzers\dotnet\cs\` and reaches every consumer. A comment is a claim (`comment-conventions.md`) (see sketch) |
| 163 | THE SYSTEM SHALL compile `PredicateParser.cs` and `PredicateSyntax.cs` into both the analyzer assembly and the runtime assembly from one source. | `Vion.Dale.Sdk.csproj:81-82` (`LinkBase="Shared"`) | `PredicateConformanceVectorTests` + `PredicateParserShould` (the SDK side) | intended | a second implementation of the grammar would drift from `Vion.Contracts`', which decides the syntax |
| 164 | WHEN a consumer's build carries a `[SuppressMessage]` naming a `DALE` id THE SYSTEM SHALL honour it as it honours a `#pragma`. | every descriptor is an ordinary `DiagnosticDescriptor` with no `NotConfigurable` tag | GAP | intended | the consumer sweep's answer for the third suppression mechanism; none of the 44 opts out of configuration |

### N. Suppressions in this repository, and the test discipline

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 165 | THE SYSTEM SHALL treat a `#pragma warning disable DALEnnn` as the author's declaration that the shape is intended; this repository's fixtures carry 27 of them across 14 files, each beside the rule it violates on purpose. | enumerated: `IntrospectionBlocks.cs:81,113,163,211,302,359,376,462`; `GatingBlocks.cs:358,364`; `LifecycleHarness.cs:381`; `ContractBindingShould.cs:209`; `GeneratedContractSurfaceShould.cs:241`; `ContractCarriedServiceRelationsShould.cs:54,175`; `DeclarativeTimerBinderShould.cs:208,326`; `MetalamaStructMemberDependencyReproShould.cs:201`; `ThrottleConfiguredShould.cs:58`; `LogicBlockIntrospectionShould.cs:107`; `PersistentDataShould.cs:356`; `ScenarioWireCodecShould.cs:189`; `EmissionPolicyShould.cs:504,520,536,553`; `CustomThresholdEmissionPolicyShould.cs:67` | the suppressed fixtures compile clean | intended | twelve of them sit in three shared fixture files rather than in suites, which is why deleting a fixture's suppression breaks tests in several suites at once |
| 166 | THE SYSTEM SHALL find no stale suppression among the 27 — every one names a diagnostic its span still triggers. | each site read this session against the rule it names | GAP | intended | the classification question 10 asks; the answer is that none is stale |
| 167 | THE SYSTEM SHALL carry a reason comment on a suppression whose intent is not obvious from the fixture. | 4 of 27 carry one — `MetalamaStructMemberDependencyReproShould.cs:201`, `IntrospectionBlocks.cs:113,163,462` | GAP | park | a lint for "every `#pragma warning disable DALE` carries a trailing reason" is a new gate script and a 23-site edit across five other areas' fixtures — a retro decision, not a pass's |
| 168 | THE SYSTEM SHALL compile every analyzer test against the stub attribute source read from `Helpers/TestAttributeStubs.cs` at three directory levels above the test assembly. | `Helpers/AnalyzerTestBase.cs:58-67` | every test in the project | intended | a build-layout dependency: the file is read off disk at runtime, not embedded, so a changed output path breaks every test in the project with a `TypeInitializationException` |
| 169 | THE SYSTEM SHALL pin the language version to `LanguageVersion.Latest` for every analyzer test compilation. | `Helpers/AnalyzerTestBase.cs:37-45` | `ObservableStructMemberDependencyAnalyzerTests` (the C# 13 `field` keyword cases) | intended | otherwise the framework's default decides which C# an analyzer is tested against |
| 170 | THE SYSTEM SHALL include, for every analyzer that keys off a contract interface, a test whose contract interface is genuinely unresolved. | only `ServiceRelationAnalyzerTests` uses `CS0246` as § 5's pin (a Dale diagnostic **beside** the compile error); `ScenarioWireTypeAnalyzerTests:177` uses `CS0246` to pin the opposite (silence) | GAP | ⚠ fix | `ContractPropertyAnalyzer`, `ContractMessageAnalyzer` and `IncludedWhenPredicateAnalyzer.IsGateable` all key off contract interfaces and none is pinned, so each could be dead in a Metalama-hosted build and every test would still pass (see sketch) |

### O. The four ledger entries addressed to this pass

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 173 | WHEN an endpoint's `Identifier =` is blank or collides with another endpoint's on the same block THE SYSTEM SHALL report a diagnostic. | `_findings.md:30-35`; the bind-time refusal is `AC-INTRO-014.3`/`014.4`; no `DALE` id covers the shape — no analyzer reads an `Identifier` named argument (grepped across `Analyzers/`) | GAP | ⚠ park | a new whole-type analysis over two attribute families and two declaration levels, plus eight deliberate `INTRO` fixtures to suppress; it failed the `INTRO` pass's size guard already and fails this one's (see sketch) |
| 174 | WHEN a `[ServiceProviderContractType]` token is empty or whitespace THE SYSTEM SHALL report a diagnostic. | `_findings.md:207-212`; `Vion.Dale.Sdk/Configuration/Contract/ServiceProviderContractTypeAttribute.cs:28-31` assigns with no validation; no `DALE` id covers it | GAP | ⚠ fix | the token is a cloud-facing stable identifier (`identifier-stability.md`) and a blank one reaches the document. A new `DALE048` at **Error**: the consumer's eight tokens and every analyzer-judged in-repo project's were searched and none is blank (see sketch) |
| 175 | WHEN two interfaces in one compilation declare the same `[ServiceProviderContractType]` token THE SYSTEM SHALL report a diagnostic. | `_findings.md:207-212`; a whole-compilation analysis, and `ServiceRelationAnalyzer.cs:50-52`'s current-assembly filter is the same boundary question row 12 states | GAP | ⚠ park | a duplicate reaches the document as two contract types with one name, but the check is a whole-compilation analysis owing `WellKnownDiagnosticTags.CompilationEnd` (§ 4, row 7) and `ServiceRelationAnalyzer`'s current-assembly boundary — its own change doc (see sketch) |
| 176 | THE SYSTEM SHALL narrow the source generator's candidate predicate so it does not run on every class whose attribute name contains `Contract`. | `_findings.md:219-223`; `Vion.Dale.Sdk.Generators/LogicClassGenerator.cs:36-40` | GAP | ⚠ park | the predicate is the incremental generator's cache key, so its breadth is a build-time cost every consumer pays with **no functional observable** — nothing silent-and-wrong to close, and unmeasurable without a build-time benchmark (see sketch) |


### P. Observables the extraction missed (second opinion, amendment 1)

| # | Behavior (EARS) | Evidence | Test today | Rec | Why |
|---|---|---|---|---|---|
| 177 | WHEN `[StructField(StringFormat = …)]` is set on a struct member that is not `string` or `string?` THE SYSTEM SHALL report `DALE033`. | `StringFormatOnNonStringAnalyzer.cs:36-37` reads the two **property** attributes only, while `StructFieldAttribute.cs:36` declares `StringFormat` and `Vion.Dale.Sdk/Introspection/TypeRefBuilder.cs:60` consumes it | GAP | ⚠ fix | the per-member analogue of `DALE033` that rows 107/110 have for `WriteOnly` and nothing has for `StringFormat`. Both live sites are `string` fields — verified: `Vion.Dale.DevHost.SmokeHost/LogicBlocks/ShowcaseBlock.cs:199` (`string Address`) and `logic-block-libraries/…/Shared/ModbusConnection.cs:38` (`string IpAddress`) — so a Warning fires nowhere today |
| 178 | THE SYSTEM SHALL report the source generator's error diagnostic with a constant message format, so a message containing a brace does not break the report. | `DiagnosticsExtensions.cs:35-40` passes the caller's `message` as the descriptor's `MessageFormat`; `LogicClassGenerator.cs:662` passes an embedded-resource name straight in | GAP | ⚠ fix | question 3's real defect: a brace in a template error message reaches `Diagnostic.Create` as a format placeholder and throws inside the generator, replacing the diagnostic with a generator crash — same anchor as row 161 |
| 179 | WHEN a property carries both emission attributes THE SYSTEM SHALL read `Unit` and the interface-conflict bucket from each, not from whichever is found first. | `MultiInterfaceConflictAnalyzer.cs:52` (bucketing) and `:108` (`GetUnit`) both use `GetAttribute(ServiceProperty) ?? GetAttribute(ServiceMeasuringPoint)` | `MultiInterfaceConflictAnalyzerTests.TwoInterfacesConflictOnUnit_ReportsDiagnostic` (single-annotated only) | ⚠ fix | row 102's sweep is two sites short: a dual-annotated interface property whose measuring point carries the differing `Unit` never registers as a conflict. Journal `:156` — sweep the shape, not the symbol |
| 180 | THE SYSTEM SHALL collect group-key constants only from a static class named exactly `PropertyGroup`, in any namespace. | `LiteralGroupKeyAnalyzer.cs:42` (`GetSymbolsWithName("PropertyGroup", …)`, an exact match); `LiteralGroupKeyAnalyzerTests.LiteralMatchingIntegratorConstant_NoDiagnostic:52-68` declares `Acme.PropertyGroup` — the intended integrator shape | `LiteralGroupKeyAnalyzerTests.LiteralMatchingIntegratorConstant_NoDiagnostic` | intended | the descriptor's *"a `PropertyGroup`-named static class"* means named `PropertyGroup`, not suffixed with it; the first consumer's `EcoPropertyGroup` / `SteeringPropertyGroup` constants are therefore never collected, which reaches nobody because it references them symbolically (`Ecocoach.EnergyManagement.Common/PropertyGroups.cs:10-12`) |
| 181 | WHEN an abstract logic block's `VisibleWhen` predicate names a property only its subclasses declare THE SYSTEM SHALL report `DALE041` at the abstract declaration. | the outcome of row 139's fix: `VisibleWhenPredicateAnalyzer.cs:41`'s `IsAbstract` guard is what suppresses it today | GAP | intended | the edge value beside the path row 139 opens, and plausibly what the guard was for — stated so an author reading the page knows the predicate must resolve where it is written. No in-repo or consumer abstract block carries a `VisibleWhen` |
| 182 | THE SYSTEM SHALL judge a nested public type by its **effective** accessibility, so a public type nested in an `internal` type is not asked for a `[PublicApi]` mark. | `INamedTypeSymbol.DeclaredAccessibility` reports `Public` for such a type regardless of its container; row 134's recursion reaches it | GAP | ⚠ fix | the edge value row 134 opens: recursion would newly demand a mark on a type nothing outside the assembly can reach — a warning with no action behind it |
| 183 | WHEN one `[PublicApiNamespace]` subsumes another THE SYSTEM SHALL report neither as stale once either matches a type. | the outcome of row 135's fix; `PublicApiDocumentationAnalyzer.cs:41-47` builds the set as an `ImmutableHashSet`, so which declaration is credited today is **unordered**, not "the first" | GAP | intended | the capability `DALE015` loses when every prefix is credited: a redundant subsumed declaration stops being reported. The tree's one live overlap is `Vion.Dale.Sdk.Modbus.Tcp/PublicApiConfig.cs:3-4`, outside the pack (row 155) |

---
---

## Failure sketches for the ⚠ rows

**Row 5 — `DALE003`'s message omits `Guid` (fix).**
An author writes `[ServiceProperty] public string Serial { get; set; }` in a nullable-disabled file,
reads the `DALE003` list while fixing something adjacent, and concludes `Guid` is unsupported —
because the message enumerates the supported set and `Guid` is not in it. *Fix:* add `Guid` to the message text at `DaleDiagnostics.cs:43`. Nothing pins the literal — grepped
both repositories; only this change doc quotes it. Severity and firing shapes unchanged.

**Row 17 — a preset attribute is invisible to every rule but `DALE019` (park).**
An integrator declares `public class Kilowatts : ServicePropertyAttribute`, writes
`[Kilowatts] public List<double> Bands { get; set; }`, and gets no `DALE003` and no `DALE008`
(probe R2): `AnalyzerHelper.HasAttribute` compares the attribute class's own full name and never
walks its base chain, so the member is judged by nothing and reaches introspection as an unsupported
type. Preset attributes are a first-class documented pattern — `examples/Vion.Examples.Presentation/
Vion.Examples.Presentation/Conventions/` ships sixteen of them, and
`Vion.Dale.Sdk.Test/Core/AttributeInheritanceShould.cs` pins that the runtime honours them.
*Parked:* the change is one helper, but it re-aims all 43 remaining rules at once and the blast
radius on out-of-repo consumers is unbounded — a fielded preset-annotated declaration that was
quietly non-conforming becomes an `Error`. Readers: every consumer with preset attributes; in-repo
`examples/Vion.Examples.Presentation/Conventions/` (16, conditional) and
`Vion.Dale.Sdk.Test/Core/AttributeInheritanceShould.cs:40,49,58` (3, under the **unconditional**
analyzer). `logic-block-libraries` searched: **no** preset attribute. The page states the by-name
rule and its one exception (`DALE019`) as they are; the ledger line names the reader. Owner `ANLZ`,
the operator promotes.

**Row 29 — no defect.** Carried because the four `TypeKind.Class` guards read as a narrowing. Probe
R1 shows they are not: a record cannot inherit `LogicBlockBase` (`CS8864`), and a record's `TypeKind`
is `Class` regardless. Stated on the page so the next reader does not re-derive it.

**Row 26 — an `internal` `[InstantiationParameter]` is invisible to its own gate (fix, Warning).**
An author writes `[InstantiationParameter][ServiceProperty] internal bool UseBackup { get; set; }`
and `[IncludedWhen("UseBackup")] public IBackup Backup { get; private set; }`. The build fails with
`DALE043` saying the *gate* has no such property, while the actual defect is the parameter's
accessibility, which no diagnostic names. *Fix:* a new `DALE044` message —
*"[InstantiationParameter] must be a public property; the binders read a block's parameters from its
public instance properties"* — reported at **Warning** through the `effectiveSeverity` overload
(row 6's precedent), so it names the real defect without turning a silent shape into a red build.
The `DALE043` `Error` on the gate stands beside it. Searched: no non-public
`[InstantiationParameter]` in `logic-block-libraries` or in any analyzer-judged in-repo project.

**Row 35 — `DALE016`'s message omits `TimeSpan` and `Guid` (fix).** Same shape as row 5, on the
struct-field whitelist (`DaleDiagnostics.cs:149`). *Fix:* add both to the message.

**Row 39 — `DALE001` on an interface suggests a compile error (fix).**
A `[ServiceInterface]` declares `IPowerMeter Meter { get; }` typed as a contract. The build fails
with `DALE001` telling the author to write `{ get; private set; }`, which is `CS0501` on an
interface. *Fix:* the `TypeKind.Interface` guard `MeasuringPointAnalyzer.cs:48-51` and
`ImmutableArrayInitializationAnalyzer.cs:51-54` already carry, with the same reason.

**Row 40 — no defect.** The Phase A *Why* rested on "no binder walks a non-block class", which is
false: `DeclarativeContractBinder` binds a block's **components**, and a component is an ordinary
non-block class, so the `Error` is well aimed exactly there. Reclassified `intended`, and the row's
Why corrected.

**Row 47 — a message struct beside its contract class (fix, a new `DALE047` at Warning).**
`_findings.md:174-179`. An author writes `[Command(From="IA", To="IB")] public struct Ping { }` at
namespace scope beside `[LogicBlockContract] public class Link { }`; it compiles, the generator
reads only nested structs, and nothing is emitted or diagnosed. *Fix:* a new `DALE047` at **Warning** on the struct.
Searched `logic-block-libraries`: all 53 message structs are nested → none. **One in-repo hit the
Phase A sketch did not report:** `Vion.Dale.Sdk.Test/Configuration/Interfaces/GeneratedContractSurfaceShould.cs:235-237`
declares exactly this shape as `BIND`'s deliberate fixture for `AC-BIND-002.1`, in a project carrying
the analyzer unconditionally. It takes a `#pragma warning disable DALE047` with its reason — a
cross-area fixture edit, a drift checkpoint, the `BIND` page untouched. Warning rather than Error is
what keeps that a two-line edit rather than a red build. The ledger entry `_findings.md:174-179`
closes.

**Row 53 — `DALE020`'s override suppression misses a base declaration (fix).**
A block inherits `public double Power { get; set; }` carrying `[ServiceProperty(Unit = "kW")]` from
its base, and implements two interfaces that declare `Power` with different units. The author has
already written the resolving attribute; `DALE020` fires anyway (probe R3) because
`type.GetMembers(name)` is own-members-only, and the message tells them to write the attribute they
have already written. *Fix:* use `AnalyzerHelper.EnumerateProperties`, whose base walk and
most-derived-first ordering are exactly what this lookup wants.

**Row 70 — `NaN` and infinity pass all three timer guards (fix).**
`[Timer(0.0 / 0.0)] private void Poll() { }` compiles, passes `TimerAttribute`'s constructor
(`NaN <= 0` is false) and reaches `DeclarativeTimerBinder.ResolveInterval`, which throws
`InvalidOperationException` at configuration — the block never starts. *Fix:* invert the analyzer's
comparison to `!(intervalValue > 0)`, which catches all three values at the door the author reads
first. Over-determined with the attribute constructor's identical comparison, which is `LIFE`'s.

**Row 74 — `DALE012` misses a base/derived identifier collision (fix).**
A shared base block declares `[Timer(60, "poll")] protected void PollBase() { }`; a derived block
declares `[Timer(5, "poll")] private void PollFast() { }`. The binder collects both
(`DeclarativeTimerBinder.cs:130-146`) and the last one registered wins, so one timer silently never
ticks — the exact silent bug `DALE012` was minted for. *Fix:* walk the base chain, deduplicating an
`override` against the virtual it overrides the way the binder does (row 75).

**Row 102 — `DALE033` reads the first emission attribute only (fix).**
`[ServiceProperty][ServiceMeasuringPoint(StringFormat = "ipv4")] public int Rssi { get; set; }` —
the measuring point's `StringFormat` is honoured for no `int` member and is silently dropped, and
`DALE033` says nothing because `ServiceProperty ?? ServiceMeasuringPoint` returned the wrong
attribute. *Fix:* loop `EmissionAttributeHelper.GetEmissionAttributes` and report per attribute at
that attribute's location, as the six emission analyzers already do.

**Row 105 — `DALE026` warns on a key the platform ships (fix).**
A consumer writes `[Presentation(Group = "status")]`. `Vion.Dale.Sdk.Core.PropertyGroup.Status` is
exactly `"status"`, but the consumer sees that class as metadata and
`compilation.GetSymbolsWithName` searches source declarations only, so the build warns
*"doesn't match any constant in a PropertyGroup-named static class"* for a constant that does.
*Fix:* collect the constants from the compilation's own assembly **and** its references, and add a
cross-assembly test on `MinChangeCrossAssemblyThresholdAnalyzerTests`' harness — the existing test
declares its own `PropertyGroup` in source and passes by construction.

**Row 118 — a `MinInterval` at the tick boundary configures a negative interval (park).**
`[ServiceProperty(MinInterval = "922337203685477.6")]` draws no diagnostic (probe P6): the
milliseconds-to-ticks product is the double `9223372036854775808`, which is **not** greater than
`long.MaxValue` as a double, so the guard passes and the cast wraps to `long.MinValue` — a negative
interval whose elapsed test is unconditionally true, the exact failure `DALE036`'s message describes.
*Why park:* `Vion.Dale.Sdk/Emission/DurationParser.cs:120-125` carries the identical comparison, so
the analyzer agrees with the runtime exactly; fixing one side alone would flag a token the runtime
accepts. Both move together, and the runtime half is `emission.md`'s (`AC-EMIT-012.3`).

**Row 139 — an abstract block's `VisibleWhen` is never validated (fix).**
A library ships `public abstract class MeterBase : LogicBlockBase` with
`[ServiceProperty][Presentation(VisibleWhen = "Detaild")]` — a typo — and the concrete block lives in
the consumer's assembly. `VisibleWhenPredicateAnalyzer.cs:41` skips abstract types, so the library's
own build is clean and the consumer gets an `Error` in code they did not write (probe Q7 vs Q7b).
`IncludedWhenPredicateAnalyzer.cs:45` has no such guard and validates the same declaration, so the
two predicate analyzers disagree about one class. *Fix:* drop the `IsAbstract` guard; the predicate
resolves against the declaring type's own service map, which an abstract block has.

**Row 134 — `DALE013`/`DALE014` never see a nested type (fix).**
`Vion.Dale.Sdk.Core` contains public nested types; none has ever been asked for a `[PublicApi]`
mark or a `<summary>`, because `GetAllTypes` walks namespaces and not type members. An undocumented
`[PublicApi]` nested type reaches the docs site with an empty entry. *Fix:* recurse into
`type.GetTypeMembers()`, the way `ServiceRelationAnalyzer.GetAllTypes` (`:302-326`) and
`EmissionAttributeHelper.EnumerateNamedTypes` (`:364-385`) already do.

**Row 135 — `DALE015` reports a live namespace as stale (fix).**
An assembly declares `[PublicApiNamespace("Vion.Dale.Sdk")]` and
`[PublicApiNamespace("Vion.Dale.Sdk.Emission")]`. Every type in the second namespace matches the
first prefix, so only the first is credited and the second is reported stale. The message's remedy
is to delete it — which silently stops `DALE014` asking for marks in that namespace.
*Fix:* credit **every** matching prefix, not `FirstOrDefault`'s.

**Row 152 — the pack condition loses the analyzers silently (park).**
A release built without the generator project (a partial build, a cache miss, a reordered CI step)
packs a `Vion.Dale.Sdk` with no `analyzers/` folder. Every consumer of that version loses all 44
diagnostics and nothing says so. *Parked, with its home named:* a pack-time error is a packaging-path change, not an area-local one,
and the tree already carries the right place for the assertion — the post-pack artifact gate
`scripts/verify-packed-assembly-versions.ps1`, wired at `.github/workflows/publish.yml:72-93` and
minted for exactly this class of silent-bad-package failure. A check that a packed `Vion.Dale.Sdk`
carries `analyzers/dotnet/cs/Vion.Dale.Sdk.Generators.dll` belongs there. Owner: the release process
(`docs/releasing.md`).

**Row 161 — `LogicClassGeneratorDBG` has no caller (fix).**
`DiagnosticsExtensions.LogDebug` is `public`, mints a descriptor, and is called by nothing.
*Fix:* delete it.

**Row 162 — the `RS2008` comment states a false reason (fix).**
*"analyzer is internal (not shipped as a NuGet package)"* — it ships inside `Vion.Dale.Sdk`.
*Fix:* state the true reason (no release-tracking files exist and adding them is a separate
decision — question 6's ledger line).

**Row 170 — three analyzers key off contract interfaces with no `CS0246` pin (fix).**
`ContractPropertyAnalyzer.IsServiceProviderContractType`, `ContractMessageAnalyzer`'s `From`/`To`
name comparison and `IncludedWhenPredicateAnalyzer.IsGateable`'s `TypeImplementsLogicInterface` all
resolve contract interfaces. In a Metalama-hosted build a generated contract interface is an error
type and `AllInterfaces` does not contain it, so a symbol-only path no-ops for every real consumer
while every stub-based test passes. *Fix:* add one `CS0246` test per analyzer that resolves by
symbol, and record on the page which families resolve by name as well.

**Row 177 — a `[StructField]`'s `StringFormat` is judged by nothing (fix, Warning).**
An author writes `[StructField(StringFormat = StringFormats.Ipv4)] int Port` inside a wire struct.
`StructFieldAttribute.cs:36` declares the knob, `Vion.Dale.Sdk/Introspection/TypeRefBuilder.cs:60`
emits it into the schema, and `StringFormatOnNonStringAnalyzer.cs:36-37` reads only
`[ServiceProperty]` / `[ServiceMeasuringPoint]`, so the misplaced hint reaches the uploaded document
unchallenged. *Fix:* extend `DALE033` to a `[StructField]` on a constructor parameter, as
`WriteOnlyStructFieldTypeRestrictionAnalyzer` already does for `WriteOnly`. Both live sites verified
`string` this session — `Vion.Dale.DevHost.SmokeHost/LogicBlocks/ShowcaseBlock.cs:199`
(`string Address`) and `logic-block-libraries/…/Shared/ModbusConnection.cs:38` (`string IpAddress`)
— so the Warning fires nowhere today, which is the condition the classification made the row on.

**Row 178 — the generator's error diagnostic uses its message as a format string (fix).**
`DiagnosticsExtensions.cs:35-40` mints the descriptor with the caller's `message` as its
`MessageFormat` and then passes `args`. `LogicClassGenerator.cs:662` passes an embedded-resource name
straight in and `:680` a Scriban parser message. A brace in either — and Scriban's own syntax is
`{{ … }}` — reaches `Diagnostic.Create` as a placeholder and throws inside the generator, so the
author gets a generator crash instead of the message that says what is wrong.
*Fix:* a constant `MessageFormat` of `{0}` with the message as the argument.

**Row 179 — `DALE020` reads the unit off the first attribute only (fix).**
Two `[ServiceInterface]`s declare `Power`; one carries `[ServiceProperty][ServiceMeasuringPoint(Unit
= "W")]` and the other `[ServiceProperty(Unit = "kW")]`. `MultiInterfaceConflictAnalyzer.cs:108`
reads `GetAttribute(ServiceProperty) ?? GetAttribute(ServiceMeasuringPoint)`, so the first
declaration's unit reads as absent and the conflict never registers — the cascade rule then picks a
winner with nothing to say it was ambiguous. `:52` carries the same expression for the bucketing
decision. *Fix:* both sites read every emission attribute, as row 102's does. This is row 102's
class two sites further; journal `:156` is the standing instruction to sweep the shape, not the
symbol.

**Row 182 — recursion would demand a mark on an unreachable type (fix).**
`internal class Outer { public class Inner { } }` — `Inner.DeclaredAccessibility` is `Public`, so
row 134's recursion reaches it and `DALE014` demands a `[PublicApi]` or `[InternalApi]` mark on a
type nothing outside the assembly can name. *Fix:* judge **effective** accessibility — walk the
containing types and skip a type any container makes non-public — in the same change as row 134,
because the recursion is what makes the shape reachable at all.

**Rows 171–172 — removed from the table.** They are Phase B mechanics under `D5`, not observables of
the analyzers: `scripts/test-style-lint.ps1` enforces two things (an article token as a PascalCase
word, `:44`/`:74`; a Triple-A marker, `:45`/`:81`) and checks **only methods that already cite an id**
(`:70`) — so "verb form" and "no collaborator name" are `testing-conventions.md` § 12 prose applied by
reading each name, and retiring the exemption alone would touch 93 of 358. The two are ordered:
**citations first, then names.** The table keeps the numbering hole so every reference in this doc
stays true.

**Row 173 — a blank or colliding endpoint identifier has no compile-time door (park).**
An author writes `[LogicBlockInterfaceBinding(Identifier = "")]`, or the same `Identifier` on two
endpoints of one block. It compiles; `AC-INTRO-014.3`/`014.4` refuse it at bind time, so the first
signal is a failed `dotnet pack` with a runtime message, minutes after the edit.
*Parked, its own change doc:* a new whole-type analysis over two attribute families and two
declaration levels, shaped like `DALE043`'s whole-type walk.
Reader: every consumer's build. `logic-block-libraries` searched: **no** `Identifier =` declaration
at all, so the first consumer cannot be reached by it. In this repository it would fire immediately:
`Vion.Dale.Sdk.Test/TestHelpers/IntrospectionBlocks.cs:82` declares `Identifier = "   "` and `:97`
`Identifier = ""`, and `Identifier = "Shared"` appears six times in that one file — the `INTRO`
fixtures for `AC-INTRO-014.3`/`014.4`, which are deliberate and would each need a
`#pragma warning disable DALE047` with its reason. That fixture edit is `INTRO`'s area, so it is a
drift checkpoint here and the owning page stays untouched (brief point 10).
`_findings.md:30-35` records that this failed the `INTRO` pass's size guard — it is this area's
size, not that one's.

**Rows 174–175 — the contract-type token has no emptiness or uniqueness guard (174 fix, 175 park).**
`[ServiceProviderContractType("")]` compiles and reaches the introspection document as a contract
type with a blank cloud-facing identifier; two interfaces sharing a token reach it as two contract
types with one name. *Row 174 → fix:* a new `DALE048` at **Error**, per type, on a blank or whitespace token. The
consumer's eight tokens and every analyzer-judged in-repo project's (`SmokeHost` 3, `AnalogIo` 4,
`DigitalIo` 4, `Sdk.Test` 5) were searched — **no blank token anywhere** — so `Error` costs no
consumer a build. This overrides `D6`'s "a new Error is always `propose`" on searched evidence, a
coordinator decision under the operator's delegation, listed for the operator.
*Row 175 → park:* the uniqueness half is a whole-compilation analysis owing
`WellKnownDiagnosticTags.CompilationEnd` (§ 4, row 7) and `ServiceRelationAnalyzer`'s
current-assembly boundary — its own change doc. The ledger line `:207-212` is rewritten to carry
only this half with both named.
**Harness note:** `Helpers/TestAttributeStubs.cs:277-279` declares the stub attribute with **no
constructor and no token property**, so no analyzer test can exercise a token value today; the stub
grows with the diagnostic. `logic-block-libraries` searched: every token is a non-empty distinct
literal.

**Row 176 — the generator's candidate predicate is over-broad (park).**
`LogicClassGenerator.cs:36-40` runs its syntactic predicate on every class in every compilation and
matches any attribute whose name *contains* `Contract`; the semantic pass afterwards makes the
output correct, so there is no functional observable — only the incremental cache key being wider
than it needs. *Parked, as the Phase A sketch itself argued:* a ledger line, not this pass's work. It is the generator's, not an
analyzer's; measuring the cost needs a build-time benchmark this pass has no harness for, and
narrowing a generator predicate risks dropping a contract shape the semantic pass currently
recovers. Reader: every consumer's build time.

---

## Unmapped tests

Tests in scope that map to no row above, and their Phase B disposition:

| Test | Why unmapped | Phase B |
|---|---|---|
| `PredicateConformanceVectorTests.VendoredVector_HasCases` | asserts the vendored `Predicates/predicate-conformance.json` is non-empty | keep, uncited — a fixture-integrity guard, not a criterion |
| `PredicateConformanceVectorTests.ParseCases_MatchGrammar` | pins the analyzer's parser against `Vion.Contracts`' vectors | keep, uncited **premise test** — it pins an implementation premise (two parsers agreeing), which `docs/spec-process.md` names as a category; the grammar is `GATE`'s |
| `PredicateConformanceVectorTests.EvalCases_AreAllInsideTheGrammar` | same | keep, uncited premise test |
| `PredicateConformanceVectorTests.StrictFailClosedCases_AreStillInsideTheGrammar` | same | keep, uncited premise test |
| `CrossAssemblyReadonlyRecordStructTests.RoslynMetadataReader_RecordStruct_IsRecordFalse_HasDeconstructMarker` | pins a Roslyn behaviour, not a Dale rule | keep, uncited premise test — it is the reason row 33 exists |
| `ObservableStructMemberDependencyAnalyzerTests` — 11 `_NoDiagnostic` exemption tests | each asserts silence for one exempted shape | merge into one `[DataRow]`-driven criterion under row 126; journal `:160`/`:165` say an exemption test proves only that we do not warn, so the page's text says exactly that |
| `ServiceElementTypeAnalyzerTests` — 17 `_NoDiagnostic` supported-type tests | one method per supported type | merge into `[DataRow]`s under row 31 |
| `StructServiceElementAnalyzerTests` — 8 `_NoDiagnostic` tests | one method per exempt shape | merge into `[DataRow]`s under rows 80/81 |
| `VisibleWhenPredicateAnalyzerTests` — 23 methods | every one maps to a `config-gating.md` criterion, not to an `ANLZ` one | keep, re-cited to the `AC-GATE-*` id each proves; `IncludedWhenPredicateAnalyzerTests` and `InstantiationParameterAnalyzerTests` show the form |
| `AnalyzerWiringShould.KeepTheProbeOutOfAnOrdinaryBuild` | maps to row 158 | cite the new `AC-ANLZ` id |
| `AnalyzerWiringShould.LeaveTheBuildOutputsOfItsDependencyGraphUntouched` | maps to row 159 | cite the new `AC-ANLZ` id |

No test in scope is a deletion candidate: every one either maps to a row, folds into a `[DataRow]`
of one, or is a premise test named above.

---

## Self-check

**Against the anchor inventory.** 44 live descriptors: every one is named on at least one row —
001 (37–41), 002 (67–68), 003 (5, 31, 32, 76, 78, 79), 004 (86–87), 005 (69–71), 007 (88),
008 (77–79), 009 (42), 010 (43–44), 011 (45), 012 (72–75), 013 (130, 134, 137), 014 (131, 134),
015 (132, 135–136), 016 (35, 80–81), 017 (82), 018 (83–85), 019 (48–50), 020 (51–55), 021 (89–92),
022 (106, 108), 023 (89, 93–94), 024 (95), 025 (56–57), 026 (103–105), 027 (96, 98), 028 (97–98),
030 (109), 031 (125–129), 032 (99–100), 033 (101–102), 034 (113–115), 035 (116), 036 (117–121),
037 (117), 038 (122–123), 039 (8, 124), 040 (107–108, 110–111), 041 (138–139), 042 (138),
043 (141, 145), 044 (26, 142–144), 045 (6, 58–66), 046 (146–150). Two retired ids: rows 3 and 11.

36 analyzers: every one is the evidence of at least one row. Two shared helpers: `AnalyzerHelper`
rows 16–17, 21–36; `EmissionAttributeHelper` rows 112–124. The generator's three pseudo-diagnostics:
rows 160–161. Packaging: rows 151–156, 163. The probe: rows 157–159.

**Against the PublicApi manifest.** `Vion.Dale.Sdk.Generators` is not in any PublicApi manifest —
`Vion.Dale.Sdk.Generators.csproj:14` sets `IsPackable=false` and the assembly ships as an analyzer,
not as a `lib` asset, so no manifest entry exists to check. The published surface this area *does*
reach is the diagnostic ids themselves, which rows 1–11 cover, and
`DiagnosticsExtensions` — `public` in an assembly with no public API contract (row 161).

**The reverse question, per anchor.** Which observables have neither a row nor a criterion?

- Every attribute in the inventory whose `AttributeTargets` no reader walks → row 129 (`[StructField]`
  off a parameter) and row 111.
- The one operation action → row 143; its containing-type filter → row 144.
- The two compilation-start precomputes → rows 114–115 (thresholds) and 103–105 (group keys); the
  "added later in the same compilation" question is row 115.
- The two whole-compilation actions → rows 12, 58–66, 130–137.
- `TimerMethodAnalyzer`'s two registrations on one declaration → rows 67–75; the `DALE012`-against-
  `DALE002`/`005` interaction is row 72's "on the second" plus row 67's independence.
- `ServiceRelationAnalyzer` and `MultiInterfaceConflictAnalyzer` reporting on one member → rows 64
  and 51 are independent by construction (different attributes); no interaction row is owed.
- The generator disabled → row 160 (the three pseudo-diagnostics vanish); no `DALE` id does, because
  all 36 analyzers are `DiagnosticAnalyzer`s, not generator outputs — stated on row 160.
- `DALE044` reported by both reporters on one declaration → the two reporters judge disjoint
  subjects (the gate's predicate vs the parameter's declaration), so one declaration draws at most
  one from each; rows 26, 142.

**The doc's own Reviewer's questions.** Question 3 describes an observable (an `Error`-severity
diagnostic outside the registry, unconfigurable by a `DALE` prefix) — row 160 carries it.
Question 6 describes an observable (`RS2008` suppressed on a false premise) — row 162 carries it.
Question 4 describes an observable (unpinned families) — row 170 carries it. Question 11 names four
ledger entries, each of which describes an observable — rows 173, 47, 174–175 and 176 carry them;
three of the four had no row until this question was asked of the table. Questions 1, 2, 5, 7, 8, 9
and 10 are process and ownership, not observables.

**The ledger, entry by entry.** `_findings.md` addresses four entries to `ANLZ` and every one is a
row: `:30-35` → 173, `:174-179` → 47, `:207-212` → 174 and 175 (the entry has two halves and owes
two rows), `:219-223` → 176.

**Counts after the classification** (grepped from the table, not tallied by hand). **181 rows** —
145 `intended`, 12 of them citing a neighbouring page's criterion rather than minting · 21 `fix` ·
12 `park` · 3 `out-of-spec` · **0 `propose`** (all eleven decided) · 27 `⚠`, each with a sketch
below the table · 75 rows carrying `GAP` in the *Test today* column · 11 unmapped-test entries.

The table grew by nine and shrank by two between Phase A and this classification: rows **177**–**183**
are the observables the second opinion found with neither a row nor a criterion, and rows **171**/**172**
were removed as Phase B mechanics under `D5`. The numbering keeps the 171/172 hole so every reference
in this document stays true.

**The `fix` rows, in the order Phase B works them** — false positives removed first (39 with its
`DALE007`/`DALE023` siblings, 53, 105, 134 with 182, 135), then the ones that add a firing shape with
their in-repo suppressions (70 with `LIFE`'s two pragmas, 74, 102 with 179, 139, 26 at Warning, 177),
then the two new ids (47 = `DALE047` at Warning with `BIND`'s pragma, 174 = `DALE048` at Error with
the stub growth), then 161 with 178, 162, 5, 35, and 170's pins.

---

## Drift checkpoints

> One line per divergence discovered during implementation:
> `YYYY-MM-DD: <what changed and why>`. Never inline in a spec page. A checkpoint that fixes a
> CLASS bug states the sibling sweep (done / N/A / handed off).

- 2026-09-04: The brief says **eighteen** csprojs carry the analyzer `ProjectReference`. The tree has
  **seventeen** files, and the split matters: eight carry it unconditionally, nine (all the examples)
  only under `Condition="'$(DaleLocalSource)' == 'true'"`. `Vion.Dale.Sdk.Generators.Test`, which the
  brief counts among the eighteen, references the generator project as a **plain library** (`:22`),
  so its own fixtures are not judged. Rows 153–156.
- 2026-09-04: The brief says only `ServiceRelationAnalyzerTests` carries the `CS0246` pin. Two suites
  use `CS0246`; `ScenarioWireTypeAnalyzerTests:177` uses it to pin the **opposite** claim (the
  analyzer stays silent beside the compile error), so § 5's pin is still only the first. Row 170 and
  question 4 say so precisely.
- 2026-09-04: The brief says `DiagnosticsExtensions`' three helpers are called from
  `LogicClassGenerator.cs` at 28 sites. The total is right; the split is 24 `LogInfo` + 4 `LogError`
  + **0 `LogDebug`**, so `LogicClassGeneratorDBG` reaches no build. Rows 160–161.
- 2026-09-04: `sdk-surface-conventions.md:259-262` lists the projects carrying the analyzer reference
  as "`Vion.Dale.Sdk`, `.DigitalIo`, `.AnalogIo`, the SmokeHost and the examples". The examples'
  reference is conditional, and `Vion.Dale.Sdk.Test`, `DevHost.Test` and the two parser plugins are
  missing from the list. Fix-sized edit to that section in Phase B (rows 153–155).
- 2026-09-04: `architecture/libraries/dale-sdk.md:39-40` says *"Analyzers DALE019–028 enforce the
  surface"*. The declarative-presentation family is `DALE019`–`DALE028` **plus** `DALE030`,
  `DALE032` and `DALE033`. Read-only from here — a checkpoint naming what that file should say, never
  an edit from this repository.
- 2026-09-04: The brief's `AnalyzerHelper.cs:261-267` for `EnumerateProperties` is `:261-273`, and
  `Vion.Dale.Sdk.csproj:90-92` for the pack line is `:92` (`:90` is its comment). Both verified at the
  call site.

---

## Spec delta (to distill)

> Minted in Phase B, after the operator's classification.

- _(none yet)_

---

## Tasks

> One-commit tasks, each tagged with ≥1 AC id. Ephemeral — they live and die with this change doc.

- _(none yet — minted in Phase B)_
