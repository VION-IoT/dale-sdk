# Process journal

Friction log for how this repo gets built — review corrections and process creaks, one line each,
written **the moment they happen**. Not a work log: what shipped is in git and the PRs. This file
records where the *process* creaked. Modeled on the architecture repo's `docs/workflow-journal.md` and
the equivalents in dale, dashboard and cloud-api.

Why: corrections and friction are felt in the moment and remembered nowhere. Without this file they
surface only when someone has accumulated enough irritation to raise them all at once — by which point
the specifics are gone and only the annoyance is left. One line, written when it happens, keeps the
specifics.

**Transcripts are not a fallback.** They age out of `~/.claude/projects/` after weeks;
[retro-0](retro/2026-08-12-review-mining-round.md) mined them once and that window is closed — the
oldest session it could still read started 2026-06-12, so everything before the Modbus-TCP-server work
was already gone. Assistant memory is not a fallback either: it is machine-local, so a colleague, a CI
runner or a second workstation never sees it, and it goes stale silently (retro-0 found two of this
repo's memory entries already false).

## Format

```
YYYY-MM-DD · <where> · <topic, PR #, or —> · <what happened, one line>
```

`where` is one of:

- `review` — the user corrected produced work in-session (**the most important line type**)
- `brief` — a brief (from the architecture repo or a prior session) was wrong, ambiguous, or missing
  something; also where a `Friction:` field would have gone if the work had not been done locally
- `gate` — the CI style gate, build, test gate or snapshot bot false-fails, false-passes, or fights the work
- `consumer` — friction in the SDK-user feedback loop (a `DF-nn` entry, an adoption blocker, a
  workaround a consumer had to keep)
- `release` — the release / example-bump / upload lane creaked
- `infra` — CI runner, package feeds, credentials
- `agent` — agent behavior or process
- `manual` — human grumble

Append at the bottom, newest last. Naming the taxonomy check a correction maps to (`D1`…`D10`, see
[`.claude/commands/vion-code-review.md`](../.claude/commands/vion-code-review.md) § 5) is worth the four
characters — retro-1's open question is which of them actually fire.

Two markers earn their keystrokes because [`process-metrics.md`](process-metrics.md) counts them and
nothing else can produce them:

- **`(second ask)`** on a `review` line the user has now had to make **twice**. Each one is a standing
  candidate for promotion to an analyzer, a gate, or a convention rule.
- **`(escape)`** on a `review` line for work that had already passed `/vion-code-review` — the review
  ran and missed it. This is the loop-quality signal; it is the number that should fall.

Both go at the end of the line, before the D-number.

**Record what happened, not what should change** — the fix is the retro's job, and pre-judging it here
loses the evidence.

**Qualitative one-liners only.** Anything countable afterwards from durable artifacts — PRs merged,
corrections per PR, gate catches, durations — is **not** journalled; the retro counts it into
[`process-metrics.md`](process-metrics.md) from git, `gh` and this file. Journal what was *felt*, when
it was felt.

## Entries

<!-- retro-0 marker: everything below this line is unread by a retro. Move the marker when a round reads it. -->

2026-08-12 · gate · cleanup-code.ps1 · `-Changed` and the drift check both saw tracked files only, so a brand-new `.cs` was neither cleaned nor reported — it surfaced as CI style drift instead. Fixed in this round rather than documented (enforcement ladder).
2026-08-12 · brief · — · briefs to this repo have been asserting `IDevHostControl` is `[PublicApi]` and predicting snapshot churn from DevHost changes. It carries no such attribute and `Vion.Dale.DevHost` declares no `PublicApiNamespace`, so no DevHost change can move the manifest. One implementing session lost a step regenerating a snapshot that could not drift.
2026-08-12 · review · — · `ScenarioStep`'s XML summary still lists `digitalInput`/`analogInput`, deleted by RFC 0010. Found while verifying doc claims for retro-0; filed rather than folded into the substrate PR. (D2)
2026-08-12 · review · retro-0 · The user asked for a correctness pass over the freshly-written convention docs; it found eight wrong claims in text that read as confident — a miscounted analyzer registry, a wrong `Diagnostic.Create` overload, two overstated frequency counts, a wrong assembly breakdown, and an over-generalised `Identifier=` decoupling rule. Every one came from reasoning about the code instead of reading it. This is D10 firing on the substrate itself. (D10)
2026-08-12 · review · #128 · First `/vion-code-review` run in this repo, on the PR that installs it. Found the PublicApi §8 mechanism stated **backwards** — `[PublicApi]` types are the manifest gate, `[assembly: PublicApiNamespace]` only drives grouping and DALE014/015 — with `Modbus.Core` (12 marked types, no assembly attribute, in the manifest) as the counterexample sitting inside my own list. Also two counts still wrong after the correctness pass above: MSTest 15→16 (`Vion.Dale.Cli.Test` uses the split packages) and 519→520 `var(--` (a token with a fallback). (second ask) (D10)
2026-08-12 · review · #128 · Same run: `process-metrics.md` defined `second asks` and `escapes` as journal markers that `process-journal.md` never documented, so two columns could only have been filled from memory — the failure the journal exists to prevent. Markers now defined at the site that has to produce them. (D9)
2026-08-12 · infra · #128 · CI `pack` broke on a toolchain shift, not on the change: with no `global.json`, the runner floated to .NET SDK 10.0.400, whose `Microsoft.CodeAnalysis.Razor.Compiler.dll` needs Roslyn 5.9 while Metalama Compiler bundles 5.5 (`LAMA0625`). Fails at build, before test and style even run, on `Vion.Dale.DevHost.Web`. Blocks every PR; main last built green 2026-08-11.
2026-08-12 · infra · #129 · Resolved. Bumping Metalama was the obvious move and it is a dead end — 2026.1.18→2026.1.23 moves the bundled Roslyn only 5.5→5.6, still short of 5.9, and no current release supports it. The actual cause was `Vion.Dale.DevHost.Web` using `Microsoft.NET.Sdk.Web` with zero Razor files: the Web SDK fed the Razor analyzer to a project that had no use for it, and that is the only place it met Metalama. Now plain `Microsoft.NET.Sdk` + a `FrameworkReference`. Side effects, both verified: the nupkg stopped shipping a duplicate `staticwebassets/` copy of the SPA (823→426 KB, nothing reads it — `WebHostService` serves only via `EmbeddedFileProvider`), and the two AspNetCore analyzers no longer apply here. **A repo with no `global.json` has an unpinned compiler; this will recur.**
2026-08-12 · gate · — · The `devhost-smoke` SKILL.md says "expect 8 tests passed" for Tier 1; it is 19. The number has drifted as the fixture grew and now reads as a failure signal when it is not.
2026-08-14 · review · DF-49 → VION-66 · First `/feedback-intake` run: the drafted Jira body came out too long and the user cut it with "tune down the verbosity". The template names eight sections (Summary/Environment/Observed/Expected/Evidence/Impact/Origin/Acceptance), which invites a paragraph under each; what the item actually needs is the verified chain plus the constraint that decides the fix, and the rest is padding a reader skips. The skill's `templates.md` should say terse, not just list the headings.
2026-08-17 · review · DF-54 → VION-69 · Same verbosity cut as the DF-49 line above, plus a second one: the draft's "Maintainer take" picked the fix (one shared snapshot) and the acceptance list then encoded that mechanism as a requirement. The user's rule — suggestions in the item are welcome, the solution is decided at implementation time — means acceptance states observable outcomes only. Two intake runs, two corrections in the same direction: the item is a problem record, not a design. (second ask)
2026-08-17 · review · DF-41 → VION-70 · Third intake run, the same two corrections again (verbosity; solution-in-the-item) — the draft advocated delete-first as "the only one that also fixes the collapsed boot window" and the acceptance list leaned on it. Two journal lines had not changed the behaviour, so this round promoted it out of prose: `templates.md` now caps section length, gives the **Bug** template the same non-binding "Directions considered" section the Story one has, and states that acceptance lists observable outcomes only. (third ask) (D9)
2026-08-17 · review · DF-31 → VION-71 · Fourth intake run, verbosity cut again — ~1200 words for a defect whose verified chain is four `file:line` lines. The run-3 `templates.md` promotion half-held: the solution half stuck (the draft's "Directions considered" nominated no winner, acceptance stayed observable, neither was corrected), only length recurred. The section-length cap is prose in a document the drafter has already read. (fourth ask) (D9)
2026-08-17 · review · feedback-intake → dale-sdk-feedback · Six runs reviewed together: protocol held every time (gate, dedupe JQL, origin, no unprompted writes) and verification found real things nobody had reported (VION-64), but the verbosity correction landed on five of six runs and the run-3 prose promotion did not bind. Fixed structurally rather than by more prose: eight sections → four with a hard ≤250-word body budget and a count-before-show step; the verification narrative is now explicitly chat-only; summaries get a closed area list; the skill was renamed to carry the repo name. Two existing items regenerated as probes before the rest are rewritten. (fifth ask) (D9)
2026-08-17 · review · VION-64 · `/vion-code-review` on the DevHost teardown fix caught the load-bearing comment being false: the D3 "blocks before hosted services" inversion was justified by `DrainThrottlers` **and** `ClearRetainedMessages` reaching the UI, but the clear raises `ServicePropertyValueCleared`, which no DevHost mock handler observes — it is inert here, and only the drain justifies the order. Written from the SDK handler's shape without checking DevHost's side of it. Same run: two independent 60 s backstops where the brief's D2 said one bound over the whole sequence (120 s worst case, unnamed as a deviation), and a `cts.Cancel(); await runner;` outside the `finally` faithfully copied from `RecycleOnRunShould` — the 6th copy of a web-runner test shape that is due a shared helper. (D2)
2026-08-18 · review · #132 · The user asked whether the Modbus break is backwards-compatible with the runtime's `Vion.Dale.Sdk` assembly. It is at the source level, but the question found a real hazard the report had not: `Vion.Dale.Sdk.Modbus.Core` carries no `[DaleSharedAssembly]`, so the plugin loader gives every plugin its own copy (`PluginLoadContext.Load` strategy 4) — and this PR puts `ModbusReceipt` / `ModbusOutcome` *inside* the RTU actor-message records, which is exactly the cross-plugin type-identity break the attribute exists to prevent. The hazard predates the PR (`IModbusRtu` already exposed `ByteOrder` and friends across that boundary) and only bites a gateway running two Modbus plugins, which is why nobody has hit it. I had checked the dependency *direction* (`GetSharedAssemblyNames` keys off Sdk's references, so Core is not auto-shared) and stopped there, without asking the next question — what now travels in a message. DigitalIo and AnalogIo are self-contained and marked; Modbus is the one extension that splits its types across an unmarked second assembly. Fixed with the attribute plus a test proven red. The Core→Sdk reference this spec required is what made the fix expressible at all — before it, Core could not name the attribute. (D2)
2026-08-18 · brief · #133 · The PR-2 brief said `LinkBackoffException` could go in `Client/Implementation` **or** `Client/Request`, "either way `[PublicApi]` (both namespaces are `[PublicApiNamespace]`-declared; DALE014 will tell you)". Only `Client.Request` is declared — `PublicApiConfig.cs` lists five namespaces and `Client.Implementation` is not among them, which is exactly why `IpAddressNotSetException` and `ConnectionTimeoutException` sit there unmarked and outside the manifest. DALE014 would not have told me: it fires on the declared namespaces only, so the wrong branch of the choice fails silently — a new `[PublicApi]` type would have been invisible to the api-reference and the snapshot. Picked `Client/Request`, beside `RequestDroppedException`.
2026-08-18 · brief · #134 · The PR-3 brief's scenario spec asked for `waitUntil MaxQueuedWait > 0` and `ExpiredCount > 0` as the "backlog is not a fault" assertions. Neither is expressible against this fixture: `above`/`below` need a numeric leaf (`ScenarioConditions.TryAsDouble`), and `MaxQueuedWait` is a `TimeSpan?`, which a scenario can only compare to null — and the accumulator sets it on *every* transaction, so non-null proves nothing anyway. The `Expired` half fails for a second, independent reason: the simulated server answers in ~0.3 ms and both loops carry in-flight gates, so no cadence the client can generate builds a queue deep enough to age a request out (measured: `expiredCount` reached 1 exactly once, at the instant `MaxQueuedAge` dropped mid-flight, then never again). The scenario asserts the same claim through numeric leaves that do hold — `Link.state == Online` and `droppedCount == 0` under maximum cadence — and the expiry demo moved to the README tour, where a human can watch the counter tick. A brief that names an assertion should name a *comparable* leaf; the struct-field types are in the introspection JSON.
2026-08-18 · brief · #134 · Same brief said to mark the CI scenario test `[TestCategory("Smoke")]`. That is MSTest; `examples/*.Test` are xunit.v3 per `testing-conventions.md` § 1, so it is `[Trait("Category", "Smoke")]`. Small, but it is the second time a brief written against the SDK-internal side of the package boundary has carried an MSTest idiom into an example.
2026-08-18 · review · #134 · Maintainer review of the step-3 PR, against the first consumer's shapes: the debug client's eight flat connection scalars (durations as `…Ms` ints) should be the consumer's `ModbusConnection`-style editable struct, and the `Comm` Ok/Error pill should be the SDK's own link verdict rather than a hand-set colour. Both were things I had *carried over* rather than decided — the scalars predate the release and I migrated their call sites without asking whether the release had made the shape obsolete, and the pill I kept imperative specifically to avoid churning three test assertions. The convention that would have caught it is in this repo already (`Vion.Examples.RichTypes`' `Coordinates` is the canonical struct-SP pattern, and the consumer cites it by path). A migration PR should ask, per surface, "does the new API make this shape obsolete?" — not just "does this still compile?". (D9)
2026-08-18 · manual · #134 · Verified while re-checking the amended UI: the DevHost's struct editor keys fields by their camelCase wire names and ignores the `[StructField]` `Title` / `Description` / `StringFormat`, and renders a `TimeSpan` field as a plain text box taking ISO-8601 (`PT3S`). `Minimum`/`Maximum` do propagate (shown as `1 – 65535`), and a nullable field does get an ∅ toggle. All of the ignored annotations are present in the introspection JSON, so this is a DevHost rendering gap, not an introspection one — but it means a struct's annotations cannot be reviewed from the DevHost form, and I had written two doc sentences claiming duration controls before checking. Check the JSON, not the form.
2026-08-19 · review · #134 · Maintainer drove the debug client by hand and found two things neither the scenario nor my "live UI check" had: setting the client's IP to `127.0.0.2` left the pill `Online` (the sim server binds the SDK's default `0.0.0.0`, so every `127.0.0.x` reaches it — while the README had claimed `127.0.0.1:15020` since before this PR), and switching *Connection enabled* off also left it `Online` (the SDK's `Link` correctly keeps its last verdict when nothing is issued; only the block knows the silence is deliberate). Same root cause for both: I verified the transitions **the scenario drives** — closed port, peer drop — and reported a live check that only re-walked those same paths. The states a human reaches by poking were untested. A one-line `Get-NetTCPConnection -LocalPort 15020` would have shown the bind, and the README asserted it in prose that nobody had checked against the code. Fixed: SimServer binds `127.0.0.1` and exposes `ListenAddress`, `LinkStatus.Disabled` is the block's overlay on a client it switched off, both with tests, and the wrong-address case is now a scenario step so a revert to `0.0.0.0` fails CI. (D2)
2026-08-19 · review · #134 · Second-order from the same round: "the SDK is the source of truth" is right for what the SDK observes and wrong as a display rule. `RefreshDiagnostics` mirrored `Link.State` faithfully and that is exactly why the pill lied — the SDK cannot know the block disabled the client, so there is no snapshot for it to keep. Worth stating where the pattern is documented (`docs/migrations/0.10.4-…`, the example README): derive the headline from the SDK's verdict, then overlay only what the SDK structurally cannot see, and leave its snapshot untouched.
2026-08-19 · review · #135 · The brief asked for struct-field descriptions "shown as help (the same idiom the top-level `DocsRow` uses)". I put them behind a `?` disclosure per struct — off by default — and the user still cut it on sight: "the inline descriptions make structs too crowded, i liked it more without the inline metadata, it is only one click away in the docs&schema pane". Right, and I had the evidence in hand: an eighteen-field `ModbusLinkSummary` is a scannable grid or it is nothing, and the description is already in the `docs & schema` JSON the ▸ toggle opens. The `[StructField]` Title as the row label stayed — that is the annotation the cloud dashboard renders and the reason the pass exists. Reading the brief's "not a wall of text per field" as "make the wall optional" instead of "there is no room for it here" is the miss; a disclosure that nobody opens is dead weight, not a compromise. (D5)
2026-08-19 · brief · #135 · The brief's §3.1 specified `[StructField(Title = …)]` on every field of both summaries, including the enum-typed ones. For an enum (or struct) field the authored Title never reaches the wire: `schema.title` carries the CLR type name and the annotation is dropped, because the identity-bearing-title routing that sends a *property's* `[Title]` to `presentation.displayName` has no per-struct-field equivalent — struct fields have no presentation sibling at all. Description does land. Found by the golden test failing on `"Link state" != "ModbusLinkState"`, not by reading. The merge is inside `Vion.Contracts`' `TypeSchema`, so this repo cannot fix it; pinned in `ModbusDiagnosticsIntrospectionShould` and handled client-side (the DevHost falls back to the wire key via `hasIdentityBearingTitle`). The dashboard will show `ModbusLinkState` for `Link.state` until contracts routes it — reported, not fixable here.
2026-08-19 · release · #134 · The `v0.10.5` publish job reported success at 08:34Z, but the packages were not restorable from nuget.org until 08:39-08:40Z, and they landed **one at a time** — `Vion.Dale.Sdk.Modbus.Rtu` was the last, ~40 s after the rest, so a bump attempted on the first green check would have failed restore for exactly one example. "The publish run is green" is not the precondition for a reference bump; "every referenced package answers 200 on the flat container" is. The check is one loop over the eleven ids, and it is worth writing into `docs/releasing.md`'s after-a-release section next time someone edits it.
2026-08-20 · review · VION-106 · Seventh intake run. Protocol held (verification, dedupe JQL, gate, one announced write) and the ≤250-word budget bound for the first time without a verbosity cut — but the user renamed the item: `emission: whole-struct diagnostics default to a permanent 4 Hz emitter` describes the current state, which is a Bug title, and this is a Story. The skill's title convention said `<area>: <symptom or need>` and left the choice to the drafter, so the pull toward "describe what's wrong" won even though the four-section body underneath was correctly need-shaped. Renamed to `emission: make publishing a diagnostics struct whole cheap by default`; the convention now binds the clause to the type and the mistakes table carries the row. Same run, unprompted-good: the user pointed at `logic-block-libraries` for the consumer's own service interfaces, which turned the report's speculative ask into a working precedent — `EmissionDefaults` (a const-string knob table whose telemetry default is 5 s, not the SDK's 250 ms), 26 `[ServiceInterface]`s carrying RFC 0004 knobs, and six near-identical `IChangeThreshold<T>` classes over their own structs. The intake had verified the SDK side and never thought to read the consumer's. Step 3's "verify before drafting" names this repo's sources only; the first consumer is one directory over and is where the demand for a deferred RFC line actually lives. (D9)
2026-08-25 · brief · VION-63 · The brief enumerates three exception shapes for the torn `_pendingActions` read — `NullReferenceException`, `IndexOutOfRangeException`, `ArgumentException` — quoting the consumer's `RealSocketDrain.IsTornQueueSnapshot` as "the best description of the failure modes". The regression test, run against the reverted fix, found a fourth on the `AdvanceTime` path: the torn `(DateTimeOffset, Action)` tuple came back with a default *Deadline*, not a null Action, so `TimeProvider.SetUtcNow(deadline)` threw `ArgumentOutOfRangeException: Cannot go back in time`. The consumer's filter deliberately excludes anything broader than the three, so that shape would have failed their test outright rather than being retried — worth relaying, even though their wrapper is release-gated for removal. Only the `FlushPendingActions` row produced a named shape (`ArgumentException: Destination array was not long enough`, from `List<T>..ctor(IEnumerable)`). The brief's other warning was wrong in the useful direction: it flagged that the flake "never reproduces locally on Windows" and that a local red might be unreachable, but with four aligned producer threads × 2000 enqueues both rows failed 3/3 locally — the data race on the backing array is not a timing artifact, exactly as the brief's `[assumed]` counter-consideration predicted.
2026-08-25 · review · VION-63 · `/vion-code-review branch`, run against the brief as intent, found that guarding the two lists was necessary but not sufficient: `AdvanceTime` takes the entry under the gate and calls `SetUtcNow(deadline)` *outside* it, so a background enqueue landing in that window stamps its deadline from a clock read the drain is about to leave behind — the next take then hands `FakeTimeProvider` a past instant, which throws `ArgumentOutOfRangeException` **and** consumes the already-`RemoveAt`-ed action. Both acceptance criteria the fix existed to meet (no `ArgumentException` under concurrent enqueue racing `AdvanceTime`; every action runs exactly once) were still violated, and the new class doc claimed the guarantee unconditionally. My own regression test had missed it because its `AdvanceTime` row drove with `TimeSpan.Zero` — the clock never moved, so the row was a slower copy of the flush row and covered nothing unique to the path it was named after. That is the review's sharper point: the brief warned about a test that passes with and without the fix, and I avoided that shape while walking into its sibling, a row that exercises a code path only nominally. The reproduction needs the drain *marching* — sub-millisecond, widely spread delays; a uniform delay piles the queue on one deadline and the clock never advances. Fixed by clamping the clock forward rather than the reviewer's suggested "set it under the gate": `SetUtcNow` fires whatever timers the externally-owned `FakeTimeProvider` carries, so holding the gate across it is the deadlock the brief's constraint 3 forbids. Red proven 4/5 runs against the lock-only build before the clamp. (D10)
2026-08-26 · review · #140 · `/vion-code-review branch` against the VION-69 brief confirmed the seam test is not vacuous (it independently traced the counterfactual), and found three things I had written more confidently than I had earned. The "proven red by reverting only the production change" claim was the sharpest: with the seam in place a literal revert does not compile, so the red run was necessarily against a hand-modelled two-snapshot shape — the PR body said so in a parenthesis and asserted the stronger thing in the sentence around it, and `testing-conventions.md` §2 makes that claim the only record there is. Same shape one level down: the DF-14 row's doc said "a referenced-but-unloaded assembly resolves from the base directory", but `typeof(CounterBlock).Assembly` is the running test assembly and is always loaded — what the row actually shows is that an assembly *hidden from the snapshot* resolves, which is worth having and is not the cold load the sentence promised. A shared MSTest process cannot stage a cold one, so the honest fix was the comment. Third, the production comment's "there is no third case" sat two lines above a live `Type.GetType` call outside the snapshot (harmless — it can only widen), which a reader auditing the invariant would trip over. All three are D10 with a D9 edge: the code was right and the prose around it overclaimed, which is the failure mode a non-author reviewer catches and an author re-reading their own reasoning does not. Also dropped a one-line `DefaultLoadedAssemblies` wrapper that existed only to be a method group (D1) and spelled out three terse locals (D8). (D10)
2026-08-26 · review · VION-70 + VION-77 · `/vion-code-review branch` against the brief, run in a fresh-context subagent, opened with a blocker my own green local run could not have caught: the "target that cannot be removed" test made the file undeletable with `FileAttributes.ReadOnly`, which is a Windows-only property — on the Linux CI runner `unlink(2)` obeys the parent directory's permissions and is unconditional for root, so the delete would have succeeded, the assertion would have failed on an unrelated line, and the `finally` would have thrown `FileNotFoundException` on top of it. Portable replacement: put a *directory* at the target path, which `File.Delete` refuses on every OS. `testing-conventions.md` §8 says not to spend review effort on what the machine baseline catches — the inverse deserves a line too: the baseline is one OS, and a test whose red depends on filesystem semantics is exactly what it cannot judge. Two more that were real: the sorted-key-order test's two `[DataRow]`s ran over the *same* three enum members, so they were one sample and not two (~1-in-6 of passing unfixed, not 1-in-36) — fixed by declaring five non-alphabetical members; and my xmldoc asserted "block ids are unique within a topology" as a fact when `DevConfigurationBuilder` mints `lb_{n}` from a counter that only advances for auto-assigned ids, so an explicit `lb_1` collides — under GUIDs that stayed harmless, and deriving service ids from the block id made it a silent cross-wire. Turned the asserted invariant into an enforced one (`RejectDuplicateBlockIds`, VION-66's collect-then-report shape) rather than softening the sentence. The review also caught four live comments that the change had falsified — the SPA's `CLAUDE.md` rule "service ids regenerate every run" and two `store.js` recycle comments — each still giving correct advice for a reason that had just stopped being true. (D2, D10)
2026-08-26 · gate · #142 · `TeardownStopSequenceShould.DeliverAServicePropertyWriteIssuedFromStopping` [free-running clock] false-failed the pack gate on a change that touches only introspection emission and the SPA's JS — neither of which that test executes. The assertion printed an **empty** event list (`Events: ` with nothing after it), so it is not a wrong final value but no publish observed at all: on a free-running clock the host is disposed immediately after `StartAsync`, and the CI runner is slow and contended enough that the `Stopping()` write's publish loses the race with teardown. Passed 5/5 locally and green on the very next CI re-run of the same commit, with no other CI failure on `main`'s last eight runs — so it is a low-rate flake, not a regression, and the stepped-clock row of the same `[DataRow]` pair never fails. Costs a re-run and, worse, a few minutes of suspecting your own diff. The stepped row is deterministic by construction; the free-running row is asserting a delivery the runtime does not guarantee without a grace period, which is exactly what VION-65 (#138) documented away for *contract* writes — the same reasoning may apply to this row.
2026-08-26 · brief · VION-105 amendment · The amendment's §1 asserted "I read its reference list, and `libraries/` does not appear in it", and its §2 asked me to resolve a mismatch between `docs/releasing.md:44-46` (which obliges `libraries/` to bump) and `scripts/set-version.ps1` (which allegedly does not touch it) — leaning to extending the script. Both premises were false: `$libraryProjects` and `$libraryMainProjectsWithVersion` have been in the script since #44, and `Vion.Diagnostics.csproj:12` says so in a comment ("bumped by scripts/set-version.ps1"). Running the script bumped all three of templates, examples and libraries with nothing manual. But the brief was not hallucinating: `releasing.md:61-63` and the script's own header comment both described it as "templates and examples", and a reader checking what the script does is told exactly the thing the brief then asserted. So the mismatch was real and one line below where the brief pointed — between the script and its *own documentation*, not between the script and the obligation. Fixed both descriptions rather than the behaviour. The lesson is narrower than "verify the brief": a summary sentence that lists two of three things is the shape that propagates, because it reads as complete. (D2)
2026-08-26 · release · VION-105 amendment · Wrote the deferred half of the 2026-08-19 `release` entry into `docs/releasing.md` at last — that entry ended "it is worth writing into `docs/releasing.md`'s after-a-release section next time someone edits it", and this was that edit. The 0.10.8 bump reproduced the same behaviour it described: the publish run went green, and the fourteen referenced packages then landed on nuget.org one at a time over ~6 minutes, with `vion.dale.sdk.http` and `vion.dale.sdk.digitalio` a full poll behind the rest. A bump attempted on the green check would have failed restore for exactly those two. The section now carries the poll loop. Noting the mechanism as much as the rule: the deferral survived a week only because it was written down with a trigger attached ("next time someone edits it") rather than as a wish — and it was a code reviewer, not me, who spotted that the trigger had fired.
2026-08-26 · brief · VION-81 hop 1 · The brief's "corrections to the issue" pushed back on the reporter's claim of "no signal at any stage", reasoning from Metalama's docs that the aspect reports LAMA51xx for constructs it cannot track and that the SDK suppresses none of it — both true, and the conclusion still wrong. A real compilation emits nothing for a struct-member read; the aspect does not consider it untrackable, it silently treats the value as a leaf. The searches the brief ran (no `NoWarn`, no suppression anywhere) were sound and proved only that nothing was *hiding* a warning, which is a different claim from one being *raised*. What settled it was planting a shape Metalama does document as untrackable — a call into a non-observable helper — in the same compilation and watching `LAMA5162` appear beside the silence. A negative about a third-party tool needs a positive control in the same run, exactly like a test does; without one, "no warning appeared" and "warnings do not appear here" are indistinguishable. Cheap to add, and it is what let hop 2 skip straight to the mechanism. (D2)
2026-08-26 · agent · VION-81 hop 2 · `ObservableStructMemberDependencyAnalyzerTests.NonObservableStructProperty_NoDiagnostic` had asserted since DALE031 shipped that an *unmarked* struct property is exempt — on the inferred rationale that only observable members matter. The aspect weaves every auto-property of a service type, not only the `[ServiceProperty]` ones, so the shape was broken exactly like the marked one and the test was pinning a live gap as intended behaviour. It was found only because the hop-2 brief marked the inference `[assumed]` and asked for it to be probed before the analyzer was touched. The general shape: an analyzer's *exemptions* are load-bearing claims about someone else's compiler, and they get written as prose in a doc comment and mirrored into a `_NoDiagnostic` test that passes by construction. `sdk-surface-conventions.md` §5 already warns that analyzers see a different compilation than their tests do — that warning applies to what a rule lets through, not only to what it catches. (D2, D10)
2026-08-26 · gate · VION-81 · `scripts/cleanup-code.ps1 -Changed` silently rewrote the fixture that IS the test. ReSharper's Full Cleanup profile ran "convert to auto-property" over `MetalamaStructMemberDependencyReproShould`, collapsing `private MeterReading? _stored;` + `public MeterReading? Stored => _stored;` into `public MeterReading? Stored { get; private set; }` and rewriting every `_stored` read to `Stored` — turning three field-rooted probes for a *field-rooted* bug into property-rooted ones, and making an assertion message that names `_stored?.ActivePowerTotalKw` describe code that no longer existed. Nothing caught it: the build was clean, the analyzer tests were unaffected, the four affected tests are `[Ignore]`d so they never ran, and the two that did run still passed *because the transformed shape is one that works*. The fresh-context reviewer found it by reading the fixture against its own comments. Fenced with `// ReSharper disable ConvertToAutoProperty*` and a comment saying why. The general shape: CLAUDE.md documents `// @formatter:off` as the escape hatch, but that only covers *formatting* — cleanupcode also applies semantic transformations, and a test whose subject is a code shape is exactly the file where those are destructive. Worth knowing before writing the next repro fixture. (D9)
2026-08-26 · review · VION-81 · `/vion-code-review branch` in a fresh-context subagent, against the hop-2 amendment as spec. Two blockers, both mine, both invisible to a green run: the cleanup rewrite above, and the analyzer's new doc comment citing `MetalamaComputedDependencyProbeShould.cs` — a filename I had renamed in the same commit, so the one pointer from the rule to its evidence was broken on arrival. Three judgment calls I took: the `readonly`-field exemption I added unasked was *asymmetric* — a get-only `public Bands Plan { get; }` is equally unassignable and still warned — so it now covers both, since "readonly fields are quiet, get-only properties are not" is a rule no author can predict; my base-class-field exemption's stated reason ("the base may live in an assembly the aspect never touches") was speculation when the checkable one was sitting in the build output, `LAMA5164`, which reports that shape upstream already; and the reviewer's own probe surfaced that base-class *properties* still fire with no test, now pinned. Also dropped the two Metalama version numbers from the descriptor summary — they were asserted in five places and rot on the next bump. The pattern across all of it: every defect was in prose asserting something about the code or about Metalama, never in the analyzer logic, which the reviewer checked hard and cleared. (D2, D10)
2026-08-26 · review · VION-81 · Two questions on the PR — "method calls are tracked: are they really? do we have a test?" and "is the updated rule brittle, can it produce false positives?" — found a live false positive that both my own tests and a fresh-context adversarial review had missed. Reading a public **field** of a struct (`_pair.A`, `Vector2.X`) is tracked by the aspect, which reports `LAMA5164` for it and then depends on the containing field anyway; DALE031 was telling the author that value goes stale when it does not. Only struct **property** reads are dropped silently. The old guard accepted `IFieldSymbol` or `IPropertySymbol` as the accessed member and nobody had asked why — it was inherited unexamined through the widening, and every existing test happened to use a `readonly record struct`, whose positional parameters compile to properties, so the whole suite was blind to the field half. Narrowing to `IPropertySymbol` fixed it with all 25 other tests unchanged. The first question was fair too: the "call a method on it" remedy had runtime evidence for a property root only, and the field root — the one VION-81 is about — rested on a detection test that proves the analyzer stays quiet, not that Metalama tracks anything. Both remedies now have runtime pins. The lesson is about what "we have a test" is worth: an analyzer `_NoDiagnostic` test asserting an exemption proves only that we don't warn, never that the exempted shape works — and an exemption is a claim about someone else's compiler, so it needs behaviour evidence or it is just a restated assumption. `testing-conventions.md` §2's "a no-throw assertion is not a test" is the same defect wearing a different hat. (D2, D5, D10)
2026-08-27 · review · VION-71 · `/vion-code-review branch` against the brief, in a fresh-context subagent, found that my new resolve-time field check ran on the **drive** path too. `ResolveServiceProviderContract` serves both `serviceProviderSet` and `serviceProviderExpect`, and I called the assert-side check unconditionally from it — so a handler declaring BOTH directions (which `[ScenarioWire]`'s own xmldoc sanctions: "the inbound struct … and/or the outbound") would have had every valid `serviceProviderSet` rejected with "assert one scalar with 'field'", an error the author cannot act on, because the drive shape has no `field` and the schema is `additionalProperties:false`. My comment three lines above the call asserted the opposite — *"A drive carries no field and skips all of it"* — which is the tell: I wrote the intent down, then didn't implement it, and no test contradicted me because no in-repo contract declares both directions. Same bug mirrored in the CLI validator. The reviewer also caught that AC-1 was only half-verified: the runner's "written but unreadable" message was unreachable in practice, because the resolver now statically rejects every shape that would produce it — dead code standing in for an acceptance criterion. Fixed by making it genuinely reachable rather than by asserting a branch nothing can hit: the fixture's nested `Limits` is now absent while nothing is enforced, so a field path through it validates and then legitimately fails at read time. The pattern worth keeping: an author's comment describing a guard is not evidence the guard exists, and "no test fails" is worth nothing for a path no fixture can reach. (D9, D10)
2026-08-27 · review · VION-71 · Two corrections on the `devhost-smoke` skill, both about writing the *change* where the *expected state* belongs. It had said "Expect **21 tests passed**" since it was written; I dutifully bumped it to 22, then to 23 within the same PR when a second Smoke case landed — which is the whole argument against it: a number that has to be edited every time the fixture grows is stale by default, and the operator's actual question is "did anything fail", not "is it still 23". Now states zero-failures as the expectation. Second, I had tagged my two new bullets with `(VION-71)` and written "a row **now** carries a field combobox" — ticket numbers and before/after framing in a skill that exists to tell a reader what to expect **today**. Both are the same defect `sdk-surface-conventions.md` §2 names for XML docs ("no RFC numbers … no design history"), and it applies to skills for the same reason: the history has a home in git and the PR, and in a procedure doc it only ages. Worth noting that the brief itself primed the first one — it said "expect 21 passed today … your work should raise that count", so the number read as a contract to maintain rather than as the already-stale artefact it was. (D2)
2026-08-27 · brief · VION-71 amendment · The brief's finding 4 said "changing `GetServiceProviderOutput`'s signature is a break; adding a sibling method is not. **Prefer adding**" — true as stated, and the weaker read of `sdk-surface-conventions.md` §3, which says the specific mechanism is deleted and its callers migrated in the SAME PR that generalises it ("breaking is no problem, the devhost is the only user"). I followed the brief and raised the tension as a question instead of resolving it; the amendment reversed it and the deletion took twenty minutes against a verified six-site caller list. What made the question worth asking rather than just complying: keeping the old member left the issue **half-fixed** — it still collapses never-written / unreadable / null into one `null`, so a headless consumer writing `Assert.AreNotEqual(false, GetServiceProviderOutput(...))` got the exact vacuous pass VION-71 exists to close, now documented rather than fixed. A brief that pre-decides a surface question on breakage grounds is deciding it on the wrong axis in a pre-1.0 repo whose convention already says breakage is cheap — the axis is whether the old shape can still express the bug. Migrating rather than deleting the one test that covered it turned its "is null until Set" assertions into `State == NeverWritten`, which is the control-surface half of the fix and had no coverage before. Declined a `docs/migrations/` page: the sole precedent is a 134-line page for two reshaped client surfaces with behavioural changes, and a one-member removal with a one-line recipe does not meet that bar — but the recipe has a trap (translating to `.Value` mechanically reproduces the defect), so it went in the PR description. (D1, D3)
2026-08-27 · review · VION-71 · Post-mortem of the DF-31→VION-71 chain: intake had verified the reported mechanism bottom-up (four `file:line`, all real) but never walked the motivating PPC contract top-down — the resolver's classification gate refuses it before the verified chain runs, so the item attributed the defect to a family it cannot bite, and the fix shipped green while the reported case stayed blocked; caught by the consumer spending the new `field` selector on it. DF-31's conditional mood ("would have to pin the timestamp") was the unread tell that nobody had ever executed the case. Skill step 3 now requires the top-down walk, and the Origin line records the verification depth (`reproduced | traced | not reproduced`). (D2)
2026-08-27 · brief · VION-127 · The brief handed me the invariant prose to carry into `tokens.css`'s header pre-written, and I pasted it — including a compressed clause that is false: "`quartenary` is held to 3.0:1 and the other three levels to 4.5:1. Five status families sat between 3.74 and 4.48 before any of this, so a flat 4.5 floor is a bar **no ramp in the file has ever cleared**." The last clause drops the word *quartenary* and so contradicts its own first sentence, and the file disproves it (light `fg-def-tertiary` is 7.61:1). The dashboard's `tokens.test.ts` scopes the claim correctly. A fresh-context `/vion-code-review` caught it; reading the source of truth would have too, and I had the file open. Same pass caught that the brief's invariant list omits the *border* bar (`VISIBLE_HAIRLINE = 1.25`, `LADDER_SEPARATION = 1.25`) — the one this whole resync repaired, since dark `brd-def-primary` was at 1.000:1 — so the header would have shipped saying "three rules" while silently leaving the repaired ramp ungoverned. The lesson is narrow and worth keeping: prose a brief supplies verbatim gets pasted, not read, precisely because it arrives looking finished. Text destined for a file is the one part of a brief to re-derive from the source rather than trust, and a header claiming to enumerate the rules is a completeness claim — check what it leaves out, not just whether what it says is true. (D2)
2026-08-28 · brief · VION-129 · The brief stated flatly that "a committed scenario is not a CI gate", having checked that the headless tests write their own scenario JSON into temp directories (`DevHostSmokeShould.cs:35`, `:114`, `:160`) and that the committed `SmokeHost/scenarios/*.scenario.json` are loaded by the live host for Tier 2 — so the regression that gates "must be a test in `Vion.Dale.DevHost.Test`". Both observations are true and the conclusion is wrong: `Vion.Dale.DevHost.Xunit.Test/CommittedScenariosShould.cs` boots a real host per committed scenario, runs every one of them and asserts the count, and `ScenarioFilesAttributeShould` pins the same roster twice more. Three hardcoded expectations went red on my new scenario. The sweep was sound and scoped to one project; the gate lives in a sibling test project, and "is not a gate" is a claim about the whole test surface, so one project's absence cannot establish it. It nearly slipped anyway: a full `dotnet test --no-build` run immediately after adding the file passed, because the scenario had not yet been copied to the test output directory — the roster only went red after the next build, so for one run a stale output directory made a red branch look green. Worth pairing: a negative about coverage needs the search that would have found the positive, and `--no-build` is only safe for a change that adds no content file. (D2)
2026-08-28 · review · RFC 0020 · The draft's §4.1 still motivated the JSON forward with "it is what makes type-different pairs like `SetDigitalOutput` → `DigitalInputChanged` bridgeable at all" — a sentence written for an earlier iteration of the design (host bridges the consumer's existing DI-face sim) that stayed plausible after the session had pivoted to provider faces, where every promoted pair is type-identical and that bridging is not the pattern. The reviewer's question ("this is not what we will do, right?") exposed it. Fixed by re-motivating the layer (drive-path reuse, nothing adapted in flight) and tightening the pairing rule from shape-compatibility to wire-type identity, so the superseded shape is now refused by the design rather than merely undocumented. The pattern worth keeping: when a design pivots mid-session, the motivating sentences written for the old shape do not go red by themselves — each "because" clause needs re-deriving against the final design, not just the mechanics around it. (D2)
2026-08-28 · gate · PR #150 · `cleanup-code.ps1 -Verify` detects drift via `git diff`, so on a dirty working tree it reports drift regardless of style — it is only meaningful after committing. The VION-131-p1 session ran it pre-commit, chased a phantom drift, and only the post-commit run said "Code style: clean". Worth a guard in the script (warn on a dirty tree) rather than tribal knowledge. (carried from the VION-131-p1 REPORT)
2026-08-28 · gate · PR #150 · ReSharper cleanupcode relocated a new private helper in `ScenarioWireCodec.cs` to just below an unrelated method's comment block, orphaning that comment from its method; caught on diff review and moved by hand. Cleanup's member reordering around adjacent comments is a hazard to glance for on every cleanup diff. (carried from the VION-131-p1 REPORT)
2026-08-28 · brief · PR #151 · The p2 brief's AC2 ("`DevelopmentOnly` in the introspection JSON for all four provider types") and its fixture item ("a small sim block binding `IDigitalOutputProvider` (and one `IDigitalInputProvider`)") were in tension — introspection only emits bound contracts, so the narrower fixture could not satisfy the broader AC. The implementing session resolved toward the AC and bound all four; right call, but a brief should scope fixture and AC together. (carried from the VION-131-p2 REPORT)
2026-08-28 · gate · PR #151 · A malformed committed scenario (the `set` step takes a bare string, not the `{property:…}` object every other kind uses) is silently DROPPED by `ScenarioFilesAttribute` — deliberate, validation is `dale scenario validate`'s job — so the symptom was a roster count off by one rather than a parse error; cost the p2 session a round. The "silence is a defect" shape, one layer up. (carried from the VION-131-p2 REPORT)
2026-08-28 · brief · VION-131-p3 · The brief's item 3 asked for "CLI parity: find where `dale` validates topology files today and apply the same predicate" and pre-authorised "if no such site exists, note it in the REPORT" — which is what happened: `dale` has `scenario validate|schema|generate` and `dev --export-topology`, and no topology-file validator at all (`Vion.Dale.Cli` references neither `Vion.Dale.DevHost` nor the SDK; it works on exported JSON). The escape hatch was the right call and cost nothing, but the ask was written as if the site were known to exist. A brief that names a parity site should carry the file, or say "if any" up front. (D2)
2026-08-28 · agent · VION-131-p3 · A new test drove `serviceProviderSet` onto a PAIRED inbound and then `waitUntil`-ed for the value to land — which races the pairing's own writer, the exact hazard the resolve-time warning it was testing exists to flag. Green solo, red once under a parallel solution run, so the flake surfaced only in the full sweep. Two things worth keeping: a warning's own test must not depend on the behaviour the warning calls unreliable, and a single `dotnet test <project>` is not enough evidence for a new test — the full solution run under load is where an ordering race shows.
2026-08-28 · brief · PR #152 · The p3 brief's CLI-parity item said "find where `dale` validates topology files today" as if the site were known to exist; none does (the CLI works on exported JSON and references neither DevHost nor SDK). The pre-authorised escape hatch made it cost nothing, but a brief naming a parity site should carry the file path or say "if any". (carried from the VION-131-p3 REPORT)
2026-08-28 · agent · PR #152 · The p3 session's first warning test drove `serviceProviderSet` onto a paired inbound and `waitUntil`-ed for the value — racing the pairing's own writer, the exact hazard the warning flags. Green solo, red under the full parallel solution run. A warning's own test must not depend on the behaviour the warning calls unreliable, and one `dotnet test <project>` is not enough evidence for a new test. (carried from the VION-131-p3 REPORT)
2026-08-28 · agent · PR #152 · chrome-devtools MCP could not attach for the Tier-2 pass (browser already running on its profile); claude-in-chrome worked, but its screenshots are scaled from the viewport so coordinate clicks land off-target — drive by a11y `ref` instead. (carried from the VION-131-p3 REPORT)
2026-08-31 · agent · VION-131-p4 · The first version of the `PublishAllStates` replay test passed against the OLD hardcoded-four-handlers code — vacuous, because `DriveServiceProviderContractAsync` is fire-and-forget, so the drive's own `ServiceProviderContractChanged` could still arrive after the "late subscriber" waiter was registered and satisfy it. Caught only by temporarily reverting the fix and re-running. Reworked to await the drive's own event first, then subscribe and ask for the replay, so the second event is provably the replay. Worth keeping: a "late subscriber" test has to pin the ordering it names, and every new behavioural test in this repo is cheap to falsify — revert the fix, watch it go red — which is the only evidence it tests the fix at all.
2026-08-31 · gate · VION-131-p4 · The p3 pairing refusal message contained an internal `"; "` ("…declared inbound; pair a contract with its provider face…"), and the web layer splits a topology `InvalidDataException` on exactly that separator (`TopologiesController.InvalidTopology`, which carries its own TODO about the coupling) — so the editor rendered one refusal as two error rows, the second a sentence fragment. Invisible until this slice made Save/validate run the check in the browser. Fixed by removing `"; "` from all three pairing messages; the real fix is the structured-errors TODO. Any message destined for that pipe is now a place to check for a semicolon.
2026-08-31 · gate · PR #153 · `cleanup-code.ps1 -Verify` on a staged-but-uncommitted tree reported drift again — the exact foot-gun the 2026-08-28 (PR #150) line recorded; second session to pay a round for it. The journal line did not prevent the recurrence; the warn-on-dirty-tree guard in the script is now scheduled (housekeeping slice) — enforcement ladder, prose to script. (carried from the VION-131-p4 REPORT)
2026-08-31 · agent · PR #153 · The first version of the PublishAllStates late-subscriber test passed against the OLD code — the drive is fire-and-forget, so the drive's own event satisfied the waiter; only reverting the fix exposed the vacuous pass. Lesson adopted into subsequent briefs: every new behavioural test is shown red against the pre-fix code (revert the fix, or write it first). (carried from the VION-131-p4 REPORT)
2026-08-31 · agent · PR #154 · Proving the `-Verify` dirty-tree guard red pre-fix required stash / dirty / run / pop / commit / re-run gymnastics, because the thing under test IS the tree state — the one case found so far where "show it red" costs more than the fix. Not avoidable; recorded so the next person budgets for it. (carried from the VION-131-p5 REPORT)
2026-08-31 · agent · PR #154 · Verifying the analyzer reference actually runs meant breaking a real [ScenarioWire] declaration and reverting it, with only the diff review guarding against a forgotten revert. The session noted a committed always-fails fixture would turn "the analyzer is wired" from a per-session ritual into a standing gate — retro candidate. (carried from the VION-131-p5 REPORT)
2026-08-31 · release · VION-131-p6 · The brief's prerequisite ("verify the 0.11.0 packages are on the feeds; if not, stop and report blocked") would have produced a false `blocked`: the tag run went green at 08:37 and the first probe minutes later found all 15 packages missing. They landed one at a time and were complete at 08:42. `releasing.md` already documents exactly this ("wait for the packages, not for the green run", ~5-6 min), so the procedure was right and the brief's stop-condition was too eager — a brief that names a wait should name its documented duration, or defer to the doc that owns it.
2026-08-31 · release · VION-131-p6 · `set-version.ps1` closes with "Examples/templates are not part of the SDK .sln and build independently after restore" — but every example and the inner template projects ARE in `Vion.Dale.Sdk.sln` (CLAUDE.md says so, and the sln lists them). A stale sentence in the script's own output, mildly misleading about what a solution build already covers after a bump.
2026-08-31 · brief · VION-131-p6 · AC3 asked for `examples.yml` green after push, but that workflow triggers only on `main` pushes touching `examples/` (plus `workflow_dispatch`) — it cannot run on a PR branch, so the AC is unmeetable before merge. The examples are covered pre-merge by the solution build/test in `publish.yml` instead. Worth knowing when writing an AC against a workflow: check its `on:` triggers.
2026-08-31 · brief · post-0.11.0 hardening · The brief's item 4 asked to "replace the 'deployed simulator / env-aware relaxation' note" in RFC 0020 §10 and the §4.8 future-work sentence. No such note or sentence exists — §10 item 1's only loose end was the flag's final name, and §4.8 had none. The dale-side brief repeats the same phantom ("noted as future work in RFC 0020 §10/§4.8"), so the belief travelled between briefs rather than coming from the file. Wrote the decided v1 position in as a new dated §10 entry instead of replacing anything. A brief that says "replace X" should quote enough of X to be greppable.
2026-08-31 · agent · post-0.11.0 hardening · An MSBuild comment in `Vion.Dale.Sdk/build/Vion.Dale.Sdk.targets` that began with the literal switch name `--exclude-development-only` is invalid XML (a comment cannot contain `--`), so every consumer project importing the packaged targets would have failed at import with MSB4024. Nothing in this repo catches it: no project here imports its own `build/*.targets` — the examples get them from the published package — so the full solution build, the tests and the style gate were all green with the file broken. It surfaced only because the AC1 verification packed the SDK to a local feed and packed ToggleLight against it. Anything under `build/` is only exercised by a real pack-and-consume round-trip.
2026-08-31 · agent · PR #156 · `git checkout <file>` to undo a temporary red-demonstration edit reverted the file's REAL changes with it; caught only on diff re-read. Prove-it-red edits in an already-modified file need copy-and-restore, never a checkout. (carried from the hardening REPORT)
2026-08-31 · infra · 0.11.1 · Release 0.11.1 shipped `Vion.Dale.Sdk`, `.DigitalIo` and `.AnalogIo` with correct nuspecs and `lib` assemblies stamped `0.0.0.0`, so every consumer of the lib assets died at startup with `FileNotFoundException` on `Vion.Dale.Sdk, Version=0.11.1.0` (ProtoActor/Plugin/Http were stamped correctly and bind to it): `AnalyzerWiringShould` (#156) shells `dotnet build` of the I/O projects in the CI configuration without CI's `/p:Version`, and CI runs the tests between the stamped build and `dotnet pack --no-build`, so the child builds rewrote those projects — and their whole `ProjectReference` graph, which is where `Vion.Dale.Sdk.dll` comes from — into the exact Release outputs pack then shipped. Local runs cannot see it: every local build is unstamped, so the clobbered and the intact assembly both read `0.0.0.0`. Found by the dale runtime's `smoke-plugin-load.ps1` (VION-IoT/dale#56), not by anything here.
2026-08-31 · agent · PR #156 · The commit that added the probe test asserted "the build runs in place and fails at compile, so no output assembly is overwritten" — a plausible claim about the probe *project*, never checked against its dependency graph, which is the half that actually gets rebuilt; the probe failing at compile is precisely why only its dependencies were clobbered. Nothing could contradict it: build, tests and style gate were all green and only the packed bytes were wrong. Second green-CI/broken-artifact incident in a week, after the `build/*.targets` MSB4024 near-miss — both were claims about a build artifact that no gate in the pipeline reads. A claim about what a build does *not* touch is a claim about its whole project graph, and it is cheap to check: build stamped, run the thing, re-read the stamp.
2026-08-31 · brief · PR #157 · The hotfix brief's suggested regression-test shape ("build with -p:Version=9.9.9, run the suite, assert the stamp survives") would, as an automated test, write a fake stamp into the very Release outputs pack ships — the hazard class under repair, and in CI it runs BEFORE pack. The implementing session caught it and fingerprinted bytes instead (also the only check that works where every local and ci.N build is legitimately 0.0.0.0). Second brief-authored hazard this incident: hardening item 6 caused the clobber, hotfix item 2 nearly re-created it. A suggested test body that mutates release-critical build output needs its caveat inline, not an escape hatch. (carried from the 0111-hotfix REPORT)
2026-08-31 · agent · PR #157 · The coordinator and implementing sessions share one checkout (C:\_gh\dale-sdk); the coordinator ran `git checkout main` while the v0.11.1 bump session held uncommitted set-version edits in the tree — only git's overwrite refusal prevented clobbering a live session's work. One session per checkout at a time is now the standing rule; the coordinator does no git writes while an implementing session is active. (coordinator observation, VION-131 workstream)
2026-08-31 · release · v0.11.2 bump · Job 2's consumer-side check "`dale list` still shows `IdealRelay` marked development-only" reads as a *failed release* on any machine whose global tool is stale: the marking landed in #156, so `dale` 0.11.0 was still installed here and renders the old unmarked list with no hint that the CLI, not the package, is the old part. `dotnet tool update -g Vion.Dale.Cli --version <release>` has to come first whenever a post-release validation exercises a CLI-surface change — the SDK-side half (the `Vion Dale:` pack notice, the filtered JSON) comes from the package's `build/*.targets` and is version-correct regardless, which is exactly what makes the mismatch quiet.
2026-09-01 · review · SDD pass 0 · User review of the fresh substrate caught four gaps in one round: two live RFC citations left in CLAUDE.md after its own freeze note landed, the mesh testing-conventions port silently missing three sections a rewrite wave needs (file layout/ordering, setup, parameterised-row discipline), Modbus Core/Tcp/Rtu with no roster home beyond "link policy" (→ one `MODB` Tier A area), and `HAL` too generic as an area code (→ `IO`). Common shape: a port/sweep that compresses or scopes a source should name what it dropped — each drop was a decision, and only the summary read as complete. (D9)
2026-09-01 · review · SDD pass 0 · Fresh-context `/vion-code-review` on the substrate before its first commit found one blocker and three conventions, every one in the gate/doc layer asserting something the tree contradicts: spec-trace scanned only top-level `*.Test` dirs, so the ratified xunit `[Trait]` citation form — whose projects all live nested under `examples/`, `libraries/`, `templates/` — could never satisfy the gate, and the self-test had exercised exactly the two forms that worked; "46 DALE descriptors" counted two retired ids as live (44 declared, ids to DALE046) while the sibling doc in the same diff said so; comment-conventions' flagship example carried an "RFC-era" clause the same doc bans two sections below; and spec-change's usage errors exited 1 — indistinguishable from the contractual not-distilled refusal — because `Write-Error` under `$ErrorActionPreference='Stop'` terminates before its `exit 2` (inherited verbatim from the lbl original, which has the same latent bug; worth relaying). Also surfaced pre-pilot: GAP rows had no gate mechanism (now a `GAP` marker spec-trace exempts-but-counts) and `libraries/` sat in no roster tier (now Tier C, named with a reason). D10 on the substrate again — caught by fresh eyes, not the author. (D9, D10)
2026-09-01 · brief · PLUG pass a1 · The pilot pass brief named `docs/snapshots/publicapi-manifest.json` entries for `Vion.Dale.Plugin` and `DaleSharedAssemblyAttribute` as the area's anchors. Neither exists, neither should, and `sdk-surface-conventions.md:216` already says so in as many words — the project has zero `[PublicApi]` types and the attribute is `[InternalApi]`. Disproving it cost a detour on a fact the repo had already written down. An area brief's anchors are the one field worth grepping before the brief ships; every other field the implementing session re-derives anyway.
2026-09-01 · review · PLUG pass a1 · The classification reply made three corrections to Phase A's output that the extraction protocol should have produced itself: bugs found in a pass are fixed in the pass when small and area-local (no Jira round-trip), AC text uses published-surface vocabulary only (no internal method names, no `file:line` — those belong in the table's Evidence column), and rows 7 and 19 needed rewording to observable form. Phase A had written AC-shaped rows in implementation vocabulary because the Evidence column was right there; the spec sentence and its evidence are different audiences and the skill's row template does not say so.
2026-09-01 · agent · PLUG pass a1 · Three of the pass's own AC sentences claimed more than any test could fail on, and only a mutation sweep found it: `007.3`'s "read-only snapshot" (the backing store already hands out a fresh collection per call), `007.2`'s eager-load skip (the resolution rules return the same instance with or without it), and `004.4`'s test, which passed while exercising `004.2` because its fixture was loaded in the host. All three were green, plausible and worthless. Writing the AC and writing the test from the same sitting produces sentences that describe the code rather than constrain it — the mutation is the only thing that tells them apart, and it belongs in the same commit as the test, not in a later sweep.
2026-09-01 · review · PLUG pass a1 · Amendment 2 landed a blocker of my own making: fix 31 closed a registry gap whose stated harm was false. The table claimed a marked plugin assembly missing from the shared registry meant its `IConfigureServices` "is never auto-invoked" — the parser invokes the plugin's own registrations directly one call earlier, so nothing was skipped. Completing the registry then made that same pass invoke them a second time. The fix was right (downstream enumerators do need a complete registry) and the reason given for it was not, and a wrong reason is what let the fallout through: I never asked who else reads the thing I was completing. A row that names a harm should name the consumer that suffers it, by `file:line`, or it is a guess wearing evidence's clothes.
2026-09-01 · review · PLUG pass a1 · Two more ACs turned out to claim behavior no mutation could reach — `AC-PLUG-004.4` (a missing framework assembly converges on the same delegation as any other unresolved bind) and the log-only halves of `AC-PLUG-002.3`/`002.7`. Third time this pass: the check that finds them is "write the mutation before the AC is final", and it now has three separate confirmations in one area. Worth a `spec-pass` skill diff rather than another journal line.
2026-09-02 · gate · #161 · `TeardownStopSequenceShould.RunTheStopSequenceOnHostRecycle_NotOnlyOnProcessExit` false-failed the pack gate on the PLUG pass — a diff that never touches DevHost. Not the #142 mechanism: the test ran 373 ms and the runner logged "Reset requested" 90 ms *after* the assertion had already failed. `POST /api/control/reset` returns 202 having only signalled the supervised loop; generation 1 keeps answering `/api/control/status` until its hosted services stop, and `PollSteppedReadyAsync` checks only `stepped && canReset` — both true on generation 1 — so the poll returned on its first request and the recorder was still empty. The test's own comment asserted the inference the code cannot support ("generation 2 answering means generation 1 completed its teardown"). Fixed by awaiting the recorder's Stopping signal — the synchronisation point `testing-conventions.md` §16 demands — instead of a poll two generations share. Second `TeardownStopSequenceShould` false-fail in a week, a different race each time.
2026-09-02 · gate · #162 · The fix PR for the recycle race false-failed on its sibling: `DeliverAServicePropertyWriteIssuedFromStopping` [free-running clock], the exact 2026-08-26 (#142) flake, empty event list — two `TeardownStopSequenceShould` false-fails on two consecutive PRs in one day. The log settled the mechanism the #142 line had only inferred: the whole stop sequence took 0.7 ms with no timeout, i.e. the block acknowledged the stop the instant `Stopping()` returned while the publish it issued was still queued on the mock handler's mailbox, and `ShutdownAsync` right behind the ack dropped it — a contended runner just has to not schedule that mailbox within a millisecond. The D3 note in `DevLogicSystemInitializer.StopAsync` forbade a grace *pause* ("decided, not an oversight") but the flake was never about time: DevHost already owns the stepper's exact quiescence predicate, so teardown now waits on the barrier between the stop ack and termination — microseconds when the mailboxes are already empty, exactly the queued publish's dispatch otherwise, bounded by the same backstop as every step. That makes `DevHost.StopAsync`'s own comment ("those publishes must reach the event stream while the server is still up") true instead of usually true. Enforcement ladder: the #142 line documented the race and it recurred within a week; the fix is the rung the note should have been.
2026-09-02 · review · SDD pilot · The skill's first version sent every extraction-found defect to Jira ("a pass ships spec + tests, never behavior changes"); the user rejected it at the first classification gate — "I don't want to end up with 100 Jira issues that never get done … fix them while wording the AC, writing the test, to end with a better and consistent state." Right, and stronger than the rule it replaced: parking a bug leaves a hole in the page (neither the bug nor the correct behavior can be an AC), whereas fixing in-pass makes the test for the correct behavior, red on the pre-fix code, the defect proof itself. The pilot then fixed three defects that way. The skill now has four verdicts (intended / fix / park / out-of-spec), a size guard, and a findings ledger instead of a ticket queue. (D3)
2026-09-02 · brief · EMIT pass a1 · The brief's anchors were under-counted three ways, all in the same direction: it named three DALE descriptors (the family is six, DALE034–DALE039) and included `MeasuringPointAnalyzer`, which reports DALE004 and is not an emission analyzer at all; it listed 7 of the 18 files in `Vion.Dale.Sdk/Emission/`, omitting the whole deadband half (the registry, the adapters, the six built-in thresholds); and it listed 12 of the 20 in-scope test files, 65 of 114 tests. Every miss was recoverable because step 2 enumerates the surface mechanically before the sweep starts — which is exactly what that step is for, and the third pass in a row where the brief's anchor list was the thing that turned out wrong. The brief should point at the folder and the descriptor range, not transcribe a subset.
2026-09-02 · agent · EMIT pass a1 · `AC-EMIT-006.2` was minted as "a series of sub-threshold steps stays suppressed however far it drifts in total" and its very first test said otherwise: the deadband compares against the last *emitted* value, so the drift accumulates and a ramp of 0.4 steps under a 1.0 threshold emits on the third. I had written the sentence from the code's shape (a comparison against `LastEmitted`) and then read the consequence backwards. The reworded AC is the more useful one — it names the reading authors actually mis-predict, and the mutation that separates them (compare against the previously *offered* value and the ramp never emits) is now the test. Fourth confirmation that the mutation, not the reading, is what tells a real AC from a plausible one.
2026-09-02 · agent · EMIT pass a1 · `AC-EMIT-010.4` (nothing is published after a block stops) has no single reddening mutation: the stop guard in the flush and the gate rebuild on the retained-state clear each enforce it alone, and only removing both reddens the test. Measured rather than assumed, and the criterion kept — it constrains a real consumer-visible property. But the skill's rule ("an AC no mutation can redden is not a requirement") reads as if a single mutation is always available; the honest third case is *over-determined*, which is neither vacuous nor cleanly provable, and it deserves a named verdict rather than a paragraph in the drift log.
2026-09-02 · agent · EMIT pass a1 · A Tier C example cannot prove a same-PR SDK fix. `Vion.Examples.Emission` ships the exact dual-annotated shape row 55 fixes and references a **published** `Vion.Dale.Sdk` package, so tightening its assertion to what the fix guarantees fails until the release bump — its `Power` streams still measure 5 and 5. Tightening it was my first move and the run caught it. Worth stating where the consumer-sweep step is defined: an example is evidence of the shape a consumer writes, never evidence that a change works.
2026-09-02 · agent · EMIT pass a1 · `spec-trace` folds an in-flight change doc's own `ADDED` delta lines in as declarations, and a delta line carries no `GAP` marker — so a legitimately GAP-marked row on the page reads as a hard FAIL for the whole implementation phase and only resolves at archive. Twenty minutes chasing a gate failure that was the doc doing its job. Either the fold should honour the page's GAP marker for the same id, or the delta grammar needs a GAP form.
2026-09-02 · review · EMIT pass a1 · The operator's classification chose option (a) for the dual-annotated fix — independent knobs per stream, no inheritance from the sibling attribute — over the softer reading where a bare `[ServiceMeasuringPoint]` would keep borrowing the property's interval. Right call and the more expensive one: it moves 13 of `logic-block-libraries`' 14 dual-annotated members from their property's interval down to the 250 ms default on the next bump, so the change note naming them is part of the deliverable rather than a courtesy. Inheritance that is invisible in the declaration is the thing that made the original defect unnoticeable for four releases; the fix should not preserve a quieter version of it.
2026-09-02 · gate · EMIT pass a1 · The `spec-pass` skill's step 8 (one Stryker run over the area, survivors read by hand) could not run at all here. `dotnet-stryker` 4.16.0 needs `--project` whenever a test project references more than one mutatable project — which `Vion.Dale.Sdk.Test` (3) and `Vion.Dale.Sdk.TestKit.Test` (2) both do — and with `--project` it finds the tests and then dies in `EnrichTestProjectsWithTestInfo`. The pilot's audit worked only because `Vion.Dale.Plugin.Test` has exactly one reference and could omit the flag. MSTest 4's Microsoft.Testing.Platform is a second, independent blocker (stryker-net#3094) that `EnableMSTestRunner=false` works around. Two of the fourteen roster areas can be audited as the skill describes; the other twelve cannot, and the skill says nothing about what to do then. Named-mutation-per-AC carried the pass instead (12 applied and measured), which is the evidence that actually matters — worth deciding whether the Stryker slot is a should-have or a nice-to-have before the next pass plans around it.
2026-09-02 · review · EMIT pass a1 · Amendment 2's root finding is the same shape as the pass's headline one, one layer up: `EmissionAttributeHelper.GetEmissionAttribute` returned `[ServiceProperty]` else `[ServiceMeasuringPoint]`, so on a dual-annotated member the measuring point's knobs were validated by none of DALE034–DALE039. I fixed first-attribute-wins in the gate and in introspection and never asked where else the same expression lived — a grep for the pattern, not for the symbol, would have found it in the same hour. When a fix names a defect shape, sweep the shape.
2026-09-02 · agent · EMIT pass a1 · Two amendment-2 defects were only reachable because a token was parsed lazily: `MinChange` is read inside `Exceeds`, so an unreadable one faulted the block on the first value that moved and a negative numeric one was accepted and then never suppressed anything. Reading it once when the member's policy is built put both on `AC-EMIT-003`'s existing fail-at-start path. The general form is worth carrying: a declaration validated at the moment it is *used* validates once per use and reports at the worst possible time — validate it where it is *built*.
2026-09-02 · agent · EMIT pass a1 · The T-008 RFC-reference sweep replaced citations by regex and left three sentences with no subject (`ShowcaseBlock.cs:50` "never resets. emitted on every change"). The greps were re-run and came back clean, because a grep for the removed token cannot see the hole it left. A prose sweep needs the changed sentences re-read, and the review caught what my verification could not.
2026-09-02 · review · EMIT pass a1 · Amendment 2 added a range guard to the duration grammar and I wrote "Both reject it now" in the change doc. The `us` branch returned from inside the switch, above the guard, so microseconds skipped it entirely — and the guard compared milliseconds while a duration's limit is a tick count, leaving a one-ulp gap at the top. Two defects in the fix for a defect, and the claim I wrote was the thing that stopped me looking: I had checked the two units my test rows used and generalised. A guard added to a switch belongs to every arm, and the way to know is to route every arm through it rather than to trust the reading.
2026-09-02 · review · EMIT pass a1 · I dropped `IChangeThresholdShould` on the reasoning that the PublicApi manifest already pins the marker. It does not: the manifest is regenerated and auto-committed by CI rather than verified against, and `DALE014` is a warning. So removing the test removed the only thing that would fail. "Another gate already covers this" needs the gate named and its failure mode checked — recording a fact and enforcing it look identical in a green pipeline.
2026-09-02 · agent · EMIT pass a1 · The components.js badge fix had no fixture that could show it: no committed block declared the throttle-disabling sentinel in any spelling but `0`/`0ms`, so Tier 2 had nothing to render and I had reported the change on reasoning alone. Adding one member to the SmokeHost (`Drift`, `MinInterval = "0s"`) made it demonstrable in about ten minutes — the badge reads `DEADBAND Δ0.5` live, and the pre-fix predicate evaluated in the same page yields `throttle 0s · Δ0.5`. §6's "grow the fixture with the feature you added" is not overhead; it is what turns a belief into evidence.
2026-09-02 · gate · GATE pass a1 · The RFC-0016 reference sweep touched 77 lines across 54 files, and my first attempt did it with a whole-file regex that also stripped trailing whitespace repo-wide — 131 diff lines where 77 belonged, most of them untouched `.ToList(),` continuations. Reverted and redone line-scoped: read the file, rewrite only lines matching the citation, never normalise anything else. The second pass was clean on whitespace and still left 55 of the 77 lines mid-sentence (`/// operator-chosen …` where `RFC 0016: ` had been the subject) or double-spaced, all repaired by hand against a printed before/after report. `spec-process.md` already warns that "a regex sweep cannot see the sentence it leaves without a subject"; the cheaper half of the lesson is that it must not see lines it was never asked to touch either — scope the rewrite to the matching line, and print every one for review rather than trusting the diff to be small enough to skim.
2026-09-02 · agent · GATE pass a1 · Reading `DevConfigurationBuilder.GetAllLogicInterfaces` produced a confident, wrong finding: it skips `!property.CanWrite`, and every in-repo example declares its property-based interface bindings get-only, so the DevHost catalog "must" be missing them and the gating example's topologies "must" be unloadable. A three-minute probe against the real `LogicBlockDefinition.FromType` showed both come back carrying their predicates. The reading was correct about the source and wrong about the behaviour, and nothing in the file says why. Worth keeping as the shape rather than the instance: a `file:line` chain is evidence about code, not about what runs, and the extraction phase is exactly where the two get conflated — the probe is what separates them, and it belongs in the pass before the row is written, not after a reviewer asks.
2026-09-02 · gate · GATE pass a1 · `scripts/test-style-lint.ps1` failed on fourteen names I had just written — `KeepDeclaredDefaultWhenNoValueIsSupplied`, `RefuseGateOutsideTheGrammar` and siblings, every one carrying an article §12 forbids. I had read §12 at the start of the pass and still wrote them, because the natural English phrasing of an assertion carries articles and the rule fights that. No process change: the gate caught all fourteen in one run and the renames took two minutes, which is the ratchet working exactly as the EMIT pass intended. Noting it so the next pass budgets a rename round rather than being surprised by one.
2026-09-02 · review · GATE pass a1 · An `Edit` that anchored on `CreateService`'s signature to insert a new method above it silently adopted `CreateService`'s doc comment: the new member ended up with two `<summary>` blocks and the old one with none. The build was clean, all 431 tests passed, `cleanupcode` reformatted the file without objecting, and DALE014 does not judge `internal` members — so nothing in the gate stack could see it, and the operator caught it by reading the diff. The lesson is about the anchor, not the edit: inserting *before* an existing declaration puts the cursor between that declaration and its doc comment, which is the one place in a C# file where a comment belongs to the thing below it. Anchor on the closing brace of the member above instead, or include the neighbour's doc lines in the match so the split is visible in the patch.
2026-09-02 · review · GATE pass a1 amendment 2 · The pass recorded a probe result that was wrong and drew a journal lesson from it. Phase A read `GetAllLogicInterfaces`' `!CanWrite` skip, hypothesised the DevHost catalog drops every get-only interface binding, ran a scratch probe, saw the binding come back, and wrote the hypothesis off as "a `file:line` chain is evidence about code, not about what runs". The reading was right: the probe's "get-only" property was settable by the time the catalog read it, so it never reproduced the shape it was built to test. A test on the committed `{ get; } = new()` shape is red on the pre-fix code. The correction is narrower than "probe more": a probe that *disconfirms* needs its fixture checked against the real shape as hard as a probe that confirms, because a disconfirming result ends the investigation and nothing downstream will re-open it. The falsification felt like the rigorous move and was the one that let the defect through.
2026-09-02 · gate · GATE pass a1 amendment 2 · `test-style-lint` caught two more article-carrying names I wrote in this amendment, having already been caught by it — and having written a journal line about it — two hours earlier. The rule loses to the natural phrasing of an assertion every time; the gate wins every time. Nothing to change beyond noticing that the gate, not the convention doc, is what actually holds this line.
2026-09-02 · review · GATE pass a1 amendment 3 · Amendment 2's M7 relaxed a null guard so the definition view could describe an endpoint with nothing behind it, and shipped a null dereference: the interface factory hands the implementation to the generated `RegisterInstance`, whose `ConditionalWeakTable` refuses a null key. The new test was green because its fixture bound the *sink* side of the contract, and the generator emits that extension class for the *sender* side only. The general shape is worth more than the fix: a generated-code seam can differ between the two halves of one contract, so a fixture that picks the quiet half tests the guard and not the path. When relaxing a guard, pick the fixture by what the guarded code *calls*, not by what is convenient to declare — and read the generated file, since nothing in the hand-written source says one side has an extension class and the other does not.
2026-09-02 · review · GATE pass a1 amendment 3 · M2 fixed catalog *discovery* and left catalog *naming* wrong: get-only bindings were listed, but always under `{Property}_{Interface}`, while the binder honours an explicit `Identifier`. That is worse than the omission it replaced — an omitted endpoint cannot be wired, a misnamed one looks wired and resolves to nothing. The pattern: when a projection is corrected to "match the binder", every property of the projection has to be checked against the binder, not just the one the review named. I matched the membership rule and left the naming rule, because the review said "discovery".
2026-09-02 · gate · GATE pass a1 amendment 3 · I wrote the refreshed scorecard's gate numbers (169 ids, 202 cited) from arithmetic on what I had added, then ran the gates and got 166 and 196. Same defect as the EMIT pass's "expect 21 tests passed": a count in a document is stale or wrong by default. Corrected by reading the gate output. The durable fix is to paste the gate line rather than compose one — the number is only ever worth having verbatim.
2026-09-02 · review · GATE pass a1 amendment 4 · The orphaned-summary mistake recurred in the same pass, in the same shape: an insertion anchored on `[LogicBlock(...)]` landed between `GatedStationBlock` and its doc comment, so the new type carried two `<summary>` blocks and the block carried none. The first occurrence already has a journal line (2026-09-02 · review · the doc-comment-stealing insertion anchor) and I had written it myself. Writing the lesson down did not change the behaviour, because the anchor is chosen while thinking about the code being added and the comment above the anchor is invisible at that moment. The enforcement ladder says a rule that keeps drawing the same correction is an enforcement gap: the cheap rung here is a check in `scripts/` for a doc comment immediately followed by another, or for a type declaration with none, over the branch's changed files — the same shape `test-style-lint` already applies to test names, and the one gate that would have caught both occurrences.
2026-09-02 · gate · GATE pass a1 amendment 4b · The brief said the parsed predicate "appears to expose the referenced identifiers — verify; otherwise walk it". It does not: `Predicate` exposes `Parse`, `TryParse`, `Evaluate`, `ToString` and nothing else, and there is no AST to walk. Verifying took one reflection dump against the 3.7.0 assembly and changed the design — resolution is checked by evaluating against synthetic placeholders rather than by reading references. Worth keeping because the brief was right to hedge: an instruction that carries its own "verify" is the cheapest kind to receive, and the thirty seconds of checking is what let the deviation be a recorded design decision instead of a silent one.
2026-09-02 · review · GATE pass a1 amendment 5 · **A Tier 2 row recorded an observation that had never been made.** Amendment 4b added `missingParameterValueProblems` to `wiring.js`, wrote a node test for it, and put its message string into the Tier 2 evidence table as an answer "from the running host" — but nothing in `wwwroot/` imported the function, so no page could ever have rendered it. The string was true (it is the one the function builds); the observation was fiction. Two things made it feel safe in the moment: the function had a passing unit test, and the string was copied from source rather than invented. Neither is a browser. `devhost-conventions.md` §1 already says a Tier 2 row is a pasted observation and not a sentence, and I read it and wrote the row anyway, because a row in my own words is indistinguishable from a row read off a screen once it is in the table.
2026-09-02 · review · GATE pass a1 amendment 5 · A new export was added to `wiring.js` with a node test, and never imported by `components.js`. Every gate stayed green: the node test exercises the module directly, the C# side knows nothing about `wwwroot`, and the SPA has no bundler to notice an unused export. So "the policy function exists and is tested" and "the page uses it" were independent facts in this UI with nothing joining them, and the second one was false for a whole amendment. It was found by reading the import list, which is the only place it was visible.
2026-09-02 · review · GATE pass a1 amendment 5 · The row-64 fix checked a gate's references by *evaluating* the predicate against typed placeholders. Evaluation short-circuits, so `Count >= 2 && Missing >= 1` returned a verdict without ever reaching the undeclared name, and every compound predicate sailed through the check the fix existed to add. Every test written with the fix used a bare single-comparison predicate, so every one of them passed — the inputs I chose could not tell the leaf case from the composed one, and the check walks a structure whose whole point is composition.
2026-09-02 · review · GATE pass a1 amendment 5 · Amendment 4 wrote "the amendment-3 ledger entry and its checkpoint sentence claimed the half was tested; both corrected" and corrected neither, while adding a fresh false mutation claim of its own — three defects in one entry, none of them visible to any gate, since `spec-trace` checks that an id exists and nothing checks whether a sentence about a mutation is true. Writing that a correction had been made read, to me at the time, as making it. (second ask)
2026-09-02 · gate · GATE pass a1 amendment 5 · The amendment-4 scorecard carried amendment 3's gate numbers unchanged — the same row this journal already has a line about, one amendment later. The earlier line was about *composing* a number; this one is different and I did not see it coming, because nothing was composed. The row was simply not re-read, and a stale paste looks exactly like a fresh one. (second ask)
2026-09-02 · gate · GATE pass a1 amendment 5 · `test-style-lint`'s `Vion.Dale.DevHost.Test/` exemption was hiding four real hits in files this pass authored or extended. Running the script once with the entry removed locally listed all four at once. The exemption is right — the CTRL area owns that project — but it skips a *file*, not a test, so the cited tests a later pass adds to an exempt project get no gate at all, and I only found that out by taking the exemption off on a hunch.
2026-09-02 · review · GATE pass a1 amendment 5 · A test cited a criterion for behaviour the criterion's text does not state — `AC-GATE-012.8` covered an unresolvable identifier while two tests cited it for an undecodable value — and separately a criterion was reworded on the page with no `MODIFIED` delta line, so page and delta disagreed. Amendment 4 was corrected for the first of those and the same defect appeared again in the same commit; this is its third occurrence in the pass. `spec-trace` is green throughout, because it checks that an id exists and never that the id says what the test proves, and reading the id felt like reading the criterion. (second ask)
