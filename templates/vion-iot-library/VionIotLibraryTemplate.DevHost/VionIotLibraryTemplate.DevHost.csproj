<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>false</ImplicitUsings>
        <Nullable>enable</Nullable>
        <IsPackable>false</IsPackable>
    </PropertyGroup>

    <ItemGroup>
        <!-- Switch to project reference on this branch (new RunFolderDrivenAsync API not yet released).
             Revert to PackageReference once Vion.Dale.DevHost.Web ≥ 0.9.0 is published via set-version.ps1. -->
        <ProjectReference Include="..\..\..\Vion.Dale.DevHost.Web\Vion.Dale.DevHost.Web.csproj"/>
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\VionIotLibraryTemplate\VionIotLibraryTemplate.csproj"/>
    </ItemGroup>
</Project>